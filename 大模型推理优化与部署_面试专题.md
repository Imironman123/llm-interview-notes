------------------------------------------------------------------------

# 来源与参考

- GitHub `wdndev/llm_interview_note` —— 「06.推理」模块（推理加速、KV Cache、量化、蒸馏剪枝、推理框架）
- GitHub `WeThinkIn/AIGC-Interview-Book` —— 「模型部署基础」「推理优化」章节
- GitHub `712sir/ai-infra-career` —— AI Infra 岗位面经与工程实践
- GitHub `km1994/LLMs_interview_notes` —— LLM 面试笔记（推理与部署部分）
- vLLM / SGLang / TensorRT-LLM 官方文档与论文（PagedAttention、RadixAttention、Chunked Prefill）
- 论文：*Efficient Memory Management for Large Language Model Serving with PagedAttention*、*Fast Transformer Decoding: One Write-Head is All You Need*（MQA）、*GQA*、*SmoothQuant*、*AWQ*、*GPTQ*、*H2O*、*StreamingLLM*、*Speculative Decoding*、*Medusa*、*EAGLE*、*Mooncake*

------------------------------------------------------------------------

# 一、推理基础：Prefill / Decode 与 KV Cache

## Q1：Prefill 和 Decode 两个阶段的本质区别是什么？为什么说一个是 compute-bound、一个是 memory-bound？

答：自回归生成可拆成两个阶段。

**Prefill（预填充）**：一次性处理整段 prompt，$`S`$ 个 token 并行进入模型，对所有位置同时计算 Attention。此时矩阵乘法的形状是 $`\lbrack S,d\rbrack \times \lbrack d,d\rbrack`$，计算量随 $`S`$ 线性增长，访存量也随 $`S`$ 增长但增长更慢，因此**算术强度（Arithmetic Intensity）高**，是 compute-bound。GPU 的 Tensor Core 被打满，SM 利用率高。

**Decode（解码）**：每步只处理 1 个新 token（batch 内多个序列时是 $`B`$ 个），需要读取全部模型权重与整个 KV Cache，但计算量极小。此时的算术强度约等于

``` math
\text{AI} = \frac{2 \cdot N_{\text{param}}}{2 \cdot N_{\text{param}}/\text{bytes\_per\_weight}} = \text{bytes\_per\_weight}
```

也就是说，每读 1 字节权重只做约 2 次浮点运算（一次乘一次加）。以 FP16 为例 $`\text{AI} \approx 2`$ FLOPs/Byte，而 A100 的机器平衡点是

``` math
\frac{\text{FP16 峰值算力}}{\text{显存带宽}} = \frac{312\ \text{TFLOPS}}{2039\ \text{GB/s}} \approx 153\ \text{FLOPs/Byte}
```

$`2 \ll 153`$，差了两个数量级，所以 Decode 是彻底的 memory-bound。

**工程含义**： 1. Prefill 阶段应关注算力利用率，用 FlashAttention、大 tile、张量并行降低单卡算力占比。 2. Decode 阶段应关注显存带宽利用率，用大 batch 摊薄权重读取成本、用 KV Cache 量化减少字节数、用投机解码提升每步产出 token 数。

**追问 1：为什么增大 batch 能提升 Decode 吞吐？** 因为权重读取是固定成本。设权重 $`N`$ 参数、每元素 $`b`$ 字节，单步读权重耗时 $`N \cdot b/\text{BW}`$。batch=$`B`$ 时这 $`B`$ 个 token 共享同一次权重读取，计算量变为 $`2BN`$，理论耗时 $`\max(2BN/\text{FLOPS},\ Nb/\text{BW})`$。当 $`B`$ 小到 $`2BN/\text{FLOPS} < Nb/\text{BW}`$ 时，加 batch 几乎不增加时间却线性提升吞吐。当 $`B > 153/2 \approx 76`$ 时进入 compute-bound，吞吐增益停止，延迟开始线性上升。这就是**batching 的收益上界**。

**追问 2：Prefill 和 Decode 混在同一个 batch 里会怎样？** 会产生「decode 被 prefill 拖慢」的问题。vLLM 通过 Chunked Prefill 把长 prompt 切块，与 decode 请求拼批，并按 token 预算调度（如每步最多 2048 个 token），保证 TPOT 不被长 prefill 尖刺破坏。

------------------------------------------------------------------------

## Q2：KV Cache 的原理是什么？请给出显存计算公式并算一下 LLaMA-70B 在 A100 80G 上的显存占用。

答：自回归生成时，第 $`t`$ 步的注意力需要用到前 $`t - 1`$ 步的 key 和 value。若每步重算，总计算量是 $`O(L^{2})`$；缓存下来后每步只需要算一个新 token 的 K、V，复杂度降为 $`O(L)`$，代价是显存。

**单 token 的 KV Cache 大小**：

``` math
\text{bytes/token} = 2 \times L \times H_{kv} \times d_{head} \times b
```

其中 2 表示 K 和 V，$`L`$ 是层数，$`H_{kv}`$ 是 KV 头数，$`d_{head}`$ 是每头维度，$`b`$ 是每元素字节数。

**整条序列**：

``` math
\text{KV bytes} = 2 \cdot L \cdot H_{kv} \cdot d_{head} \cdot b \cdot S \cdot B
```

$`S`$ 为序列长度，$`B`$ 为并发数。

**LLaMA-70B 具体参数**：$`L = 80`$，$`H = 64`$（注意力头），$`H_{kv} = 8`$（GQA，8 组），$`d_{head} = 128`$，FP16 即 $`b = 2`$。

单 token KV：

``` math
2 \times 80 \times 8 \times 128 \times 2 = 327,680\ \text{Bytes} \approx 0.3125\ \text{MB/token}
```

即 **320 KB/token**。

模型权重：70B × 2 Bytes = **140 GB**，所以 A100 80G 单卡放不下，必须至少 2 卡张量并行（TP=2，每卡 70GB）；实际工程上常用 **TP=4**（每卡 35GB），留出 KV Cache 空间。

取 TP=4，每卡权重 35 GB，A100 80G 扣掉权重、框架开销（约 2-3 GB）、CUDA Graph 与激活缓冲（约 2-4 GB），可用于 KV Cache 约 **40 GB**。

最大并发 token 数：

``` math
\frac{40 \times 1024\ \text{MB}}{0.3125\ \text{MB}} \approx 131,072\ \text{tokens}
```

若平均每条序列 $`S = 2048`$（含 prompt 与输出），则

``` math
B_{\max} \approx \frac{131072}{2048} = 64
```

即 **TP=4 时约 64 路并发**。若改用 FP8 KV Cache（$`b = 1`$），KV 占用减半，并发翻倍到约 128。

**追问 1：为什么 LLaMA-70B 用 GQA 而不是 MHA？如果用 MHA 会怎样？** 若 $`H_{kv} = H = 64`$，单 token KV 变成 $`2 \times 80 \times 64 \times 128 \times 2 = 2,621,440`$ B $`= 2.5`$ MB/token，是 GQA 的 8 倍。同样 40 GB 只能撑 16K tokens，约 8 路并发。GQA 把 KV Cache 压缩了 8 倍，这是 70B 级别模型能上线服务的直接前提。

**追问 2：KV Cache 显存和 batch 是线性关系吗？** 是。KV Cache 是唯一随 $`B \cdot S`$ 线性增长的项，因此**它才是并发上限的决定因素**，而不是权重。这就是 PagedAttention 出现的动机：只要能减少 KV Cache 的浪费（碎片 + 预留），并发就能直接提高。

------------------------------------------------------------------------

## Q3：TTFT 和 TPOT 分别是什么？为什么它们常常互相冲突？

答：**TTFT（Time To First Token）**是首 token 延迟，主要决定因素是 prefill 的计算时间与排队时间：

``` math
\text{TTFT} \approx T_{\text{queue}} + \frac{2 \cdot N_{\text{param}} \cdot S_{\text{prompt}}}{\text{有效 FLOPS}} + T_{\text{prefill\_comm}}
```

**TPOT（Time Per Output Token）**是解码阶段每个 token 的平均延迟，memory-bound：

``` math
\text{TPOT} \approx \frac{N_{\text{param}} \cdot b + \text{KV\_bytes\_read}}{\text{BW} \cdot \eta}
```

**端到端延迟**近似为 $`\text{E2E} \approx \text{TTFT} + (S_{\text{out}} - 1) \cdot \text{TPOT}`$。

冲突来源：TTFT 关心「快速开始」，需要尽快、独占式地算完这条 prompt；TPOT 关心「持续稳定」，需要足够大的 batch 来摊薄权重读取。而把大量请求拼成一个大 batch 一起做 prefill，会让后到的请求排队，TTFT 恶化；反之为了 TTFT 优先处理每条 prefill，decode 的 batch 就被打散，TPOT 抖动。这是**吞吐—延迟权衡的核心矛盾**。

**追问：业界怎么缓解这个矛盾？** 1. **Chunked Prefill**：把长 prompt 分块，与 decode 拼批，用统一 token budget 调度，避免 prefill 独占造成 TPOT 尖刺。 2. **PD 分离（Prefill-Decode Disaggregation）**：prefill 和 decode 部署在不同 GPU 池，各自按自己最优的 batch 策略运行，KV Cache 通过高速网络传输。 3. **SLA 分层**：把 TTFT 敏感的请求（如对话首响）路由到低负载实例，批处理任务走大 batch 高吞吐池。

------------------------------------------------------------------------

## Q4：吞吐与延迟的权衡曲线长什么样？Batching 策略有哪几种？

答：典型曲线是：batch 增大 → 吞吐近似线性上升 → 到达 compute-bound 拐点后吞吐饱和 → 继续增大则延迟线性恶化、吞吐甚至下降（因为显存打满、调度开销上升）。拐点位置约等于机器平衡点对应的 batch 数，对 A100 FP16 约 $`B \approx 76`$。

**主要 Batching 策略**：

1.  **Static Batching**：一次收集固定数量请求，一起跑完再返回。简单，但短序列被长序列拖累（padding 浪费 + 木桶效应），GPU 利用率随序列长度方差急剧下降。
2.  **Dynamic / Continuous Batching（Inflight Batching）**：以**迭代（iteration）为调度粒度**，每一步检查有没有请求结束、有没有新请求到达，结束后立刻把新请求插入空位。对比 static batching，同样 SLA 下吞吐可提升数倍到十几倍（Orca 论文报告 36 倍量级）。这是 vLLM、TensorRT-LLM、TGI 的默认模式。
3.  **Sequence Packing / 变长批**：把多条短序列拼进一个 batch 的 token 维度，配合 FlashAttention 的 varlen kernel 消除 padding，等价于把有效 batch 拉大。
4.  **Chunked Prefill**：见上，主要是为了稳定 TPOT。
5.  **优先级 / SJF 调度**：按剩余输出长度估计排序，短任务优先，可显著降低平均延迟。

**追问：Continuous Batching 的调度循环具体是怎样的？** 每步做四件事：(a) 从等待队列取出可准入的请求（受 token budget 与显存块数约束）；(b) 为它们分配 KV 块（PagedAttention 的 block table）；(c) 把当前 running 集合拼成一个 batch 前向一次；(d) 采样、判断停止条件，回收结束序列的 KV 块。调度器的核心指标是「每步 token 预算」与「可用 KV block 数」。

------------------------------------------------------------------------

# 二、KV Cache 优化

## Q5：MQA 和 GQA 是怎么减少 KV Cache 的？各自的取舍是什么？

答：**MHA** 中每个头独立拥有 K、V，KV Cache 正比于 $`H`$。**MQA（Multi-Query Attention）** 让所有 query 头共享**一份** K、V，即 $`H_{kv} = 1`$，KV Cache 缩小 $`H`$ 倍（对 LLaMA-70B 是 64 倍）。**GQA（Grouped-Query Attention）**是折中：把 $`H`$ 个 query 头分成 $`G`$ 组，每组共享一份 K、V，$`H_{kv} = G`$。

``` math
\text{KV 压缩比} = \frac{H}{H_{kv}}
```

- LLaMA-2 70B：$`H = 64,H_{kv} = 8`$，压缩 8 倍。
- LLaMA-3 8B：$`H = 32,H_{kv} = 8`$，压缩 4 倍。
- Mistral 7B / LLaMA-2 7B：$`H_{kv} = 8`$ 或 $`H`$，视版本。

**取舍**：MQA 压缩最狠但质量损失明显（训练不稳定、指标下降）；GQA 在压缩 4–8 倍时质量几乎无损，且推理时 KV 读取量减少、decode 更快，已成为事实标准。工程上还有 uptraining（用原 MHA 权重初始化 GQA 并继续预训练少量步数）来平滑过渡。

**追问：为什么 MQA/GQA 不减少 FLOPs？** 因为 query 头数不变，$`QK^{\top}`$ 的输出维度不变，Attention 的主要 FLOPs 由 query 侧决定。MQA/GQA 减少的是**KV 的访存量与缓存量**，因此收益主要体现在 memory-bound 的 decode 阶段，prefill 阶段（compute-bound）收益有限。

------------------------------------------------------------------------

## Q6：PagedAttention 解决了什么问题？它的核心机制是什么？

答：**问题**：朴素实现中，每条序列预留一段连续的、按最大长度分配（如 2048）的 KV 显存。而输出长度不可预知，导致三类浪费——**内部碎片**（预留了但没用满，实测浪费可达 60–80%）、**外部碎片**（不同长度请求交替分配造成的空洞）、**无法共享**（同一条 prompt 的多个采样、beam search、并行采样各存一份）。

**PagedAttention 的机制**：借鉴操作系统的虚拟内存分页。 1. 把 KV Cache 切成固定大小的 **block**（vLLM 中默认 16 个 token 一块）。 2. 每个 block 存 $`16 \times H_{kv} \times d_{head} \times 2 \times b`$ 字节，物理上不要求连续。 3. 每条序列维护一张 **block table**（类似页表），逻辑块号 → 物理块号。 4. Attention kernel 按 block table 做 gather，在 kernel 内部完成非连续地址的读取。

**收益**： - 内部碎片从「平均半条序列」降到「最多一个 block（16 token）」，浪费率从 60–80% 降到 **4% 以下**（论文实测）。 - 支持 **Copy-on-Write 共享**：同 prompt 的 $`n`$ 个采样共享 prefix 的物理块，只有分叉处才复制。parallel sampling 的显存可省 $`n`$ 倍。 - 支持 **beam search 的块共享与释放**。 - 显存省下来直接转化为并发数提升，vLLM 相比 FasterTransformer/Orca 吞吐提升 2–4 倍（同延迟约束下）。

**追问 1：block size 为什么选 16？** 权衡两件事：太小 → block table 变大、kernel 内 gather 次数多、调度开销上升；太大 → 内部碎片增加、共享粒度变粗。16 是实测的经验最优点。TensorRT-LLM 支持 32/64/128 的可配置 block，长序列场景用大 block 减少元数据开销。

**追问 2：PagedAttention 会降低 kernel 效率吗？** 会引入少量非连续访存开销，但通过 kernel 内按 block 顺序连续读取（每个 block 内 16 个 token 的 KV 是连续的），加上 write 阶段是 append，实际开销很小，被显存利用率提升带来的收益完全盖过。

------------------------------------------------------------------------

## Q7：Prefix Caching 与 RadixAttention 分别是什么？SGLang 的 RadixAttention 有什么特别？

答：**Prefix Caching（前缀缓存）**：多条请求若共享相同前缀（系统提示词、Few-shot 示例、多轮对话历史），可以把这段前缀的 KV Cache 算一次、复用多次，避免重复 prefill。收益 = 省下的 prefill 计算，直接降低 TTFT。

朴素实现是「精确匹配整段前缀」（如需要显式指定 `prefix_id`），只能命中完全相同的请求前缀。

**RadixAttention（SGLang）** 用 **基数树（Radix Tree）** 管理所有已缓存前缀： 1. 把 token 序列插入一棵压缩前缀树，树节点对应一段 KV 的物理位置。 2. 新请求到来时，沿树做**最长前缀匹配**，命中的部分直接复用其 KV，只对未命中的后缀做 prefill。 3. 用 **LRU + 引用计数** 做淘汰：引用计数为 0 的节点才可被回收。 4. 支持 **树结构的自动分叉与合并**：例如多轮对话中，第 2 轮的请求天然复用第 1 轮的整棵树。

这个设计让缓存命中是**自动的**，不需要用户标注，且能处理任意长度的公共前缀（不限于整段）。在多轮对话、Agent（同一 system prompt + 工具定义反复出现）、RAG（长文档重复）场景下，命中率极高，TTFT 可下降数倍。

**追问：Prefix Caching 有什么代价？** (a) 显存被缓存占用，需要精细的淘汰策略，否则挤压并发；(b) 缓存查找本身有 CPU 开销（哈希/树遍历），高 QPS 下可能成为瓶颈，工程上要做 token 级哈希与增量匹配；(c) 请求结果正确性要求 KV 与位置编码严格一致，位置相关的 RoPE 需要按偏移正确重算（这也是为什么通常只缓存前缀，不从中间复用）。

------------------------------------------------------------------------

## Q8：KV Cache 量化怎么做？INT8 / FP8 的效果与实现细节？

答：KV Cache 是 Decode 阶段的主要访存对象，把它从 FP16 压到 INT8/FP8 有双重收益：显存减半 → 并发翻倍；带宽压力减半 → TPOT 下降。

**常见方案**： 1. **Per-token（per-head）量化**：对每个 token 的 K、V 分别算 scale/zero-point。因为 K 的分布中**通道（channel）方向存在少数异常值**（outlier），per-tensor 量化会被 outlier 拉爆量化范围，导致精度崩坏。per-token 量化能显著缓解。 2. **保留 K 的高精度**：K 的量化误差对注意力分数影响比 V 更大（因为要经过 softmax 放大差异）。实践中常对 K 用 per-channel 量化、V 用 per-token 量化，或 K 保留 FP16、只量化 V。 3. **FP8（E4M3/E5M2）**：比 INT8 动态范围大得多，不需要 zero-point，实现更简单，H100 / L40S 等有原生支持。K 用 FP8-E4M3、V 用 FP8-E5M2 是常见组合。 4. **分组量化**：把 $`d_{head}`$ 切成 64/128 一组，per-group scale，进一步降低误差。

**精度影响**：在 LLaMA / Qwen 系列上，INT8 KV Cache 的困惑度上升通常 \< 0.1，下游任务（MMLU 等）掉点 \< 1%。更激进的 **INT4 KV Cache** 会明显掉点，需要配合 token 级分组与保留策略。

**工程细节**：量化/反量化需要额外的 kernel。如果每步都反量化整个 cache，会引入显著开销，反而抵消带宽收益。因此实践做法是**在 Attention kernel 内部直接读量化后的 KV 并在线反量化**（fused dequant-attention），如 TensorRT-LLM 的 INT8/FP8 KV kernel、vLLM 的 FP8 KV cache 支持。

**追问：为什么量化 KV Cache 的收益在长上下文时更大？** 因为 KV 读取量正比于 $`S`$。$`S`$ 越长，KV 读取在 decode 总访存中的占比越高（权重读取是固定的），量化带来的带宽节省就越显著。在 $`S = 32K`$ 时，KV 读取可能已经超过权重读取成为主要瓶颈。

------------------------------------------------------------------------

## Q9：KV Cache 驱逐与压缩：H2O、StreamingLLM / Attention Sink 的原理是什么？

答：核心观察：不是所有 KV 都同等重要。

**StreamingLLM 与 Attention Sink**：研究发现，Transformer 的注意力会大量集中在**序列最开头的前几个 token**（称为 attention sink），即使这些 token 语义上无关（例如 BOS）。这是因为 softmax 需要有一个「无处可去」的汇点。结论：保留「开头的 sink token + 最近的滑动窗口」，中间的 token 直接丢弃，模型仍能稳定工作，从而支持**无限长流式推理**，显存恒定。

``` math
\text{保留集} = \{ 1..k_{\text{sink}}\} \cup \{ t - w..t\}
```

**H2O（Heavy Hitter Oracle）**：用累积注意力权重识别「重击者」。对每个 token 累计它从后续所有 token 收到的注意力分数，保留分数最高的 top-$`k`$ 个（约 20%）加上最近的窗口。它是**动态**保留，比 StreamingLLM 更贴合内容重要性。

**其他方案**： - **Scissorhands / SnapKV**：SnapKV 用「观察窗口」的注意力统计来选择要保留的 prompt token，是无训练的、更精准的压缩。 - **量化 + 驱逐组合**：重要 token 保留 FP16，次要 token 用 INT4 甚至丢弃。 - **层级压缩**：不同层的 KV 重要性不同，浅层可以更激进。 - **KV Cache 合并（merge）**：把相似的历史 KV 加权平均合并成更少的条目。

**代价与追问：这些方法的共同风险是什么？** (a) 对需要**长距离精确检索**的任务（如多文档 QA、代码补全）会掉点，因为被驱逐的 token 可能恰好是答案所在；(b) 大多数方法需要训练或校准，且 kernel 实现复杂（动态稀疏索引难以高效实现，实际加速比常低于理论压缩比）；(c) 与 PagedAttention 的交互复杂，频繁驱逐会破坏分页的连续性假设，通常需要独立的稀疏 kernel。

------------------------------------------------------------------------

## Q10：Chunked Prefill 和 Continuous Batching 的区别与联系？

答：**Continuous Batching** 是**批级别的调度策略**：以 iteration 为粒度动态增删请求，解决的是「batch 内序列完成时间不同导致空转」的问题。

**Chunked Prefill** 是**请求内部的切分策略**：把一条长 prompt 的 prefill 计算切成多个 chunk（例如每块 512 token），跨多个 iteration 完成。它解决的是「一条长 prefill 阻塞整个 batch 的 decode」的问题。

两者是正交的、必须配合使用：调度器在每个 iteration 里，把「若干个 decode 请求 + 若干 chunk of prefill 请求」按 **token budget** 拼成一个混合 batch。

**收益**： - 消除 prefill 造成的 TPOT 尖刺（P99 TPOT 显著下降）； - 短 prefill 的请求不再被长 prompt 卡住，TTFT 更平滑； - GPU 在 decode 为主时也能用 prefill 的计算填补算力空闲，**提高 GPU 利用率**； - 允许更大的 `max_num_batched_tokens`（如 8192），而不会 OOM。

**代价**： - 长 prompt 的 TTFT 会略微变长（因为分块后串行执行）； - 调度器与注意力 mask 实现更复杂（需要 varlen + 正确的块内因果 mask）。

**追问：vLLM 中** `max_num_batched_tokens` **怎么设？** 它决定每个 iteration 处理的总 token 数上限。设太小 → prefill 被切得过碎，TTFT 上升；设太大 → 单步耗时波动大、显存峰值高。经验值：与 `max_num_seqs` 和 GPU 显存配合，常见 2048–8192。vLLM V1 默认开启 chunked prefill，且把这个参数作为核心性能旋钮。

------------------------------------------------------------------------

# 三、推理引擎选型

## Q11：vLLM / SGLang / TensorRT-LLM / TGI / LMDeploy / llama.cpp 该怎么选？

| 答： | 引擎 | 核心技术 | 优势 |
|----|----|----|----|
| **vLLM** | PagedAttention、Continuous Batching、Chunked Prefill | 生态最全、模型支持最广、易用、社区活跃 | 通用服务端部署、快速上线、研究 |
| **SGLang** | RadixAttention、前缀共享、结构化解码（压缩 FSM） | 多轮/Agent/RAG 前缀复用强，约束解码快 | Agent、多轮对话、结构化输出 |
| **TensorRT-LLM** | 图编译、fused kernel、FP8/INT4、inflight batching | 单卡性能天花板最高（NVIDIA 全家桶） | 极致性能、成本敏感、NVIDIA 独占环境 |
| **TGI** | Rust 实现、令牌流式、动态批处理 | 部署运维成熟、HF 生态集成好 | HuggingFace 体系、企业级 Serving |
| **LMDeploy** | TurboMind、持久化 batch、W4A16 | 国产卡/低显存场景优秀、量化成本低 | 显存受限、InternLM/Qwen 系 |
| **llama.cpp** | GGUF、CPU/混合推理、多种量化 | 端侧/CPU 可用、内存占用低 | 本地部署、边缘设备、个人开发者 |

**选型口诀**：追求极致性能选 TensorRT-LLM；通用性与迭代速度选 vLLM；Agent/多轮前缀复用选 SGLang；显存紧张或国产硬件选 LMDeploy；端侧/CPU 选 llama.cpp。

**追问：为什么不总是用 TensorRT-LLM？** (a) 编译期长，每次换模型/换 shape 都要重新 build engine；(b) 模型支持滞后，新架构（如新 MoE、新 attention 变体）适配慢；(c) 强绑 NVIDIA，异构集群不可用；(d) 调试困难，黑盒编译。因此生产上常见「vLLM 做主力 + TensorRT-LLM 做核心大模型」的混合架构。

------------------------------------------------------------------------

## Q12：PD 分离（Prefill-Decode Disaggregation）是什么？Mooncake 做了什么？

答：**动机**：Prefill 是 compute-bound、需要大算力与长序列并行；Decode 是 memory-bound、需要大 batch 与低延迟。两者放在同一个实例上，会互相干扰：prefill 尖刺破坏 TPOT，decode 占用显存挤压 prefill 并发。且两者对硬件的偏好不同（prefill 想要高算力，decode 想要高带宽/大显存）。

**PD 分离**：把 prefill 和 decode 拆到**不同的 GPU 池**。 - **P 池**：只跑 prefill，追求高算力利用率，可以大 batch、可以用较便宜的算力卡。 - **D 池**：只跑 decode，追求大 batch 与稳定的低 TPOT，需要大显存（KV Cache）。 - **KV 传输**：prefill 完成后，把 KV Cache 通过高速网络（NVLink / RDMA / RoCE）传给 decode 实例。这是关键瓶颈——一条 $`S`$ 长度序列的 KV 是 $`S \times 0.3125`$ MB（70B GQA），$`S = 4K`$ 时约 1.25 GB，$`S = 32K`$ 时 10 GB，对网络带宽要求极高。

**Mooncake（月之暗面 Kimi 的服务架构）** 的关键设计： 1. **以 KV Cache 为中心的分离式架构**：把 KV Cache 当作一等公民，用 **KVCache 池**（利用集群闲置的 CPU/DRAM/SSD 组成分层缓存）来承载。 2. **Chunked Pipeline Parallelism**：把长 prefill 切块，在多层间流水，减少单块等待。 3. **以 KVCache 为调度核心的全局调度器**：调度决策不只看 GPU 负载，还看「这条请求的 prefix 是否已在某个节点缓存中」，优先路由到有缓存命中的节点，实现「缓存换计算」。 4. **分层缓存（transfer engine）**：GPU HBM → CPU DRAM → SSD/远端，通过 RDMA 做零拷贝传输，实测在长上下文、高负载场景下吞吐提升数倍。

**追问：PD 分离的代价是什么？** (a) KV 传输占用带宽，若网络是瓶颈，收益会被吃掉甚至为负——因此对**短 prompt、短输出**的场景不值得；(b) 架构复杂度高，需要额外的调度器、传输引擎、故障恢复；(c) 需要两套资源配置，资源利用率在低负载时可能更差。经验上，PD 分离在**长上下文 + 高负载**下收益最大。

------------------------------------------------------------------------

# 四、量化

## Q13：量化的基本概念：对称 vs 非对称、per-tensor vs per-channel vs per-group？

答：**量化**把浮点权重/激活映射到低比特整数：

``` math
x_{q} = \text{round}\left( \frac{x}{s} \right) + z,\quad\quad\widehat{x} = s \cdot (x_{q} - z)
```

$`s`$ 是 scale（缩放因子），$`z`$ 是 zero-point（零点）。

**对称量化**（$`z = 0`$）：$`s = \frac{\max|x|}{2^{b - 1} - 1}`$，映射到 $`\lbrack - 2^{b - 1},2^{b - 1} - 1\rbrack`$。实现简单、无零点偏移，但范围被 $`( + \max, - \max)`$ 中的较大者决定，若分布不对称则浪费码点。

**非对称量化**：$`z \neq 0`$，$`s = \frac{x_{\max} - x_{\min}}{2^{b} - 1}`$，$`z = \text{round}( - x_{\min}/s)`$。能充分利用码点，适合激活（如 ReLU 后全正、或分布偏斜）。

**粒度**： - **Per-tensor**：整个张量一个 $`s`$。最省元数据，但对 outlier 最敏感。 - **Per-channel / Per-axis**：每个输出通道一个 $`s`$。权重量化的标准做法，精度损失小。 - **Per-group**：每 $`g`$ 个元素（如 128）一个 $`s`$。GPTQ / AWQ 在 INT4 下必须用 per-group（$`g = 128`$），否则精度崩。元数据开销 = $`b/g`$ 比特/权重（INT4 + $`g = 128`$ → 额外 0.125 bit/weight）。

**追问：为什么权重量化比激活量化容易？** 权重是静态的，可以在离线阶段用校准数据仔细优化（GPTQ 逐列误差补偿、AWQ 搜索最优缩放），而且权重分布相对均匀。激活是动态的、依赖输入，且存在**系统性 outlier**（某些通道的激活值比其他通道大 100 倍以上），必须用 SmoothQuant 这类方法把难度从激活转移到权重。

------------------------------------------------------------------------

## Q14：GPTQ / AWQ / SmoothQuant 三大权重量化方法的核心思想？

答：**GPTQ（2022）**：基于 **OBQ（Optimal Brain Quantization）** 的逐层后训练量化。 - 对每一层，用校准数据的输入 $`X`$ 计算 Hessian $`H = 2XX^{\top}`$。 - 逐列量化权重，每量化一列就用 Hessian 的逆把误差补偿到**尚未量化**的剩余列上，使输出误差最小化：

``` math
\min_{\widehat{W}} \parallel WX - \widehat{W}X \parallel_{2}^{2}
```

- 用 Cholesky 分解 + 分块（lazy batch update）加速，让 175B 模型能在几小时内量化完。
- 特点：INT4 + per-group(128) 精度好，但量化过程需要校准集、耗时；对激活不做处理，因此通常用于 weight-only 场景。

**AWQ（Activation-aware Weight Quantization, 2023）**： - 核心洞察：**不是所有权重同等重要**，只有约 1% 的「显著权重」（对应激活值大的通道）需要保护。 - 做法：不做混合精度（硬件不友好），而是**对显著通道按比例放大权重、同时等比例缩小对应激活**，数学上等价、但对量化误差更鲁棒：

``` math
W' = W \cdot \text{diag}(s),\quad\quad X' = X \cdot \text{diag}(s)^{- 1}
```

- 缩放因子 $`s`$ 通过对激活分布的搜索得到。因为保护了 outlier 通道，AWQ 在 INT4 下的精度常优于 GPTQ，且量化速度更快（不需要反向传播/Hessian），泛化性好（对指令微调模型尤其稳）。

**SmoothQuant（2022）**： - 解决的是 **W8A8** 场景：激活的 outlier 使 INT8 激活量化极难。 - 核心：把激活的量化难度**数学等价地迁移到权重**上：

``` math
Y = (X \cdot \text{diag}(s)^{- 1}) \cdot (\text{diag}(s) \cdot W) = X'W'
```

- $`s_{j} = \max(|X_{j}|)^{\alpha}/\max(|W_{j}|)^{1 - \alpha}`$，$`\alpha \in \lbrack 0,1\rbrack`$ 常取 0.5 作为迁移强度。
- 效果：激活变得平滑易量化，权重虽略有 outlier 但因分布均匀且可 per-channel 量化，仍能保持精度。使 LLaMA-70B 等大模型首次做到 W8A8 且精度几乎无损。

**追问：三者能组合吗？** 可以且常见。SmoothQuant 处理 W8A8（激活 + 权重都 INT8）；GPTQ/AWQ 处理 W4A16（只量化权重，激活保持 FP16）。工程上还有 **W4A8**（AWQ 权重 + SmoothQuant 激活）用于平衡精度与显存，但 kernel 实现复杂，支持度不如前两者。

------------------------------------------------------------------------

## Q15：GGUF 和 bitsandbytes 分别是什么？各自定位？

答：**GGUF（GPT-Generated Unified Format）**：llama.cpp 使用的模型文件格式，前身是 GGML。 - 单文件封装：权重 + 超参 + tokenizer + 元数据，便于分发。 - 支持**混合量化**（mixture of quantizations）：不同张量用不同精度，如 `Q4_K_M` 表示大部分 4-bit、关键层（attention 的 V、feed-forward 的 down_proj）用更高精度，K-quants 用**分块的量化块 + 两级 scale**（super-block 内嵌 sub-block scale）降低误差。 - 常见规格：`Q8_0` ≈ FP16 的 50%，`Q4_K_M` ≈ 25-30%，`Q2_K` 极小但掉点明显。 - 定位：**端侧 / CPU / 内存受限**场景，支持 CPU + GPU 混合卸载（把部分层放显存）。

**bitsandbytes（BNB）**：HuggingFace 生态的**在线量化**库。 - `load_in_8bit`（LLM.int8()）：基于混合精度分解，对 outlier 通道用 FP16 计算、其余用 INT8，解决 INT8 激活 outlier 问题。 - `load_in_4bit` 配合 `nf4`（NormalFloat4）：**信息论最优**的 4-bit 数据类型，专为正态分布权重设计，分位数量化。配合 double quantization（对 scale 再量化一次，再省约 0.37 bit/参数）。QLoRA 的基础。 - 定位：**训练/微调友好**（QLoRA）、**快速实验**，推理速度不如 GPTQ/AWQ 的专用 kernel，因为反量化在运行时进行。

**追问：推理部署为什么一般不用 bitsandbytes？** 因为它以「可微、可用于反向传播」为设计目标，权重以量化格式存储但每次前向都要反量化，没有针对推理优化的 fused kernel，吞吐显著低于 vLLM 的 AWQ/GPTQ/FP8 实现。生产推理首选 AWQ/GPTQ/FP8，bitsandbytes 用于微调阶段。

------------------------------------------------------------------------

## Q16：FP8 训练与推理的现状？INT4 权重的反量化开销有多大？

答：**FP8 格式**：E4M3（4 位指数、3 位尾数，动态范围大，用于权重与激活）与 E5M2（5 位指数、2 位尾数，范围更大精度更低，用于梯度）。相对 INT8 的优势：不需要 zero-point、动态范围指数级更大、与 FP16 的转换是位操作级简单。H100/H200/L40S/B200 有原生 FP8 Tensor Core。

**FP8 训练（Transformer Engine）**：核心难点是**缩放因子（scaling factor）的管理**。 - **Delayed scaling**：用历史若干步的 amax 滑动窗口估计当前 scale，延迟更新，避免每步同步开销。 - **Per-tensor scaling** 简单但精度略差；**per-tensor + per-block（microscaling, MXFP8）** 是趋势（Blackwell 原生支持 MX 格式）。 - 实践结论：FP8 混合精度训练（前向/反向的 GEMM 用 FP8，其余用 BF16）在 LLaMA 级模型上精度与 BF16 基本持平，吞吐提升 1.3-1.8 倍。

**INT4 权重的反量化开销**：W4A16 的核心问题是**权重需要先反量化成 FP16 才能与激活做 GEMM**。

``` math
\text{访存节省} = \frac{16}{4} = 4 \times ,\quad\quad\text{但需要}\ \frac{1}{g}\text{量级的 scale 读取}
```

以 $`g = 128`$ 为例，每个 group 额外读 2 字节（FP16 scale），开销为 $`2/128 = 0.0156`$ 字节/权重，相对 0.5 字节/权重（INT4）约 3%，可忽略。

真正的开销在**计算侧**： - 若采用「先反量化再 GEMM」，反量化 kernel 本身要读写一遍数据，访存翻倍，**收益可能归零**。 - 正确做法是 **fused dequant-GEMM**：在 GEMM 的 inner loop 里加载 INT4 权重后立即转 FP16 参与 tensor core 计算（如 TensorRT-LLM、Marlin、vLLM 的 AWQ/GPTQ kernel）。Marlin 通过精细的 shared memory 布局与异步拷贝，把 W4A16 做到接近 FP16 GEMM 的 3-4 倍速度。

**追问：为什么 INT4 在 decode 阶段收益明显、prefill 阶段收益小？** Decode memory-bound，权重读取是瓶颈，4 倍压缩直接 4 倍减少访存。Prefill compute-bound，主力是激活侧的 FLOPs，权重量化减少的访存不是瓶颈，且反量化增加了计算开销，净收益有限甚至为负。

------------------------------------------------------------------------

## Q17：量化对精度的影响怎么评估？有哪些坑？

答：**评估层次**： 1. **困惑度（PPL）**：最敏感、最先暴露问题。在 WikiText-2 / C4 上算，通常要求上升 \< 0.2。 2. **下游任务零样本准确率**：MMLU、ARC、HellaSwag、GSM8K。GSM8K 这种**多步推理**任务对量化最敏感，常出现 W4 掉 5-10 个点而 PPL 只涨 0.1 的情况。 3. **生成质量人工评估**：重复、截断、格式崩坏。 4. **长上下文与特殊能力**：RAG 检索准确率、代码编译通过率、多语言。长上下文场景下量化误差会被放大。 5. **鲁棒性**：不同校准集（用通用语料校准却跑代码任务）会导致明显掉点。

**常见坑**： - **只用 PPL 判断**：会漏掉推理能力退化。 - **校准集分布不匹配**：GPTQ/AWQ 的校准集应与目标业务分布接近。 - **指令微调模型 vs 基座模型**：AWQ 论文指出微调模型的激活 outlier 更强，量化更危险，需单独验证。 - **KV Cache 量化叠加权重量化**：误差累积，需联合评估。 - **不同层敏感度不同**：第一层和最后一层通常最敏感，可以保留高精度（混合精度量化）。

------------------------------------------------------------------------

# 五、蒸馏、剪枝与投机解码

## Q18：知识蒸馏在 LLM 中怎么做？和量化、剪枝的区别是什么？

答：**知识蒸馏（KD）**：让小模型（student）模仿大模型（teacher）的输出分布，而不仅是硬标签。

``` math
\mathcal{L}_{\text{KD}} = \text{KL}\left( p_{\text{teacher}}(x)^{1/T}\, \parallel \, p_{\text{student}}(x)^{1/T} \right) \cdot T^{2}
```

$`T`$ 是温度，软化分布以传递「暗知识」（如错误类别间的相对概率）。

LLM 中的变体： 1. **序列级蒸馏（SeqKD）**：用 teacher 生成的序列作为 SFT 数据训练 student。最简单，工业界最常用（如各种「小模型复现大模型能力」）。 2. **词分布蒸馏**：直接匹配 logits，需要 teacher 与 student 词表一致。 3. **中间层蒸馏**：匹配 hidden states 或 attention 矩阵（如 MiniLM 的 attention transfer）。 4. **CoT 蒸馏**：用 teacher 的推理链训练 student，能显著提升小模型的推理能力（DeepSeek-R1-Distill 系列）。 5. **On-policy 蒸馏**：student 自己生成、teacher 打分，缓解 exposure bias。

**三者区别**：蒸馏改变**模型结构**（小模型），量化改变**数值精度**（同结构），剪枝改变**参数量与稀疏结构**。三者可叠加：先剪枝 → 再蒸馏恢复精度 → 最后量化部署。

**追问：为什么蒸馏能超过直接在小模型上做 SFT？** Teacher 的软标签包含了类别间的相似性结构（如「猫」和「狗」的概率都高于「汽车」），这是一个比 one-hot 信息量更大的监督信号，起到正则化作用。另外 teacher 生成的 CoT 提供了「如何思考」的过程监督，这部分是硬标签无法提供的。

------------------------------------------------------------------------

## Q19：结构化剪枝与非结构化剪枝有什么区别？2:4 稀疏为什么重要？

答：**非结构化剪枝**：按单个权重的重要性（如 $`|w|`$、Wanda 的 $`|w| \cdot \parallel x \parallel`$）把不重要的权重置零。 - 优点：可以剪到很高稀疏度（90%+）而精度损失小。 - 缺点：产生**不规则稀疏**，通用 GPU 无法高效加速（除非常稀疏到 90%+ 才用 SpMM，且实际收益有限）。

**结构化剪枝**：剪掉整个**通道 / 注意力头 / 层 / 神经元**。 - 优点：直接缩小矩阵维度，通用硬件立刻加速，模型变小。 - 缺点：粒度粗，精度损失大，通常需要重训练/蒸馏恢复。 - 常见做法：**层剪枝**（如 Sheared LLaMA 用「每层对 loss 的贡献」决定剪哪层，再继续训练）、**头剪枝**（去掉冗余注意力头，MHA→GQA 某种意义上也是头分组）。

**2:4 稀疏（NVIDIA Ampere+）**：这是结构化与非结构化之间的**硬件折中点**。约束：每 4 个连续权重中最多 2 个非零。硬件（Sparse Tensor Core）可以跳过零权重，理论**算力翻倍**、访存减半。

``` math
\text{每 4 元素} \rightarrow \text{恰好 2 个非零} + \text{2 bit 索引元数据}
```

- 需要**稀疏感知训练 / 微调**（SR-STE、ASP 等）来恢复精度，直接后训练剪枝会掉点。
- 实测在 LLM 上，2:4 + 微调可以基本无损；但与小 batch 的 decode 场景配合时收益受限（仍是 memory-bound，稀疏主要省的是计算不是访存除非连元数据一起省）。

**追问：为什么 LLM 的剪枝比 CNN 难？** (a) LLM 参数冗余度虽高但对结构化剪枝更敏感，剪 20% 通道就可能显著掉点；(b) 重训练成本极高，无法像 CNN 那样从头训；(c) 各层重要性差异大且随任务变化；(d) 推理时的量化已能带来大部分部署收益，剪枝的边际收益相对低，因此工业界「量化优先，剪枝谨慎」。

------------------------------------------------------------------------

## Q20：投机解码（Speculative Decoding）的原理是什么？请推导期望加速比。

答：**核心思想**：Decode 是 memory-bound，GPU 算力大量空闲。用一个**便宜的小模型（draft）**先连续生成 $`\gamma`$ 个候选 token，再用**大模型（target）**一次性并行验证这 $`\gamma`$ 个位置（一次前向，代价约等于生成 1 个 token）。被接受的部分直接保留，第一个被拒绝的位置用 target 的分布重新采样。

**为什么无损**：验证步骤用「接受-拒绝采样」保证最终输出分布与 target 模型完全一致：

``` math
\text{接受概率} = \min\left( 1,\frac{p_{\text{target}}(x)}{p_{\text{draft}}(x)} \right)
```

若拒绝，则从修正分布 $`p'(x) \propto \max(0,p_{\text{target}}(x) - p_{\text{draft}}(x))`$ 中重采样。数学上可证明输出严格服从 target 分布，**因此不影响质量**（这是不同于量化/剪枝的关键优势）。

**期望加速比推导**： 设 draft 的单 token 接受率为 $`\alpha`$（每步独立同分布），draft 一次提出 $`\gamma`$ 个 token。

单轮期望接受 token 数：

``` math
E\lbrack\text{accepted}\rbrack = \sum_{i = 1}^{\gamma}\alpha^{i} = \frac{\alpha(1 - \alpha^{\gamma})}{1 - \alpha}
```

加上验证被拒后 target 重新采样的那 1 个 token，单轮产出：

``` math
E\lbrack\text{out}\rbrack = \frac{1 - \alpha^{\gamma + 1}}{1 - \alpha}
```

设 target 单次前向代价为 $`c_{t}`$（因为验证 $`\gamma`$ 个 token 与生成 1 个 token 耗时接近，是 memory-bound），draft 单次前向代价为 $`c_{d}`$（$`c_{d} \approx c_{t}/r`$，$`r`$ 是 draft 相对 target 的加速系数）。

单轮耗时 $`\approx c_{t} + \gamma c_{d} = c_{t}(1 + \gamma/r)`$。

``` math
\text{加速比} = \frac{E\lbrack\text{out}\rbrack \cdot c_{t}}{c_{t}(1 + \gamma/r)} = \frac{1 - \alpha^{\gamma + 1}}{(1 - \alpha)(1 + \gamma/r)}
```

**数值例子**：$`\alpha = 0.8`$，$`\gamma = 5`$，draft 是 target 的 1/10 大小（$`r = 10`$）。

``` math
E\lbrack\text{out}\rbrack = \frac{1 - {0.8}^{6}}{1 - 0.8} = \frac{1 - 0.2621}{0.2} = 3.69
```

``` math
\text{加速比} = \frac{3.69}{1 + 5/10} = \frac{3.69}{1.5} \approx 2.46 \times
```

**关键结论**： - 加速比主要受 $`\alpha`$ 支配。$`\alpha`$ 从 0.8 降到 0.6，$`\gamma = 5`$ 时 $`E\lbrack\text{out}\rbrack = \frac{1 - {0.6}^{6}}{0.4} = 2.33`$，加速比降到 1.55。 - $`\gamma`$ 有最优值：$`\gamma`$ 太大时，边际接受概率 $`\alpha^{\gamma}`$ 衰减，但 draft 成本线性增长。 - $`\alpha`$ 与 **draft 与 target 的分布接近程度**高度相关。同系列小模型（如 LLaMA-3 8B draft + 70B target）$`\alpha`$ 可达 0.7-0.9。

**追问 1：draft 模型怎么选？** (a) **同系列小模型**：分布最接近、$`\alpha`$ 高，最简单有效；(b) **EAGLE / Medusa 式的「自带 draft 头」**：见下题；(c) **N-gram / 检索式 draft**：用 prompt 里的重复片段或检索库做 draft，零额外模型成本，对摘要、改写、代码补全（大量复制原文）场景 $`\alpha`$ 极高。

**追问 2：投机解码在什么情况下会变慢？** (a) $`\alpha`$ 低（draft 与 target 差距大、任务陌生）；(b) **大 batch 场景**：batch 已经很大时 target 已进入 compute-bound，验证 $`\gamma`$ 个 token 不再是「免费」，加速比急剧下降甚至为负——这是最重要的工程约束，投机解码主要服务于**低 batch / 低延迟**场景；(c) prefill 阶段无收益；(d) draft 模型额外占用显存。

------------------------------------------------------------------------

## Q21：Medusa、Lookahead Decoding、EAGLE 分别怎么改进投机解码？

答：三者都解决同一个痛点：**独立 draft 模型成本高、且 draft 与 target 分布有偏差**。

**Medusa（2023）**：不引入独立 draft 模型，而是在 target 模型的最后一层 hidden state 上挂 **多个解码头（Medusa Heads）**，每个头预测「未来第 $`k`$ 个位置」的 token（$`k = 1,2,3,4`$）。 - 多个头并行预测 → 组成一棵候选树（用 top-k + 笛卡尔积）。 - 用 **Tree Attention** 一次性验证整棵树（构建树形 attention mask，让每个节点只能看到自己的祖先）。 - 用 **典型接受（typical acceptance）** 而非严格接受-拒绝：当 target 概率高于阈值（如 $`\max(0.09,\epsilon \cdot e^{- H})`$，$`H`$ 为熵）就接受，避免在高温采样时接受率过低。这是**有损**的，但实践中质量影响可忽略。 - 优点：不改变 base 模型，训练成本低（只训头），部署时单模型。

**Lookahead Decoding（2023，JAX）**：完全**无需额外模型或训练**。 - 维护一个 $`(n,n)`$ 的 **Jacobi 迭代轨迹窗口**，并行地在多个位置做「猜测」。 - 用 n-gram 池缓存历史出现的 $`(token_{i},...,token_{j}) \rightarrow token_{j + 1}`$ 映射。 - 每步：从 n-gram 池里查表生成候选（Lookahead Branch），用 target 并行验证（Verification Branch），接受最长匹配前缀。 - 优点：零训练、零额外显存、对任意模型可用；缺点：加速比通常低于 EAGLE（1.5-2x），且对文本重复性依赖强。

**EAGLE（2023-2024）**：目前效果最好的方案之一。 - 核心洞察：在**特征空间（hidden state）**做自回归比在 **token 空间**做自回归更容易——因为 hidden state 包含更丰富的信息，且避免了采样噪声。 - 结构：EAGLE 头接收「target 模型的倒数第二层 hidden state + 上一 token 的 embedding」，预测下一个 hidden state，再经 target 的 LM head 得到 token 分布。 - **EAGLE-2**：加入**动态 draft 树**，根据上一轮的接受情况自适应调整树的形状和深度（用「置信度」估计每个候选的期望接受长度），进一步提速。 - **EAGLE-3**：多层特征融合、训练时用 test-time 模拟，加速比可达 **3-6 倍**，接受长度 $`\tau`$ 常在 3.5-5。 - 代价：需要单独训练 draft 头（在 target 的 hidden state 上），有一定工程复杂度。

**对比总结**：

| 方法         | 额外模型   | 需要训练       | 典型加速比 | 特点               |
|--------------|------------|----------------|------------|--------------------|
| 标准投机解码 | 独立小模型 | 否（现成模型） | 1.5-2.5x   | 最简单、无损       |
| Medusa       | 解码头     | 是（轻量）     | 2-3x       | 单模型、树验证     |
| Lookahead    | 无         | 否             | 1.5-2x     | 零成本、依赖重复性 |
| EAGLE-3      | draft 头   | 是             | 3-6x       | 效果最好、工程复杂 |

**追问：为什么 EAGLE 比标准投机解码快？** 因为 draft 步骤的成本极低：EAGLE 头只是一个单层 Transformer，每步只处理 1 个 token 的 hidden state，成本远低于跑一个小语言模型的完整 forward。同时它利用了 target 的中间特征，分布更接近 target，$`\alpha`$ 更高。两个因素叠加，使 $`r`$（draft/target 成本比）极大而 $`\alpha`$ 保持高位。

------------------------------------------------------------------------

# 六、并行与分布式推理

## Q22：推理场景下 TP / PP / EP 分别怎么用？通信开销如何？

答：**张量并行（TP）**：把一个层的权重矩阵按行/列切分到多卡。 - 列并行：$`Y = \lbrack XW_{1},XW_{2}\rbrack`$，各卡算部分输出，然后 all-gather。 - 行并行：$`Y = X_{1}W_{1} + X_{2}W_{2}`$，各卡算部分和，然后 all-reduce。 - Transformer 中标准组合：Attention 的 QKV 用列并行、输出投影用行并行；FFN 的 up/gate 用列并行、down 用行并行。每层 2 次 all-reduce。 - 通信量：每层每次 all-reduce 传输 $`2 \cdot \frac{(b - 1)}{b} \cdot B \cdot S \cdot d \cdot \text{bytes}`$（ring all-reduce 的 2(b-1)/b 系数）。因为是**每层都要通信**，延迟敏感，**必须放在 NVLink 域内**（单机 8 卡），跨机 TP 性能急剧下降。

``` math
\text{TP 通信量/层} \approx 2 \times 2 \times B \cdot S \cdot d \cdot b
```

**流水并行（PP）**：把模型按层切成若干 stage，每卡一个 stage。 - 通信量小：只在 stage 边界传递 $`B \cdot S \cdot d \cdot b`$ 的激活。 - 但存在 **bubble（气泡）**：传统 PP 下 GPU 大量空转。$`\text{bubble 比例} \approx \frac{p - 1}{m + p - 1}`$（$`p`$ 为 stage 数，$`m`$ 为 micro-batch 数）。 - 推理优化：**micro-batch 流水**（把 batch 切小、连续灌入）、**interleaved PP**、以及推理特有的 **one-forward-one-backward（1F1B）** 变体。 - 适合跨机部署，因为通信不频繁、对延迟不敏感。

**专家并行（EP）**：MoE 模型专用。把不同专家放到不同卡。 - 通信是 **all-to-all**：每个 token 要被路由到它选中的专家所在卡，算完再 all-to-all 回来。 - 通信量与「每 token 选中的专家数 $`k`$」和负载均衡程度相关。 - 好处：MoE 总参数量巨大但每 token 激活参数少，EP 是唯一可行的部署方式（如 DeepSeek-V3 671B 总参数、37B 激活，用 EP 部署）。 - 难点：**负载不均衡**（热门专家被打爆）+ all-to-all 通信开销。

**对比**：

| 维度     | TP          | PP            | EP                   |
|----------|-------------|---------------|----------------------|
| 切分方式 | 层内矩阵    | 层间          | 专家                 |
| 通信频率 | 每层        | 每 stage      | 每 MoE 层            |
| 通信原语 | all-reduce  | p2p send/recv | all-to-all           |
| 通信量   | 大          | 小            | 中-大                |
| 网络要求 | NVLink      | 以太网可      | 高带宽（all-to-all） |
| 适用     | 单机 8 卡内 | 跨机          | MoE 模型             |

**追问：为什么推理的 PP 比训练的 PP 更难做？** 训练时 micro-batch 可以切得很小且总有数据灌入，bubble 可控。推理时 batch 大小由线上负载决定，低负载时 micro-batch 不够，气泡巨大，而且推理对延迟极其敏感（不能靠堆积 batch 来填流水线）。因此推理场景常用 **TP 为主、PP 为辅、EP 用于 MoE** 的组合，并且 PD 分离后 P 池和 D 池可以各自选择并行策略（P 池偏好 TP+PP，D 池偏好 TP+EP）。

------------------------------------------------------------------------

## Q23：给定模型和卡数，如何规划部署？（以 LLaMA-70B + 8×A100 80G 为例）

答：**第一步：算权重显存**。

``` math
\text{权重显存} = N_{\text{param}} \times \text{bytes/param}
```

- FP16：$`70 \times 10^{9} \times 2 = 140`$ GB
- INT8：70 GB
- INT4（AWQ/GPTQ）：35 GB

**第二步：选并行策略**。8 卡 A100 80G 共 640 GB HBM。FP16 下权重 140 GB，若用 **TP=8**，每卡 17.5 GB，剩余 62.5 GB 给 KV Cache 和其他开销。这是 70B 单机部署的标准配置。

**第三步：算 KV Cache 容量**。LLaMA-70B GQA：$`H_{kv} = 8`$，$`d_{head} = 128`$，$`L = 80`$，FP16。

``` math
\text{KV/token} = 2 \times 80 \times 8 \times 128 \times 2 = 320\ \text{KB}
```

除去框架开销（vLLM 约 2 GB）、激活与 CUDA Graph（约 3 GB），每卡可用于 KV 约 $`80 - 17.5 - 5 = 57.5`$ GB。

``` math
\text{总 KV 容量} = \frac{57.5 \times 1024}{0.3125} \approx 188,416\ \text{tokens（每卡）}
```

注意：**KV Cache 在 TP 下是按头切分的**，8 卡各存 $`H_{kv}/8 = 1`$ 个 KV 头，所以总容量是每卡容量（因为每卡只存 1/8 的 KV，但每卡也只用 1/8 的空间）。实际上：总 token 容量 = 单卡可用 KV / 单卡每 token KV。

单卡每 token KV $`= 2 \times 80 \times 1 \times 128 \times 2 = 40,960`$ B $`= 0.04`$ MB。

``` math
\text{总容量} = \frac{57.5 \times 1024}{0.04} \approx 1,472,000\ \text{tokens}
```

若平均序列长度 $`S = 4096`$，则

``` math
B_{\max} = \frac{1,472,000}{4096} \approx 359
```

但这是**显存上限**，实际并发还受限于算力（每步 token 预算、调度器限制）。实践中 vLLM 会用 `gpu_memory_utilization=0.9` 自动分配，并报告 `maximum concurrency`。

**第四步：估算吞吐**。Decode 单步理论耗时（TP=8，每卡读 17.5 GB 权重）：

``` math
T_{\text{step}} \approx \frac{17.5\ \text{GB}}{2039\ \text{GB/s}} \approx 8.6\ \text{ms}
```

加上 KV 读取、通信、kernel launch 开销，实测单步约 **15-25 ms**。若 $`B = 128`$，则

``` math
\text{吞吐} = \frac{128\ \text{tokens}}{0.02\ \text{s}} = 6400\ \text{tokens/s}
```

这是单机的量级参考。

**第五步：优化决策树**。 - 显存不够 → INT4 量化权重（省 4 倍）+ FP8 KV Cache（省 2 倍）。 - 延迟不够 → 投机解码、减小 TP（减少通信）、PD 分离。 - 吞吐不够 → 增 batch、Chunked Prefill、prefix caching。 - 成本太高 → 混合部署（小模型承接简单请求）、量化、扩缩容。

**追问：TP=8 一定比 TP=4 好吗？** 不一定。TP 越大，每层 all-reduce 的参与者越多、通信开销越大（虽然 ring all-reduce 通信量近似常数，但**延迟随卡数上升**），且单卡算力被稀释（每卡的计算量变小，kernel 变小 → 效率降低）。TP=4 时每卡权重 35 GB，剩 45 GB 给 KV，仍然够用，且通信延迟更低。经验上选择「能装下模型的最小 TP」，再根据 KV 需求和延迟指标调整。70B 用 TP=4 常比 TP=8 更快（延迟维度）。

------------------------------------------------------------------------

# 七、服务化工程

## Q24：推理服务的核心指标有哪些？怎么做压测？

答：**核心指标**： 1. **TTFT（P50/P90/P99）**：首 token 延迟，决定「感知响应速度」。 2. **TPOT / ITL（Inter-Token Latency）**：每 token 延迟，决定「打字流畅度」。用户能感知卡顿的阈值约 50-100 ms。 3. **E2E Latency**：$`= \text{TTFT} + (S_{\text{out}} - 1) \times \text{TPOT}`$。 4. **吞吐**：tokens/s（总）、requests/s、tokens/s/GPU。 5. **QPS / 并发数**：注意 QPS 与并发的换算 $`\text{并发} = \text{QPS} \times \text{E2E}`$（Little’s Law）。 6. **GPU 利用率**：SM 占用率、显存带宽利用率、MFU（Model FLOPs Utilization）。Decode 场景 MFU 常低于 5%，这是正常现象（memory-bound）。 7. **SLA**：如 TTFT P99 \< 1s、TPOT P99 \< 100 ms。 8. **错误率 / 超时率 / OOM 次数**。

**压测方法**： - 工具：**vLLM benchmark_serving**、**Locust**、**wrk/k6**、**genai-perf（NVIDIA）**、**LLMPerf**。 - 关键：**用真实的输入/输出长度分布**，而不是固定长度。因为 TTFT 由输入长度主导、E2E 由输出长度主导，长度分布的尾部决定 P99。 - 必须报告：并发扫描（1, 4, 8, 16, 32, 64, 128…），画出「并发 vs 吞吐 vs P99 延迟」三条曲线，找 SLA 约束下的最优点。 - 区分 **固定并发（closed-loop）** 与 **固定 QPS（open-loop）**：后者更接近真实流量，能暴露排队问题。

**追问：为什么不能只看平均延迟？** 因为 LLM 请求的长度是重尾分布，且调度是批处理。长请求、prefill 尖刺、KV 压力都会在 P99 上放大。平均值掩盖了用户真实感受到的最差体验。工程上应以 P95/P99 作为 SLA 基准，并关注「尾部延迟随时间的变化」（是否有随缓存膨胀而恶化的趋势）。

------------------------------------------------------------------------

## Q25：推理服务 OOM 和显存碎片怎么排查？

答：**OOM 的分类与排查**：

1.  **权重 + KV + 激活超过 HBM**。
    - 排查：`nvidia-smi`、`torch.cuda.memory_summary()`、vLLM 启动日志里的 `GPU blocks: N, CPU blocks: M`。
    - 处理：降低 `gpu_memory_utilization`、量化权重、减小 `max_model_len`、限制 `max_num_seqs`、启用 FP8 KV。
2.  **显存碎片**。PyTorch caching allocator 保留已释放的块，长时间运行后可能出现「总空闲够但无连续大块」。
    - 缓解：`PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True`；避免动态 shape 频繁变化；固定 `max_num_batched_tokens`；启用 CUDA Graph（固定地址，减少动态分配）。
    - vLLM 场景：PagedAttention 已经把 KV 分页化，碎片主要出现在权重/激活侧，以及**长时间运行后 block table 的元数据膨胀**。
3.  **KV Cache 撑爆**：并发突发、超长上下文请求、多轮对话历史累积。
    - 处理：`max_model_len` 硬限制、请求准入控制（admission control，显存不足时排队而非直接分配）、抢占（preemption）——vLLM 支持 recompute 与 swap 两种抢占策略：swap 把 KV 换到 CPU（慢但保留计算），recompute 直接丢弃并重算（省显存）。
4.  **内存泄漏**：Python 侧的引用未释放、CUDA Graph 捕获泄漏、未释放的临时张量。
    - 排查：观察显存是否随时间单调上升；用 `torch.cuda.memory_allocated()` 与 `memory_reserved()` 对比（差值大说明碎片/缓存问题）。
5.  **多进程/多卡**：TP 各 rank 显存不均（rank 0 常多存 embedding、输出层、采样相关张量）。
    - 处理：vLLM 有 `--disable-log-stats` 等开关，TensorRT-LLM 用 `pp` 均衡；实际中给 rank 0 预留更多。

**追问：**`gpu_memory_utilization=0.9` **是越高越好吗？** 不是。太高会导致：(a) 与其他进程（如监控、其他模型）争抢；(b) CUDA Graph 捕获、激活峰值时 OOM；(c) 系统预留不足，驱动层分配失败。0.85-0.92 是常见区间。在 PD 分离的 D 池上可以适当调高，在混部环境要调低。

------------------------------------------------------------------------

## Q26：模型加载与冷启动怎么优化？多模型混部怎么做？

答：**冷启动优化**： 1. **权重加载加速**： - 用 **safetensors** 替代 pickle，支持 mmap 零拷贝加载。 - 从 **本地 NVMe / 内存盘**读取，避免每次拉对象存储。 - **并行加载**：多线程/多进程读分片（`max_workers`），TP 各 rank 只加载自己需要的分片（vLLM/TensorRT-LLM 支持）。 - 量化格式加载更快（INT4 文件更小）。 2. **编译与预热**： - **CUDA Graph 预热**：提前捕获常用 batch size / sequence length 组合的图，避免首请求触发捕获（耗时数秒）。 - **TensorRT engine 预编译并缓存**，避免在线 build（70B 可能要 10+ 分钟）。 - **预热请求**：服务就绪前先跑一批合成请求，把 kernel autotune、显存池、缓存都热起来。 3. **镜像与依赖**：合并层的镜像、预下载 tokenizer 与 chat template、避免运行时联网。 4. **弹性策略**：**保持最小副本常驻**（避免缩容到 0）、用 **预留实例 + 突发实例**、把冷启动时间纳入扩缩容的决策（扩容要提前于负载上升）。

**多模型混部**： 1. **时间片轮转/共享 GPU**：多个模型共用一个 GPU，用 HTTP 层调度切分时间。简单但互相干扰，延迟抖动大。MPS / MIG 是硬件级隔离方案（MIG 可把一个 A100 切成 7 个实例）。 2. **大小模型分层路由**：一个小的「路由模型」判断请求难度，简单请求（闲聊、分类、改写）走小模型，复杂请求走大模型。可显著降低平均成本。 3. **共享底层 + LoRA 适配器**：多个业务微调模型共享同一个 base model 的权重，只挂不同的 LoRA adapter。vLLM 支持动态加载/卸载 adapter，显存开销极小（几十 MB 级），切换开销低。这是**多租户场景的最优解**。 4. **PD 分离 + 异构资源**：P 池用高算力卡、D 池用大显存卡，甚至 D 池可以用较老的卡（decode memory-bound，对算力要求低）。

**追问：LoRA 混部时 batch 怎么组织？** vLLM 的 `--enable-lora` 支持在一个 batch 内混合不同 adapter 的请求。实现上是把不同 adapter 的请求分组，用 **SGMV（Segmented Gather Matrix-Vector）** kernel 一次算完所有组的 LoRA 增量：$`\Delta y = \sum_{g}^{}(x_{g}A_{g})B_{g}`$。这样 batch 内不同 adapter 的请求可以共存，吞吐损失很小（比单独的 PagedAttention 慢 5-15% 左右）。

------------------------------------------------------------------------

# 八、成本优化

## Q27：如何计算单位 token 成本？怎么做性价比选型？

答：**成本公式**：

``` math
\text{成本/1M tokens} = \frac{\text{每小时机器成本}}{\text{每小时产出 tokens}} \times 10^{6}
```

**具体计算**（A100 80G 云上约 \$1.5-3/h，取 \$2/h）：

某 70B 服务，单机（8×A100）实测吞吐 4000 tokens/s（decode），则

``` math
\text{每小时产出} = 4000 \times 3600 = 1.44 \times 10^{7}\ \text{tokens}
```

``` math
\text{单机成本} = 8 \times 2 = 16\ \text{\$/h}
```

``` math
\text{成本/1M tokens} = \frac{16}{14.4} \approx \$ 1.11
```

若用 INT4 量化把并发翻 2.5 倍、吞吐升到 10000 tokens/s：

``` math
\text{成本/1M tokens} = \frac{16}{36} \approx \$ 0.44
```

**注意区分**： - **Prefill 成本**由输入 token 决定（compute-bound，算力密集型）； - **Decode 成本**由输出 token 决定（memory-bound，带宽密集型）。 - 典型商用 API 的输出价格是输入的 3-4 倍，正是这个原因。**缓存命中的输入价格可以低到 1/10**，因为省掉了 prefill 计算。

**性价比选型决策**： 1. **先看任务难度分布**：如果 80% 请求是小模型能解决的，用 8B 模型处理、20% 路由到 70B，平均成本可能降 5-10 倍。 2. **算「每美元的能力」**：不是模型越小越便宜。如果 7B 模型在任务上准确率只有 60% 需要重试，而 70B 一次成功，70B 的有效成本可能更低。要按「成功完成一个任务的成本」而非「每 token 成本」来算。 3. **考虑 SLA**：延迟敏感场景不能无限堆 batch，成本会更高。 4. **考虑缓存**：长 system prompt / RAG 场景下 prefix caching 能把成本降一个数量级。 5. **考虑硬件**：L40S / A10 / 4090 在 decode 场景的性价比常优于 A100/H100（H100 的算力优势在 memory-bound 的 decode 上发挥不出来），除非需要 FP8 或 NVLink 大 TP。

**追问：为什么 H100 在 decode 上性价比未必高？** H100 相对 A100：算力约 3 倍（FP16 dense 989 vs 312 TFLOPS），但**显存带宽只提升 1.6 倍**（3.35 vs 2.0 TB/s）。Decode 是 memory-bound，主要吃带宽，因此 H100 的 decode 吞吐仅约 A100 的 1.6-1.8 倍，而价格贵 2-3 倍。所以纯 decode 场景 A100/L40S 性价比更高；H100 的价值在于 FP8、大 KV 容量、prefill 算力，以及需要 NVLink 做大规模 TP/EP 的场景。

------------------------------------------------------------------------

## Q28：请求路由（大小模型分流）怎么设计？

答：**架构**：请求 → 路由层 → {小模型池, 中模型池, 大模型池}。

**路由信号**： 1. **规则**：请求来源、token 数、是否带工具、是否有图片、业务标签。最快、可解释。 2. **小分类器**：用一个几百 M 的模型（或 BERT 级）预测「难度」或「哪个模型能答对」。成本极低（\< 1% 的推理成本），可作为前置。 3. **级联（cascade）**：先小模型答，用**置信度 / 自一致性 / 验证器**判断是否可靠，不可靠再升级到大模型。准确性最高，但简单请求延迟会累加。 4. **语义路由（如 RouteLLM）**：用偏好数据训练一个二分类器，直接预测「大模型是否显著更好」，在成本与质量间做最优权衡。 5. **基于缓存的路由**：优先路由到「prefix 已缓存」的节点（Mooncake 的核心思想）。

**关键设计点**： - **路由错误的代价不对称**：把小请求误判给大模型只是浪费钱，把难请求误判给小模型会伤害体验。因此阈值应偏向保守。 - **要监控路由准确率**与各模型池的质量指标（用户反馈、重试率、人工抽检）。 - **降级链**：大模型池过载时，自动降级到中/小模型 + 排队提示，而不是直接 503。 - **实测收益**：RouteLLM 等论文报告在保持 95% 的 GPT-4 质量下，可降低成本 50-85%。

------------------------------------------------------------------------

# 九、手撕 / 计算题

## Q29（计算题）：推导 Attention 的 FLOPs 与算术强度

答：设序列长度 $`S`$、注意力头数 $`H`$、每头维度 $`d`$、batch 为 1，忽略 bias 与 softmax 的非线性部分。

**单头 Attention 的计算**：

``` math
\text{Attention}(Q,K,V) = \text{softmax}\left( \frac{QK^{\top}}{\sqrt{d}} \right)V
```

其中 $`Q,K,V \in \mathbb{R}^{S \times d}`$。

1.  $`QK^{\top}`$：$`\lbrack S,d\rbrack \times \lbrack d,S\rbrack \rightarrow \lbrack S,S\rbrack`$，FLOPs $`= 2S^{2}d`$。
2.  除以 $`\sqrt{d}`$ 与 softmax：约 $`O(S^{2})`$，可忽略（相对于 $`S^{2}d`$）。
3.  $`\text{scores} \cdot V`$：$`\lbrack S,S\rbrack \times \lbrack S,d\rbrack \rightarrow \lbrack S,d\rbrack`$，FLOPs $`= 2S^{2}d`$。

单头合计 $`4S^{2}d`$。$`H`$ 个头：

``` math
\text{FLOPs}_{\text{core}} = 4S^{2}dH = 4S^{2}d_{h}H
```

若记 $`d_{\text{model}} = H \cdot d_{h}`$，则

``` math
\text{FLOPs}_{\text{core}} = 4S^{2}d_{\text{model}}
```

**QKV 与输出投影**（每个都是 $`\lbrack S,d_{\text{model}}\rbrack \times \lbrack d_{\text{model}},d_{\text{model}}\rbrack`$）：

- Q、K、V 投影（K、V 输出维度是 $`H_{kv}d_{h}`$）：$`2Sd_{\text{model}}^{2} + 2 \cdot 2Sd_{\text{model}}H_{kv}d_{h}`$
- 输出投影：$`2Sd_{\text{model}}^{2}`$

**FFN（SwiGLU）**：三个矩阵 $`d_{\text{model}} \rightarrow d_{ff}`$ 与 $`d_{ff} \rightarrow d_{\text{model}}`$（SwiGLU 有两个 up 投影）：

``` math
\text{FLOPs}_{\text{FFN}} = 2 \cdot 3 \cdot S \cdot d_{\text{model}} \cdot d_{ff} = 6Sd_{\text{model}}d_{ff}
```

常用 $`d_{ff} = 4d_{\text{model}}`$（LLaMA 用 $`\frac{8}{3}d_{\text{model}}`$），则 $`\text{FLOPs}_{\text{FFN}} \approx 24Sd_{\text{model}}^{2}`$。

**整模型**：

``` math
\text{FLOPs}_{\text{total}} \approx L\left( 4S^{2}d_{\text{model}} + 24Sd_{\text{model}}^{2} \right) \cdot 3
```

最后乘 3 是训练时的前向+反向（约 3 倍前向）。推理只有前向，不加这个系数。

**推理的经验公式**（$`S`$ 较小时，$`S^{2}`$ 项可忽略）：

``` math
\text{FLOPs}_{\text{infer}} \approx 2 \cdot N_{\text{param}} \cdot S
```

（前向：每个参数贡献 2 FLOPs 乘加，乘 token 数。）

**算术强度**： - **Prefill**：计算 $`2NS`$ FLOPs，访存需要读全部权重 $`N \cdot b`$ 字节 + KV。$`S`$ 大时：

``` math
\text{AI}_{\text{prefill}} \approx \frac{2NS}{Nb} = \frac{2S}{b}
```

$`b = 2`$（FP16）：$`\text{AI} = S`$。当 $`S > 153`$ 时超过 A100 的机器平衡点 → **compute-bound**。

- **Decode**：每步 1 个 token，计算 $`2N`$ FLOPs，访存 $`Nb`$ 字节：

``` math
\text{AI}_{\text{decode}} = \frac{2N}{Nb} = \frac{2}{b} = 1\ (\text{FP16})
```

远低于 153 → **memory-bound**。这从第一性原理上解释了 Q1。

**追问：FlashAttention 减少了 FLOPs 吗？** 没有。FlashAttention 通过 **tiling + online softmax** 避免了把 $`S \times S`$ 的注意力矩阵写回 HBM，把访存从 $`O(S^{2})`$ 降到 $`O(S^{2}d^{2}/M)`$（$`M`$ 为 SRAM 大小）。它是 IO-aware 的优化，减少的是 HBM 读写而非 FLOPs，因此在 memory-bound 的注意力上获得 2-4 倍加速，并让显存占用从 $`O(S^{2})`$ 降到 $`O(S)`$。

------------------------------------------------------------------------

## Q30（计算题）：给定 LLaMA-70B、A100 80G×8，求 KV Cache 显存、最大并发与吞吐上限

答：见 Q23 的完整推导，此处给出**可直接口述的速算口径**。

**模型参数（LLaMA-2/3 70B）**：$`L = 80`$、$`H = 64`$、$`H_{kv} = 8`$（GQA）、$`d_{h} = 128`$、$`d_{\text{model}} = 8192`$。

**1) 单 token KV（FP16）**：

``` math
2 \times L \times H_{kv} \times d_{h} \times 2 = 2 \times 80 \times 8 \times 128 \times 2 = 327,680\ \text{B} = 320\ \text{KB}
```

**2) 权重显存**：FP16 = 140 GB；INT8 = 70 GB；INT4 = 35 GB。

**3) TP=8 每卡分配**：权重 $`140/8 = 17.5`$ GB；每卡 KV 头 $`= 8/8 = 1`$，单卡每 token KV $`= 2 \times 80 \times 1 \times 128 \times 2 = 40,960`$ B $`= 40`$ KB。

**4) 每卡可用 KV**：$`80 \times 0.9 - 17.5 - 3 \approx 51.5`$ GB（框架+激活预留约 3 GB）。

``` math
\text{Token 容量} = \frac{51.5 \times 1024}{0.04} \approx 1,318,400\ \text{tokens}
```

**5) 最大并发**（序列长 $`S`$）：

| $`S`$ | 最大并发（FP16 KV） | FP8 KV |
|-------|---------------------|--------|
| 2048  | 643                 | 1286   |
| 4096  | 321                 | 643    |
| 8192  | 160                 | 321    |
| 32768 | 40                  | 80     |

**6) 吞吐上限（TP=8，Decode）**：每步每卡读 17.5 GB 权重，A100 带宽 2039 GB/s：

``` math
T_{\text{ideal}} = \frac{17.5}{2039} \approx 8.6\ \text{ms},\quad\quad T_{\text{real}} \approx 15\text{-}25\ \text{ms}
```

$`B = 128`$、$`T = 20`$ ms 时：

``` math
\text{吞吐} = \frac{128}{0.020} = 6400\ \text{tokens/s}
```

单机 8 卡，即 **800 tokens/s/GPU**。若 INT4 量化权重（每卡 4.4 GB），理论单步降到 \$\$2.2 ms，实际约 8-12 ms，吞吐可到 12000+ tokens/s。

**关键结论速记**： - KV/token（70B GQA FP16）= **320 KB**，这是最重要的常数。 - 权重 = 参数量 × 字节数（FP16 2、INT8 1、INT4 0.5）。 - 并发上限 ≈ 可用 KV 显存 / (序列长 × 每 token KV)。 - Decode 单步时间下界 ≈ 每卡权重字节 / 带宽。

------------------------------------------------------------------------

## Q31（计算题）：投机解码期望加速比的完整推导与敏感性分析

答：完整公式推导见 Q20，此处做敏感性分析。

``` math
\text{Speedup}(\alpha,\gamma,r) = \frac{1 - \alpha^{\gamma + 1}}{(1 - \alpha)(1 + \gamma/r)}
```

**场景 A：draft 为 target 的 1/10 成本（**$`r = 10`$**），**$`\alpha = 0.8`$

| $`\gamma`$ | $`E\lbrack\text{out}\rbrack`$ | 分母 $`1 + \gamma/r`$ | 加速比   |
|------------|-------------------------------|-----------------------|----------|
| 1          | 1.80                          | 1.1                   | 1.64     |
| 3          | 2.95                          | 1.3                   | 2.27     |
| 5          | 3.69                          | 1.5                   | **2.46** |
| 7          | 4.16                          | 1.7                   | 2.45     |
| 10         | 4.46                          | 2.0                   | 2.23     |

最优 $`\gamma \approx 5\text{-}7`$，加速比约 **2.5×**。

**场景 B：**$`\alpha = 0.6`$**，**$`r = 10`$

| $`\gamma`$ | $`E\lbrack\text{out}\rbrack`$ | 加速比 |
|------------|-------------------------------|--------|
| 3          | 1.96                          | 1.51   |
| 5          | 2.33                          | 1.55   |
| 8          | 2.50                          | 1.39   |

最优加速比只有 **1.55×**。可见 $`\alpha`$ 是最关键的参数。

**场景 C：**$`\gamma \rightarrow \infty`$ **的极限**

``` math
\lim_{\gamma \rightarrow \infty}\text{Speedup} = \frac{1}{(1 - \alpha)} \cdot \frac{r}{\gamma}|_{\text{发散}}
```

实际上因为 $`1 + \gamma/r`$ 线性增长而 $`E\lbrack\text{out}\rbrack`$ 收敛到 $`\frac{1}{1 - \alpha}`$，加速比在 $`\gamma`$ 大时反比于 $`\gamma`$，必然下降。对 $`\gamma`$ 求导可得最优条件近似：

``` math
\alpha^{\gamma} \approx \frac{1}{1 + r}
```

**场景 D：EAGLE 式的极小 draft 成本（**$`r = 100`$**，只跑一个单层头）**，$`\alpha = 0.8`$：

| $`\gamma`$ | 加速比 |
|------------|--------|
| 3          | 2.91   |
| 5          | 3.60   |
| 7          | 3.98   |

可以达到 **4× 左右**，与 EAGLE-3 报告的 3-6 倍吻合。

**关键结论**： 1. 加速比 ≈ $`\min\left( \frac{1}{1 - \alpha},r \right)`$ 量级，两个因素缺一不可。 2. 提高 $`\alpha`$ 的手段：选分布更接近的 draft、用 EAGLE 特征级 draft、用 n-gram 辅助。 3. 降低 $`c_{d}`$ 的手段：用更小的 draft、单层头、共享 target 的 embedding。 4. **大 batch 时加速比消失**：因为 $`c_{t}`$ 不再被「验证 $`\gamma`$ 个 token 的额外成本」摊掉，验证变成实打实的计算。设 batch 已经很大（compute-bound），验证 $`\gamma`$ 个 token 的成本是 $`c_{t} \times \gamma`$（approximate），则加速比变为

``` math
\frac{E\lbrack\text{out}\rbrack}{\gamma + \gamma/r} \rightarrow 0\ \text{当}\ \gamma\ \text{大}
```

这就是为什么投机解码只在**低并发、低延迟**场景有效（如单用户 IDE 补全、实时对话）。

------------------------------------------------------------------------

## Q32（计算题）：Batch 大小与吞吐/延迟的定量关系

答：设模型 $`N`$ 参数、每元素 $`b`$ 字节、GPU 带宽 BW、FP16 算力 $`F`$。

**单步耗时**：

``` math
T(B) = \max\left( \frac{2BN}{F},\frac{Nb + \text{KV}(B)}{\text{BW}} \right)
```

简化为 $`T(B) = \max(c_{1}B,c_{0})`$，其中

``` math
c_{1} = \frac{2N}{F},\quad\quad c_{0} = \frac{Nb}{\text{BW}}
```

**吞吐**：

``` math
\text{Throughput}(B) = \frac{B}{T(B)} = \min\left( \frac{B}{c_{1}B},\frac{B}{c_{0}} \right) = \min\left( \frac{1}{c_{1}},\frac{B}{c_{0}} \right)
```

**代入 70B / A100 / TP=8**： - $`N_{\text{per card}} = 8.75 \times 10^{9}`$ 参数 - $`c_{1} = \frac{2 \times 8.75 \times 10^{9}}{312 \times 10^{12}} = 5.6 \times 10^{- 5}`$ s/token = **0.056 ms/token** - $`c_{0} = \frac{8.75 \times 10^{9} \times 2}{2039 \times 10^{9}} = 8.58 \times 10^{- 3}`$ s = **8.58 ms**

**拐点**：$`B^{*} = c_{0}/c_{1} = \frac{8.58}{0.056} = 153`$。

- $`B < 153`$：memory-bound，$`T = 8.58`$ ms 恒定，吞吐 $`= B/0.00858`$，随 $`B`$ 线性上升。$`B = 32`$ 时吞吐 3729 tokens/s，$`B = 128`$ 时 14918 tokens/s。
- $`B > 153`$：compute-bound，$`T = 0.056B`$ ms，吞吐饱和在 $`\frac{1}{c_{1}} = 17857`$ tokens/s。

**延迟**：$`T(B)`$ 在 $`B < 153`$ 时恒定（延迟不随 batch 恶化，这是 batching 免费的部分），$`B > 153`$ 后线性上升。

``` math
\text{Latency}(B) = \left\{ \begin{matrix}
c_{0} & B \leq 153 \\
c_{1}B & B > 153
\end{matrix} \right.\ 
```

**结论**：在 memory-bound 区间内加 batch 是**免费吞吐**；超过拐点后是**延迟换吞吐**。工程上最优工作点通常在拐点附近（$`B \approx 100\text{-}200`$），既能拿到大部分吞吐，又保持延迟可控。

**追问：现实中为什么达不到这个理论上限？** (a) KV Cache 读取也要占带宽（长序列时占比显著）；(b) all-reduce 通信（TP=8 时每层两次）；(c) kernel launch 与调度开销；(d) 采样（top-k/top-p）是 memory-bound 的小 kernel，开销不低；(e) 显存分配与碎片；(f) 序列长度不均导致 padding/气泡。因此实测 MFU 与理论值常有 2-3 倍差距。

------------------------------------------------------------------------

## Q33（计算题）：量化能省多少显存和多少钱？

答：以 70B 模型、8×A100 80G 为例。

**权重显存**：

| 精度                 | 每参数字节 | 权重总量 | 每卡（TP=8） |
|----------------------|------------|----------|--------------|
| FP16                 | 2          | 140 GB   | 17.5 GB      |
| INT8                 | 1          | 70 GB    | 8.75 GB      |
| INT4（+scale 约 3%） | 0.53       | 37 GB    | 4.6 GB       |

**KV Cache**：

| 精度       | 每 token（70B GQA） | 相对 FP16 |
|------------|---------------------|-----------|
| FP16       | 320 KB              | 1×        |
| INT8 / FP8 | 160 KB              | 2×        |
| INT4       | 80 KB               | 4×        |

**综合收益（TP=8，每卡 80 GB，预留 4 GB）**：

| 配置           | 每卡权重 | 每卡可用 KV | Token 容量 | 并发（S=4096） |
|----------------|----------|-------------|------------|----------------|
| FP16 + FP16 KV | 17.5     | 58.5 GB     | 1.46M      | 357            |
| INT4 + FP16 KV | 4.6      | 71.4 GB     | 1.79M      | 436            |
| INT4 + FP8 KV  | 4.6      | 71.4 GB     | 3.57M      | 872            |
| INT8 + INT8 KV | 8.75     | 67.25 GB    | 3.36M      | 821            |

**成本影响**：假设吞吐随并发近似线性（直到 compute-bound）： - FP16 基线：6400 tokens/s，成本 \$1.11 / 1M tokens。 - INT4 + FP8 KV：并发提升约 2.4 倍，但很快进入 compute-bound（拐点 $`B \approx 153`$，即约 153 路），实测吞吐提升约 1.8-2.2 倍，成本降到 **\$0.5-0.6 / 1M tokens**。

**注意「并发提升 ≠ 吞吐提升」**：并发上限是**显存约束**，吞吐上限是**算力约束**。量化解决前者，但一旦越过拐点，多出来的并发只能改善排队延迟（TTFT），不能继续提升吞吐。这时需要的是**更多卡**或**更小的模型**。

**追问：INT4 量化到 70B 的精度风险？** AWQ INT4 + group=128 在 LLaMA-70B 上 MMLU 掉点通常在 0.5-1.5 个点，GSM8K 可能掉 2-5 个点。需要对业务关键指标做完整回归测试。建议策略：**主力模型用 FP8/INT8，成本敏感场景用 INT4，并保留 FP16 作为降级/对照**。

------------------------------------------------------------------------

# 十、高频快速问答（口述训练）

**Q：为什么 Decode 阶段 GPU 利用率很低但依然很快？** 因为 Decode 是 memory-bound，SM 在等数据。MFU 低（\<5%）不代表效率低，衡量指标应换成**显存带宽利用率**（应接近 70-90%）。

**Q：为什么不用更大的 batch 把 GPU 喂饱？** batch 增大受限于 KV Cache 显存，且超过 compute-bound 拐点后延迟线性恶化，违反 SLA。这是「显存—算力—延迟」的三角约束。

**Q：FlashAttention 在推理中还有用吗？** Prefill 阶段极其有用（长 prompt 的 $`O(S^{2})`$ 显存和 HBM 读写）。Decode 阶段因为 $`S_{q} = 1`$，收益较小，但 FlashDecoding / PagedAttention 的 kernel 仍然做了 KV 的分块与并行。

**Q：为什么 vLLM 默认不做量化？** vLLM 支持 AWQ/GPTQ/FP8，但不默认开启，因为量化需要校准、可能掉点，且不同业务对精度容忍度不同。默认走 FP16 保证正确性。

**Q：如果线上 P99 突然恶化，排查顺序？** (a) 看是不是流量突增 → 队列长度、并发数；(b) 看是不是长请求混入 → 输入输出长度分布；(c) 看显存 → 是否触发抢占（vLLM 日志有 preemption 计数）；(d) 看 KV Cache 命中率 → 是否缓存被挤出；(e) 看 GPU 状态 → 是否有其他进程、是否降频（温度/功耗墙）；(f) 看网络 → 跨机 TP 的 all-reduce 延迟。

**Q：为什么长上下文推理这么贵？** Prefill FLOPs 正比于 $`S`$（线性，但常数大），Attention 部分正比于 $`S^{2}`$；KV Cache 显存正比于 $`S`$，直接压缩并发数。$`S`$ 从 4K 涨到 128K，KV 从 1.25 GB 涨到 40 GB（70B GQA），并发从 300+ 掉到个位数。这催生了 PD 分离、prefix caching、KV 压缩等一整套技术。

------------------------------------------------------------------------

# 附：核心公式速查表

| 量 | 公式 |
|----|----|
| KV Cache 单 token | $`2LH_{kv}d_{h}b`$ |
| KV Cache 总量 | $`2LH_{kv}d_{h}b \cdot S \cdot B`$ |
| 权重显存 | $`N_{\text{param}} \times \text{bytes}`$ |
| 前向 FLOPs | $`2N_{\text{param}}S`$ |
| Attention FLOPs | $`4S^{2}d_{\text{model}}`$（核心）+ $`24Sd_{\text{model}}^{2}`$（FFN，$`d_{ff} = 4d`$） |
| 算术强度（decode） | $`2/b`$ |
| 算术强度（prefill） | $`2S/b`$ |
| 机器平衡点 | $`\text{FLOPS}/\text{BW}`$（A100 FP16 ≈ 153） |
| 单步 decode 下界 | $`N_{\text{per-card}}b/\text{BW}`$ |
| Batch 拐点 | $`B^{*} = c_{0}/c_{1} = \frac{Nb/\text{BW}}{2N/F} = \frac{Fb}{2\,\text{BW}}`$ |
| 端到端延迟 | $`\text{TTFT} + (S_{\text{out}} - 1) \times \text{TPOT}`$ |
| Little’s Law | $`\text{并发} = \text{QPS} \times \text{E2E}`$ |
| 投机解码期望产出 | $`\frac{1 - \alpha^{\gamma + 1}}{1 - \alpha}`$ |
| 投机解码加速比 | $`\frac{1 - \alpha^{\gamma + 1}}{(1 - \alpha)(1 + \gamma/r)}`$ |
| 量化 | $`x_{q} = \text{round}(x/s) + z`$ |
| PP bubble 比例 | $`\frac{p - 1}{m + p - 1}`$ |
| 成本/1M tokens | $`\frac{\text{\$/h}}{\text{tokens/h}} \times 10^{6}`$ |

------------------------------------------------------------------------

本文件整理日期：2026-09-12
