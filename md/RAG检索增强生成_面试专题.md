------------------------------------------------------------------------

**来源与参考**

本文题目与思路整理自以下公开面经 / 题库 / 开源资料（按仓库名列出，便于自行查阅原文）：

- GitHub `wdndev/llm_interview_note` —— 大模型面试笔记，含 RAG、向量数据库、微调等章节
- GitHub `WeThinkIn/AIGC-Interview-Book` —— AIGC 算法岗面试宝典，含 RAG 与 LLM 应用工程
- GitHub `liyupi/mianshiya` —— 编程面试题库（含大模型应用方向问答）
- GitHub `ather-techie/rag-interview-system` —— RAG 面试题整理与系统设计
- 其他公开资料：RAG 原始论文（Lewis et al., 2020）、RAGAS 论文、Self-RAG / CRAG / Dense X Retrieval 论文、Milvus / Qdrant / FAISS 官方文档、BGE / M3E / GTE 模型卡、LangChain / LlamaIndex 官方文档。

> 说明：文中数字为工业界常见经验值，不同业务需以自身评测集回归为准，切勿当教条背诵。

------------------------------------------------------------------------

## 目录

1.  RAG 基础与演进
2.  文档处理：解析、分块与元数据
3.  向量化与索引
4.  检索策略
5.  重排 Rerank
6.  生成与幻觉
7.  RAG 评估
8.  工程与性能
9.  手撕 / 系统设计

------------------------------------------------------------------------

# 一、RAG 基础与演进

## Q1：为什么需要 RAG？它解决了大模型的哪些问题？

答：LLM 有三个先天缺陷，RAG 分别对症下药。

1.  **知识截止（Knowledge Cutoff）**：预训练数据有时间边界，模型不知道昨天发生的事。RAG 把外部知识库作为”外挂记忆”，更新知识只需更新索引，不用重训。
2.  **幻觉（Hallucination）**：模型在缺乏依据时会”编造”，尤其是长尾事实、专有名词、内部数据。RAG 提供可引用的证据上下文，把生成从”凭记忆回答”变成”阅读理解”。
3.  **私有/领域知识缺失**：企业内部文档、CRM 数据、产品手册不在预训练语料里。通过微调注入的成本高、更新慢，且容易灾难性遗忘；RAG 天然支持细粒度权限与实时更新。

附带收益：**可溯源**（给出引用，用户可验证）、**成本可控**（相比继续扩大参数或长上下文推理，检索 token 更便宜）、**合规可审计**（知识来源白盒）。

**追问：RAG 的”三大失败点”是什么？** 面试常被问”RAG 效果不好，怎么排查”。标准拆解： - **检索不到（Retrieval Failure）**：召回阶段就没拿到正确 chunk —— 归因于分块、Embedding、查询表达。 - **召回了但排不上（Ranking Failure）**：正确 chunk 在候选里但被淹没 —— 归因于缺少 Rerank 或融合权重失衡。 - **排上了但没用对（Generation Failure）**：上下文给了模型却答错/漏答 —— 归因于 prompt 组织、lost in the middle、上下文过长稀释注意力。 排查顺序永远是”先看召回率，再看排序，最后怪生成”，因为生成的天花板由召回决定。

------------------------------------------------------------------------

## Q2：RAG、微调、长上下文，三者在工程上怎么取舍？

答：这三者不是互斥，而是**成本 / 时效 / 能力**三角的不同切面。用一张表说清楚：

| 维度 | RAG | 微调（SFT/LoRA） | 长上下文 |
|----|----|----|----|
| 知识更新 | 改索引，分钟级 | 需重训，小时~天级 | 改 prompt，即时 |
| 幻觉控制 | 有依据，可引用 | 仍可能编造 | 有依据但注意力会稀释 |
| 注入的能力 | 事实型知识 | 风格/格式/推理模式/领域表达 | 单次任务的全量信息 |
| 成本结构 | 索引+检索+推理，边际成本低 | 训练一次性成本高，推理可低（小模型） | 推理 token 成本随长度线性/平方涨 |
| 可解释/权限 | 强（可做行级权限） | 弱（知识烧进权重） | 中 |
| 适合场景 | 事实问答、文档库、频繁更新 | 输出风格固定、任务模式固定、需低延迟小模型 | 单文档长文理解、代码库整体分析、多文档对比 |

**决策口诀（面试可直接说）**： - 知识**会变**、要求**可溯源** → RAG。 - 知识**稳定**、要的是**行为/风格/格式**改变 → 微调。 - 信息是**一次性、整体相关**（如”读完整本合同再回答”） → 长上下文。 - 三者常组合：**RAG 提供事实 + 微调控制输出格式 + 长上下文做少量文档的深度推理**。例如用 RAG 召回 30 个 chunk，用微调过的小模型做结构化抽取，用长上下文把召回的完整章节一起喂入做跨段推理。

**追问：什么情况下应该果断放弃 RAG？** 1. 知识量极小（\< 几十页），直接塞进 system prompt 更稳； 2. 问题是**全局聚合型**（“总结这份年报的所有风险点”）—— 向量检索是局部相似度匹配，天生不擅长全库聚合，此时应上 **GraphRAG / 分层摘要 / SQL 查询**； 3. 需要的是严格的数值计算或精确匹配 —— 应走工具调用（代码解释器 / DB 查询），而不是让模型读文本算。

------------------------------------------------------------------------

## Q3：请完整描述 RAG 的全流程。

答：标准链路是 **索引（Indexing）→ 检索（Retrieval）→ 重排（Rerank）→ 生成（Generation）→ 评估（Evaluation）**，且评估要反向驱动前面每一环。

    【离线索引】
    原始文档(PDF/Word/HTML/DB)
       │ ① 解析 Parser：OCR / 版面分析 / 表格结构化 / 公式转 LaTeX
       ▼
    纯文本 + 结构信息
       │ ② 清洗：去页眉页脚、去水印、修复断行、统一编码
       ▼
       │ ③ 分块 Chunking：固定长度/递归/语义/父子/late chunking
       ▼
    Chunk 集合 (+元数据: doc_id, page, title路径, 时间, 权限, 部门)
       │ ④ Embedding：BGE-M3 / text-embedding-3 等
       ▼
    向量库(Milvus/Qdrant/pgvector) + 倒排索引(BM25/ES)   ← 双路索引

    【在线服务】
    用户 Query
       │ ⑤ 查询理解：意图识别 / 改写 / 指代消解 / HyDE / 多查询扩展
       ▼
       │ ⑥ 多路召回：稠密 top50 + 稀疏 top50 (+ 元数据过滤 / 权限过滤)
       ▼
       │ ⑦ 融合：RRF / 加权求和 → 候选 top30
       ▼
       │ ⑧ 重排 Rerank：Cross-Encoder / BGE-Reranker / LLM → top5
       ▼
       │ ⑨ 上下文组装：排序去冗、加引用编号、控制 token 预算
       ▼
       │ ⑩ 生成：LLM + 引用约束 + 拒答指令 → 流式输出
       ▼
       │ ⑪ 后处理：引用校验、幻觉检测、格式化
       ▼
    答案 + 溯源

**每环的工程要点（面试追问会逐个挖）**： - 解析：OCR 质量直接决定上限，“垃圾进垃圾出”。 - 分块：chunk 太小丢上下文，太大稀释语义，见 Q7。 - Embedding：与检索任务、语言、领域强相关，中文别硬套英文模型。 - 召回：宁可多召回（top50~100），把精度交给 Rerank。 - Rerank：ROI 最高的一步，通常能把端到端准确率提 10~20 个点。 - 生成：控制上下文长度 + 引用编号 + 明确拒答。 - 评估：离线建 golden set，线上 A/B + 人工反馈闭环。

------------------------------------------------------------------------

## Q4：Naive RAG、Advanced RAG、Modular RAG 的演进区别？

答：这是面试区分”背过概念”和”做过工程”的经典题。

**Naive RAG（朴素 RAG，2020-2022）** 流程：`分块`` → ``向量化`` → ``余弦检索`` top-k → ``拼`` prompt → ``生成`。 问题：① 分块粗暴（固定 512 token 硬切）；② 只做单路稠密检索，关键词/数字/缩写召回差；③ 没有重排，top-k 里噪声大；④ 无法处理多跳、聚合、对比类问题；⑤ 无评估、无法定位问题环节。

**Advanced RAG（进阶 RAG，2023）** 核心思想：在**检索前（Pre-Retrieval）**和**检索后（Post-Retrieval）**加优化。 - 检索前：查询改写、HyDE、多查询扩展、指代消解、元数据路由（先判断该查哪个索引）。 - 检索中：混合检索（BM25 + Dense）、RRF 融合、元数据过滤、SQL/向量联合。 - 检索后：Rerank（Cross-Encoder）、上下文压缩（LLMLingua / 抽取式压缩）、去重、重排序（把最相关放头尾，规避 lost in the middle）。

**Modular RAG（模块化 RAG，2024）** 把 RAG 从”一条流水线”升级为”可编排的积木”，典型模块： - **Routing**：先判断走 RAG / 走工具 / 直接答（如”你好”不需要检索）。 - **Adaptive Retrieval**：判断”这个问题需要检索吗、需要几轮检索”。 - **Iterative / Multi-hop**：多轮检索-推理-再检索（Self-Ask、IRCoT）。 - **Self-RAG / CRAG**：模型自我批判检索结果，触发”重检索 / 改写 / 拒答”。 - **GraphRAG**：结合知识图谱做实体关系多跳与全局摘要。 - **Memory / 对话历史管理**：多轮上下文压缩与指代消解。

**一句话总结**：Naive 是”能跑”，Advanced 是”调优两步”（前+后），Modular 是”把每一步变成可插拔、可被模型自主决策的模块”。面试时能说出”我项目里用了哪些模块、为什么”远比背定义得分高。

------------------------------------------------------------------------

# 二、文档处理：解析、分块与元数据

## Q5：PDF 解析有哪些坑？表格、公式、扫描件怎么处理？

答：PDF 是”打印格式”而非”语义格式”，它只记录字形坐标，不记录段落/表格/阅读顺序。所以解析是 RAG 里**质量最不可控**的一环。

**坑位清单与对策**：

| 坑 | 现象 | 对策 |
|----|----|----|
| 双栏排版 | 文字左右栏交错串行 | 用版面分析模型（LayoutLMv3、PP-Structure、MinerU、Unstructured）先做区域检测再按栏排序 |
| 页眉页脚/页码 | 每个 chunk 都带”XX公司 第3页”噪声 | 按位置裁掉页面上下 8%~10% 区域 + 跨页重复行去重 |
| 表格 | 被拉成一行乱序文本 | 表格检测 + 结构化（Table Transformer / PaddleOCR 表格识别），转 Markdown 或 HTML 保留行列关系 |
| 公式 | 变乱码或多行错位 | 用 Nougat / Mathpix / UniMERNet 转 LaTeX，保留 `$...$` |
| 扫描件 | 提取不出文字 | OCR 前置（PaddleOCR / Tesseract / 商用云 OCR），必要时先做去噪、纠偏、二值化 |
| 断行连字符 | “inter-” | 正则修复软连字符；中英文按语言规则合并断行 |
| 图片/图表 | 图内信息丢失 | 用 VLM（GPT-4o / Qwen-VL）对图生成 caption，把 caption 作为 chunk 一起索引（多模态 RAG） |
| 阅读顺序 | 段落顺序错乱 | 使用带 reading-order 输出的解析器（MinerU、Unstructured hi_res） |

**工程建议**： 1. **不要指望一个解析器打天下**，按文档类型路由：纯文本 PDF 用 PyMuPDF（快），复杂版式用 MinerU/Unstructured（准但慢）。 2. **保留结构**：标题层级（H1/H2/H3）要保留，它是后面”父子分块”和”标题路径元数据”的基础。 3. **建立解析质量抽检机制**：随机抽 50 篇人工比对，解析准确率低于 95% 就先修解析，别急着调检索。 4. 页面图 + 文本双存：方便前端高亮原文、做引用锚点。

**追问：为什么说”解析没做好，后面所有优化都是徒劳”？** 因为检索的召回上限 = 索引里存在正确内容的概率。如果表格被解析成乱序数字、公式变乱码，那无论你的 Embedding 多强、Rerank 多准，都不可能召回到”语义正确”的内容。工程上应把解析质量作为 first-class 指标（字符准确率、表格还原率、标题层级完整率）。

------------------------------------------------------------------------

## Q6：主流分块策略有哪些？各自适用场景？

答：分块决定了检索的最小语义单元，是 RAG 里**性价比最高的调参点之一**。

1.  **固定长度分块（Fixed-size）** 按 token/字符数硬切，加 overlap。优点：实现简单、chunk 长度均匀、便于批量。缺点：会从句子/段落中间切断，语义破碎。 参数：chunk 256~512 token，overlap 10%~20%（即 50~100 token）。

2.  **递归字符分块（Recursive Character Splitting）** 按分隔符优先级递归切：`\n\n`（段落）→ `\n`（行）→ `。`/`.`（句）→ 空格 → 字符。优先在语义边界切，尽量满足长度约束。 这是 LangChain `RecursiveCharacterTextSplitter` 的默认思路，是**最通用的工业默认值**。

3.  **语义分块（Semantic Chunking）** 对相邻句子做 Embedding，计算余弦相似度，在相似度**骤降处**（如滑动窗口内相似度低于阈值或低于分位数）切分。优点：chunk 内部语义一致性强。缺点：需额外 Embedding 计算、阈值难调、chunk 长度不可控（可能极长或极短），需兜底 max token 限制。

4.  **父子分块（Parent-Child / Small-to-Big）** 用**小块做检索、大块做生成**：子块（128~256 token）建向量索引保证检索精度；命中子块后返回其父块（1024~2048 token）或其所在章节给 LLM 保证上下文完整。 这是解决”精确检索 vs 上下文完整”矛盾的最佳实践，几乎所有成熟系统都用。实现上父块也可做摘要索引（用 LLM 给每个父块生成摘要，用摘要检索，返回原文）。

5.  **Late Chunking（延迟分块）** 传统做法是”先切块再各自 Embedding”，导致每个 chunk 的向量丢失了文档全局上下文（尤其指代”该公司”“上述条款”无法消解）。 Late Chunking 的做法：**先把整篇文档（或长窗口段落）送入长上下文 Embedding 模型做 token 级编码，得到每个 token 的上下文相关向量，再按 chunk 边界做池化（mean pooling）**。这样每个 chunk 向量里已经”看过”全文，指代和主题信息被编码进去。 代价：需要长上下文 Embedding 模型（如 jina-embeddings-v2/v3、BGE-M3 长窗口），算力开销大、不能任意切分后独立增量。适合文档内指代密集的法律合同、技术规范。

6.  **其他补充**

    - **按结构分块**：Markdown 标题、HTML 标签、代码函数级切分（代码 RAG 必用 AST/函数边界）。
    - **按句子窗口**：以句子为单位索引，检索时返回该句 ±N 句（Sentence-Window Retrieval）。
    - **Agentic Chunking**：用 LLM 判断语义边界（贵，研究性）。

**追问：分块策略怎么选？给个决策路径。** - 默认：**递归分块 + 父子结构**（子 256 token / 父 1024~2048 token）。 - 文档结构清晰（Markdown/HTML/规范手册）→ 结构分块优先。 - 指代密集、上下文依赖强（合同、论文）→ 试 Late Chunking。 - 代码库 → 按函数/类切分，附文件路径 + 依赖关系元数据。 - 表格/图表 → 单独成块，绝不和正文混切。

------------------------------------------------------------------------

## Q7：chunk size 和 overlap 怎么权衡？给具体数字。

答：核心矛盾：**chunk 越小 → 向量语义越聚焦、匹配越准，但上下文越碎、易丢关键信息；chunk 越大 → 上下文完整，但向量被稀释（一个向量要代表多个主题），检索精度下降，且占用 LLM 上下文预算。**

**经验数字（可作面试默认回答）**：

| 场景 | chunk size | overlap | 说明 |
|----|----|----|----|
| 通用中文问答 | 256~512 token | 10%<sub>20%（50</sub>100） | 起步默认值 |
| 事实型短问答 / FAQ | 128~256 | 20~50 | 一条 QA 一块最好 |
| 技术文档 / 手册 | 512~1024 | 100~200 | 需要完整步骤 |
| 法律 / 合同 | 512~1024（+父块 2048） | 100~200 | 条款完整性优先，配父子分块 |
| 代码 | 按函数（\< 1024） | 0~少量 | 函数边界切，附文件路径 |

**overlap 的作用与风险**：overlap 保证跨边界的句子至少在一个 chunk 里完整出现，缓解”切断语义”。但 overlap 过大会导致：索引膨胀（存储和成本 +20%~50%）、检索结果高度重复（top-5 里 3 个是同一段的重叠版本），浪费宝贵的上下文窗口。所以 overlap 一般 **10%~20% 足够**，超过 30% 通常是坏味道。

**怎么科学定这两个参数？** 1. 建 **50~200 条 golden QA 集**（问题 + 标准答案 + 正确 chunk 出处）； 2. 网格搜索 `size ∈ {128, 256, 512, 1024}` × `overlap ∈ {0, 10%, 20%}`； 3. 看指标：**Context Recall / Recall@k**（能不能召回正确出处）优先，兼顾 chunk 数量（影响延迟成本）； 4. 选指标拐点，不要选指标最高的极端值（防过拟合）。

**追问：如果 chunk 变小后 Recall 涨了但回答质量掉了，怎么办？** 这是典型”检索精度↑、上下文完整性↓“的信号，解法就是**父子分块（small-to-big）**：用小块检索命中，回溯返回父块或相邻句窗口给 LLM。这样检索指标和生成质量可以同时拿到。

------------------------------------------------------------------------

## Q8：元数据（Metadata）在 RAG 里怎么设计？为什么重要？

答：元数据是 RAG 从”玩具”走向”企业级”的关键，它承担三件事：**过滤（Filter）、路由（Route）、溯源（Cite）**。

**必备字段设计**：

    {
      "chunk_id": "doc123_p17_c3",
      "doc_id": "doc123",
      "source": "员工手册2025.pdf",
      "title_path": ["第3章 薪酬福利", "3.2 年假规定"],   // 标题层级路径
      "page": 17,
      "chunk_index": 3,
      "prev_chunk_id": "doc123_p17_c2",                  // 用于上下文扩展
      "next_chunk_id": "doc123_p17_c4",
      "char_range": [1203, 1876],                        // 前端高亮锚点
      "created_at": "2025-03-01",
      "updated_at": "2025-06-01",
      "doc_type": "policy",
      "department": "HR",
      "access_level": ["hr", "manager"],                 // 权限过滤
      "tenant_id": "tenant_a",                           // 多租户隔离
      "language": "zh",
      "version": "v2",
      "tags": ["年假", "休假", "考勤"]
    }

**元数据的三大用途**： 1. **预过滤（Pre-filtering）**：用户问”2024 年之后的报销政策”，先用 `updated_at >= 2024` 过滤再向量检索，避免”检索到过时信息被模型当真”。**注意预过滤会破坏 HNSW 图连通性导致召回率下降**，需评估（FAISS 不支持高效过滤，Milvus/Qdrant 支持 filtered search，pgvector 用分区/部分索引）。 2. **路由**：多知识库场景，先按意图/元数据决定查哪个 collection（HR 库 / 产品库 / 法务库），避免跨域噪声。 3. **溯源与权限**：答案右侧显示”来源：员工手册2025.pdf 第17页”，用户可点开原文高亮。权限上做 **chunk 级 ACL**，检索时把用户权限作为必选过滤条件（**在向量库层过滤，绝不能检索后再过滤，否则存在越权泄露风险**）。

**追问：向量数据库做元数据过滤有什么性能陷阱？** 两类过滤模式： - **Post-filtering（先检索后过滤）**：先取 top-k 再过滤，若过滤条件很严（如只保留 1%），可能取回 100 个结果只剩 1 个，召回率暴跌。 - **Pre-filtering（先过滤后检索）**：暴力法（brute force on filtered set）准确但慢；图索引法（在 HNSW 上带过滤遍历）快但过滤率高时图会”断连”，召回下降。 实践建议：过滤率 \< 10% 时优先用暴力/分区分片；或改用支持高效混合过滤的引擎（Milvus 的 bitset、Qdrant 的 filterable HNSW、pgvector 的 partial index + WHERE 下推）。

------------------------------------------------------------------------

# 三、向量化与索引

## Q9：中文场景 Embedding 模型怎么选？BGE / M3E / GTE / text-embedding-3 对比。

答：选型看四个维度：**语言（中/英/多语）、长度（512/8192）、维度（影响存储与检索速度）、是否支持稀疏/多向量、是否需要 API**。

| 模型 | 语言 | 维度 | 最大长度 | 特点与适用 |
|----|----|----|----|----|
| **BGE 系列**（bge-large-zh-v1.5 / bge-m3） | 中文强，m3 多语 | 1024 / m3 为 1024 稠密+稀疏 | 512 / m3 支持 8192 | 中文 SOTA 之一，**bge-m3 同时输出 dense + sparse + colbert 三路**，一个模型搞定混合检索，企业私有化首选 |
| **M3E**（m3e-base / m3e-large） | 中文 | 768 / 1024 | 512 | 中文开源老牌，训练数据偏通用中文语料，社区生态好，但已逐步被 BGE 超越 |
| **GTE**（gte-large-zh / gte-Qwen2） | 中英 + 多语 | 1024 / 1536+ | 512 / 8192（Qwen2版） | 阿里系，GTE-Qwen2 在 MTEB 榜表现强，长文本能力好 |
| **text-embedding-3-small / large** | 多语（中文可用但非最优） | 1536 / 3072（支持降维） | 8191 | OpenAI API，免运维、稳定、支持 Matryoshka 降维；但数据出域、成本随量涨、中文长尾略弱于 BGE |
| **Conan-embedding / stella 等** | 中英 | 1024+ | 512~8192 | MTEB 中文榜刷榜模型，可关注但需自己验证业务 |

**选型决策树**： 1. 数据不能出域 / 要私有化 → 开源本地：**bge-m3**（首选，三路输出）或 bge-large-zh-v1.5。 2. 追求榜单效果、能接受 API → text-embedding-3-large 或 GTE-Qwen2。 3. 长文档、指代密集 → 选 8192 长窗口（bge-m3 / GTE-Qwen2 / jina-v3），配合 Late Chunking。 4. **最重要的一步：永远在自己的业务 golden set 上跑对比**（Recall@10、MRR）。榜单第一不等于你业务第一，领域词（如医疗、法律）差距尤其大。

**追问：Embedding 维度越高越好吗？** 不是。维度↑通常效果↑但存储和检索成本随维度线性增长（1 亿 chunk × 1024 维 float32 ≈ 400GB）。常用优化： - **降维**：Matryoshka（MRL）训练支持截断，如 3072 → 1024 → 512，效果损失很小。 - **量化**：int8 量化省 4 倍存储，float16 省 2 倍，配合量化感知检索。 - **二值化（Binary Embedding）**：32 倍压缩，用 Hamming 距离粗筛 + 原向量精排，BGE 系列有官方二值版本。

------------------------------------------------------------------------

## Q10：对比学习、难负样本（Hard Negative）在 Embedding 训练里的作用？

答：Embedding 模型的训练目标就是**让相似文本的向量靠近、不相似的远离**，标准做法是对比学习（InfoNCE 损失）：

    L = -log( exp(sim(q, d+) / τ) / Σ_j exp(sim(q, d_j) / τ) )

其中 `d+` 是正样本，`d_j` 包含 1 个正样本 + N-1 个负样本（in-batch negatives），τ 是温度系数（常取 0.01~0.05）。

**负样本质量决定模型上限**，分三档： 1. **随机负样本（Random）**：从语料随机抽，太容易，模型很快学会区分，梯度饱和，学不到细粒度。 2. **批内负样本（In-batch）**：同一 batch 内其他样本的配对当作负样本，免费且有效，但受 batch size 限制，且可能”假负样本”（batch 内恰好语义相关）。 3. **难负样本（Hard Negative）**：**与 query 高度相似但实际不相关**的样本 —— 这才是关键。挖掘方式： - 用当前模型检索 top-100，取排名靠前但不是正确答案的当作难负样本（如 rank 5~50）； - BM25 召回但 Embedding 排后的（互补信号）； - 用 LLM 生成”看似相关实则错误”的干扰项。

**BGE 系列的核心经验（可背）**：他们发现”**用检索系统自己挖出来的难负样本，比随机负样本效果提升显著**”，并提出了 `RetroMAE` 预训练 + 对比学习微调的两段式。BGE 论文中明确说：**训练时难负样本数量不是越多越好**，过多过难的负样本会导致训练不稳定（假负样本噪声），需要”课程学习”式的难度递进。

**实践要点**： - 用**自己业务数据**挖难负样本（通用模型在自己领域挖的负样本最有效）。 - 注意**假负样本（False Negative）**问题：batch 内或挖掘时，某个”负样本”其实和 query 相关，会污染训练。缓解：用 Rerank 模型或 LLM 过滤假负样本；用去偏损失（如 GradCache、去偏 InfoNCE）。 - 大 batch（如 1024~8192）+ 难负样本是 BGE/GTE 级别的标配。

------------------------------------------------------------------------

## Q11：向量数据库怎么选？Milvus / FAISS / Qdrant / pgvector / Elasticsearch 对比。

答：**FAISS 是库不是数据库**，这是最容易答错的点。

| 系统 | 定位 | 索引能力 | 过滤/多租户 | 分布式 | 适合场景 |
|----|----|----|----|----|----|
| **FAISS** | 向量检索**库**（C++/Python） | 最全：Flat/IVF/HNSW/PQ/IVF-PQ/OPQ/GPU | 弱（需自己实现 ID 过滤） | 无（需自己封装） | 单机、离线实验、算法研究、原型；亿级以内单机可扛 |
| **Milvus** | 分布式向量**数据库** | HNSW/IVF/DiskANN/SCANN 等，GPU 支持 | 强：标量过滤、分区（Partition）、多租户、bitset | 原生分布式（Kafka/Pulsar + 存算分离） | 十亿级、企业级、多租户 SaaS，云原生（Milvus/Zilliz） |
| **Qdrant** | 向量数据库（Rust） | HNSW + 量化（Scalar/Product/Binary） | 强：filterable HNSW、payload 索引 | 支持（Raft 集群） | 中等规模、强过滤需求、易部署、性能好，中小团队首选 |
| **pgvector** | PostgreSQL 扩展 | IVFFlat / HNSW | 最强（复用 PG 的 SQL、事务、JOIN、行级权限 RLS） | 靠 PG 主从/分片 | 已有 PG 栈、数据量 \< 千万、需要 SQL 联合查询、强一致性 |
| **Elasticsearch** | 搜索引擎 | dense_vector（HNSW）+ 原生 BM25 | 强：filter、聚合、权限 | 原生分布式 | **已用 ES 做日志/搜索**、需要 BM25+向量混合、成熟运维体系 |

**选型决策**： - 已有 ES 集群且量级中等 → **直接用 ES 的 dense_vector + BM25 混合检索**，省一个组件，混合检索天然支持。 - 已有 PostgreSQL、数据量不大（\< 500 万 chunk）、要事务和 SQL → **pgvector**，运维成本最低。 - 从零做、要强过滤和多租户、想快速上线 → **Qdrant**（Rust 单二进制，部署极简，filterable HNSW 是杀手锏）。 - 十亿级、需要分布式和存算分离、企业级 SLA → **Milvus**。 - 做算法对比实验、离线评测 → **FAISS**（方便换索引类型做 benchmark）。

**追问：什么时候不该用向量数据库？** chunk 数 \< 10 万且 QPS 不高时，`numpy` 暴力点积（Flat）在几十毫秒内就能算完，加一层缓存即可，引入向量库反而增加运维负担。向量库的价值在**规模（百万级以上）+ 过滤 + 高并发 + 持久化**。

------------------------------------------------------------------------

## Q12：HNSW、IVF、PQ 的原理与参数调优？

答：ANN（近似最近邻）的核心是**用少量精度换巨大速度**。三大主流结构：

### HNSW（Hierarchical Navigable Small World，图索引）

**原理**：构建多层图，上层稀疏（长跳，快速接近目标），下层稠密（短跳，精确定位）。查询从顶层入口点开始贪心搜索，逐层下降。类似”跳表 + 最近邻图”。 **建图参数**： - `M`：每个节点的最大出边数（常 16~64）。M 越大，图越密，召回越高但内存和建图时间越大。**M=16 是常用默认，高召回场景 32~48**。 - `efConstruction`：建图时的候选队列大小（常 100~500）。越大图质量越好，建图越慢。 **查询参数**： - `efSearch`：搜索时候选队列大小（常 64~512）。**这是唯一能在查询时调召回/延迟权衡的参数**：ef 越大召回越高、延迟越大。生产上常设 128~256。

**特点**：召回率最高（同等延迟下）、内存占用大（图结构 + 原始向量）、不支持高效的删除（需标记删除 + 定期重建）、**过滤条件严格时图会断连导致召回下降**。

### IVF（Inverted File，倒排文件索引）

**原理**：先用 k-means 把向量聚成 `nlist` 个簇（centroid），查询时只搜距离最近的 `nprobe` 个簇，而非全库。 **参数**： - `nlist`：簇数，经验 `nlist ≈ 4 * sqrt(N)`（1M 向量 → 约 4000）。 - `nprobe`：查询时探测的簇数（常 1~10% of nlist）。nprobe↑ 召回↑ 延迟↑。 **特点**：内存较省、支持增量添加、**需要训练（k-means）**、召回率低于同规模 HNSW、簇边界附近的向量容易被漏掉。适合**超大规模 + 内存受限 + 可接受稍低召回**。

### PQ（Product Quantization，乘积量化）

**原理**：把 D 维向量切成 m 个子段（如 128 维切 8 段，每段 16 维），每段用 k-means 聚成 256 个中心（8 bit），于是每个向量被编码成 m 个字节。**存储压缩到 1/m 甚至 1/32**，距离用查表近似计算。 **参数**：`m`（子段数，需整除 D）、`nbits`（通常 8）。 **特点**：极致压缩（1 亿 × 128 维 float32 = 51GB → PQ 后约 1.6GB）、有一定精度损失、常与 IVF 组合成 **IVF-PQ**（工业级经典组合）。升级版 **OPQ**（先做正交旋转再量化）和 **SQ（标量量化）**、**二值量化**。 **组合技巧**：**IVF-PQ 做粗筛 + 原始向量（或 float16）做精排**，兼顾速度和精度。

### 参数调优方法论（面试加分）

1.  **先定指标**：`Recall@k`（相对 brute-force 全量检索的召回率）+ P99 延迟 + 内存占用 + QPS。
2.  **网格搜索**：HNSW 调 `M, efConstruction, efSearch`；IVF 调 `nlist, nprobe`（nlist 影响建索引时间，nprobe 影响查询）。
3.  **经验起点**：HNSW `M=32, efConstruction=200, efSearch=128`；IVF `nlist=4096, nprobe=32`；内存紧张上 IVF-PQ `m=32/64, nbits=8`。
4.  **上线后**：数据增量大 → HNSW 定期重建（或用 DiskANN / 增量索引），IVF 可增量添加但要监控簇分布漂移。

**追问：过滤（filter）对 HNSW 的影响为什么这么大？** HNSW 的贪心搜索依赖图连通性。若过滤条件只保留 1% 的节点，遍历时大量邻居被过滤掉，图变成”孤岛”，贪心路径断裂，召回率可能从 95% 掉到 60%。解法： - 过滤率高（\> 50%）：用暴力搜索 filtered subset（更快更准）。 - 过滤率中等：用 Qdrant 的 filterable HNSW（带过滤的图遍历 + 额外连接）、Milvus 的 bitset 加速。 - 过滤率极低但过滤条件固定（如 tenant_id）：**按 tenant 分 collection / partition**，物理隔离，各建各的索引。

------------------------------------------------------------------------

## Q13：什么是多向量（Multi-Vector）与 ColBERT？和单向量检索的区别？

答：**单向量（Bi-Encoder）**：整段文本压成一个向量，检索 = 一次余弦相似度计算。快，但**信息被压缩损失**（一段 500 token 的话，“重点”可能只在其中一句）。

**多向量 / 延迟交互（Late Interaction）**：不为整段生成一个向量，而是**为每个 token 保留一个向量**（一段 128 token → 128 个 128 维向量），检索时用 **MaxSim** 计算：

    Score(Q, D) = Σ_{i∈Q} max_{j∈D} (q_i · d_j)

即对每个 query token，找到文档中与它最相似的 token，求和。这既保留了预计算的效率（文档向量离线算好），又实现了类似 Cross-Encoder 的细粒度交互，故称”**延迟交互（Late Interaction）**”。

**ColBERT / ColBERTv2 / BGE-M3 的 colbert 向量**： - ColBERTv2 用残差压缩（residual compression）把存储压到原来的 1/6~1/10； - BGE-M3 直接同时输出 dense + sparse(lexical) + colbert 三路向量，一个模型三路检索。

**对比**：

|  | Bi-Encoder（单向量） | ColBERT（多向量） | Cross-Encoder |
|----|----|----|----|
| 交互时机 | 无（向量独立） | 延迟交互（token 级 MaxSim） | 全交互（拼一起过模型） |
| 索引存储 | 1 向量/chunk | N 向量/chunk（N=token 数） | 不可索引 |
| 检索速度 | 极快（ANN） | 中（需专门索引，如 PLAID） | 极慢（每对都要前向） |
| 精度 | 中 | 高（接近 Cross-Encoder） | 最高 |
| 角色 | 召回阶段 | 召回/精排之间 | 精排阶段 |

**工程实践**：“**dense 召回 → colbert 精排 → cross-encoder 终排**”是效果最好的三段式，但成本高。多数团队用更简单的”**dense 召回 → cross-encoder 精排**”，把 colbert 作为可选增强。

------------------------------------------------------------------------

# 四、检索策略

## Q14：BM25 原理是什么？为什么稀疏检索至今不可替代？

答：BM25（Best Matching 25）是 TF-IDF 的改良版，核心是**词频饱和 + 文档长度归一化**：

    score(D, Q) = Σ_{q_i ∈ Q} IDF(q_i) · [ f(q_i,D)·(k1+1) ] / [ f(q_i,D) + k1·(1 - b + b·|D|/avgdl) ]

- `f(q_i, D)`：词 q_i 在文档 D 中的词频。
- `k1`（常 1.2~2.0）：控制词频饱和速度，**词频不是越多越相关，超过一定次数收益递减**（TF 饱和）。
- `b`（常 0.75）：控制长度归一化强度，b=1 完全归一化，b=0 不归一化。
- `IDF(q_i)`：逆文档频率，越罕见的词权重越高。

**为什么至今不可替代**： 1. **精确匹配能力**：产品型号 `XR-2000B`、错误码 `ERR_5031`、人名、缩写、数字 —— 这些**低频精确词，稠密向量往往召回不到**，BM25 一击即中。 2. **零训练、可解释**：无需 Embedding 模型，每个词的贡献可解释（便于 debug 为什么召回这个）。 3. **OOV 鲁棒**：新词、专有名词无需重新训练。 4. **成本极低**：倒排索引，CPU 即可，毫秒级。 5. **互补性**：BM25 擅长字面（lexical），稠密擅长语义（semantic），两者融合（hybrid）几乎总是优于单路。

**中文 BM25 的关键**：**分词**。中文需先分词（jieba / HanLP / pkuseg），或用 n-gram。工程上建议**分词 + 领域词典**（把产品名、公司术语加进词典，否则会被切碎）。也可用 BM25 + 字级 bigram 兜底。

------------------------------------------------------------------------

## Q15：混合检索（Hybrid Search）怎么做？RRF 融合原理与参数？

答：混合检索 = **稀疏（BM25/SPLADE）+ 稠密（Embedding）多路召回，再融合排序**。目的：语义匹配 + 字面匹配互补，通常比单路 Recall@10 提升 5~15 个点。

**融合方法一：加权求和（Weighted Sum）**

    score = α · normalize(dense_score) + (1-α) · normalize(bm25_score)

α 常取 0.5~0.7（稠密权重略高）。**缺点**：两路分数量纲不同，必须归一化（min-max / z-score）；归一化对异常值敏感；α 需按业务调。

**融合方法二：RRF（Reciprocal Rank Fusion，推荐）**

    RRF(d) = Σ_{r ∈ R} 1 / (k + rank_r(d))

其中 `rank_r(d)` 是文档 d 在第 r 路召回中的排名（从 1 开始），`k` 是平滑常数，**常取 60**（论文默认值，作用是压低头部排名的过度影响）。

**RRF 的优点**： - **只用排名，不用分数** → 免归一化，天然解决量纲不一致问题； - 对某一路的异常分数鲁棒； - 无需调权重，多路（3 路、5 路）天然可加； - 实现极简，一行公式。

**调参**：k 越大，各排名之间的差异越小（越平均）；k=60 是通用默认。若某路更可信，可给该路加系数 `w_r / (k + rank)`。

**工程实现**：召回阶段各路并行取 top 50~100，RRF 融合后得到 top 30，交给 Rerank。Elasticsearch 8.x 原生支持 RRF（`rrf` retriever），Milvus 2.4+ 支持 `hybrid search + RRF ranker`，Qdrant 支持 `Query API` 的 fusion。

**追问：为什么 RRF 通常比加权求和好？** 因为不同检索器的分数分布**不可比也不稳定**：BM25 分数是无上界的实数，余弦相似度在 \[-1,1\]，且分布随 query 长度和数据分布剧烈变化。归一化只能做”当前这批结果”的相对归一，query 之间不可比。RRF 直接丢弃分数只保留序，反而抓住了最稳定的信号 —— **“谁排前面”比”高多少分”更可靠**。

------------------------------------------------------------------------

## Q16：查询改写（Query Rewriting）有哪些方法？HyDE、多查询扩展、指代消解怎么用？

答：用户 query 往往短、口语、有指代、缺上下文，与文档的书面表达存在 **vocabulary gap**。查询改写是”检索前优化”的核心手段。

1.  **指代消解 + 省略补全（Query Rewriting for Conversation）** 多轮对话中”它多少钱？“→ 需结合历史补全为”iPhone 16 Pro 的价格是多少？“。 做法：用 LLM 结合最近 N 轮对话重写 query。**这是多轮 RAG 的必备步骤**，否则检索完全失效。

2.  **HyDE（Hypothetical Document Embeddings）** 思路：让 LLM **先生成一段假想的答案文档**，再用这段假答案去检索。 原理：query 是”问题”的分布，文档是”答案”的分布，两者在向量空间有 gap；生成假答案后，它与真实文档处于**同一分布**，相似度更高。

- Query: "RAG 的 chunk size 一般多大？"
      → LLM 生成假答案: "在 RAG 系统中，chunk size 通常设置为 256 到 512 个 token，
         并配合 10% 到 20% 的 overlap。对于技术文档可能需要 512-1024..."
      → 用假答案的向量去检索

  **优点**：对短 query、术语不匹配效果显著（Recall@10 常提升 5~15 点）。 **缺点**：① 多一次 LLM 调用（延迟 +300ms~1s）；② 若生成方向错误（幻觉），会把检索带偏；③ 对事实型精确查询（找型号）可能反而不如原 query。 **工程折中**：HyDE 与原 query **双路召回**，RRF 融合，避免带偏。

3.  **多查询扩展（Multi-Query / Query Expansion）** 让 LLM 把 1 个 query 改写成 3~5 个不同角度的 query（同义改写、拆解子问题、领域术语替换），分别检索后合并去重。

- "怎么优化 RAG 速度？"
      → ["RAG 延迟优化方法", "向量检索加速技术", "减少 LLM 上下文长度 技巧"]

  **优点**：提高召回（多角度覆盖）。**缺点**：检索次数 × N，延迟和成本上升；可能引入噪声。**建议 N ≤ 3~4，并配合 RRF + Rerank 过滤噪声**。

4.  **查询分解（Query Decomposition / Sub-query）** 复杂问题拆成子问题：“对比 A 和 B 的优缺点” → \[“A 的优点”,“A 的缺点”,“B 的优点”,“B 的缺点”\]，各自检索再综合。适合多跳 / 对比 / 复合问题。

5.  **意图识别与路由（Intent Routing）** 先分类 query 意图：闲聊 / 事实查询 / 聚合统计 / 需要工具 / 超纲。据此决定：直接回答、走 RAG、走 SQL、走 Agent、拒答。避免对”你好”也做一次昂贵的向量检索。

6.  **Step-Back Prompting（后退提问）** 把具体问题抽象成更一般的问题再检索：“2025年Q3的营收是多少” → 后退为”公司近年营收情况”，检索到财务概览后再回答细节。适合需要背景知识的推理。

**追问：这些改写方法怎么选？会不会引入延迟？** 按 ROI 排序推荐： - **必做**：多轮场景的指代消解（不做则完全失效）。 - **高 ROI**：多查询扩展（3 路，提升召回明显）。 - **场景化**：短 query / 术语 gap 大 → HyDE；复杂复合问题 → 查询分解。 - **性能兜底**：改写走**小模型**（Qwen-7B / GPT-4o-mini）而非大模型；改写步骤设置 **timeout（如 500ms）**，超时就直接用原 query 降级，保证可用性。

------------------------------------------------------------------------

## Q17：Self-RAG、CRAG、Adaptive RAG 分别是什么？

答：这三个是”让 RAG 学会自我决策”的代表工作，面试高频。

**Self-RAG（Self-Reflective RAG）** 核心：训练 LLM 生成**反思标记（reflection tokens）**，在推理过程中自我判断： - `Retrieve`：这个问题需要检索吗？（不需要就直接回答） - `IsREL`：检索到的段落相关吗？ - `IsSUP`：我生成的每个论断被段落支持吗？（衡量忠实度） - `IsUSE`：这个回答对用户有用吗？

流程：按需检索 → 对每段打分（相关/支持）→ 生成时用标记引导 → 最终输出附置信度。 **代价**：需要专门训练（或至少 fine-tune）带反思能力的模型，工程实现重。**思想可借用**：不做训练，用 prompt 让 LLM 输出这些判断（打折扣但可用）。

**CRAG（Corrective RAG）** 核心：**给检索结果加一个”质检员 + 纠错器”**。 - 用一个轻量评估器（微调的 T5 / 小模型）给检索结果打三档：**Correct（正确）/ Ambiguous（模糊）/ Incorrect（错误）**。 - Correct → 直接用于生成（可再做 knowledge refinement，把 chunk 拆成 strip 精炼）。 - Incorrect → 丢弃，触发 **Web 搜索**或重写 query 后重检索。 - Ambiguous → 两者结合。

一句话：**CRAG = 检索质量评估 + 条件性纠错（重检索 / 联网）**。

**Adaptive RAG（自适应 RAG）** 核心：**按问题难度动态选择策略**。用一个分类器（或 LLM）判断 query 复杂度： - 简单（如”什么是 RAG”）→ **No Retrieval**，直接回答； - 中等 → **Single-step Retrieval**，一次检索 + 生成； - 复杂（多跳、对比）→ **Multi-step Retrieval**，多轮检索-推理。

收益：简单问题省延迟省成本，复杂问题保证质量。**这是工程上最容易落地的一个**（成本上非常有价值）。

**对比总结表**：

| 方法         | 核心机制               | 解决什么              | 工程落地难度   |
|--------------|------------------------|-----------------------|----------------|
| Self-RAG     | 反思标记自评（需训练） | 忠实度、按需检索      | 高             |
| CRAG         | 检索质量三分类 + 纠错  | 检索结果噪声/错误     | 中             |
| Adaptive RAG | 难度分类→动态策略      | 成本/延迟与非对称难度 | 低（推荐先做） |

**追问：这些思想落到工程里最实用的一个点是什么？** **分级路由**：90% 的用户 query 是简单的（问候、FAQ、单跳事实），只有 10% 复杂。对全部 query 都跑”多查询扩展 + 混合检索 + 重排 + 大模型生成”是巨大浪费。工程上用一个轻量分类器（甚至规则 + 小模型）做成本分级： - Level 0：命中 FAQ 库/缓存 → 直接返回（\< 50ms） - Level 1：单跳检索 + 小模型生成（~1s） - Level 2：多查询 + 混合检索 + Rerank + 大模型（~3s） 可降低平均延迟和成本 50%+，同时不牺牲难问题的质量。

------------------------------------------------------------------------

# 五、重排 Rerank

## Q18：Cross-Encoder 和 Bi-Encoder 的区别？为什么 Rerank 值得做？

答：**这是 RAG 面试最核心的一道题之一。**

|  | Bi-Encoder（双塔） | Cross-Encoder（交叉编码器） |
|----|----|----|
| 结构 | query 和 doc 分别编码，各自一个向量 | query 和 doc **拼在一起**送入同一个模型 |
| 交互 | 无（只在最后算余弦） | 全交互（Transformer 内部每个 token 互相关注） |
| 输出 | 向量相似度 | 一个相关性分数（0~1 或 logit） |
| 预计算 | ✅ doc 向量可离线算好、建索引 | ❌ 必须 query+doc 联合前向，无法预计算 |
| 复杂度 | 检索 O(N) 向量比对（ANN 后 O(log N)） | 每对一次前向 → N 对就是 N 次前向 |
| 速度 | 极快（毫秒级百万级） | 慢（单对 1~10ms，100 对约 100ms~1s） |
| 精度 | 中 | **高（显著优于双塔）** |
| 角色 | 召回（retrieval） | 精排（rerank） |

**为什么 Cross-Encoder 更准？** 因为它允许 query 的每个 token 和 doc 的每个 token 做 attention 交互，能捕捉”否定”“条件”“数字对应”等细粒度语义。Bi-Encoder 把 doc 压成一个向量，信息瓶颈严重。

**为什么不能直接用 Cross-Encoder 检索？** 要对全库 N 个 doc 打分，就要 N 次前向 → 100 万 doc 不可能（一次前向 2ms → 2000 秒）。所以必须”**双塔粗召回 → Cross-Encoder 精排**”这个两级架构。

**Rerank 的收益（可直接背的数字）**： - 典型提升：端到端答案准确率 **+10~20 个点**，是 RAG 所有优化里 ROI 最高的单项。 - 召回 50 条 → Rerank 取 top 5，通常比直接取稠密 top 5 的准确率高 15+ 点。 - 延迟代价：rerank 50 条用 BGE-Reranker-base（GPU）约 30~80ms，可接受。

**常用 Rerank 模型**： - **BGE-Reranker-v2-m3 / bge-reranker-large**：中文首选，开源可私有化，效果稳定。 - **Cohere Rerank（rerank-v3.5）**：API，多语效果好，省事。 - **Jina Reranker**、**mxbai-rerank**：备选。 - **LLM Rerank**：用 GPT-4o 让模型对候选打分/排序，效果最好但最贵最慢（见 Q20）。

**追问：Rerank 会不会让效果好但延迟爆炸？怎么平衡？** 平衡策略： 1. **控制候选数**：召回 top 30~50 用于 rerank（不要 1000 条都塞进去，延迟线性增长收益递减）。 2. **用 GPU + 批处理**：Cross-Encoder 可 batch 推理（batch=32），吞吐大幅提升。 3. **模型选型**：base 版（110M）足够，large 版（335M/560M）精度更高但慢 3 倍，按 SLA 选。 4. **异步/流式**：先返回”正在检索”，rerank 完成后流式吐答案。 5. **两段式**：向量粗排 top 200 → 小 cross-encoder 取 top 20 → 大 cross-encoder 取 top 5（超大规模才需要）。

------------------------------------------------------------------------

## Q19：ColBERT 的延迟交互如何工作？为什么它介于两者之间？

答：见 Q13。核心补充：

**MaxSim 计算过程**：

    Query 编码 (保留每个 token 的向量): Q = [q_1, q_2, ..., q_m]  (m×d)
    Doc  编码 (保留每个 token 的向量): D = [d_1, d_2, ..., d_n]  (n×d)

    Score = Σ_{i=1..m} max_{j=1..n} (q_i · d_j)

**为什么”延迟”**：文档的 token 级向量**离线预计算并建索引**（这是”早”的部分），交互（MaxSim）**推迟到查询时**才做（这是”晚”的部分）。Bi-Encoder 完全没有交互，Cross-Encoder 在编码时就交互（所以不能预计算），ColBERT 卡在中间，故称 Late Interaction。

**存储优化（ColBERTv2）**： 朴素做法每 token 一个 128 维 float32 向量，一段 500 token 的 chunk 要 500×128×4 = 256KB，是单向量（1024×4=4KB）的 64 倍，不可接受。 ColBERTv2 用**残差压缩量化**：每个 token 向量 = 质心（centroid，聚类得到）+ 残差（量化到 1~2 bit），压缩到原来的 1/6~1/10。BGE-M3 的 colbert 向量也做了类似优化。

**检索效率**：朴素 MaxSim 要对全库算过于昂贵，ColBERTv2 配套 **PLAID 引擎**做两阶段（先用质心倒排粗筛候选，再精算 MaxSim），可做到千万级毫秒响应。

**实践结论**：ColBERT 在”精度接近 Cross-Encoder、速度接近 Bi-Encoder”这个点上，但**工程复杂度高**（需专门索引和引擎）。实际项目中： - 若已有成熟向量库 + BGE-Reranker，直接用”dense 召回 + cross-encoder 精排”，性价比更高； - 若检索量极大（亿级）且延迟敏感，ColBERT/PLAID 的价值才凸显； - BGE-M3 提供 colbert 向量的”轻量版”，可作为 rerank 的廉价替代。

------------------------------------------------------------------------

## Q20：LLM Rerank 怎么做？和专用 Reranker 怎么选？

答：**LLM Rerank** = 用一个通用 LLM（GPT-4o / Qwen-72B）对候选文档打分或排序。三种主流 prompt 范式：

1.  **Pointwise（逐条打分）**：把 (query, doc) 给 LLM，让它输出 0~10 相关分。优点：可并行、可批量；缺点：分数校准差、跨 query 不可比。
2.  **Listwise（列表排序）**：把 query 和 N 个候选一起给 LLM，让它输出排序或 top-k 编号。**效果最好**，因为模型能看到候选间对比。缺点：受上下文长度限制（候选不能太多，通常 ≤ 20），且位置偏见（position bias）。
3.  **Pairwise（两两比较）**：比较 doc A 和 doc B 哪个更相关，用锦标赛/排序合并得到全局序。**最准但最贵**（O(N²) 次调用或 O(N log N) 用排序算法）。

**LLM Rerank vs 专用 Reranker 对比**：

|  | 专用 Reranker（BGE-Reranker） | LLM Rerank |
|----|----|----|
| 精度 | 高 | 略高 / 相当（listwise） |
| 延迟 | 50 条 ~50ms | 50 条需 1~5s（多轮调用） |
| 成本 | 极低（自托管） | 高（token 计费） |
| 稳定性 | 高（分数可比） | 差（输出格式不稳、需解析重试） |
| 可控性 | 好 | 可通过 prompt 定制（如”按时间新近性优先”） |
| 适用 | 在线高并发主链路 | 离线重排、低 QPS 高价值场景、复杂业务规则 |

**工程建议（面试加分）**： - **在线主链路用专用 Reranker**（BGE-Reranker / Cohere Rerank），保证延迟。 - **LLM Rerank 用在高价值低频场景**（如合同审查、深度研究报告）或**离线**。 - **混合**：专用 Reranker 先选 top 20 → LLM 对 20 条做 listwise 终排取 top 5。兼顾速度和效果。

**追问：LLM Rerank 有哪些坑？** 1. **位置偏见（Position Bias）**：LLM 倾向于认为靠前的候选更相关（或反过来，倾向中间的）。缓解：随机打乱候选顺序多次投票（成本×k），或做顺序敏感性校准。 2. **长度偏见**：长文本看起来”信息更多”，易被打高分，即使内容冗余。 3. **自洽性差**：同一对 query-doc 两次调用分数可能不同（温度、量化误差）。需 temperature=0 + 多次平均。 4. **输出解析失败**：模型不按格式输出（“我认为第3个最好”而非 “3”）。需 robust parser + 重试 + 兜底回退到向量分数。 5. **分数不可比**：不同 query 的 LLM 打的分数尺度不同，不能跨 query 设全局阈值。

------------------------------------------------------------------------

## Q21：rerank 和召回数量怎么权衡？给一套具体参数。

答：这是**必须给出具体数字**的工程题。

**推荐参数（企业级通用配置，可直接作为答案）**：

    【召回阶段】
    稠密召回:        top 50   (HNSW efSearch=128)
    稀疏召回(BM25):  top 50
    多查询扩展:      3 路，每路 top 30   → 合并去重后约 60~80 条
    RRF 融合(k=60):  → 保留 top 30

    【精排阶段】
    Cross-Encoder Rerank: 30 条 → 打分 → 取 top 5

    【生成阶段】
    送入 LLM: top 5 个 chunk，每个 300~500 token
    总上下文: 1500~2500 token（留足空间给对话历史和输出）

**权衡逻辑**： - **召回宁多勿少**：召回是”上限”，漏了后面救不回来。所以召回阶段放宽（top 50~100），精度问题交给 rerank。 - **rerank 候选不宜过多**：延迟与候选数线性增长；且根据经验，**正确文档排在前 30 的概率已超 95%**（若 RRF 融合合理），继续扩大候选收益极小。 - **最终 top-k 不宜过多**：喂给 LLM 的 chunk 越多，噪声越多、lost in the middle 越严重、成本越高。**top 3~5 是甜点区**。

**参数怎么定（面试要说方法论）**： 1. 在 golden set 上画曲线：`Recall@k`（k=5,10,20,50,100）vs 最终答案准确率。 2. 找到**答案准确率的拐点**：常见是召回 30~50、精排 top 3~5 时准确率饱和。 3. 加延迟约束：给定 SLA（如 P95 \< 2s），反推 rerank 候选数上限（如 GPU 上 30 条 rerank 约 50ms，50 条约 80ms）。 4. 用 **NDCG@k / MRR** 评估 rerank 前后排序质量提升。

**追问：如果召回了 50 条但正确答案根本不在里面，怎么办？** 说明是**召回问题**，调 rerank 无用。排查方向： 1. 看 golden set 里”正确答案不在召回集”的比例（即 Recall@50 是多少）。若 \< 90%，必须优化召回。 2. 检查分块：答案是否被切断、是否在表格里没解析出来。 3. 检查 Embedding：是否用了领域不匹配的模型 → 换 BGE-M3 / 微调 Embedding。 4. 检查查询：是否要加多查询扩展 / HyDE / 指代消解。 5. 检查过滤条件：是否元数据过滤太严把正确 chunk 滤掉了（这是隐藏杀手）。

------------------------------------------------------------------------

# 六、生成与幻觉

## Q22：上下文怎么注入 Prompt？有哪些工程技巧？

答：上下文注入不是”把 chunk 拼起来”那么简单，格式设计直接影响模型能否用对信息。

**推荐的 Prompt 结构**：

    [系统指令]
    你是XX公司的客服助手。请严格依据下方【参考资料】回答用户问题。
    规则：
    1. 只使用参考资料中的信息，不得依据常识或记忆补充。
    2. 若参考资料不足以回答，必须明确回复"根据现有资料无法回答，建议您联系XX"，不得猜测。
    3. 每个论断后用 [编号] 标注来源，如"年假为5天[1]"。
    4. 回答简洁，分点陈述，不要复述问题。

    [参考资料]
    [1] 来源：《员工手册2025.pdf》第3章 3.2节 第17页
    内容：员工入职满一年可享受带薪年假5天，满三年10天...

    [2] 来源：《考勤制度.pdf》第2章 第5页
    内容：年假需提前3个工作日在OA系统提交申请...

    [用户问题]
    我入职两年了，年假有几天？怎么申请？

    [回答]

**关键技巧清单**： 1. **加编号 + 强制引用**：让模型输出 `[1]` `[2]`，既提升忠实度（模型知道要”用”资料），又便于前端做引用高亮和后验校验。 2. **每段附来源元数据**（标题/页码）：模型能据此判断信息时效性和权威性，用户可溯源。 3. **明确拒答指令**：这是抑制幻觉最有效的一招。没有”不知道就说不知道”的指令，模型 100% 会硬编。 4. **控制位置**：把最相关的 chunk 放**开头和结尾**（规避 lost in the middle，见 Q24）。 5. **去冗余**：多个 chunk 内容重复时先去重（overlap 会导致大量重复），否则浪费 token 还稀释注意力。 6. **限制总长度**：控制在 2000~4000 token 内。过长不仅贵，效果反而下降。 7. **给文档加分隔符**：`<doc id="1">...</doc>`，让模型清楚边界，避免跨文档串味。 8. **指令放在最后**：部分模型对 prompt 末尾的指令遵循度更高（可把核心约束在开头和结尾各说一遍）。

**追问：如果模型不遵守”只用参考资料”的指令怎么办？** 1. **换更强的模型**（指令遵循能力差异巨大，GPT-4o/Claude 明显优于 7B 小模型）。 2. **用 few-shot**：给 2~3 个”资料不足时拒答”的示例，比纯指令有效得多。 3. **后验校验**：生成后做一个”答案是否被上下文支持”的检测（用 NLI 模型或 LLM judge 检查每句，即 **faithfulness 校验**），不支持就重生成或降级。 4. **结构化输出**：要求模型输出 JSON `{"answer": "...", "citations": [1,3], "confidence": 0.8}`，用约束解码（JSON mode）强制格式。 5. **上下文做压缩**：如果资料太长且噪声多，模型更容易”跑偏”，先做抽取式压缩再注入。

------------------------------------------------------------------------

## Q23：引用与溯源怎么做才可靠？

答：溯源是 RAG 相对纯 LLM 的核心优势，但**模型标注的引用经常是错的**（“幻觉引用”），必须做校验。

**三道防线**： 1. **生成时强约束**：要求模型在每个论断后标注 `[编号]`，且编号必须来自给定列表。用 JSON schema / 约束解码限制输出。 2. **生成后校验（关键）**： - **引用有效性检查**：模型输出的编号是否在提供的 chunk 列表内（防编造 `[7]` 但只有 5 个）。 - **归因校验（Attribution Check）**：对每个论断，检查其是否真的被所引用的 chunk 支持。做法：把 (论断, 引用的 chunk) 送给 NLI 模型或 LLM judge，判断 entailment / contradiction / neutral。**这是检测”幻觉引用”的唯一可靠方法**。 - **未标注论断检测**：答案里有事实性陈述但没标引用 → 高风险，标记或重生成。 3. **UI 层做锚点**：chunk 元数据里存 `char_range`（在原文中的字符偏移），前端点击引用直接跳到 PDF 对应位置高亮。用户能一眼验证，信任度大增。

**工程实现（伪代码）**：

    def verify_citations(answer, chunks):
        claims = split_into_claims(answer)          # 拆成原子论断
        for claim in claims:
            cited = extract_citations(claim)         # 提取 [1][2]
            if not cited:
                flag(claim, "uncited"); continue
            for cid in cited:
                if cid not in chunks:
                    flag(claim, "invalid_citation"); continue
                if not nli_entails(chunks[cid], claim):  # 归因校验
                    flag(claim, "unsupported")
        return flagged_claims

对 flagged 的论断：重生成 / 降低置信度提示 / 直接删掉该句。

**追问：如果校验发现大量幻觉引用，根因可能在哪？** - 检索质量差（top-5 里根本没有答案，模型只能编）→ **先看 Recall@5**。 - chunk 太长，一句论断对应的是长文里的一个细节，NLI 判不准 → 缩短 chunk 或用句子级校验。 - prompt 没有强调引用纪律 → 加 few-shot。 - 模型能力不足 → 换模型。**记住：80% 的幻觉问题根源在检索，不是生成。**

------------------------------------------------------------------------

## Q24：什么是 Lost in the Middle？工程上怎么规避？

答：**Lost in the Middle**（Liu et al., 2023）是一个被实验反复验证的现象：当把多个文档放进长上下文时，LLM 对**开头**和**结尾**位置的信息利用最好，对**中间位置**的信息利用率显著下降，呈 U 形曲线 —— 即使模型声称支持 128K 上下文。

**实验结论**： - 把答案放在上下文开头或结尾，准确率最高； - 放在中间（尤其 10~20 个文档时），准确率可能下降 20~30 个点； - 上下文越长、文档越多，中间信息的丢失越严重； - 并非所有模型都同样严重，但在大多数模型上都存在。

**成因推测**：注意力机制的位置偏见、训练数据中关键信息的分布偏向首尾、以及长序列下注意力被稀释。

**工程规避手段（面试重点，要具体）**： 1. **控制注入数量**：别迷信”塞更多上下文效果更好”。**top 5 往往优于 top 20**。宁可精排到 3~5 条高质量 chunk。 2. **重排序（Reorder）**：把 rerank 分数最高的 chunk 放在**最前面和最后面**，次相关的放中间。即”**头尾放重点**”策略。 `原始排序:`` [1,2,3,4,5] ``(1最相关)``    ``重排后:``   [1,3,5,4,2]  ``或`` [1,5,2,4,3]` 3. **压缩上下文**：用抽取式压缩（只保留与 query 相关的句子）或 LLMLingua 类方法，把 5 个 chunk 压到 1/3 长度，减少噪声。 4. **分而治之**：文档太多时，先对每个 chunk 单独生成”小结论”，再把小结论汇总（map-reduce / refine 模式），而不是一次性全塞。 5. **显式索引标注**：给每段标 `[1]` `[2]`，并在指令里说”请综合所有参考资料，注意不要遗漏中间的 \[3\]“。有实验显示显式提示能缓解一部分。 6. **评估验证**：在 golden set 上做”位置扰动实验”——把正确 chunk 放在不同位置，测准确率变化，量化自己系统的 lost-in-middle 严重程度，再决定是否要重排。

**追问：既然有 lost in the middle，为什么不用更大上下文窗口直接塞全部文档？** 三个原因： 1. **注意力稀释**：上下文越长，模型对单个信息的注意力越弱（这也是 RAG 优于长上下文的原因之一）。 2. **成本**：128K token 的推理成本是 4K 的几十倍（还涉及 KV cache 显存）。 3. **延迟**：prefill 时间随长度近似平方增长（注意力复杂度 O(n²)），首 token 延迟可能从 0.5s 涨到 10s+。 4. **噪声**：全塞进去意味着大量不相关文档，反而干扰（“distractor”效应）。 所以正确做法是**先检索精排压缩，再注入少量高质量上下文**，而不是用长窗口硬抗。

------------------------------------------------------------------------

## Q25：拒答策略怎么设计？知识冲突怎么处理？

答：**拒答（Abstention）** 是 RAG 系统安全性的底线 —— 宁可说”不知道”，也不能编。

**拒答的触发条件（多信号融合，不要只靠 prompt）**： 1. **检索侧信号**：最高 rerank 分数低于阈值（如 cross-encoder 分数 \< 0.3）→ 说明没有相关内容。**这是最可靠的信号**。 2. **生成侧信号**：LLM 自评置信度低、或在 prompt 引导下输出”资料不足”。 3. **校验侧信号**：归因校验（Q23）发现主要论断都没有上下文支持。 4. **覆盖度信号**：query 的关键实体/意图在检索结果中覆盖率低。

**分级响应（而非二元拒答）**： - **完全无相关资料** → 明确拒答 + 引导（“建议咨询XX部门/请查看XX文档”）。 - **部分相关** → 回答已知部分，显式说明”关于XX部分，现有资料未涵盖”。 - **资料有冲突** → 见下方冲突处理。 - **超出权限** → “您没有权限访问相关资料”。

**阈值怎么定**：在 golden set 上做 ROC 分析，权衡 **误拒率（False Abstention，本可回答却拒答，伤害体验）** 和 **幻觉率（Hallucination，不该答却答错，伤害信任）**。业务上通常**宁可多拒一点**（幻觉一次的成本远高于拒答一次），可设成”低于阈值时给出答案但加风险提示”。

**知识冲突处理**（企业 RAG 常见：新旧制度并存、多来源说法不一）： 1. **源头治理**：索引时做**版本控制与去重**，把过时文档下线（软删除 + 时效元数据）。这是根本解法。 2. **时效优先**：元数据带 `updated_at`，检索时可加权（新文档分数 × 1.1）或直接过滤旧版本。 3. **权威分级**：给来源设权威度（官方制度 \> FAQ \> 论坛），prompt 里告诉模型”若冲突，以官方制度为准”。 4. **显式呈现冲突**：当检测到多个 chunk 说法矛盾（用 NLI 判断 contradiction）时，**不要把冲突藏起来**，而是呈现给用户：“关于年假天数，2024版手册规定5天，2025版规定7天，建议以最新的2025版为准。” 5. **prompt 指令**：明确写”若不同参考资料存在冲突，请指出冲突并说明各来源说法”，避免模型随机选一个。

**追问：怎么在评估里衡量拒答质量？** 单独建一个 **“不可回答集”（unanswerable set）**：100 条知识库里确实没有答案的问题。指标： - **拒答准确率**：正确拒答的比例（应 \> 90%）。 - **误拒率**：在可回答集上，本可答却拒答的比例（应 \< 5%）。 两个指标要同时看，只优化一个必然翻车。

------------------------------------------------------------------------

# 七、RAG 评估

## Q26：RAGAS 的四个核心指标是什么？怎么算？

答：**RAGAS**（Retrieval Augmented Generation Assessment）是当前最主流的 RAG 评估框架，核心思想是**用 LLM-as-judge 免人工标注地算指标**。

四个核心指标：

**1. Faithfulness（忠实度）** —— 衡量幻觉

    Faithfulness = 被上下文支持的论断数 / 答案中的总论断数

做法：把答案拆成原子论断（claims），逐个判断能否由检索到的 context 推出（entailment）。**这是最重要的指标**，低就意味着幻觉。

**2. Answer Relevancy（答案相关性）** —— 衡量答非所问

    做法：用 LLM 根据答案反向生成 N 个可能的提问，
         计算这些生成问题与原问题的平均余弦相似度
    Answer Relevancy = mean(cos_sim(q_original, q_generated_i))

注意：它**不判断事实正确性**，只判断”答案是否切题”。一个错误但切题的答案也能得高分，所以要配合 faithfulness 看。

**3. Context Precision（上下文精确率）** —— 衡量检索结果的信噪比

    Context Precision@k = Σ_{k} (Precision@k × relevance_k) / 相关文档总数

衡量”检索到的 chunk 里，有多少是真正相关的，且相关的排得靠不靠前”。它带排序权重，所以能反映 rerank 的效果。

**4. Context Recall（上下文召回率）** —— 衡量有没有漏

    Context Recall = 标准答案中的论断被检索上下文覆盖的数量 / 标准答案总论断数

**需要标准答案（ground truth）**。这是**最能反映检索能力**的指标，也是分层排查的起点。

**指标与排查的对应关系（面试金句）**： - Context Recall 低 → **检索/分块问题**（内容没召回） - Context Precision 低 → **排序问题**（召回了但没排前面，需 rerank） - Faithfulness 低 → **生成问题**（模型编造，需改 prompt/换模型） - Answer Relevancy 低 → **prompt/理解问题**（答非所问）

**RAGAS 使用注意**： - 依赖 LLM judge，**用强模型评弱模型才可信**（用 GPT-4o 评 7B 模型），别用同级别模型自评。 - 成本：评一条样本约 3~10 次 LLM 调用，1000 条 golden set 成本可能几十美元；建议只对抽样或关键集评。 - 中文场景可用 Qwen-72B / DeepSeek 做 judge，但需先人工校验 judge 与人工的一致性（**Spearman 相关性 \> 0.7 才可用**）。

------------------------------------------------------------------------

## Q27：检索侧的经典指标有哪些？Recall@k、MRR、NDCG 怎么算？

答：评估检索质量**不能只看 RAGAS**（它偏生成侧），检索侧要用经典 IR 指标：

**1. Recall@k（召回率@k）**

    Recall@k = 前 k 个结果中包含的相关文档数 / 全部相关文档数

最直观的检索指标。RAG 场景中**最重要**，因为漏召回是致命的（后面无法补救）。常看 Recall@5 / @10 / @50。

**2. Precision@k（精确率@k）**

    Precision@k = 前 k 个结果中的相关文档数 / k

RAG 里最终 top-k 的精确率直接影响上下文信噪比。

**3. MRR（Mean Reciprocal Rank，平均倒数排名）**

    MRR = (1/|Q|) Σ_{q} 1 / rank_first_relevant(q)

只看**第一个相关文档出现的位置**（rank 1 → 1.0；rank 2 → 0.5；rank 3 → 0.33）。适合**只有一个正确答案**的场景（如事实问答、FAQ）。对”答案唯一”的 RAG 场景非常适用。

**4. NDCG@k（Normalized Discounted Cumulative Gain）**

    DCG@k  = Σ_{i=1..k} rel_i / log2(i+1)
    NDCG@k = DCG@k / IDCG@k    （IDCG = 理想排序的 DCG）

支持**多级相关性**（rel_i 可以是 0/1/2/3），并且**位置折扣**（越靠后权重越低，因为用户/模型更关注前面）。是评估**排序质量**最全面的指标，**尤其适合评估 Rerank 的效果**。

举例（说明折扣的作用）：

    相关文档在 rank1 和 rank3：
      NDCG@3 = (1/log2(2) + 1/log2(4)) / (1/log2(2) + 1/log2(3))
             = (1 + 0.5) / (1 + 0.63) = 0.92

**5. Hit Rate@k**：前 k 里有没有（至少一个）相关文档，0/1 取均值。简单粗暴，常用于快速判断。

**6. MAP（Mean Average Precision）**：对多个相关文档的排序质量求平均，适合”多个正确答案”的场景。

**怎么选指标（面试回答）**： \| 场景 \| 推荐指标 \| \|—\|—\| \| 事实问答（答案唯一） \| MRR、Recall@10、Hit Rate@5 \| \| 多答案 / 综合问答 \| NDCG@10、MAP \| \| 评估 Rerank 效果 \| **NDCG@10**（带位置折扣，最敏感） \| \| 分层排查 \| **Recall@k 优先**（决定上限） \|

**追问：离线指标涨了，线上效果没涨，为什么？** 经典问题，原因通常有： 1. **离线集不代表线上分布**：golden set 是自己造的，线上 query 更口语、更多长尾、更多多轮指代。解法：从线上日志采样真实 query 构建评测集。 2. **指标与业务目标错位**：NDCG 提升可能来自无关长尾，核心业务 query 没变好。解法：**分桶评测**（按 query 类型、频次分层看指标）。 3. **生成侧瓶颈**：检索 Recall 已经 95%+，继续提升召回对端到端没用（天花板在生成）。解法：看 faithfulness / 答案准确率，而不是只盯检索指标。 4. **线上有额外约束**：延迟、成本、并发导致实际用了降级策略（如小模型、少候选）。

------------------------------------------------------------------------

## Q28：LLM-as-judge 有哪些坑？怎么保证评估可信？

答：RAG 评估大量依赖 LLM 打分，但 LLM judge 有一堆系统性偏差，用不好会让整个评估体系失效。

**六大坑**： 1. **位置偏见（Position Bias）**：对比 A/B 两个答案时，倾向于偏好**第一个**（或第二个，取决于模型）。缓解：**交换顺序各评一次，取平均或只采纳一致的结果**（position-swap consistency）。 2. **长度偏见（Verbosity Bias）**：偏好更长的答案。缓解：prompt 里明确”长度不是质量指标”，或在评分标准里加长度惩罚项。 3. **自我偏好（Self-preference Bias）**：GPT-4 评 GPT-4 生成的答案分数偏高。缓解：**不要用同一个模型既当选手又当裁判**；多模型交叉评判。 4. **分数聚集 / 校准差**：LLM 爱给 7~8 分，区分度低；不同批次分数不可比。缓解：用**成对比较（pairwise）替代绝对打分**（A vs B 谁更好，二选一，信噪比高得多）；或用**参考基准（reference-based）**评分。 5. **对格式/风格的偏好**：markdown 漂亮、分点清晰的答案容易被高分，即使内容空洞。缓解：评分标准要锚定**事实正确性**而非形式。 6. **不一致性（自洽性）**：temperature \> 0 时同一输入两次评分不同。缓解：temperature=0 + 多次采样取多数/平均。

**保证可信的做法**： 1. **人工标注校准集**：人工标 200~300 条，计算 LLM judge 与人工的**一致性**（Cohen’s Kappa / Spearman 相关系数）。**Kappa \> 0.6、Spearman \> 0.7 才认为可用**，否则调整 prompt 或换模型。 2. **打分标准极细化**：把 rubric 写清楚（“faithfulness=1.0 表示每个论断都能在 context 中找到原文支持；0.5 表示一半论断无支持；0 表示完全编造”），并给 few-shot 示例。 3. **用强模型评判**：judge 模型的能力必须 ≥ 被评模型。 4. **多 judge 投票**：GPT-4o + Claude + Qwen 三家投票取多数，减少单模型偏见。 5. **指标交叉验证**：RAGAS 指标 + 人工抽检 + 线上反馈（点赞/点踩）三者对照，单一信号不可信。 6. **固定评测集版本**：golden set 一旦确定就冻结，改动要记录版本号，否则指标没有可比性。

**追问：线上评估（online eval）怎么做？** 离线指标再好也只是代理。线上要做： 1. **A/B 实验**：分流 5%~10% 流量试新配置，看核心指标（答案采纳率、点赞率、追问率、会话轮次、人工转接率）。 2. **隐式反馈**：用户是否复制答案、是否追问同一问题（说明没答好）、会话是否立刻结束（说明满意）、是否点开引用查看。 3. **显式反馈**：点赞/点踩 + 原因标签（不准确/不相关/太啰嗦）。 4. **日志回流**：把线上低分样本加入 golden set，形成”**评估-优化-再评估**”闭环。这是 RAG 系统持续变好的唯一路径。 5. **Guardrail 指标**：幻觉率、拒答率、越权访问率、P99 延迟、单次成本。

------------------------------------------------------------------------

# 八、工程与性能

## Q29：RAG 的延迟怎么优化？P99 从 8s 降到 1s 的思路。

答：先把延迟**拆解归因**，再针对性优化。典型 RAG 请求的耗时分布：

    查询理解(改写/HyDE)  :  0 ~ 800ms   （LLM 调用，可删可并行）
    Embedding 编码        :  10 ~ 50ms   （小模型，快）
    向量检索 + BM25       :  20 ~ 100ms  （ANN 毫秒级，BM25 也快）
    Rerank               :  50 ~ 500ms  （Cross-Encoder，GPU 上 50 条约 50~100ms）
    LLM 生成             : 1 ~ 10s      ← 绝对大头（首 token 延迟 + 逐 token 生成）
    后处理/校验           :  0 ~ 500ms

**结论：生成占 70%~90% 的延迟，优化必须打在这里。**

**优化手段（按 ROI 排序）**：

1.  **流式输出（Streaming）** —— 感知延迟的杀手锏 首 token 时间（TTFT）从”等全部生成完”降到 300~500ms，用户立刻看到字在冒。**实际总时长没变，但体感从 8s 变 0.5s**。必做。

2.  **缓存（多层缓存）**

    - **精确缓存**：query 完全一致直接返回（Redis，命中率 10%~30%，尤其是 FAQ 场景）。
    - **语义缓存**：query 向量相似度 \> 0.95 视为同一问题（GPTCache 思路），命中率可到 30%~40%，但需谨慎（相似问题答案可能不同，阈值要保守）。
    - **Embedding 缓存 / Rerank 缓存**：相同 chunk 不重复编码。
    - **Prompt 前缀缓存（KV Cache / Prefix Caching）**：系统指令这类固定前缀让推理框架复用 KV，可省 30%+ 的 prefill 时间（vLLM/SGLang 支持）。

3.  **并行化** 稠密检索与稀疏检索**并行**（不是串行）；多查询扩展的 N 路检索并行；权限/元数据过滤与检索合并成一次调用（Milvus/Qdrant 支持带过滤检索）。

4.  **降级与分级路由**（见 Q17 追问） 简单问题走快路径，别所有 query 都跑全链路。可省 50%+ 平均延迟。

5.  **裁掉非必要步骤**

    - HyDE/多查询扩展只在”高价值 query”上开（用小模型预判）；
    - 给改写步骤设 500ms timeout，超时降级用原 query；
    - 校验（归因校验）异步做，不阻塞答案返回。

6.  **模型与推理优化**

    - 用**小模型做简单任务**（改写用 7B，只把生成交给大模型）；
    - **量化**（AWQ/GPTQ int4）提升吞吐 2~3 倍；
    - 用 **vLLM / SGLang** 做 continuous batching + PagedAttention，高并发下吞吐提升数倍；
    - 投机解码（speculative decoding）降生成延迟 1.5~2 倍。

7.  **减少输出长度**：prompt 里要求”简洁、分点、不超过 N 字”。输出 token 数直接决定生成时间。

**追问：如果用户说”你们系统慢”，你怎么定位？** 上线**全链路 trace**（LangSmith / Langfuse / OpenTelemetry），记录每个 span 的耗时：改写 X ms、检索 Y ms、rerank Z ms、首 token T ms、总生成 G ms。**没有 trace 就没有优化**。按 P50/P95/P99 分位数看，往往发现 P99 慢是因为某一步在长尾 query 上爆炸（如上下文超长导致 prefill 巨慢），而非平均慢。

------------------------------------------------------------------------

## Q30：RAG 的成本怎么优化？

答：成本 = **索引成本（一次性/增量） + 检索成本 + 生成成本（token 费用）**。生成通常占 70%+。

**优化清单**： 1. **减少注入 token**：top 5 而非 top 20；上下文压缩（只留相关句）；去重（overlap 导致的重复内容）。**这是最直接的省钱手段**。 2. **分级路由**：简单问题用小模型甚至不用模型（缓存/FAQ），只把复杂问题给大模型。 3. **小模型替代**：改写、意图识别、rerank、归因校验全走小模型（Qwen-7B / GPT-4o-mini / bge-reranker 自托管），只有主生成走大模型。 4. **自托管 vs API 的临界点**：当 token 量足够大（如日均 \> 千万 token），自托管开源模型（Qwen/Llama + vLLM）比 API 便宜得多，但需算 GPU 折旧和运维人力。粗略临界点：持续 QPS \> 2~5 时自托管开始划算。 5. **Embedding 成本**：离线索引是一次性成本；**增量更新只对新增/修改的 chunk 做 Embedding**（不要全量重跑）。用开源 Embedding 自托管，1 亿 chunk 一次性索引成本可控（GPU 几小时）。 6. **缓存**：语义缓存直接省掉整次 LLM 调用（最省钱）。 7. **批处理（Batch API）**：离线任务（批量评测、批量摘要）用 batch 接口，价格常为在线的 50%。 8. **输出长度控制**：输出 token 单价通常是在线的 3~4 倍，控制啰嗦就是省钱。 9. **量化部署**：int4 量化让单卡吞吐翻倍，摊薄 GPU 成本。

**追问：增量更新索引怎么做？** （这是高频工程题） - **新增文档**：解析 → 分块 → 只对新 chunk 做 Embedding → 写入向量库。HNSW 支持插入但要关注图结构变化；IVF 支持增量添加。 - **修改文档**：**先按 doc_id 删除该文档的所有旧 chunk，再插入新 chunk**（必须做，否则新旧内容并存导致知识冲突）。 - **删除文档**：按 doc_id 批量删除（HNSW 删除是标记删除，需定期 compact/重建）。 - **Embedding 模型更换**：**必须全量重建**（新模型向量与旧向量不可比！这是最容易踩的坑，混用会导致检索完全失效）。做法：新建一个 collection 用新模型全量索引，双写/灰度切流，验证后下线旧 collection。 - **分块策略调整**：同样需要全量重建。 - **调度**：用消息队列（Kafka/RabbitMQ）驱动异步索引流水线，支持失败重试；建立文档指纹（hash）避免重复索引。

------------------------------------------------------------------------

## Q31：多租户与权限隔离怎么做？

答：企业 RAG 的硬性要求，做不好就是**数据泄露事故**。

**核心原则：在检索层做过滤，绝不能”检索后再过滤”**。（检索后过滤意味着无权限内容已经进入了 LLM 上下文，一旦模型复述就泄露了，且日志里可能留痕。）

**隔离方案（按隔离强度递增）**：

| 方案 | 做法 | 隔离强度 | 成本 | 适用 |
|----|----|----|----|----|
| **元数据过滤** | 每个 chunk 带 `tenant_id`/`acl` 字段，检索时作为必选 filter | 中 | 最低 | 租户数少、数据量小、信任向量库的过滤 |
| **Partition/Collection 隔离** | 每个租户一个 partition 或 collection | 高 | 中 | 租户数中等（几十~几百），Milvus partition / Qdrant collection |
| **独立实例/库** | 每个租户独立部署一套向量库 | 最高 | 高（运维爆炸） | 大客户、强合规要求（金融、医疗） |

**推荐实践（分层混合）**：

    1. 大客户（KA）→ 独立 collection 或独立实例，物理隔离
    2. 中小客户 → 共享 collection，用 tenant_id + partition key 逻辑隔离
    3. 权限维度（租户内）：
       - chunk 元数据带 access_level / allowed_roles / allowed_users
       - 检索时注入用户身份 filter: 
         filter = (tenant_id == "t1") AND (access_level IN user.roles)
    4. 向量库层强制：给查询加"必选过滤"(must filter)，而非 optional，
       并在业务层做二次校验（defense in depth）

**其他要点**： 1. **权限继承**：文档级权限要下沉到 chunk 级（父文档的 ACL 复制给所有子 chunk）。 2. **权限变更的时效性**：用户离职/调岗后，权限应立即失效。做法：**权限实时查询（从权限系统拿当前角色）而非快照在元数据里**，元数据里只存”部门/密级”这类稳定标签，动态权限在检索时 join。 3. **引用溯源也要鉴权**：用户点击引用查看原文时，要再校验一次权限（防止通过引用 URL 越权）。 4. **审计日志**：记录谁在什么时候检索了什么、返回了什么 chunk，满足合规审计。 5. **测试**：专门的权限测试用例（用低权限账号尝试检索高密文档，必须失败）。 6. **走漏风险**：多租户共享 Embedding 模型没有泄露风险（模型无记忆），但**共享缓存有风险** —— 语义缓存的 key 必须包含 tenant_id 和用户权限指纹，否则 A 用户的问题会命中 B 用户的缓存答案。

------------------------------------------------------------------------

## Q32：GraphRAG 是什么？什么时候该用知识图谱？

答：**GraphRAG（Microsoft, 2024）** 的核心：从文档中抽取**实体和关系**构建知识图谱，并预先生成**社区摘要（community summaries）**，检索时结合图谱结构与文本块。

**构建流程**： 1. 用 LLM 从每个 chunk 抽取实体（Entity）和关系（Relation）→ 构建图（节点=实体，边=关系，带上原文出处）。 2. 用 Leiden 算法做**社区检测**（把紧密关联的实体聚成社区）。 3. 用 LLM 为每个社区生成**摘要**（社区报告）。 4. 检索时两种模式： - **Local Search（局部搜索）**：query 命中某些实体 → 扩展其邻居/关系子图 → 结合相关文本块回答。适合**具体实体的问题**（“A 公司和 B 公司什么关系”）。 - **Global Search（全局搜索）**：query 是全局性问题（“这批文档的主要风险主题是什么”）→ map-reduce 遍历所有社区摘要 → 汇总。**这是向量 RAG 完全做不到的**。

**GraphRAG vs 向量 RAG**：

|  | 向量 RAG | GraphRAG |
|----|----|----|
| 擅长 | 局部事实检索、单跳问答 | **多跳推理、全局聚合、关系查询** |
| 全局问题 | ❌ 检索是局部的，无法聚合全库 | ✅ 社区摘要可 map-reduce |
| 多跳 | 弱（需 iterative retrieval 补救） | ✅ 图遍历天然多跳 |
| 索引成本 | 低（只需 Embedding） | **极高**（每个 chunk 都要 LLM 抽实体，成本可能是向量 RAG 的 10~100 倍） |
| 增量更新 | 容易 | 难（加文档可能改变社区结构，需重建） |
| 可解释 | 中 | 高（可展示实体关系图） |

**什么时候用 GraphRAG**： - 问题涉及**跨文档、多实体关系**（“哪些供应商同时给 A 和 B 供货”）； - 需要**全局主题/趋势总结**（“这批合同的主要风险类别有哪些”）； - 领域本身有强结构化知识（医疗、法律、金融风控、供应链）； - 有预算（GraphRAG 的索引成本是硬门槛）。

**大多数场景的务实做法**： 1. 先做好向量 RAG + 混合检索 + Rerank（解决 80% 问题）； 2. 用**元数据 + 结构化字段**做一个”轻图谱”（如用 doc 的标题层级、文档间引用关系）； 3. 只有当多跳/聚合问题占比 \> 20% 且预算充足时才上 GraphRAG； 4. 也可混合：**图检索用于关系/聚合类 query，向量检索用于事实类 query**，用路由分发。

**追问：GraphRAG 落地最大的坑是什么？** 1. **成本**：一个 100 万 token 的语料，抽实体用 GPT-4o 可能花费数百到上千美元，社区摘要再翻倍。 2. **实体消歧**：LLM 抽出的实体大量重复（“苹果” vs “Apple Inc.” vs “苹果公司”），必须做实体对齐/归一化，否则图是碎的。 3. **增量更新困难**：新增文档可能导致社区结构变化，Microsoft 的方案基本要求全量重建。 4. **延迟**：Global search 要 map-reduce 所有社区摘要，延迟高（秒级到十秒级）。 5. **抽不准**：LLM 抽关系的准确率在垂直领域可能只有 70~80%，错边会污染推理路径。

------------------------------------------------------------------------

# 九、手撕 / 系统设计

## Q33（系统设计）：请从 0 设计一套企业文档问答 RAG 系统，说明每个环节的选型与理由。

**场景假设**：某 5000 人企业，内部文档 50 万份（PDF/Word/Confluence/飞书/Slack），日均 2 万次问答，要求：答案可溯源、权限严格隔离、P95 延迟 \< 3s、支持文档日更新、中文为主。

### 一、整体分层架构

    ┌──────────────────────────────────────────────────────────────────┐
    │  接入层  Web / 企业微信 / 飞书 / API                             │
    │  鉴权(SSO/OAuth) + 限流 + 会话管理                                │
    └───────────────────────────┬──────────────────────────────────────┘
                                ▼
    ┌──────────────────────────────────────────────────────────────────┐
    │  编排层 (Orchestrator)   LangGraph / 自研 Pipeline                │
    │  ├ 意图路由：闲聊 / FAQ / 检索问答 / 工具调用 / 拒答               │
    │  ├ 分级路由：L0 缓存 → L1 单跳 → L2 多跳                          │
    │  ├ 多轮管理：会话历史、指代消解、话题切换检测                       │
    │  └ 全链路 Trace（Langfuse）：每步耗时 + 中间结果                    │
    └───────────────────────────┬──────────────────────────────────────┘
            ┌───────────────────┼───────────────────┐
            ▼                   ▼                   ▼
    ┌───────────────┐  ┌────────────────┐  ┌──────────────────┐
    │  查询理解      │  │  检索层         │  │  生成层           │
    │ 指代消解       │  │ 稠密召回 HNSW   │  │ Prompt 组装       │
    │ 多查询扩展(3路)│  │ BM25 稀疏召回   │  │ 引用编号注入      │
    │ HyDE(按需)     │  │ 元数据/权限过滤 │  │ LLM 流式生成      │
    │ 意图分类(小模型)│  │ RRF 融合 k=60  │  │ 引用校验(异步)    │
    └───────────────┘  │ Rerank top30→5 │  └──────────────────┘
                       └────────────────┘
                                │
    ┌───────────────────────────┴──────────────────────────────────────┐
    │  数据层                                                           │
    │  向量库 Milvus / Qdrant（chunk 向量 + 元数据 + ACL）              │
    │  倒排 ES（BM25 + 全文检索 + 高亮）                                │
    │  Redis（精确缓存 + 语义缓存 + 会话）                              │
    │  PostgreSQL（文档元数据、权限映射、评测集、反馈）                  │
    │  MinIO/S3（原始文件 + 页面图）                                    │
    └───────────────────────────┬──────────────────────────────────────┘
                                ▼
    ┌──────────────────────────────────────────────────────────────────┐
    │  离线索引流水线（Kafka 驱动，异步）                                │
    │  文档采集 → 解析(MinerU/PyMuPDF路由) → 清洗 → 分块(父子)           │
    │  → Embedding(BGE-M3) → 写入向量库 + ES → 建索引 → 质量抽检         │
    │  触发方式：新增/修改 webhook + 定时全量校验 + 增量队列              │
    └──────────────────────────────────────────────────────────────────┘
                                ▼
    ┌──────────────────────────────────────────────────────────────────┐
    │  评估与运营层                                                     │
    │  离线：golden set(1000条) → RAGAS + NDCG/Recall/MRR              │
    │  在线：A/B 实验 + 点赞点踩 + 人工抽检 + 日志回流                   │
    │  Dashboard：Recall@10 / Faithfulness / P95延迟 / 成本 / 拒答率     │
    └──────────────────────────────────────────────────────────────────┘

### 二、各环节选型与理由

**1. 文档解析** - 路由策略：纯文本 PDF/Word → PyMuPDF（快）；复杂版式/扫描件 → MinerU / Unstructured（准）；表格 → Table Transformer 转 Markdown；公式 → Nougat/UniMERNet 转 LaTeX；图片 → VLM 生成 caption。 - **理由**：解析是质量天花板，宁可慢一点也要准。建立解析抽检机制（200 篇人工比对，字符准确率目标 \> 97%）。

**2. 分块** - **父子分块**：子块 256 token（检索用），父块 1024~2048 token（生成用），overlap 15%。 - 分块器：递归字符分块，按标题层级优先切（保留 `title_path` 元数据）。 - 表格独立成块（不切），附所属章节元数据。 - **理由**：小块保证检索精度，父块保证上下文完整，是解决”检索准 vs 上下文全”矛盾的标准方案。

**3. Embedding** - **BGE-M3**（1024 维，8192 长窗口，同时输出 dense + sparse + colbert）。 - 选型理由：中文效果一流、可私有化（数据不出域，企业刚需）、一个模型支持混合检索的三路信号、长窗口便于 late chunking 探索。 - 用业务数据做**难负样本微调**（可选，效果提升明显时再做）。

**4. 向量库与索引** - **Milvus**（数据量 50 万文档 → 约 500 万~2000 万 chunk，需分布式与多租户分区）。 - 索引：**HNSW**（M=32, efConstruction=200, efSearch=128），内存充足优先 HNSW 保召回；若内存吃紧改 IVF-PQ。 - 隔离：按 `dept_id` 分 partition，`tenant_id` + `access_level` 做 must-filter。 - **理由**：Milvus 支持 partition + bitset 过滤 + 分布式扩展，适合企业多租户；HNSW 召回最好，50 万文档规模内存可承受。 - 同时保留 **ES** 做 BM25（含分词词典 + 领域词典）和引用高亮。

**5. 检索** - 稠密 top 50（HNSW）+ BM25 top 50（ES），并行。 - **RRF 融合 k=60** → top 30。 - 查询改写：多轮指代消解（必做，小模型）+ 多查询扩展 3 路（L2 才开）+ HyDE（仅在短 query/召回为空时触发）。 - 权限过滤在向量库层用 must-filter 完成（dept + access_level + tenant）。 - **理由**：混合检索补足 BM25 的字面匹配（型号、错误码、人名）与稠密的语义匹配；RRF 免调权重且鲁棒。

**6. Rerank** - **BGE-Reranker-v2-m3**（GPU，batch 推理），30 条 → top 5。 - 延迟约 50~80ms，P95 可接受。 - 高价值低频场景（合同审查）额外加 LLM listwise 终排。 - **理由**：ROI 最高的单项优化，实测端到端准确率 +15 点左右。

**7. 生成** - 上下文组装：按 rerank 分数**头尾重排**（规避 lost in the middle），去重，总长控制 2500 token 内。 - Prompt：系统指令（只用资料 + 强制引用 `[n]` + 拒答规则）+ 带来源元数据的 chunk + few-shot 拒答示例。 - 模型：主力用闭源强模型（GPT-4o / Claude）或自托管 Qwen-72B；简单 query 走 Qwen-14B 降本。 - **流式输出**（首 token \< 500ms）。 - 生成后：引用有效性校验（同步，快）+ 归因校验（NLI，异步，发现幻觉则修正/提示）。 - **理由**：引用提升可信度，拒答指令压幻觉，流式优化体感延迟。

**8. 评估** - 离线：1000 条 golden set（覆盖各类型 query + 不可回答集 100 条），指标 Recall@10 / NDCG@10 / MRR / RAGAS 四件套，分层看（按部门、按 query 类型）。 - 在线：A/B 分流 10%，看点赞率、采纳率、追问率、P95 延迟、成本。 - 日志回流：低分样本自动进”待标注池”，每周迭代一次评测集。 - **理由**：没有评估就没有优化方向；离线保证回归不退化，在线验证真实收益。

**9. 性能与成本** - 缓存：Redis 精确缓存 + 语义缓存（阈值 0.95，key 含 tenant/权限指纹）+ Prompt 前缀 KV cache。 - 分级路由：L0 缓存 → L1 单跳（~1s）→ L2 全链路（~3s），预计 70% 走 L0/L1。 - 并行：稠密 + 稀疏 + 多查询并行；校验异步。 - 增量索引：Kafka 驱动，文档变更只重索引该文档的 chunk；Embedding 模型升级时全量重建 + 双写灰度。 - **理由**：延迟与成本大头在生成，分级 + 缓存 + 流式是三个最有效的杠杆。

### 三、关键取舍问答（面试官会追问的点）

**Q：为什么不用纯向量检索？** A：企业文档里大量精确查询（错误码、产品型号、法条编号、人名），稠密向量对低频精确 token 召回差；混合检索实测 Recall@10 比纯稠密高 10~15 个点。

**Q：为什么召回 50 条但只喂 5 条给 LLM？** A：召回宁多勿少（漏了救不回），生成宁精勿多（噪声 + lost in the middle + 成本）。50 → rerank → 5 是精度和成本的最优平衡点。

**Q：怎么保证答案不出错？** A：四道防线 —— 检索质量（Recall 保证答案在上下文里）、rerank（保证排前面）、prompt 强制引用 + 拒答指令、生成后归因校验。且明确”检索是幻觉的第一根因”，优化优先级永远是检索 \> 生成。

**Q：数据量涨到 10 亿 chunk 怎么办？** A：Milvus 水平扩容（加 query node / data node）+ sharding；索引从 HNSW 换 DiskANN（内存放不下时）；引入两段式 rerank（小模型粗排 200 → 20 → 大模型取 5）；对热门文档做预计算缓存。

------------------------------------------------------------------------

## Q34（手撕/系统设计）：如何为一个已上线的 RAG 系统做”效果优化”？（诊断题）

答：这是最考工程能力的题 —— 不给场景，只给”效果不好”。正确回答是**一套可执行的诊断流程**，而不是罗列技巧。

**第一步：量化问题（不要凭感觉）** 1. 建 golden set：从线上日志采样 300~1000 条真实 query，人工标注标准答案 + 正确 chunk 出处。**没有这个，后面全是盲调**。 2. 跑分层指标，定位瓶颈在哪一环：

| 指标                    | 低的话说明       | 下一步                      |
|-------------------------|------------------|-----------------------------|
| **Context Recall@5**    | 内容根本没召回   | → 查分块/Embedding/查询改写 |
| **Context Precision@5** | 召回了但没排前面 | → 加/换 Rerank，调 RRF 权重 |
| **Faithfulness**        | 模型编造         | → 改 prompt、换模型、加校验 |
| **Answer Relevancy**    | 答非所问         | → 改 prompt、检查上下文噪声 |

**第二步：按瓶颈分支深挖**

**分支 A：Context Recall 低（80% 的问题在这里）** - 用”答案不在召回集”的 bad case 逐条人工看： - 内容是否在分块时被切断 → 调 chunk size / overlap，或改父子分块； - 是否在表格/图片里没被解析 → 修解析（这是隐藏最深的原因）； - 是否为精确匹配（型号/编号）→ 加 BM25 混合检索； - 是否为表达 gap（用户口语 vs 文档书面）→ 加多查询扩展 / HyDE； - 是否被元数据过滤误杀 → 检查 filter 条件（高频踩坑）； - 是否多轮指代未消解 → 查会话历史拼接。

**分支 B：Context Precision 低** - 对比”rerank 前 top5” vs “rerank 后 top5” 的准确率，量化 rerank 收益； - 检查 RRF 的 k 和权重（是否某一路噪声过大）； - 增加 rerank 候选数（30 → 50）看是否还有提升； - 换更强的 reranker（base → large → LLM listwise）。

**分支 C：Faithfulness 低（检索没问题但答案编）** - 看 bad case 是”上下文里有答案但模型不用”还是”上下文没有答案模型硬编”： - 前者 → 上下文太长/噪声多/lost in the middle → **头尾重排 + 上下文压缩**； - 后者 → **拒答指令 + few-shot 拒答示例 + 提高拒答阈值**； - 换模型（指令遵循能力差异大）； - 加归因校验，不支持的论断直接删。

**分支 D：延迟/成本不达标** - 上 trace 看耗时分布，打在大头（生成）上：流式、缓存、分级路由、小模型、量化。

**第三步：验证与上线** 1. 每次只改一个变量，在 golden set 上跑回归，确认指标提升； 2. A/B 实验（10% 流量），看线上真实指标； 3. 确认提升后再推全量；记录改动到”优化日志”（改了啥、为什么、指标变化），形成团队资产。

**面试加分收尾**：“我会反复强调一个原则 —— **先测量再优化，先把 Recall 做上去再谈生成**。因为生成的天花板由召回决定，而 80% 的 RAG 效果问题根源在检索侧（分块、解析、Embedding、过滤），不在 prompt。”

------------------------------------------------------------------------

## Q35（手撕）：手写一个最简 RAG 的核心逻辑（伪代码/真代码）

答：面试可能让你现场写”给定句库，实现检索 + 生成”或”实现 RRF 融合”。

**1. 最简 RAG 主流程（Python 伪代码）**

    from typing import List, Tuple
    import numpy as np

    class SimpleRAG:
        def __init__(self, embed_model, llm, chunks: List[str], metas: List[dict]):
            self.embed, self.llm = embed_model, llm
            self.chunks, self.metas = chunks, metas
            # 离线索引
            self.emb = np.array([embed_model.encode(c) for c in chunks])
            self.emb /= np.linalg.norm(self.emb, axis=1, keepdims=True)

        def retrieve(self, query: str, top_k: int = 5) -> List[Tuple[str, dict, float]]:
            q = self.embed.encode(query)
            q = q / np.linalg.norm(q)
            sims = self.emb @ q                      # 余弦相似度（向量已归一化）
            idx = np.argsort(-sims)[:top_k]          # 小规模直接暴力排序
            return [(self.chunks[i], self.metas[i], float(sims[i])) for i in idx]

        def generate(self, query: str, contexts) -> str:
            ctx = "\n\n".join(
                f"[{i+1}] 来源《{m.get('source')}》第{m.get('page')}页\n{c}"
                for i, (c, m, _) in enumerate(contexts)
            )
            prompt = f"""你是企业知识助手。请严格依据【参考资料】回答，不要使用常识补充。
    每个论断后用 [编号] 标注来源。若资料不足以回答，请明确说明"根据现有资料无法回答"。

    【参考资料】
    {ctx}

    【问题】{query}
    【回答】"""
            return self.llm.invoke(prompt)

        def query(self, q: str) -> dict:
            ctxs = self.retrieve(q, top_k=5)
            answer = self.generate(q, ctxs)
            return {"answer": answer,
                    "sources": [m for _, m, _ in ctxs]}

**2. RRF 融合（高频手撕题）**

    def rrf_fusion(rankings: List[List[str]], k: int = 60, top_n: int = 30) -> List[str]:
        """
        rankings: 多路召回的结果，每路是一个按相关性降序排列的 doc_id 列表
        k: RRF 平滑常数，论文默认 60
        """
        scores = {}
        for ranking in rankings:
            for rank, doc_id in enumerate(ranking, start=1):   # rank 从 1 开始
                scores[doc_id] = scores.get(doc_id, 0.0) + 1.0 / (k + rank)
        return sorted(scores, key=lambda d: -scores[d])[:top_n]

**3. HNSW 参数与建索引（伪代码）**

    # Milvus 示例
    index_params = {
        "index_type": "HNSW",
        "metric_type": "IP",          # 内积（向量归一化后等价余弦）
        "params": {"M": 32, "efConstruction": 200}
    }
    search_params = {"metric_type": "IP", "params": {"ef": 128}}   # efSearch

    collection.search(vectors=[q_vec], anns_field="embedding",
                      param=search_params, limit=50,
                      expr='tenant_id == "t1" and access_level in ["hr","all"]')  # must-filter

**4. 易错点（面试官爱挑的）** - 余弦相似度**必须先归一化**向量，否则等价于内积（结果不同）； - `rank` 从 1 开始，不是 0（`1/(k+0)` 会爆炸）； - 多路召回结果要**去重**（同一 doc_id 在多路出现时 RRF 分数应累加，这本身就是 RRF 的设计）； - 检索时**权限过滤必须在向量库层做**，不能检索后过滤； - 元数据要与 chunk 一一对应，切分时不能错位（常见 bug）。

------------------------------------------------------------------------

# 附录：高频速答卡（面试前 30 分钟速览）

| 主题 | 一句话记忆点 |
|----|----|
| 为什么 RAG | 知识截止 + 幻觉 + 私有知识；可溯源、可实时更新、成本低 |
| RAG vs 微调 | 知识会变/要溯源 → RAG；改风格/行为 → 微调；一次性整体信息 → 长上下文 |
| 全流程 | 索引 → 检索 → 重排 → 生成 → 评估（评估要反向驱动每一环） |
| 分块默认值 | 子块 256 token / 父块 1024~2048，overlap 10%~20% |
| 分块演进 | fixed → recursive → semantic → parent-child（主力）→ late chunking |
| Embedding 中文首选 | BGE-M3（dense+sparse+colbert 三路，8192 长窗口，可私有化） |
| 训练关键 | 难负样本挖掘 + 大 batch + 防假负样本 |
| ANN 默认值 | HNSW: M=32, efConstruction=200, efSearch=128；IVF: nlist≈4√N, nprobe 1~10% |
| 混合检索融合 | RRF，k=60，只用排名不用分数，免归一化 |
| 查询改写 | 多轮指代消解(必做) \> 多查询扩展(3路) \> HyDE(短query) \> 查询分解(复杂问题) |
| Self/CRAG/Adaptive | 自评反思 / 检索质检+纠错 / 难度分级动态策略（最易落地） |
| Rerank | Bi-Encoder 召回 + Cross-Encoder 精排；召回 50 → 融合 30 → 精排 5 |
| Rerank 收益 | 端到端准确率 +10~20 点，ROI 最高 |
| 幻觉根因 | 80% 在检索侧，不在生成侧 |
| Lost in the middle | 头尾放重点，控制 chunk 数（top5 常优于 top20） |
| 拒答 | 多信号融合 + 分级响应；宁多拒不少编 |
| RAGAS 四件套 | Faithfulness / Answer Relevancy / Context Precision / Context Recall |
| 指标归因 | Recall 低→检索；Precision 低→排序；Faithfulness 低→生成；Relevancy 低→prompt |
| 检索经典指标 | Recall@k、MRR（答案唯一）、NDCG@10（评 rerank） |
| 延迟大头 | 生成占 70~90%；三板斧 = 流式 + 缓存 + 分级路由 |
| 增量索引坑 | 换 Embedding 模型必须全量重建（新旧向量不可比） |
| 权限 | 必须在向量库层 must-filter，检索后过滤 = 泄露 |
| GraphRAG | 多跳 + 全局聚合才用；索引成本高 10~100 倍；增量更新难 |
| 系统设计口诀 | 解析保准 → 父子分块 → 混合召回 → RRF → Rerank → 引用约束生成 → 评估闭环 |

------------------------------------------------------------------------

本文件整理日期：2026-09-12
