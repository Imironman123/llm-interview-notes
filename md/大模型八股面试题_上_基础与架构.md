------------------------------------------------------------------------

**来源与参考**

本文内容整理自公开面经仓库、原始论文与工程实践，主要参考：

- GitHub [wdndev/llm_interview_note](https://github.com/wdndev/llm_interview_note)：大模型面试八股笔记，涵盖 Transformer、位置编码、MoE、推理优化等
- GitHub [WeThinkIn/AIGC-Interview-Book](https://github.com/WeThinkIn/AIGC-Interview-Book)：AIGC 算法岗面试宝典
- GitHub [km1994/LLMs_interview_notes](https://github.com/km1994/LLMs_interview_notes)：大模型面试题笔记
- GitHub [Junvate/LLM-Algorithm-Intern-Guide](https://github.com/Junvate/LLM-Algorithm-Intern-Guide)：LLM 算法实习指南
- 原始论文：*Attention Is All You Need* (2017)、*RoPE* (2021)、*FlashAttention 1/2/3*、*Switch Transformer*、*DeepSeekMoE*、*Chinchilla*、*LLaMA 1/2/3*、*YaRN*、*vLLM/PagedAttention* 等

------------------------------------------------------------------------

# 一、Transformer 架构细节

## Q1：请写出 Self-Attention 的完整公式，并推导其时间复杂度与空间复杂度。

**答：**

标准 Scaled Dot-Product Attention（*Attention Is All You Need*, 2017）：

``` math
\text{Attention}(Q,K,V) = \text{softmax}\left( \frac{QK^{\top}}{\sqrt{d_{k}}} + M \right)V
```

其中 $`Q \in \mathbb{R}^{n \times d_{k}}`$，$`K \in \mathbb{R}^{n \times d_{k}}`$，$`V \in \mathbb{R}^{n \times d_{v}}`$，$`n`$ 为序列长度，$`M`$ 为 mask 矩阵（padding mask 或 causal mask，被 mask 位置取 $`- \infty`$，softmax 后权重为 0）。

**维度变化：**

| 步骤 | 操作                               | 输出维度           |
|------|------------------------------------|--------------------|
| 1    | $`XW_{Q}`$                         | $`n \times d`$     |
| 2    | $`QK^{\top}`$                      | $`n \times n`$     |
| 3    | $`/\sqrt{d_{k}}`$ + mask + softmax | $`n \times n`$     |
| 4    | $`\cdot V`$                        | $`n \times d_{v}`$ |
| 5    | $`\cdot W_{O}`$                    | $`n \times d`$     |

**复杂度推导：**

- $`QK^{\top}`$：矩阵乘 $`(n \times d_{k}) \cdot (d_{k} \times n)`$，计算量 $`O(n^{2}d_{k})`$
- $`\text{softmax}`$：对 $`n^{2}`$ 个元素做归一化，$`O(n^{2})`$
- $`\cdot V`$：$`(n \times n) \cdot (n \times d_{v})`$，计算量 $`O(n^{2}d_{v})`$

因此 Self-Attention 的**时间复杂度为** $`O(n^{2}d)`$（取 $`d_{k} = d_{v} = d`$），**空间复杂度为** $`O(n^{2})`$（必须显式或隐式存注意力矩阵）。

相比之下，线性层（FFN、投影）是 $`O(nd^{2})`$。所以： - 当 $`n < d`$（短序列）：瓶颈在线性层 $`O(nd^{2})`$ - 当 $`n > d`$（长序列）：瓶颈在注意力 $`O(n^{2}d)`$，呈**二次增长**

设模型层数为 $`L`$，则整个 Transformer 的前向复杂度为 $`O(L \cdot n^{2}d + L \cdot nd^{2})`$。编码器因无 causal mask 可并行，解码器每步生成是 $`O(nd^{2})`$ 级别的增量计算。

**追问：** 为什么长文本推理（如 128K 上下文）显存会爆？因为 KV Cache 大小为 $`2 \times L \times n \times d_{kv} \times \text{bytes}`$（2 表示 K 和 V）。以 LLaMA-2-7B 为例（$`L = 32`$，$`d_{kv} = 4096`$，FP16），$`n = 4096`$ 时 KV Cache 约 2GB；$`n = 128\text{K}`$ 时膨胀到约 64GB，远超模型权重本身。这正是 GQA / MQA / PagedAttention 要解决的问题。

------------------------------------------------------------------------

## Q2：Self-Attention 里为什么要除以 $`\sqrt{d_{k}}`$？

**答：**

核心是**控制点积的方差，防止 softmax 进入饱和区导致梯度消失**。

推导：假设 $`q`$ 和 $`k`$ 的各维度是独立同分布、均值 0、方差 1 的随机变量（实际中初始化通常如此）。则点积

``` math
q \cdot k = \sum_{i = 1}^{d_{k}}q_{i}k_{i}
```

其均值 $`E\lbrack q \cdot k\rbrack = 0`$，方差

``` math
\text{Var}(q \cdot k) = \sum_{i = 1}^{d_{k}}\text{Var}(q_{i}k_{i}) = d_{k}
```

即方差随维度线性增长，点积的标准差为 $`\sqrt{d_{k}}`$。当 $`d_{k}`$ 很大（如 128），点积数值会达到几十甚至上百的量级。

这会导致两个问题： 1. **softmax 饱和**：输入极值差距大时，softmax 输出接近 one-hot，最大项趋近 1，其余趋近 0； 2. **梯度消失**：softmax 的雅可比在饱和区趋于 0，反向传播梯度 $`\partial\text{softmax}/\partial z \rightarrow 0`$，参数几乎不更新。

除以 $`\sqrt{d_{k}}`$ 后，$`\text{Var}(q \cdot k/\sqrt{d_{k}}) = 1`$，点积被拉回合理区间，softmax 分布更平滑，梯度更稳定。

**追问：** 为什么不是除以 $`d_{k}`$？除以 $`d_{k}`$ 会让方差变成 $`1/d_{k}`$，当维度大时点积过小，softmax 输出趋于均匀分布（最大熵），注意力失去区分度，也无法有效学习。$`\sqrt{d_{k}}`$ 是使方差归一化为 1 的那个临界值，这是论文 *Attention Is All You Need* 原文的解释。

**追问 2：** 这个假设在实际训练中还成立吗？不完全。深层的 $`q,k`$ 分布方差会漂移（尤其 Pre-LN 之后），这也是为什么后续有 QK-Norm（如 ViT-22B、Gemma 2 在 QK 上做 LayerNorm）来显式稳定 logits 尺度。

------------------------------------------------------------------------

## Q3：Multi-Head Attention 的作用是什么？参数量如何估算？

**答：**

**公式：**

``` math
\text{MultiHead}(Q,K,V) = \text{Concat}(\text{head}_{1},\ldots,\text{head}_{h})W_{O}
```

``` math
\text{head}_{i} = \text{Attention}(QW_{Q}^{i},KW_{K}^{i},VW_{V}^{i})
```

其中 $`W_{Q}^{i} \in \mathbb{R}^{d \times d_{k}}`$，$`W_{K}^{i} \in \mathbb{R}^{d \times d_{k}}`$，$`W_{V}^{i} \in \mathbb{R}^{d \times d_{v}}`$，$`W_{O} \in \mathbb{R}^{hd_{v} \times d}`$。通常取 $`d_{k} = d_{v} = d/h`$，保证总计算量与单头 $`d`$ 维注意力相当。

**作用：**

1.  **多子空间表示**：每个头在不同投影子空间上学习不同的注意力模式。论文可视化的结果：有的头关注局部相邻词、有的关注句法依赖（如动词-宾语）、有的关注指代关系。
2.  **提升表达能力**：单头时注意力权重矩阵被 softmax 约束为一行和 1，表达能力受限；多头相当于在 $`h`$ 个低维空间并行做注意力，再拼接融合，等价于一种「集成」。
3.  **保持计算量不变**：因为 $`d_{k} = d/h`$，多头总 FLOPs 与单头全维几乎相同，只是把一次大矩阵乘拆成 $`h`$ 次小矩阵乘。

**参数量估算（以 LLaMA 风格，无 bias，**$`d_{k} = d_{v} = d/h`$**）：**

``` math
\text{Params} = 4d^{2} = \underset{W_{Q}}{\underbrace{d^{2}}} + \underset{W_{K}}{\underbrace{d^{2}}} + \underset{W_{V}}{\underbrace{d^{2}}} + \underset{W_{O}}{\underbrace{d^{2}}}
```

即注意力部分参数量**与头数** $`h`$ **无关**，只与 $`d`$ 有关。

以 LLaMA-2-7B（$`d = 4096`$，$`h = 32`$，$`L = 32`$）为例： - 单层注意力：$`4 \times 4096^{2} \approx 67\text{M}`$ - 单层 FFN：$`3 \times d \times d_{ff} = 3 \times 4096 \times 11008 \approx 135\text{M}`$（SwiGLU 三矩阵） - 单层合计约 202M，32 层约 6.5B，加上 embedding（$`32000 \times 4096 \approx 131\text{M}`$）与少量 norm，约 6.7B。这与官方公布的 6.74B 一致。

**追问：** 头数是越多越好吗？不是。头数过多会导致每个头维度 $`d_{k}`$ 太小，表达能力下降，且 GPU 上小矩阵乘效率低。实践中 $`d/h`$ 通常取 64~128（LLaMA-2-7B: $`4096/32 = 128`$；LLaMA-3-8B: $`4096/32 = 128`$）。另外，大量研究表明**很多注意力头是冗余的**，可以剪枝（*Are Sixteen Heads Really Better than One?*）。

------------------------------------------------------------------------

## Q4：FFN 的作用是什么？中间层维度放大比例为什么是 4x？SwiGLU 为什么是 8/3 x？

**答：**

**FFN 结构：**

标准 Transformer FFN 是两层 MLP 加非线性：

``` math
\text{FFN}(x) = \max(0,xW_{1} + b_{1})W_{2} + b_{2}
```

SwiGLU（*GLU Variants Improve Transformer*, 2020）版本：

``` math
\text{FFN}_{\text{SwiGLU}}(x) = \left( \text{Swish}(xW_{\text{gate}}) \odot xW_{\text{up}} \right)W_{\text{down}}
```

**作用：**

1.  **非线性变换与特征交互**：注意力是加权求和（本质线性），FFN 提供逐位置的非线性，是主要的「记忆/知识存储」单元。研究（*Transformer Feed-Forward Layers Are Key-Value Memories*）表明 FFN 第一层权重充当「键」，第二层充当「值」，存储事实性知识。
2.  **升维-降维**：先把 $`d`$ 维映射到高维 $`d_{ff}`$ 再压回，类似 kernel trick，在高维空间更容易线性可分。
3.  **参数占比大**：FFN 通常占模型总参数的 **2/3 左右**。LLaMA-2-7B 中 FFN 约 135M/层，是注意力的 2 倍。

**为什么是 4x？**

原始论文取 $`d_{ff} = 4d`$ 纯粹是工程经验（当时 $`d_{model} = 512`$，$`d_{ff} = 2048`$），并没有强理论依据。后续研究（如 *Transformer Quality in Linear Time*）尝试用 $`d_{ff} \approx 4d`$ 的近似最优性解释：在固定参数量预算下，$`d_{ff}`$ 与 $`d`$ 之间存在一个使 loss 最低的平衡点。

**SwiGLU 为什么是 8/3 x？**

SwiGLU 有**三个**权重矩阵（gate/up/down），参数量为 $`3dd_{ff}`$；标准 FFN 只有两个，$`2dd_{ff}'`$。若要在相同参数量下对比，需满足：

``` math
3dd_{ff} = 2dd_{ff}' \Longrightarrow d_{ff} = \frac{2}{3}d_{ff}'
```

代入 $`d_{ff}' = 4d`$ 得 $`d_{ff} = \frac{8}{3}d \approx 2.67d`$。

但 LLaMA 实际上**没有**用 8/3x，而是用了 $`d_{ff} = \frac{8}{3}d`$ 再对齐到 256 的倍数： - LLaMA-1/2-7B：$`d = 4096`$，$`\frac{8}{3}d = 10922.7 \rightarrow`$ **11008**（即 2.6875x） - LLaMA-2-13B：$`d = 5120 \rightarrow`$ 13824 - LLaMA-2-70B：$`d = 8192 \rightarrow`$ 22016

之所以实际比 8/3 略大，是因为对齐到硬件友好的倍数。也有一些模型用 4x + SwiGLU（如 PaLM 在部分配置），参数量会更大。

**追问：** 为什么不直接用 ReLU？SwiGLU 在多个 benchmark 上比 ReLU/GELU FFN 有稳定提升（论文 *GLU Variants Improve Transformer* 的消融实验），代价是参数增加 50%，但若按等参数对齐则几乎无额外成本。目前主流开源模型（LLaMA、Qwen、Mistral、DeepSeek）全部使用 SwiGLU。

------------------------------------------------------------------------

## Q5：残差连接的作用？Pre-LN、Post-LN、RMSNorm 有什么区别？

**答：**

**残差连接：**

``` math
x_{l + 1} = x_{l} + \mathcal{F}(x_{l})
```

梯度回传时 $`\frac{\partial x_{l + 1}}{\partial x_{l}} = I + \frac{\partial\mathcal{F}}{\partial x_{l}}`$，恒等项 $`I`$ 保证梯度可以**无损地跨层传播**，缓解深层网络的梯度消失，使训练上百层成为可能（*Deep Residual Learning*, ResNet）。在 Transformer 中残差还有「保持信息通路」的作用，让注意力层可以专注学习增量。

**三种放置方式：**

| 方案 | 公式 | 特点 |
|----|----|----|
| **Post-LN** | $`x_{l + 1} = \text{LN}(x_{l} + \mathcal{F}(x_{l}))`$ | 原始 Transformer。LN 在残差之后，残差主通路上有 LN，梯度被归一化缩放，**深层难训**，需 warmup |
| **Pre-LN** | $`x_{l + 1} = x_{l} + \mathcal{F}(\text{LN}(x_{l}))`$ | GPT-2 起主流。LN 在子层内部，残差主通路**恒等无缩放**，梯度流更稳，**不需要 warmup**，可用更大学习率 |
| **Post-LN with init** | 配合 $`\frac{1}{\sqrt{2L}}`$ 缩放初始化 | 如 GPT-1/原始论文，需要复杂 warmup |

**为什么 Pre-LN 成为主流？** 论文 *On Layer Normalization in the Transformer Architecture* (2020) 证明 Post-LN 的输出层附近梯度较大，靠近输入的层梯度小，导致需要 warmup；Pre-LN 则梯度尺度更一致，训练更稳。代价是 Pre-LN 最终性能略逊（因为深层残差累加使数值方差不断增大，等价于「隐式的深度衰减」），因此出现了 **DeepNorm**（*DeepNet*, 2022）在 Pre-LN 基础上对残差做 $`\alpha`$ 缩放：

``` math
x_{l + 1} = \text{LN}(\alpha \cdot x_{l} + \mathcal{F}(x_{l})),\quad\alpha = (3L)^{1/4}\ \text{(encoder)}
```

这样既有 Post-LN 的性能又保持 Pre-LN 的稳定性，可训练 1000 层。

**RMSNorm**（*Root Mean Square Layer Normalization*, 2019）：

``` math
\text{RMSNorm}(x) = \frac{x}{\text{RMS}(x)} \cdot \gamma,\quad\text{RMS}(x) = \sqrt{\frac{1}{d}\sum_{i = 1}^{d}x_{i}^{2} + \epsilon}
```

与 LayerNorm 的区别：

``` math
\text{LN}(x) = \frac{x - \mu}{\sigma} \cdot \gamma + \beta,\quad\sigma = \sqrt{\frac{1}{d}\sum(x_{i} - \mu)^{2}}
```

- **去掉了均值中心化**（re-centering）和偏置 $`\beta`$，只保留缩放
- 计算量更小（少一次均值归约和减法），实测**提速 7%~64%**（论文数据）
- 参数量更少（无 $`\beta`$）
- 效果与 LN 相当甚至更好，因为实验发现 LN 的 re-centering 对性能贡献极小

LLaMA、Qwen、Gemma、Mistral 全系使用 RMSNorm + Pre-LN。

**追问：** LN vs BN 在 NLP 里为什么选 LN？BN 在 batch 维度归一化，依赖 batch 统计量：(1) 序列长度可变，padding 导致统计不准；(2) 推理时 batch=1，统计量不稳定；(3) NLP 任务不同样本的长度差异大，LN 在特征维度归一化对每个 token 独立，与 batch 无关。此外 LN 在自回归生成中天然适合（逐 token 计算）。

------------------------------------------------------------------------

# 二、位置编码

## Q6：Transformer 为什么需要位置编码？绝对位置编码有哪些？

**答：**

Self-Attention 的置换等变性（permutation equivariance）：对输入做任意排列 $`\pi`$，

``` math
\text{Attn}(P_{\pi}X) = \text{Attention}(QX,KX,VX) = P_{\pi} \cdot \text{output}
```

即打乱输入顺序，输出也同序打乱，**注意力本身感知不到顺序**。而语言是有序的（“猫吃鱼” vs “鱼吃猫”），所以需要显式注入位置信息。

**绝对位置编码：**

1.  **正弦位置编码 (Sinusoidal, 原始论文)**：

``` math
PE_{(pos,2i)} = \sin\left( \frac{pos}{10000^{2i/d}} \right),\quad PE_{(pos,2i + 1)} = \cos\left( \frac{pos}{10000^{2i/d}} \right)
```

- 不加参数，与词嵌入相加。优点：可外推到比训练更长的序列（理论）；缺点：实际外推效果差。

  其设计巧妙之处：对固定偏移 $`k`$，$`PE_{pos + k}`$ 可表示为 $`PE_{pos}`$ 的线性变换（旋转矩阵），即模型能学到相对位置关系。

2.  **可学习位置编码 (Learned, BERT/GPT-2)**：$`E_{pos} \in \mathbb{R}^{n_{\max} \times d}`$，作为可训练参数。优点：灵活；缺点：**无法外推**，超过 $`n_{\max}`$ 无定义，且每个位置独立，参数量随最大长度线性增长。

3.  **相对位置编码 (Relative, Shaw et al. 2018 / T5 的 Bucket 版)**：编码的是 $`i - j`$ 的相对距离而非绝对位置。T5 把相对距离分桶（log-spaced buckets）成 32 个可学习 embedding 加到注意力 logits 上。优点：更好的长度泛化；缺点：计算开销、实现复杂。

4.  **RoPE / ALiBi**：见下两题。

------------------------------------------------------------------------

## Q7：详细推导 RoPE 旋转位置编码，为什么它具备远程衰减？外推问题是什么？

**答：**

RoPE（*RoFormer: Enhanced Transformer with Rotary Position Embedding*, 2021）的核心思想：**用旋转矩阵编码绝对位置，使内积自然表达相对位置**。

**目标**：寻找函数 $`f(q,m)`$（$`m`$ 为位置），使得

``` math
\langle f(q,m),f(k,n)\rangle = g(q,k,m - n)
```

即注意力内积只依赖相对距离 $`m - n`$。

**二维情况推导**：把 $`d`$ 维向量两两分组，对第 $`i`$ 组（位置 $`m`$）定义旋转矩阵：

``` math
R_{\Theta,m}^{i} = \begin{pmatrix}
\cos m\theta_{i} & - \sin m\theta_{i} \\
\sin m\theta_{i} & \cos m\theta_{i}
\end{pmatrix},\quad\theta_{i} = 10000^{- 2(i - 1)/d}
```

则

``` math
f(q,m) = R_{\Theta,m}q
```

关键性质：旋转矩阵满足 $`R_{m}^{\top}R_{n} = R_{n - m}`$（正交性 + 角度可加），于是

``` math
(R_{m}q)^{\top}(R_{n}k) = q^{\top}R_{m}^{\top}R_{n}k = q^{\top}R_{n - m}k
```

内积**只依赖** $`m - n`$，正是我们要的。

**实际实现**（高效形式）：将 $`q`$ 按相邻两维分组 $`(q_{1},q_{2}),(q_{3},q_{4}),\ldots`$，旋转后

``` math
\begin{pmatrix}
q_{1}' \\
q_{2}'
\end{pmatrix} = \begin{pmatrix}
q_{1}\cos m\theta_{1} - q_{2}\sin m\theta_{1} \\
q_{1}\sin m\theta_{1} + q_{2}\cos m\theta_{1}
\end{pmatrix}
```

工程上用 `rotate_half` 技巧实现：

    def apply_rotary_pos_emb(q, k, cos, sin):
        # cos/sin: [seq_len, head_dim]
        q_embed = (q * cos) + (rotate_half(q) * sin)
        k_embed = (k * cos) + (rotate_half(k) * sin)
        return q_embed, k_embed

    def rotate_half(x):
        x1, x2 = x.chunk(2, dim=-1)
        return torch.cat((-x2, x1), dim=-1)

注意：HuggingFace 的实现把前一半和后一半配对（而非相邻），这是 **GPT-NeoX 风格**，与原始论文的相邻配对等价（只是维度重排）。

**远程衰减性质**：当 $`m - n`$ 增大时，内积 $`\sum_{i}^{}q_{i}k_{i}\cos((m - n)\theta_{i})`$ 中各频率成分的 $`\cos`$ 项逐渐失去相位一致性（不同 $`\theta_{i}`$ 周期不同），高频项首先随机化，总和的期望趋于 0，方差下降。这使 RoPE 天然**偏好关注近距离 token**，符合语言的局部性先验。论文用 $`\theta_{i}`$ 的等差递减序列构造了这一性质。

**外推问题（Extrapolation）**：RoPE 训练时只见过位置 $`\lbrack 0,L)`$。推理时若用位置 $`L + 1`$，虽然旋转矩阵对任意 $`m`$ 都有定义（**理论上无限外推**），但模型学到的注意力模式与新的相位分布不匹配，性能在超过训练长度后**断崖式下降**。

三种主流扩展方法：

| 方法 | 核心思路 | 公式/要点 |
|----|----|----|
| **PI (Position Interpolation)** | 把超长位置压缩回训练范围 | 把位置 $`m`$ 映射为 $`m' = m \cdot L/L'`$，线性插值。代价：相邻 token 位置差变小，分辨率损失，微调 1000 步可恢复 |
| **NTK-aware** | 改变 base $`\theta`$ 使不同频率分量插值程度不同 | $`\theta_{i}' = b'^{- 2i/d}`$，$`b' = b \cdot \left( \frac{L'}{L} \right)^{d/(d - 2)}`$。高频不动（保持局部精度），低频外推 |
| **YaRN** (*YaRN: Efficient Context Window Extension*, 2023) | NTK-by-parts + 注意力温度缩放 | 对不同波长分段处理：波长 \< 训练长度的**不插值**，波长 \> 训练长度的**完全插值**，中间过渡。同时把 attention logits 除以温度 $`t`$ 调整熵 |

**追问：** 为什么 RoPE 是「相对」位置编码但用「绝对」位置实现？因为它作用在 $`q,k`$ 上而非加在 embedding 上，内积里自然抵消为相对量。这也是它比正弦编码更好的原因之一：正弦编码加在输入上，经过多层线性变换后相对性被破坏。

------------------------------------------------------------------------

## Q8：ALiBi 是什么？和 RoPE 比有什么优劣？

**答：**

ALiBi（*Train Short, Test Long: Attention with Linear Biases*, 2021）**完全不使用位置嵌入**，而是直接在注意力分数上减去一个与距离成正比的偏置：

``` math
\text{softmax}\left( q_{i}k_{j}^{\top} - m \cdot (i - j) \right),\quad i > j
```

其中 $`m`$ 是每个头固定的斜率，取几何序列（如 $`2^{- 1},2^{- 2},\ldots,2^{- 8}`$，头数 8 时）。$`i,j`$ 为位置，距离越远惩罚越大。

**优点：** - **零额外参数**，实现简单（一行代码加 bias 到 mask） - **长度外推能力强**：训练 1K 可外推到 2K+，无需微调，因为外推时惩罚只是线性增长，不引入未见过的新相位 - 在相同预算下，短序列训练效果与正弦/RoPE 相当或更好

**缺点：** - 相对 RoPE 在长上下文**微调后**的上限略低（RoPE + 微调能到 128K+，ALiBi 的线性偏置假设过强） - 对所有头用固定斜率，缺乏灵活性 - 现代主流大模型（LLaMA、Qwen、DeepSeek）**几乎全部转向 RoPE**，ALiBi 主要用于 BLOOM、MosaicML MPT 等

**一句话对比**：ALiBi 用「加法偏置」表达相对位置，天生外推好但表达力有限；RoPE 用「乘法旋转」表达相对位置，表达力强但需专门的外推技术（PI/NTK/YaRN）。

------------------------------------------------------------------------

# 三、注意力变体与推理优化

## Q9：MHA、MQA、GQA 的区别是什么？显存和吞吐如何权衡？

**答：**

三者的区别在于 **K、V 的头数是否与 Q 相同**。

| 类型 | Q 头数 | KV 头数 | KV Cache 相对大小 | 代表模型 |
|----|----|----|----|----|
| **MHA** | $`h`$ | $`h`$ | $`1 \times`$ | GPT-2/3、LLaMA-1 |
| **MQA** | $`h`$ | $`1`$ | $`1/h`$ | PaLM、Falcon、ChatGLM |
| **GQA** | $`h`$ | $`g`$ ($`1 < g < h`$) | $`g/h`$ | LLaMA-2-70B、LLaMA-3 全系、Qwen2 |

**GQA**（*GQA: Training Generalized Multi-Query Transformer Models*, 2023）是 MQA 与 MHA 的插值：把 $`h`$ 个 Q 头分成 $`g`$ 组，每组共享一对 K/V。LLaMA-3-8B：$`h = 32`$，$`g = 8`$，KV Cache 减少到 1/4。

**KV Cache 显存公式：**

``` math
\text{Mem}_{KV} = 2 \times L \times n \times d_{kv} \times g \times \text{bytes}
```

以 LLaMA-3-8B（$`L = 32`$，$`n_{\text{kv}} = 8`$，$`d_{\text{head}} = 128`$，即 $`d_{kv} = 1024`$）为例，batch=1、$`n = 8192`$、FP16：

``` math
2 \times 32 \times 8192 \times 1024 \times 2\text{B} \approx 1.07\text{GB}
```

若是 MHA（$`d_{kv} = 4096`$）则为 **4.3GB**，差 4 倍。

**权衡：**

- **质量**：MHA \> GQA \> MQA。MQA 在多任务上略有掉点（尤其需要多样注意力模式的任务），GQA 通过分组 + 少量训练（uptrain，约 5% 的预训练算力）即可达到接近 MHA 的质量。
- **吞吐**：自回归解码是 **memory-bandwidth bound**。每步生成都要把整个 KV Cache 从 HBM 读进 SRAM，计算量只是 $`O(nd)`$ 的矩阵向量乘。KV Cache 越小 → 每次读的数据越少 → 单步延迟越低、可支持的 batch 越大 → 总吞吐越高。MQA/GQA 因此显著提升推理吞吐（MQA 论文报告最高 **12x** 加速）。
- **训练**：MHA 训练最稳，MQA/GQA 收敛略慢但可接受。

**追问：** GQA 的 $`g`$ 怎么选？经验是让 KV Cache 大小与「计算/带宽平衡点」匹配。LLaMA-3-70B 用 $`h = 64,g = 8`$；8B 用 $`h = 32,g = 8`$；1B 用 $`h = 16,g = 8`$。常见做法是保持 $`g = 8`$ 左右（即 KV 头数 8），这样 $`d_{kv} = 8 \times 128 = 1024`$，正好是硬件友好的规模。

------------------------------------------------------------------------

## Q10：FlashAttention 的原理是什么？1/2/3 三代各有什么改进？

**答：**

**核心问题**：标准注意力要显式物化 $`n \times n`$ 的注意力矩阵，写回 HBM（显存）。以 $`n = 8192`$、$`h = 32`$、FP16 为例，单个矩阵就是 $`8192^{2} \times 2\text{B} = 128\text{MB}`$，乘以层数和 batch，HBM 读写成为瓶颈。**注意力的瓶颈不是 FLOPs，而是 HBM 访存（memory-bound）**。

**FlashAttention 1**（*FlashAttention: Fast and Memory-Efficient Exact Attention with IO-Awareness*, 2022）核心是 **tiling + recomputation**：

1.  **Tiling（分块）**：把 $`Q,K,V`$ 沿序列维切成小块（如 $`B_{c} \times d`$、$`B_{r} \times d`$），把它们从 HBM 加载到 SRAM（片上高速缓存，约 20MB），在 SRAM 内完成分块矩阵乘和 softmax，只把最终输出 $`O`$（$`n \times d`$）写回 HBM。**注意力矩阵从不完整物化到 HBM**，显存从 $`O(n^{2})`$ 降到 $`O(n)`$。

2.  **Online Softmax**：softmax 需要全局的 $`\max`$ 和 $`\sum`$，但分块计算时看不到全部。FlashAttention 维护运行中的统计量 $`(m_{i},\ell_{i})`$：

``` math
m_{i}^{new} = \max(m_{i}^{old},{\widetilde{m}}_{i}),\quad\ell_{i}^{new} = e^{m_{i}^{old} - m_{i}^{new}}\ell_{i}^{old} + \sum_{j}^{}e^{{\widetilde{x}}_{ij} - m_{i}^{new}}
```

``` math
O_{i}^{new} = \frac{\ell_{i}^{old}e^{m_{i}^{old} - m_{i}^{new}}O_{i}^{old} + \sum_{j}^{}e^{{\widetilde{x}}_{ij} - m_{i}^{new}}V_{j}}{\ell_{i}^{new}}
```

每处理一个新块就用**重标定因子** $`e^{m^{old} - m^{new}}`$ 修正历史累积结果。这样只需遍历一次即可算精确 softmax，**结果与标准注意力完全等价**（exact attention）。

3.  **Recomputation（重计算）**：反向传播需要注意力矩阵，但不存它，而是在 backward 时用存下来的 $`(m,\ell)`$ 和 $`Q,K,V`$ 块**重新计算**前向。这实际上比标准实现还快，因为省掉了 HBM 读写，代价是多算一点 FLOPs。

**效果**：注意力显存从 $`O(n^{2})`$ 降到 $`O(n)`$，训练速度提升 2~4x（BERT-large 训练提速 15%），支持到 64K 序列。

**FlashAttention-2**（2023）的改进（主要是**并行度和 work partitioning**）：

1.  **减少非 matmul 的 FLOPs**：FA1 中每块都要做 softmax 的重标定（对应乘以 $`e^{m^{old} - m^{new}}`$），这些是逐元素操作，占了不少时间。FA2 调整了循环顺序（外层遍历 K/V，内层遍历 Q），使得**对** $`O`$ **的重标定只需在循环外做一次**，而非每块都做。
2.  **更好的并行**：FA1 在 batch 和 head 维并行，序列长时（$`n`$ 大、batch 小）GPU 利用率低。FA2 额外在**序列维（Q 的块）**并行，即使在 batch=1、$`n = 16\text{K}`$ 时也能占满 SM。
3.  **更好的 warp 分工**：FA1 中 4 个 warp 共享同一块 $`K,V`$，通过 shared memory 通信（有同步开销）。FA2 改为**每个 warp 处理一部分 Q 行、只与自己的 K/V 块交互**，减少通信。

结果：FA2 比 FA1 再快约 **2x**，达到理论 FLOPs 的 50~73%（A100）。

**FlashAttention-3**（2024，针对 Hopper H100）利用新硬件特性：

1.  **异步 WGMMA（TMA + warpgroup matmul）**：Hopper 的 Tensor Core 支持异步执行，FA3 让矩阵乘与 softmax 计算**重叠**（software pipelining），隐藏 softmax 的延迟。
2.  **FP8 支持**：利用 Hopper 的 FP8 Tensor Core，吞吐翻倍，同时用 block quantization 控制精度。
3.  **Warp-specialization**：生产者 warp 负责搬运数据（TMA），消费者 warp 负责计算，进一步流水线化。

结果：FA3 在 H100 上达到 **1.5~2x** 于 FA2，接近理论峰值 75%（FP16）。

**追问：** FlashAttention 是近似注意力吗？**不是**，它是 exact attention，数学上与标准实现完全等价，只是改变了计算顺序（类似分块矩阵乘）。市面上有些「省显存注意力」如 Longformer 的 sliding window 是近似的，FlashAttention 不是。

**追问 2：** 为什么 FlashAttention 在推理（decode 阶段）收益不大？因为 decode 时 $`q`$ 只有 1 个 token，$`Q`$ 矩阵很小，分块收益低，瓶颈在 KV Cache 的读取上（memory bandwidth）。此时更适合 PagedAttention + 投机解码。

------------------------------------------------------------------------

## Q11：PagedAttention 与 vLLM 的原理？

**答：**

**问题**：LLM 服务的 KV Cache 是动态增长的，且长度事先未知。传统实现为每个请求预分配一个「最大长度」的连续显存块，导致：

- **内部碎片**：实际用了 30%，预留 100%，浪费 70%
- **外部碎片**：不同请求的块大小不一，显存难以复用

vLLM 论文实测：现有系统只有 **20%~40%** 的显存用于存真实 KV，其余全浪费。

**PagedAttention**（*Efficient Memory Management for Large Language Model Serving with PagedAttention*, SOSP 2023）借用操作系统的**虚拟内存分页**思想：

1.  把 KV Cache 切成固定大小的 **block**（如 16 个 token 一块）
2.  block 在物理显存上**可以不连续**，通过 **block table**（类似页表）映射逻辑块 → 物理块
3.  序列的 KV 访问通过查表拼接，注意力 kernel 按块索引

**好处：**

- **显存浪费 \< 4%**（只有最后一块的内部碎片）
- **内存共享**：并行采样（同一 prompt 多个输出）、beam search 时，多个序列可**共享 prompt 部分的物理块**（写时复制，copy-on-write），显存节省显著
- **吞吐提升**：vLLM 报告相比 FasterTransformer/Orca 吞吐提升 **2~4x**（同延迟下）

**其他 vLLM 技术**：Continuous Batching（迭代级调度，新请求随时插入，而非等整批完成）、Chunked Prefill（把长 prompt 的 prefill 切块与 decode 混合调度，避免 prefill 阻塞）、Prefix Caching（跨请求复用相同前缀的 KV 块）。

**追问：** PagedAttention 和 FlashAttention 冲突吗？不冲突，可组合。vLLM 的注意力 kernel 内部就用了 FlashAttention/FlashDecoding 的分块思想，同时从 block table 读取 KV。

------------------------------------------------------------------------

## Q12：稀疏注意力与 Sliding Window Attention

**答：**

动机：$`O(n^{2})`$ 注意力在长序列上不可承受。稀疏注意力假设「注意力矩阵是稀疏的」，只计算部分位置对。

**主要类型：**

1.  **Sliding Window Attention (SWA)**：每个 token 只关注前 $`w`$ 个 token（*Longformer*, 2020；Mistral 7B 用 $`w = 4096`$）。复杂度从 $`O(n^{2}d)`$ 降到 $`O(nwd)`$。

    - Mistral 的关键洞察：**信息可以跨层传播**。第 $`k`$ 层 token $`i`$ 能访问到 $`i - k \cdot w`$ 的信息，所以 $`L`$ 层的 SWA 理论感受野是 $`L \times w`$。Mistral-7B 用 $`L = 32,w = 4096`$ 得到 128K 的感受野。
    - 实现上通常用 **Rolling Buffer Cache**，KV Cache 固定大小 $`w`$，新 token 覆盖旧位置，显存恒定。

2.  **Dilated / Strided Attention**：*Longformer* 用不同头设置不同 dilation，扩大感受野。

3.  **Block-Sparse**：*BigBird*（2020）结合「全局 token + 滑动窗口 + 随机稀疏」，理论上有图灵完备性和万能逼近性证明。

4.  **Native Sparse Attention (NSA)**（DeepSeek, 2025）与 **MoBA**：用可学习的方式动态选择要关注的关键块，兼顾训练和推理，硬件友好。

5.  **Linear Attention / Mamba（SSM）**：彻底避开 $`O(n^{2})`$，用 RNN 式递推实现 $`O(nd^{2})`$，但表达力有损，混合架构（如 Jamba、Zamba）是当前趋势。

**追问：** 稀疏注意力一定能加速吗？不一定。理论 FLOPs 降低不等于实际加速，因为： - 稀疏模式不规整，GPU 无法有效利用 Tensor Core（需要 dense 块） - 需要 gather/scatter，访存开销大 - 短序列（$`n < 2\text{K}`$）时 dense 注意力本身就很快 所以真正落地好的稀疏方案（SWA、NSA、MoBA）都强调**块状规整 + kernel 融合**。FlashAttention 在 $`n = 2\text{K}`$ 时已经能到 70% 峰值利用率，稀疏方案必须超过这个基线才有意义。

------------------------------------------------------------------------

# 四、模型架构演进

## Q13：Encoder-only、Decoder-only、Encoder-Decoder 的区别？为什么现在主流是 Decoder-only？

**答：**

| 架构 | 注意力 | 代表 | 预训练目标 | 擅长任务 |
|----|----|----|----|----|
| **Encoder-only** | 双向（无 mask） | BERT、RoBERTa、DeBERTa | MLM（掩码语言模型） | 分类、NER、抽取、句向量 |
| **Decoder-only** | 因果（causal mask） | GPT、LLaMA、Qwen、DeepSeek | CLM（自回归） | 生成、对话、通用 |
| **Encoder-Decoder** | 编码器双向 + 解码器因果 + 交叉注意力 | T5、BART、mT5 | Span corruption / 去噪 | 翻译、摘要、seq2seq |

**为什么 Decoder-only 胜出？**

1.  **统一范式**：任何任务都可表述为「给定前缀，续写」。分类可以「续写答案字母」，翻译可以「续写译文」，无需为不同任务设计 head。这带来了 **zero-shot / few-shot / ICL 能力**（GPT-3 发现的关键），而 Encoder-only 的 MLM 目标天然不支持生成。

2.  **训练效率与目标一致性**：CLM 每个 token 都提供监督信号（$`n`$ 个 token 提供 $`n`$ 个 loss），而 MLM 只对 15% 的 mask token 计算 loss；且 CLM 的预训练目标与推理时的使用方式（逐 token 生成）**完全一致**，没有 train-test gap。Encoder-Decoder 的交叉注意力增加了参数量和工程复杂度（两套注意力机制）。

3.  **Scaling 友好**：CLM 目标简单、易于大规模并行、随参数增长的 scaling 曲线平滑（*Scaling Laws*, Kaplan 2020 就是在 decoder-only 上拟合的）。

4.  **KV Cache 与推理生态**：Decoder-only 只有自注意力，KV Cache 管理简单，PagedAttention、Continuous Batching 等技术都针对它优化；Encoder-Decoder 需要同时缓存编码器输出与解码器 KV，调度更复杂。

5.  **上下文学习能力**：只有因果语言模型能通过「把示例放进 prompt」来 in-context 学习，这是现代 LLM 应用的基础（prompt engineering、CoT）。

**追问：** Decoder-only 有什么劣势？(1) 双向上下文建模弱，纯理解任务（如抽取、NER）上同规模 BERT 可能更强；(2) 生成是串行的，推理慢于编码器式的一次性前向；(3) 对短文本分类等任务，「生成答案」比「分类头」更贵。工程上用「Encoder 做 embedding + Decoder 做生成」的混合方案（如 RAG 里的 retriever）来互补。

------------------------------------------------------------------------

## Q14：LLaMA 1/2/3 各有哪些关键改动？

**答：**

**LLaMA-1**（2023.02，*LLaMA: Open and Efficient Foundation Language Models*）相对于 GPT-3 的改动：

| 组件 | LLaMA 的选择 | 相比原始 Transformer |
|----|----|----|
| 归一化 | **RMSNorm** | 替代 LayerNorm，去掉均值中心化 |
| 激活 | **SwiGLU** | 替代 ReLU/GELU，$`d_{ff} = \frac{8}{3}d`$ |
| 位置编码 | **RoPE** | 替代可学习绝对位置编码 |
| 偏置 | **所有线性层去 bias** | 减少参数，提升稳定性 |
| 归一化位置 | **Pre-Norm**（GPT-3 是 Post-Norm） | 训练更稳 |
| 词表 | **SentencePiece BPE，32K** | GPT-3 是 50K BPE |

规模：7B / 13B / 33B / 65B，训练数据 1T~1.4T token。7B 在多数 benchmark 上超过 GPT-3 175B。

**LLaMA-2**（2023.07）的改动：

1.  **上下文长度 2K → 4K**
2.  **引入 GQA**（仅 34B/70B）：70B 用 $`h = 64,g = 8`$，大幅降低推理 KV Cache
3.  **训练数据 2T token**（+40%），并做了更严格的数据清洗
4.  **RLHF 对齐**：发布 Chat 版本，使用拒绝采样 + PPO，以及 Ghost Attention (GAtt) 解决多轮对话中系统提示遗忘
5.  词表仍为 32K

**LLaMA-3**（2024.04）的改动：

1.  **词表 32K → 128K**：这是最显著的改动。原有 32K 词表对中文、代码、多语言压缩率低，128K 词表使**序列长度减少约 15%**，等价于推理加速 15%，并大幅提升多语言/代码能力
2.  **上下文 4K → 8K**（LLaMA-3），后续 3.1 扩展到 **128K**（用 RoPE scaling + 长文本继续预训练）
3.  **GQA 全系标配**（8B 用 $`h = 32,g = 8`$；70B 用 $`h = 64,g = 8`$）
4.  **训练数据 15T+ token**（LLaMA-2 的 7 倍），其中代码数据增加 4 倍
5.  **8K 词表 + 15T 数据 = 更好的 token 效率**，论文强调「数据规模是 scaling law 的关键杠杆」
6.  数值稳定性改进（如 attention 的 softmax 用 FP32 累积）

**LLaMA-3.2** 进一步加入 **Vision 支持**（跨模态）与 **1B/3B 小模型**（用于端侧）；LLaMA-4（若发布）预期走向 MoE。

**追问：** 词表扩到 128K 有什么代价？(1) embedding 层参数量 4 倍（$`128000 \times 4096 \approx 524\text{M}`$，占 8B 模型的 6.5%）；(2) 输出层 softmax 计算量增大；(3) 训练时新词表的 embedding 需要充分训练。但对多语言模型来说词表扩大带来的序列缩短收益远大于成本。

------------------------------------------------------------------------

## Q15：Qwen、GLM、DeepSeek 的核心设计各是什么？

**答：**

**Qwen（通义千问）系列**：

- **Qwen1/1.5**：LLaMA 式架构（RMSNorm + SwiGLU + RoPE + Pre-Norm），但**词表 151,851**（比 LLaMA-3 的 128K 还大），中文压缩率极佳。Qwen1.5 提供 0.5B~110B 全尺寸。
- **Qwen2**：引入 GQA（全系）、**双 Chunk 注意力（DCA）用于长上下文**、YaRN 支持 128K，训练数据 7T token。
- **Qwen2.5**：数据从 7T 扩到 **18T**，数学与代码能力大幅增强（Qwen2.5-Math / Coder 独立发布），并开源了 **Qwen2.5-72B** 逼近 Llama-3.1-70B。
- **Qwen3**（2025）：引入 **Dense + MoE 双版本**（30B-A3B、235B-A22B），支持 **Thinking / Non-Thinking 双模式切换**（一个模型内通过特殊 token 控制是否输出推理链），并做了 **Strong-to-Weak Distillation**（用大模型蒸馏小模型）。
- **Qwen-VL / Qwen-Audio / Qwen-Omni**：多模态扩展，用 visual encoder + cross-attention/adapter 接入 LLM。

**GLM（智谱）系列**：

- **GLM-130B**（2022）：基于 *GLM: General Language Model Pretraining with Autoregressive Blank Infilling*，用 **自回归填空（Autoregressive Blank Infilling）** 目标统一了 NLU 与 NLG。架构上采用 **Post-LN + DeepNorm**（而非 Pre-LN），用 2D 位置编码，训练极稳定。
- **ChatGLM-6B / ChatGLM2 / ChatGLM3**：将 GLM 架构改为更接近标准的 Decoder-only（ChatGLM2 起改用 RoPE + SwiGLU + MQA/GQA，去掉了 GLM-130B 的复杂结构）。ChatGLM2 的 **Multi-Query Attention** 让 6B 模型在消费级显卡可跑。
- **GLM-4 / GLM-4-9B**：128K 上下文，GLM-4-9B 采用 **GQA + RMSNorm + SwiGLU** 的标准组合，并开源了 All Tools 版本（Function Call 能力强）。
- **GLM-4.5 / GLM-4.6**（2025）：MoE 架构，主打 **Agentic / Coding** 能力。

**DeepSeek 系列**：

- **DeepSeek-V1/V2**：V2 的核心是 **MLA（Multi-head Latent Attention）** 与 **DeepSeekMoE**。
- **MLA**：把 K/V 压缩到一个低维隐向量 $`c_{t}^{KV} \in \mathbb{R}^{d_{c}}`$（$`d_{c} \ll d`$），推理时再把 $`c_{t}^{KV}`$ 投影出 K/V。这样 **KV Cache 只存** $`c_{t}^{KV}`$，显存降到 MHA 的 **~4%**（论文称 KV Cache 减少 93.3%），且通过矩阵吸收（absorb）技巧，Q 与 K 的内积可重写为 $`c^{Q}`$ 与 $`c^{KV}`$ 的内积，**性能超过 MHA**（不像 MQA/GQA 有损）。
- **DeepSeekMoE**：**细粒度专家分割**（把专家切得更细，$`mN`$ 个专家激活 $`mK`$ 个）+ **共享专家隔离**（固定激活若干共享专家，捕获通用知识，减少专家冗余）。见 MoE 章节。
- **DeepSeek-V3**（2024.12）：**671B 总参数，37B 激活**，使用 **FP8 混合精度训练**（首次在超大规模验证，节省显存与带宽）、**MTP（Multi-Token Prediction）** 辅助训练目标（每步预测多个未来 token，增强数据效率并支持投机解码）、**无辅助损失的负载均衡**（auxiliary-loss-free load balancing，用 bias 项动态调整而非加 loss，避免损害性能）、**DualPipe 流水线并行**。训练成本仅 278.8 万 H800 小时（约 558 万美元）。
- **DeepSeek-R1**（2025.01）：用 **纯 RL（GRPO）** 训练出推理能力，R1-Zero 验证了「无需 SFT，纯 RL 即可涌现长链推理」，R1 则加入冷启动数据 + 多阶段训练。开源后引发广泛复现。

**追问：** MLA 为什么能做到「KV Cache 小但性能不减」？因为它压缩的是 K/V 的**隐空间表示**，而非减少 KV 头数。GQA 损失的是「不同 Q 头需要不同 K/V」的表达力，MLA 则是把 K/V 投影到低维再做注意力，低维表示足够（论文做了消融，$`d_{c}`$ 取 $`4d_{h}`$ 时性能无损），且上投影矩阵可与其他参数合并（推理时把 $`W^{UK}`$ 吸收进 $`W^{Q}`$，$`W^{UV}`$ 吸收进 $`W^{O}`$），不增加计算。这是 2024 年最受关注的架构创新之一。

------------------------------------------------------------------------

# 五、MoE 混合专家

## Q16：稀疏 MoE 的原理是什么？Top-k 门控怎么做？

**答：**

MoE 的核心思想：**用远大于激活量的参数总量来提升模型容量，但每次前向只激活一小部分**，从而在不显著增加 FLOPs 的前提下扩大参数量。

**标准 MoE 层**（*Outrageously Large Neural Networks*, 2017；*Switch Transformer*, 2021）：

``` math
y = \sum_{i = 1}^{N}g_{i}(x) \cdot E_{i}(x)
```

其中 $`E_{i}`$ 是第 $`i`$ 个专家（通常是一个 FFN），$`g_{i}(x)`$ 是门控权重。稀疏 MoE 中只有 Top-k 个 $`g_{i}`$ 非零。

**Top-k Gating（Switch Transformer，k=1）**：

``` math
g(x) = \text{softmax}(\text{TopK}(xW_{g} + \epsilon,\ k))
```

具体流程： 1. 用可学习矩阵 $`W_{g} \in \mathbb{R}^{d \times N}`$ 计算每个专家的 logits 2. 取 Top-k 个（通常 $`k = 1`$ 或 $`2`$），其余置 0 3. 对 Top-k 做 softmax 归一化得到权重 4. 加权求和被选中专家的输出

Switch Transformer 用 $`k = 1`$（最简）、$`N = 2048`$ 专家，在相同 FLOPs 下达到 T5-XXL 的 7 倍参数量，**预训练速度提升 7x**。

**关键结论：**

- **参数量 vs FLOPs 解耦**：MoE 的总参数量 = $`N \times`$ 单专家参数，但每个 token 只算 $`k`$ 个专家，FLOPs 与 $`k`$ 成正比而非 $`N`$
- **显存换计算**：需要把所有专家权重放进显存（或通过专家并行/offload 分担），所以 MoE 是「显存密集」而非「计算密集」
- DeepSeek-V3：671B 总参 / 37B 激活，激活率仅 5.5%

------------------------------------------------------------------------

## Q17：MoE 的负载均衡损失怎么设计？为什么需要它？

**答：**

**问题：** 如果放任门控学习，会出现**赢者通吃（rich-get-richer）**：某些专家因为初期被选得多、训练得更充分、从而更常被选，最终少数几个专家承担了大部分 token，其余专家「饿死」。这导致： - 有效参数量远小于总参数量（专家冗余） - 专家并行的 GPU 负载不均，快的卡等慢的卡，吞吐下降

**辅助损失（Auxiliary Loss）设计：**

*Switch Transformer* 的负载均衡损失：

``` math
\mathcal{L}_{\text{aux}} = \alpha \cdot N \cdot \sum_{i = 1}^{N}f_{i} \cdot P_{i}
```

其中： - $`f_{i} = \frac{1}{T}\sum_{t = 1}^{T}\mathbb{1}\{\arg\text{top}_{k}(x_{t}) = i\}`$：分配给专家 $`i`$ 的 token 比例 - $`P_{i} = \frac{1}{T}\sum_{t = 1}^{T}p_{i}(x_{t})`$：专家 $`i`$ 的平均门控概率 - $`N`$ 是专家数，$`\alpha`$ 是超参（Switch 中取 0.01）

该式在 $`f_{i} = P_{i} = 1/N`$（完全均衡）时取最小值 $`1`$。直觉：$`f_{i}`$ 不可导，$`P_{i}`$ 可导，用 $`P_{i}`$ 提供梯度；同时惩罚「高 $`f_{i}`$ 且高 $`P_{i}`$」的专家。

*GShard* 版本用 $`f_{i} \cdot P_{i}`$ 的类似形式，另有 **router z-loss**（*ST-MoE*）惩罚 logits 的平方以防止门控数值爆炸：

``` math
\mathcal{L}_{z} = \frac{1}{B}\sum_{i}^{}\log^{2}\sum_{j}^{}e^{x_{ij}}
```

**DeepSeek-V3 的「无辅助损失」负载均衡（Auxiliary-Loss-Free Load Balancing）**：

辅助损失有个副作用——它会影响主任务的优化方向，损害模型性能。DeepSeek-V3 改用**动态 bias**：给每个专家一个偏置 $`b_{i}`$，只用于**选择专家**（不影响门控权重）：

``` math
g_{i}' = \left\{ \begin{matrix}
s_{i}, & s_{i} + b_{i} \in \text{TopK} \\
0, & \text{otherwise}
\end{matrix} \right.\ 
```

训练中若专家 $`i`$ 过载则 $`b_{i} \leftarrow b_{i} - \gamma`$，欠载则 $`b_{i} \leftarrow b_{i} + \gamma`$（$`\gamma`$ 是小步长）。这样负载均衡通过选择偏置实现，**不污染梯度**，性能更好。DeepSeek-V3 报告这是其超越 V2 的关键之一。

此外还有 **专家容量（Expert Capacity）** 机制：每个专家最多处理 $`C = \lceil\frac{T \cdot k}{N} \cdot \text{capacity\_factor}\rceil`$ 个 token，超出的 token 被「丢弃」（通过残差直接传递），防止单个专家过载。现代实现（DeepSeek-V3）用 **节点受限路由（node-limited routing）** 降低跨节点通信。

**追问：** 辅助损失系数 $`\alpha`$ 怎么调？太大会损害主任务性能（模型为了均衡牺牲质量），太小则专家坍缩。ST-MoE 建议 $`\alpha = 0.01`$，并指出 $`\alpha`$ **与专家数量正相关**（专家越多需要的均衡压力越大）。DeepSeek-V3 直接放弃了 loss 形式。

------------------------------------------------------------------------

## Q18：DeepSeekMoE 的细粒度专家与共享专家是什么？和 Switch Transformer 有何不同？

**答：**

**DeepSeekMoE**（*DeepSeekMoE: Towards Ultimate Expert Specialization in Mixture-of-Experts Language Models*, 2024）的两个核心设计：

**1. 细粒度专家分割（Fine-Grained Expert Segmentation）**

传统 MoE 中，专家数量少（如 8/64 个），每个专家粗粒度高，导致**知识混杂**——一个专家被迫同时学习数学、语法、常识等多种知识，专业化不充分。

DeepSeekMoE 的做法：保持参数量不变，把每个专家**切成** $`m`$ **份**（FFN 中间维度变为原来的 $`1/m`$），同时**激活数从** $`K`$ **增加到** $`mK`$。

``` math
\text{总参数}:N \times d_{ff} \rightarrow mN \times \frac{d_{ff}}{m}\ (\text{不变}),\quad\text{激活}:K \rightarrow mK
```

例如 $`N = 16,K = 2 \rightarrow`$ $`m = 4`$ 时变成 $`N = 64,K = 8`$。**组合空间从** $`C(16,2) = 120`$ **暴增到** $`C(64,8) \approx 4.4 \times 10^{9}`$，专家组合的灵活度极大提升，每个专家可以更专注地学习某一细分知识。

**2. 共享专家隔离（Shared Expert Isolation）**

把一部分专家设为**始终激活**的「共享专家」（如 2 个），其余为路由专家。共享专家负责捕获**通用知识**（语法、基础语义），路由专家则专注于**领域特定知识**。

``` math
y = \underset{\text{共享专家，始终激活}}{\underbrace{\sum_{i = 1}^{N_{s}}E_{i}(x)}} + \underset{\text{路由专家，Top-k}}{\underbrace{\sum_{j = N_{s} + 1}^{mN + N_{s}}g_{j}(x)E_{j}(x)}}
```

**好处**：避免多个路由专家重复学习相同的通用知识（参数冗余），让路由专家更「专」。论文消融显示，共享专家隔离在相同激活量下带来稳定提升。

**与 Switch Transformer 的对比：**

| 维度       | Switch Transformer    | DeepSeekMoE                           |
|------------|-----------------------|---------------------------------------|
| 激活专家数 | $`k = 1`$             | $`k = K_{s} + mK_{r}`$                |
| 专家粒度   | 粗（标准 FFN）        | 细（FFN 切成 $`1/m`$）                |
| 共享专家   | 无                    | 有（$`N_{s}`$ 个隔离共享）            |
| 负载均衡   | 辅助损失              | 无辅助损失（V3）+ 辅助损失（V2 兼有） |
| 路由策略   | token-choice Top-1    | token-choice Top-k + 节点受限路由     |
| 规模       | 2048 专家 / 1.6T 参数 | V2: 236B/21B 激活；V3: 671B/37B       |

**DeepSeek-V3 的 MoE 配置**：每层 1 个共享专家 + 256 个路由专家，激活 1 + 8 = 9 个；专家中间维度 $`d_{ff} = 2048`$（很小，符合细粒度）；用 **sigmoid** 而非 softmax 做门控（更稳定），配合前述的动态 bias 负载均衡。

**追问：** 细粒度专家有什么代价？(1) 激活专家数增加 → 路由和通信开销增大（尤其专家并行时每层要 all-to-all 通信）；(2) 每个专家的 batch size 变小，GPU 矩阵乘效率下降；(3) 路由决策更频繁，调度开销上升。DeepSeek-V3 用 **DeepEP 通信库**（高吞吐 all-to-all）和 **专家并行 + 张量并行混合**来缓解。

------------------------------------------------------------------------

# 六、分词与词表

## Q19：BPE、WordPiece、Unigram、SentencePiece 的原理与区别？

**答：**

**BPE（Byte Pair Encoding）**：

- **原理**：自底向上。初始词表是**所有单字符**（或字节），迭代地找出语料中**出现频率最高的相邻 pair**，合并为新 token 加入词表，重复直到达到目标词表大小。
- **算法**：统计 pair 频次 $`O(n)`$ → 合并最高频 pair → 更新语料 → 重复 $`V`$ 次。
- **代表**：GPT-2/3/4、LLaMA（用 SentencePiece 的 BPE 模式）、RoBERTa。
- **GPT-2 的 Byte-level BPE**：在**字节**（256 个）而非字符上做 BPE，任何文本都能编码（无 OOV），并用空格变换（`Ġ` 表示前导空格）保留空白信息。

**WordPiece**：

- **原理**：与 BPE 类似的合并思路，但**合并准则不同**：不是选频次最高的 pair，而是选**使语料似然提升最大**的 pair：

``` math
\text{score}(a,b) = \frac{\text{count}(ab)}{\text{count}(a) \cdot \text{count}(b)}
```

- **子词标记**：非词首子词加 `##` 前缀（如 `playing` → `play` + `##ing`）
- **代表**：BERT、DistilBERT、ELECTRA
- **与 BPE 的区别**：BPE 只最大化频次（贪心），WordPiece 考虑「互信息」式的准则（PMI 形式），倾向于合并「单独出现少、一起出现多」的 pair。

**Unigram（Unigram Language Model）**：

- **原理**：**自顶向下**。先初始化一个大词表（所有子串），然后**迭代删除**那些「删除后语料似然下降最小」的 token，直到达到目标大小。配合 EM 算法估计每个 token 的概率 $`p(x_{i})`$，句子概率为 $`\prod p(x_{i})`$。
- **优势**：**概率化建模**，支持多种分词方案（可用 Viterbi 求最优切分，也可采样），天然适合 **subword regularization**（训练时随机采样分词，相当于数据增强）
- **代表**：T5、ALBERT、mBART、XLNet

**SentencePiece**：

- 不是一种分词算法，而是**一个工具库/框架**，把 BPE 和 Unigram 都实现了，并且**直接在原始文本（含空格）上操作**，不需要预分词（pre-tokenization）。
- 关键点：把空格也当作一个普通字符（用 `▁` 表示），因此**可逆**（detokenize 能精确还原原始文本，包括空格位置），避免了语言特定的预分词规则（中文分词、泰语无空格）。
- 支持 `byte_fallback`（未登录字符回退到字节）、`user_defined_symbols`（保留特殊 token）。

| 维度     | BPE          | WordPiece    | Unigram      | SentencePiece   |
|----------|--------------|--------------|--------------|-----------------|
| 方向     | 自底向上合并 | 自底向上合并 | 自顶向下剪枝 | 框架（含两者）  |
| 准则     | 频次最大     | 似然提升最大 | 似然损失最小 | —               |
| 概率模型 | 无           | 无           | 有           | 取决于算法      |
| 代表模型 | GPT/LLaMA    | BERT         | T5           | LLaMA/T5/ALBERT |

**追问：** LLaMA 用的是什么？LLaMA-1/2 用 **SentencePiece 的 BPE 模式**（32K），LLaMA-3 用 **tiktoken 风格的 BPE**（128K）。注意 LLaMA-2 tokenize 中文极差——一个中文汉字可能被拆成 2~3 个 token，导致中文句子长度是英文的 2 倍以上，这是 LLaMA-3 扩词表的重要动机。

------------------------------------------------------------------------

## Q20：词表大小如何影响模型？中文分词有什么特殊问题？

**答：**

**词表大小的影响：**

| 词表小（如 32K） | 词表大（如 128K/256K） |
|----|----|
| 序列长（token 数多） | 序列短（压缩率高） |
| embedding 参数量小 | embedding 参数量大（$`V \times d`$） |
| 每 token 信息量低，长文本时注意力开销大（$`O(n^{2})`$） | 每 token 信息量高，推理步数少、速度快 |
| 稀有词被拆成多子词 | 稀有词可能整体成词，但训练不充分 |
| 输出 softmax 计算量小 | 输出层计算量大 |

**定量例子**：LLaMA-2（32K 词表）对中文的压缩率约为 **1 汉字 ≈ 2.5 token**；Qwen（151K）和 LLaMA-3（128K）约为 **1 汉字 ≈ 0.6~0.7 token**。同一段中文文本，Qwen 的 token 数是 LLaMA-2 的 **约 1/3**，意味着推理速度快 3 倍、可容纳的上下文多 3 倍。这是 Qwen 在中文任务上效率优势的重要来源。

**经验规律**：*Scaling Laws with Vocabulary*（2024）等研究表明，在固定计算预算下，**最优词表大小随模型规模与数据量增长**（近似 $`V_{opt} \propto C^{0.5}`$ 或与参数量呈幂律关系）。小模型用小词表（避免 embedding 占比过大），大模型用大词表。

**中文分词的三个特殊问题：**

1.  **字符 vs 词的歧义**：中文无空格，分词边界模糊（「南京市长江大桥」）。解决方案是**不强做词级分词，直接用字节/字符 + BPE 学习子词**，让模型自己学出合理的切分粒度。
2.  **UTF-8 字节问题**：一个汉字占 3 个 UTF-8 字节。若在字节级做 BPE，一个汉字至少 3 个 token，极其低效（GPT-2 早期版本的问题）。解决方案是**先按 Unicode 字符切分，再做 BPE**（SentencePiece 的做法），或在大规模中文语料上训练使常用汉字成为独立 token。
3.  **多语言混合的词表分配**：中英混合时，若中文语料占比低，BPE 会把中文切得很碎。需要在训练分词器时**对中文语料上采样**，或使用足够大的词表（128K+）给中文留出配额。Qwen 的 151K 词表中，相当一部分是中文汉字与词组。

**追问：** 词表越大越好吗？不是，存在 U 形曲线： - 太小：序列过长，注意力/FFN 计算量增大，且语义碎片化 - 太大：embedding 和输出层参数占比过高（对 1B 小模型，256K 词表的 embedding 可能占 30%+ 参数），且大量低频 token 训练不充分（梯度信号稀疏），造成「死 token」 实践共识：**7B~70B 模型用 128K 左右，1B 级用 32K~64K，超大模型可到 256K**。

------------------------------------------------------------------------

# 七、归一化与激活函数

## Q21：LayerNorm / RMSNorm / DeepNorm 对比

**答：**（RMSNorm 与 DeepNorm 的公式见 Q5，此处补充对比与选型）

| 维度 | LayerNorm | RMSNorm | DeepNorm |
|----|----|----|----|
| 公式 | $`(x - \mu)/\sigma \cdot \gamma + \beta`$ | $`x/\text{RMS}(x) \cdot \gamma`$ | $`\text{LN}(\alpha x + \mathcal{F}(x))`$ |
| 中心化 | 有 | 无 | 有（外层 LN） |
| 可学习参数 | $`\gamma,\beta`$（$`2d`$） | $`\gamma`$（$`d`$） | $`\gamma,\beta`$ + 缩放 $`\alpha`$ |
| 计算开销 | 需算均值与方差 | 只算 RMS | 同 LN + 一次缩放 |
| 训练稳定性 | 好 | 好（需注意初始化） | 极好（1000 层） |
| 主要使用者 | 原始 Transformer、BERT | LLaMA、Qwen、Gemma | GLM-130B、DeepNet |

**为什么 RMSNorm 能去掉均值？** 论文指出 LayerNorm 的成功主要来自**缩放不变性**（scale invariance），而非 re-centering。去掉均值后： - 计算量减少约 10~15%（少了均值归约与减法） - 参数减少 $`d`$ 个/层 - 实验显示效果与 LN 持平（部分任务略好） - 在 bf16/fp16 下数值更友好（避免了减均值带来的精度损失）

**注意**：RMSNorm 对输入尺度的假设更弱，但如果输入均值严重偏离 0（如某些激活后），效果可能略差。实践中配合 Pre-Norm 和合理的初始化，问题不大。

------------------------------------------------------------------------

## Q22：GELU、SwiGLU、GeGLU 有什么区别？

**答：**

**GELU**（*Gaussian Error Linear Unit*, 2016）：

``` math
\text{GELU}(x) = x \cdot \Phi(x) = x \cdot \frac{1}{2}\left\lbrack 1 + \text{erf}\left( \frac{x}{\sqrt{2}} \right) \right\rbrack
```

近似形式（tanh 版，实际实现常用）：

``` math
\text{GELU}(x) \approx 0.5x\left( 1 + \tanh\left\lbrack \sqrt{\frac{2}{\pi}}\left( x + 0.044715x^{3} \right) \right\rbrack \right)
```

- **思想**：以「输入越大越可能被保留」的概率视角，对输入做随机正则化（dropout）的期望
- **性质**：处处可导（不像 ReLU 在 0 处不可导），负半轴有小的非零梯度（不像 ReLU 完全为 0），缓解 dying ReLU
- **代表**：BERT、GPT-2/3、ViT

**SwiGLU**：

``` math
\text{SwiGLU}(x) = \text{Swish}_{\beta}(xW_{\text{gate}}) \odot (xW_{\text{up}}),\quad\text{Swish}_{\beta}(x) = x \cdot \sigma(\beta x)
```

即 GLU（Gated Linear Unit）的门控机制 + Swish 激活。$`\beta = 1`$ 时 Swish = SiLU。

**GeGLU**：与 SwiGLU 结构相同，只是把激活换成 GELU：

``` math
\text{GeGLU}(x) = \text{GELU}(xW_{\text{gate}}) \odot (xW_{\text{up}})
```

**三者的关系**：SwiGLU 和 GeGLU 都属于 **GLU 变体家族**，核心是**门控机制**——一路做激活后作为「门」，另一路作为「值」，逐元素相乘。门控让网络可以动态控制信息流（学到「什么时候放行、放行多少」），比单纯的激活函数表达力更强。

**对比结论**（*GLU Variants Improve Transformer*, 2020 的消融）：

| 激活       | 相对效果             | 参数量       | 使用者               |
|------------|----------------------|--------------|----------------------|
| ReLU       | 基线                 | $`2dd_{ff}`$ | 原始 Transformer     |
| GELU       | 略优于 ReLU          | $`2dd_{ff}`$ | BERT、GPT            |
| **SwiGLU** | **最优（多项任务）** | $`3dd_{ff}`$ | LLaMA、Qwen、Mistral |
| GeGLU      | 与 SwiGLU 接近，略逊 | $`3dd_{ff}`$ | T5 v1.1、部分模型    |

**实践共识**：SwiGLU 是当前事实标准，因为它在等参数量下稳定占优，且 SiLU 本身比 GELU 计算便宜（$`\sigma`$ 比 $`\text{erf}`$ 便宜）。代价是多一个投影矩阵，但如 Q4 所述，通过把 $`d_{ff}`$ 降到 $`\frac{8}{3}d`$ 可以在等参数下对比。

**追问：** 为什么不把门控用在注意力上？实际上有——**Gated Attention**、**GAU（Gated Attention Unit）** 就是尝试，但收益不如 FFN 门控明显，因为注意力本身已有 softmax 的非线性与加权机制。另一个相关方向是 **Sigmoid Attention**（用 sigmoid 替代 softmax）和 **Gated DeltaNet** 等线性注意力变体。

------------------------------------------------------------------------

# 八、生成与采样

## Q23：贪心、Beam Search、Top-k、Top-p、Temperature、重复惩罚分别是什么？

**答：**

**解码目标**：给定 $`p(x_{t}|x_{< t})`$，选择下一个 token。不同策略在**质量、多样性、速度**之间权衡。

**1. 贪心搜索（Greedy）**：每步取 $`\arg\max`$。速度快，但容易陷入局部最优、生成重复文本，缺乏多样性。

**2. Beam Search**：维护 $`k`$ 个候选序列，每步扩展并保留累计 log 概率最高的 $`k`$ 个：

``` math
\text{score}(y_{1:t}) = \frac{1}{t^{\alpha}}\sum_{i = 1}^{t}\log p(y_{i}|y_{< i})
```

（长度归一化用 $`\alpha`$ 控制，避免偏向短句）。可以搭配 **长度惩罚** 和 **早停**。 - **优点**：对翻译等「有确定答案」的任务效果好，能搜索到更高概率的序列 - **缺点**：**对开放式生成反而变差**（生成平淡、重复、无趣），因为高概率序列往往是最保守的；计算量是 $`k`$ 倍 - **现状**：机器翻译仍常用，对话/创作类任务基本不用

**3. Temperature**：调整 logits 的锐利程度

``` math
p_{i} = \frac{\exp(z_{i}/T)}{\sum_{j}^{}\exp(z_{j}/T)}
```

- $`T \rightarrow 0`$：趋于 one-hot，退化为贪心（确定性）
- $`T = 1`$：原始分布
- $`T > 1`$：分布更平缓，多样性上升，但可能生成不连贯内容
- 实践：创意写作 $`T = 0.8 \sim 1.0`$，代码/数学 $`T = 0 \sim 0.3`$

**4. Top-k 采样**：只从概率最高的 $`k`$ 个 token 中采样，其余置 0 后重新归一化。 - 固定 $`k`$，不管分布形状。若分布很尖（模型很确定），保留 $`k = 50`$ 会引入大量噪声；若分布很平，$`k = 50`$ 又可能截断掉合理选项。

**5. Top-p（Nucleus Sampling）**（*The Curious Case of Neural Text Degeneration*, 2020）：取累积概率达到 $`p`$ 的**最小** token 集合：

``` math
V^{(p)} = \arg\min_{V'}\left\{ \sum_{x \in V'}^{}p(x) \geq p \right\}
```

然后在该集合内重新归一化采样。 - **自适应**：分布尖锐时集合小（只保留几个），分布平坦时集合大 - 实践：$`p = 0.9 \sim 0.95`$，通常与 Top-k 联合使用（先 Top-k 再 Top-p）

**6. 重复惩罚（Repetition Penalty）**：

``` math
z_{i} = \left\{ \begin{matrix}
z_{i}/\theta, & z_{i} > 0 \\
z_{i} \cdot \theta, & z_{i} \leq 0
\end{matrix} \right.\ ,\quad\theta > 1
```

对已出现过的 token 降低其 logits。（OpenAI 的实现形式）变体包括： - **Presence Penalty**：只要出现过就统一减一个常数 - **Frequency Penalty**：按出现次数成比例减 - **No Repeat N-gram Size**：禁止任何 $`n`$-gram 重复出现（硬约束） - **DRY (Don’t Repeat Yourself)** 惩罚：对重复的更长序列加重惩罚

**追问：** Top-p 和 Top-k 一起用时谁先？通常**先 Top-k 再 Top-p**（HuggingFace 默认顺序），但社区也有认为应反过来。实践中差别不大，关键是把两者之一调好。此外 **min-p** 采样（只保留概率 ≥ $`\text{min\_p} \times p_{\max}`$ 的 token）在 2024 年后被认为在高 temperature 下比 top-p 更稳定。

------------------------------------------------------------------------

## Q24：对比解码（Contrastive Decoding）是什么？

**答：**

对比解码（*Contrastive Decoding: Open-ended Text Generation as Optimization*, 2022）的核心思想：**用「专家模型」和「业余模型」的分布差来放大前者优于后者的部分**。

``` math
p_{\text{CD}}(x_{t}) \propto \exp\left( (1 + \alpha)\log p_{\text{expert}}(x_{t}) - (1 - \beta)\log p_{\text{amateur}}(x_{t}) \right)
```

（DExperts 形式）或更简单的形式：

``` math
\text{score}(x_{t}) = \log p_{\text{expert}}(x_{t}) - \alpha\log p_{\text{amateur}}(x_{t})
```

其中「专家」是大模型，「业余」是小模型（或同一个模型的不同 prompt/不同层）。**直觉**：业余模型也会预测出「the」「of」这类通用高频词，两者相减后这些「谁都能预测的词」被抑制，剩下的是只有强模型才能预测出的、真正有信息量的词。这同时缓解了**重复**和**退化**问题。

**相关变体：**

- **DExperts**：专家 + 反专家（anti-expert）的解码时组合
- **DoLa（Decoding by Contrasting Layers）**：用同一模型的**不同层**做对比——高层负责事实性、低层负责语法。$`\text{score} = \log p_{\text{late layer}} - \log p_{\text{early layer}}`$，提升事实性，**无需额外模型**
- **VCD（Visual Contrastive Decoding）**：多模态场景，用「无图像」或「失真图像」的输出来对比，缓解幻觉
- **ICD（Instruction Contrastive Decoding）**：对比「有指令」vs「无指令」的分布，缓解幻觉

**优点**：无需训练、可直接后处理、对事实性和重复问题有效。 **缺点**：需要两次前向（2x 计算），超参 $`\alpha`$ 敏感，对「业余模型」的选择有依赖。

**追问：** 和投机解码（Speculative Decoding）有什么区别？完全不同的目的——投机解码是用小模型**草拟**、大模型**验证**来**加速**（不改变输出分布，配合 speculative sampling 可保证等价于大模型采样）；对比解码是用小模型做**减法**来**改质**（改变输出分布，提升质量）。两者可结合使用。

------------------------------------------------------------------------

# 九、评估与 Scaling Law

## Q25：困惑度 PPL 是什么？如何计算与解释？

**答：**

**定义**：语言模型在测试集上的**平均负对数似然**的指数：

``` math
\text{PPL}(W) = \exp\left( - \frac{1}{N}\sum_{i = 1}^{N}\log p(w_{i}|w_{1},\ldots,w_{i - 1}) \right)
```

等价于 $`\text{PPL} = 2^{H}`$（$`H`$ 为交叉熵，以 bits 为单位时），或 $`e^{H}`$（以 nats 为单位）。

**解释**： - PPL 可理解为模型在每一步的「**平均候选困惑度**」——模型在预测下一个 token 时，等效地在多少个等概率选项中犹豫。PPL=10 意味着模型的不确定度相当于在 10 个词里均匀选择。 - **越低越好**，且**每个 token 一个 bit 以上**（PPL \> 2）才是合理的。 - 与交叉熵的关系：$`\text{PPL} = e^{\mathcal{L}}`$，所以 loss 从 2.0 降到 1.9，PPL 从 $`e^{2} = 7.39`$ 降到 $`e^{1.9} = 6.69`$。

**注意事项（面试常挖坑）：**

1.  **PPL 依赖分词器**！不同 tokenizer 下同一个句子的 token 数不同，PPL 不可直接比较。例如 LLaMA-2（32K 词表）和 LLaMA-3（128K 词表）对同一中文文本的 PPL**不可比**。跨模型比较需要统一到 **bits-per-byte（BPB）**：

``` math
\text{BPB} = \frac{\mathcal{L} \cdot n_{\text{tokens}}}{n_{\text{bytes}} \cdot \ln 2}
```

- （即把 loss 按字节数归一化）这是 LLaMA 论文报告 PPL 时的常用做法。

2.  **数据泄漏**：若测试集在预训练数据中，PPL 会异常低（如 GPT-4 在某些 benchmark 上「PPL 极低」被怀疑泄漏）。

3.  **PPL 好 ≠ 下游任务好**：PPL 衡量的是「语言建模能力」，与指令遵循、推理、事实性相关性中等。很多模型 PPL 更低但对话质量更差。

4.  **上下文长度影响**：长上下文下 PPL 通常更低（更多信息），比较时需固定窗口。

**追问：** 为什么现在更少提 PPL？因为 LLM 进入「对齐时代」后，PPL 与人类偏好的相关性下降了。现在更看重 MMLU、GSM8K、HumanEval、MT-Bench、Chatbot Arena Elo 等下游指标，以及 LLM-as-a-Judge。

------------------------------------------------------------------------

## Q26：Scaling Law 是什么？Kaplan 和 Chinchilla 有什么分歧？

**答：**

**Kaplan et al. 2020**（*Scaling Laws for Neural Language Models*, OpenAI）：

发现模型 loss $`L`$ 与参数量 $`N`$、数据量 $`D`$、计算量 $`C`$ 之间存在**幂律关系**：

``` math
L(N) = \left( \frac{N_{c}}{N} \right)^{\alpha_{N}},\quad L(D) = \left( \frac{D_{c}}{D} \right)^{\alpha_{D}},\quad L(C) = \left( \frac{C_{c}}{C} \right)^{\alpha_{C}}
```

拟合出的指数（近似）：$`\alpha_{N} \approx 0.076`$，$`\alpha_{D} \approx 0.095`$，$`\alpha_{C} \approx 0.050`$。

**Kaplan 的关键结论**： - 给定计算预算 $`C \approx 6ND`$，**最优的模型规模** $`N_{opt}`$ **增长快于数据规模**，即 $`N_{opt} \propto C^{0.73}`$，$`D_{opt} \propto C^{0.27}`$ - 推论：**「模型越大越好」**，数据量相对次要——这直接催生了 GPT-3（175B，只训练 300B token）和一大批「大参数、小数据」模型

**Chinchilla（Hoffmann et al. 2022）**（*Training Compute-Optimal Large Language Models*, DeepMind）：

重新做了更严格的实验（固定学习率调度、扫更多模型规模），发现 Kaplan 的结论有系统性偏差（学习率未随规模调整等因素）。Chinchilla 的结论：

``` math
N_{opt} \propto C^{0.50},\quad D_{opt} \propto C^{0.50}
```

即 **模型规模和训练数据应同比例增长，约每个参数配 20 个 token**：

``` math
D_{opt} \approx 20 \times N_{opt}
```

**实例**：Chinchilla 用 70B 参数 + 1.4T token 训练，**全面超越** 280B 参数 + 300B token 的 Gopher（参数量 4 倍但数据不足）。

**两者的核心分歧**：

|                   | Kaplan 2020      | Chinchilla 2022     |
|-------------------|------------------|---------------------|
| 最优 $`N`$ 的指数 | $`C^{0.73}`$     | $`C^{0.50}`$        |
| 最优 $`D`$ 的指数 | $`C^{0.27}`$     | $`C^{0.50}`$        |
| 数据/参数比       | ~1.7 token/param | **~20 token/param** |
| 推论              | 堆参数           | 参数量与数据并重    |

**Chinchilla 之后的变化**：现代模型的数据量**远超** Chinchilla 最优。LLaMA-3-8B 用 15T token（1875 token/param），DeepSeek-V3 用 14.8T。原因是：

1.  **推理成本约束**：Chinchilla 只优化「训练算力」，但实际部署时**推理成本随参数量线性增长**。用更小的模型 + 更多的数据，可以在相同推理成本下获得更好效果（*Beyond Chinchilla-Optimal*, 2024 提出的「推理感知的最优」）。
2.  **数据质量提升**：高质量数据（代码、数学、教科书）的边际收益更高，且有多轮 epoch 的可能（小模型可以训 4~20 epoch 不过拟合）。
3.  **数据墙担忧**：高质量 token 总量估计在 $`10^{13} \sim 10^{14}`$ 量级，接近耗尽，因此「多训几遍」成为必要选择。

**追问：** Scaling Law 在什么情况下失效？(1) **涌现能力（Emergent Abilities）** 处，某些任务能力不是平滑增长而是突然出现（见下题）；(2) **数据质量**是关键变量，同样的 token 数，清洗过的数据效果差很多（LLaMA-3 论文强调 15T 数据的清洗管线）；(3) **架构变化**（MoE、线性注意力）会改变常数项但幂律指数大体保留；(4) **多模态 / 多语言**的 scaling 需要分别拟合。

------------------------------------------------------------------------

## Q27：涌现能力（Emergent Abilities）是真的吗？有什么争议？

**答：**

**「涌现」现象**（*Emergent Abilities of Large Language Models*, Wei et al. 2022）：某些能力（如多步算术、符号推理、指令遵循）在小模型上接近**随机水平（0%）**，而模型规模超过某个阈值后**突然跃升到高分**，呈现**非连续性**、不可从小模型外推的特征。

**争议（*Are Emergent Abilities of Large Language Models a Mirage?*, Schaeffer et al., NeurIPS 2023 最佳论文）**：

核心论点：**涌现是「度量选择」造成的幻觉，而非模型能力的真实质变**。

论据： 1. **非线性/不连续的指标会伪造涌现**。若指标是「精确匹配」（exact match）或「多选题正确率」，每个样本是 0/1 的二值判断，模型输出质量**平滑改善**时，只有当「输出正确」的概率跨过某个阈值，指标才会快速上升，产生「突然出现」的假象。 2. **换成连续、线性的指标（如 token-level 的编辑距离、正确 token 的对数似然、多项选择中正确选项的概率）后，涌现消失**——曲线变成平滑的幂律上升。 3. 论文做了大量实验：把同一批模型的指标从 exact match 换成 token edit distance，所有「涌现曲线」都变平滑；反之，把小模型上本已平滑的指标换成不连续指标，可以**人为制造出涌现**。

**综合结论（面试建议的平衡回答）：**

- **从「模型能力」角度**：平滑、可预测。Scaling law 在连续指标上稳定成立。
- **从「任务表现/下游可用性」角度**：确实存在「跨过某个规模才能实际用起来」的现象。因为实际任务往往要求**完整正确的输出**（如代码能否跑通、数学题答案是否正确），而非「部分正确」。这时「平滑的能力提升」确实会表现为「突然可用」。
- **实践含义**：不要把涌现当成「魔法」而放弃小模型；对小规模应用，应针对性地做**数据配比与微调**（小模型在特定任务上可以超过大模型），而不是奢望涌现。

**追问：** 什么能力是真正「质变」的？目前证据较多的是 **in-context learning（ICL）**：小模型几乎无法利用 few-shot 示例，大模型可以。但这也可以用「小模型的 ICL 概率提升太小、恰好不足以改变 argmax 输出」来解释。此外 **CoT（思维链）** 是规模依赖的典型——小模型生成 CoT 反而有害（*Chain-of-Thought Prompting Elicits Reasoning in Large Language Models*, 2022 发现 CoT 在 ~100B 以下几乎无收益），这被认为是任务需要「多步组合」时，每步准确率的乘积效应导致的相变。

------------------------------------------------------------------------

# 附录：高频速记与易错点

1.  $`\sqrt{d_{k}}`$ 是为了把点积方差归一到 1；除以 $`d_{k}`$ 会导致 softmax 过于均匀。
2.  **多头注意力参数量与** $`h`$ **无关**，为 $`4d^{2}`$（无 bias）。
3.  **FFN 占模型参数约 2/3**，是主要的知识存储单元。
4.  **SwiGLU 的** $`d_{ff}`$ **不是 4x**，等参数下是 $`8/3 \times d`$（LLaMA 实际取 11008/4096 ≈ 2.69x）。
5.  **Pre-LN 不需要 warmup**，Post-LN 需要；DeepNorm 是两者的折中。
6.  **RoPE 用绝对位置实现相对位置**；内积只依赖 $`m - n`$。
7.  **KV Cache 显存** $`\propto`$ **KV 头数**，GQA/MQA 是主要优化手段；MLA 走的是低秩压缩路线。
8.  **FlashAttention 是 exact attention，不是近似**；其收益来自减少 HBM 访存。
9.  **PagedAttention 解决的是 KV Cache 的碎片与共享问题**，与 FlashAttention 正交。
10. **Decoder-only 胜出**是因为目标统一、train-test 一致、scaling 友好、支持 ICL。
11. **Chinchilla 的最优是 ~20 token/param**，但现代模型远超这个值（推理成本 + 数据质量驱动）。
12. **词表影响 PPL 可比性**，跨模型比较用 BPB。
13. **MoE 的负载均衡**从辅助损失（Switch）演进到无辅助损失的动态 bias（DeepSeek-V3）。
14. **采样**：开放式生成用 Top-p/Top-k + temperature，不要用 beam search；需要确定性时用低 temperature 而非贪心（可选）。
15. **涌现能力争议**：指标不连续 → 假涌现；任务可用性 → 真相变。

------------------------------------------------------------------------

**本文件整理日期：2026-09-12**
