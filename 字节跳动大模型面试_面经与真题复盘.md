------------------------------------------------------------------------

# 说明

本文件与同目录下的《字节跳动大模型面试复习手册 2025至2026.docx》是**分工关系，不是替代关系**：

| 维度 | 复习手册（docx） | 本文件（md） |
|----|----|----|
| 定位 | 知识底座：把八股讲清楚、讲透原理 | 实战复盘：流程、真题、答法、部门差异 |
| 内容 | Transformer 架构、位置编码推导、归一化对比、MoE 原理、训练/微调/RLHF、分布式并行、推理优化 | 岗位版图、面试轮次节奏、高频真题清单与答题框架、手撕代码模板、准备时间线、避坑 |
| 使用方式 | 先通读建立体系，遇到不懂回查 | 面试前 1~2 周反复过，按「题目→考察点→框架→要点」自测 |
| 重叠部分 | 少量（如 Attention 原理） | — 重叠处本文件只给「字节会怎么追问」的角度，不重复推导 |

**建议使用顺序**：先用 docx 打底 → 用本文件做「模拟面试脚本」开口讲 → 把讲不顺的地方回 docx 补原理 → 面试前一天只看本文件的「反问清单 + 避坑清单」。

同目录另有配套专题：《大模型八股面试题_上_基础与架构.md》《大模型八股面试题_下_训练与微调.md》《RAG检索增强生成_面试专题.md》《AI_Agent开发_面试专题.md》，可作为本文件真题部分的延伸阅读。

------------------------------------------------------------------------

# 来源与参考

本文件的题目框架与流程描述，综合参考了以下公开面经与题库仓库（仅作来源标注，具体题目经过归类与重写，不对应任何可识别的候选人）：

- **jingtian11/EasyOffer** — 内含字节跳动等大厂面经目录，按公司/岗位归档，覆盖算法岗一二三面记录，是面试流程节奏描述的主要参照
- **wdndev/llm_interview_note** — 大模型面试笔记，涵盖 LLM 基础、训练、推理、RAG、Agent 等板块，是真题分类体系的参照
- **WeThinkIn/AIGC-Interview-Book** — 内含「大厂高频面试题」章节，覆盖 AIGC/大模型方向高频题与八股整理
- **datawhalechina/daily-interview** — 每日面试题系列，涵盖算法与机器学习基础题，用于补充手撕与算法题清单
- 其他：牛客网公开面经区、知乎/小红书公开复盘帖中的「字节大模型/算法岗」标签内容（用于归纳面试风格共性，未引用具体个人陈述）

> 注：各家面经存在时效性差异，2024—2026 年字节的面试重心明显从「八股背诵」转向「项目深挖 + 工程细节 + 开放思考」，本文件按新趋势加权。

------------------------------------------------------------------------

# 一、字节大模型相关岗位版图与面试侧重

字节内部做「大模型」的团队非常分散，**投递前必须搞清楚自己面的是哪一类岗**，因为面试官构成和考察权重完全不同。

## 1.1 Seed（豆包大模型团队）——研究/预训练导向

- **做什么**：豆包大模型底座预训练、多模态（视觉/语音/视频生成）、后训练（SFT/RLHF/RLVR）、评测与数据。
- **面试官画像**：多为博士背景研究员，论文导向。
- **考察权重**：预训练与架构原理 40% / 论文与前沿理解 25% / 代码与工程 20% / 项目 15%。
- **典型问法**：
  - 「你读过哪些近两年的预训练论文？挑一篇讲讲它的核心 contribution 和局限。」
  - 「如果让你从零训一个 7B，你的数据配比怎么定？怎么验证配比是对的？」
  - 「Scaling Law 在你的实际工作里怎么用的？」
- **特点**：会深挖你的论文/科研项目，对「为什么这个设计能 work」的追问极其执着；会问 loss spike、训练不稳定这类一线问题。

## 1.2 AML / 火山引擎 —— 平台与工程落地导向

- **做什么**：火山方舟（模型服务平台）、训练推理加速、模型压缩、异构调度、大规模分布式训练框架。
- **面试官画像**：工程背景强的算法工程师。
- **考察权重**：工程与系统 40% / 推理优化 25% / 算法基础 20% / 项目 15%。
- **典型问法**：
  - 「千卡训练，GPU 利用率上不去，你怎么定位瓶颈？」
  - 「vLLM 的 PagedAttention 解决什么问题？continuous batching 的代价是什么？」
  - 「一个 70B 模型要在单机 8 卡上跑到 200 tokens/s/req 以内延迟，你怎么做？」
- **特点**：极度重视**数字**——吞吐、延迟、显存占用、通信量，答不出量级会被判定为「没真做过」。

## 1.3 抖音 / 电商 / 飞书等业务线算法岗 —— 业务落地导向

- **做什么**：内容理解、搜索推荐、广告、客服/办公 Copilot、评论审核、商品理解、多模态内容生成。
- **面试官画像**：业务线算法工程师，KPI 导向。
- **考察权重**：项目与业务理解 40% / 场景设计 30% / 基础与代码 30%。
- **典型问法**：
  - 「你做的这个模型，上线后业务指标提升了多少？怎么证明是这个模型带来的？」
  - 「给你抖音每天上亿条评论，怎么用 LLM 做低成本的违规识别？」
  - 「设计一个电商场景的商品标题生成，怎么保证不违规、不夸大？」
- **特点**：**万变不离业务价值**。任何一个技术点最后都会被问「所以 ROI 是多少」「为什么不直接用现成的 API」。

## 1.4 AI Lab（研究型，偏前沿探索）

- 更接近高校实验室节奏，多模态生成、世界模型、具身智能等方向。
- 面试会大量聊论文、聊 idea，代码环节可能要求实现较复杂的模块（如 diffusion sampling、VQ-VAE 相关）。
- 与 Seed 的区别：Seed 更聚焦 LLM 主线与产品化，AI Lab 更发散。

## 1.5 数据-算法岗（Data-LLM / 数据智能）

- **做什么**：训练数据构建与清洗、数据质量评估、合成数据、数据配比实验。
- **典型问法**：
  - 「怎么设计一套数据质量打分体系？」
  - 「去重你怎么做？MinHash 的原理和参数怎么选？」
  - 「合成数据会不会导致模型崩溃（model collapse）？你怎么避免？」
- **特点**：容易被认为是「脏活」，实际面试反而深挖细节，因为数据决定上限。

## 1.6 一句话选岗建议

| 你的背景                      | 优先投           |
|-------------------------------|------------------|
| 有顶会论文、做过预训练/后训练 | Seed、AI Lab     |
| 做过推理加速、分布式、CUDA    | AML / 火山引擎   |
| 有推荐/搜索/广告/NLP 业务经验 | 抖音、电商、飞书 |
| 做过数据工程、数据质量        | 数据-算法岗      |

------------------------------------------------------------------------

# 二、面试流程详解

## 2.1 标准流程（5 轮）

    简历筛选 → 一面(基础+项目) → 二面(深挖+场景) → 三面/交叉面(系统设计+开放题) → HR面 → (部分岗位加一轮) 主管面/加面

## 2.2 各轮详解

**（0）简历筛选**

- 形式：HR + 业务方双筛。实习/校招有笔试（部分岗位），社招一般无笔试。
- 关注点：**项目关键词是否匹配 JD**、是否有可验证的量化成果、学历与论文（研究岗）。
- 通过率参考：大模型相关岗位竞争激烈，简历关可能筛掉 70%+。**简历上写「熟悉 Transformer」几乎等于没写**，要写「做过什么、提升了多少」。

**（1）一面：基础 + 项目（60 分钟）**

- 节奏：自我介绍 5 min → 项目 20~25 min → 八股 20 min → 手撕 10~15 min → 反问 5 min。
- 考察重点：
  - 项目是否「真做过」——会针对你写的每个数字追问来源。
  - 基础八股：Transformer、Attention、归一化、位置编码、损失函数。
  - 手撕：通常是 Attention / LayerNorm / Softmax 这类必背题，偶尔是 LeetCode medium。
- 一面面试官通常是与你同级或高一级的工程师，**态度相对友好，是「筛掉明显不合格」的一轮**。

**（2）二面：深挖 + 场景（60~75 分钟）**

- 节奏：项目深挖 30 min（会反复问「为什么」直到你答不出）→ 场景设计 20 min → 代码/算法 15 min → 反问。
- 考察重点：
  - 技术决策能力：为什么选 A 不选 B，代价是什么。
  - 场景题：给定业务问题，从数据到上线全链路设计。
  - 会有压力面成分（见第三节）。
- **这一轮是淘汰率最高的一轮**。面试官往往是你未来直属 leader 的平级，会判断「这人能不能独立扛事」。

**（3）三面 / 交叉面：系统设计 + 开放题（45~60 分钟）**

- 节奏：系统设计 30 min → 开放思考题 15 min → 职业规划/反问 15 min。
- 考察重点：
  - 端到端系统设计：如设计一个企业级 RAG、设计一个多模态内容审核系统。
  - 开放题：大模型能力边界、幻觉、AGI 路径、技术趋势判断。
  - 交叉面可能由**其他部门**的面试官进行，考察「跨团队沟通」与「技术表达是否通俗」。
- 三面通常是 leader/总监级，**更看格局与判断力，不纠细节语法**。

**（4）HR 面（30 分钟）**

- 考察：稳定性、动机、薪资预期、是否有竞业/其他 offer。
- 高频问题：「为什么从上一家离职」「手上还有哪些 offer」「期望薪资」「能接受多大的不确定性」。
- 注意：HR 面**也会挂人**，尤其是在薪资预期严重超出预算、或对岗位理解明显错位时。

## 2.3 实习 vs 校招 vs 社招的差异

| 维度 | 实习 | 校招 | 社招 |
|----|----|----|----|
| 轮次 | 2~3 轮（常无 HR 面或合并） | 4~5 轮 | 4~5 轮 + 可能加面 |
| 侧重 | 基础扎实度、学习能力、代码 | 基础 + 项目 + 潜力 | 项目深度 + 业务价值 + 落地能力 |
| 项目要求 | 能讲清楚课程/科研项目即可 | 最好有 1~2 段相关实习/比赛 | 必须有可量化、可背调的真实产出 |
| 八股深度 | 中等，偏经典 | 较深，会追推导 | 偏应用，问「你实际怎么用的」 |
| 手撕 | 必考，难度中等 | 必考，可能 2 题 | 视岗位，工程岗必考 |
| 时长 | 每轮 45~60 min | 每轮 60 min | 每轮 60~90 min |
| 特殊点 | 看重「可培养性」 | 比实习更看「天花板」 | 会问为什么跳槽、能否快速上手 |

## 2.4 时间线参考

- 实习：全年滚动，暑期实习集中在 3—5 月投递，**提前批**在 1—2 月。
- 校招：提前批 7—8 月，正式批 9—10 月，补录 11—12 月。
- 社招：全年，流程通常 2~4 周走完；**字节流程算快的，但交叉面协调有时会拖到 4~6 周**。

------------------------------------------------------------------------

# 三、字节面试风格特点（与其他大厂的差异）

1.  **项目深挖到「答不出」为止** 字节的典型风格是：你说「用了 LoRA 微调」，他会问「为什么用 LoRA 不用全参」「rank 怎么选的」「为什么不选 8 选 16」「A 矩阵和 B 矩阵谁初始化成 0」「合并回原权重后效果有变化吗」。**准备项目时要自己先问自己 5 层「为什么」**。

2.  **「为什么这么做」优于「做了什么」** 面试官不关心你调用了什么库，关心你的**决策过程**。同样的项目，讲清楚「我对比了 3 种方案，A 因为显存不行被排除，B 因为延迟不达标被排除，最终选 C 并做了 X 优化」，比「我用 C 完成了任务」高一档。

3.  **喜欢问手撕代码，且偏爱「写核心算子」** 不满足于 LeetCode，喜欢让你**手写 LayerNorm/Attention/RoPE/KV Cache**。考察点不是「能不能跑」，而是「有没有考虑数值稳定性、边界、shape、mask」。

4.  **重视工程细节与数据规模** 会具体问「数据多少条」「多少 token」「几张卡」「训了多久」「显存峰值多少」「QPS 多少」。**答不出量级 ≈ 没真做过**。哪怕估算也要给一个合理区间。

5.  **喜欢开放题和思考题** 尤其在三面。「你觉得大模型三年内会取代什么岗位」「幻觉能根治吗」「如果给你一个无限算力的预算你会做什么」。**没有标准答案，考察思考的层次和结构化程度**。

6.  **压力面特征** 表现形式：频繁打断、质疑你的结论（「你确定？」「这个数字对吗？」）、连续追问到你卡壳、故意给出错误假设看你是否盲从。 **应对原则**：

    - 不慌，先确认问题：「您是想问 XX 这个角度吗？」
    - 不硬编：不确定就说「这块我没有实测过，我的理解是……可能不对，回去我会验证」。**字节面试官对「坦诚承认不知道」的容忍度，远高于「编造」**。
    - 被质疑时先复述对方观点，再说自己的依据，不要立刻自我否定。

7.  **反问环节真的会被评估** 不问问题或只问「团队氛围怎么样」会被扣分。好的反问见第七节。

------------------------------------------------------------------------

# 四、高频真题分类整理

> 每道题按 **【考察点】→【答题框架】→【要点】** 组织。加粗的是字节问得特别多的。

## 4.1 基础八股类（字节常问的角度）

### Q1：**手推 Self-Attention 的完整计算过程，并说明每个矩阵的 shape。**

- 【考察点】是否真理解，而非背公式。
- 【框架】输入 X (B, L, d) → 三个投影 W_q/W_k/W_v → Q,K,V (B, h, L, d_head) → scores = QKᵀ/√d_head → mask → softmax → 加权求和 → 输出投影。
- 【要点】
  - 除以 √d_head 的原因：**防止点积随维度增大而方差变大，导致 softmax 进入饱和区（梯度趋近 0）**。要能说出「假设 q、k 各分量独立同分布均值 0 方差 1，点积方差为 d_head，所以除以 √d_head 把方差拉回 1」。
  - 复杂度：**时间 O(L²·d)，空间 O(L²)**，这是长文本的核心矛盾。
  - 多头的作用：**在不同子空间学习不同模式的关联**，而不是「增加参数量」（可以指出多头并不增加 FLOPs，总维度不变）。

### Q2：**MHA / MQA / GQA 的区别？为什么现在主流用 GQA？**

- 【考察点】工程直觉 + 对推理瓶颈的理解。
- 【框架】定义差异 → 显存/带宽影响 → 为什么 GQA 是折中。
- 【要点】
  - MHA：每个 head 独立 K/V。MQA：所有 head 共享一组 K/V。GQA：分组共享（如 8 组）。
  - **推理 decode 阶段是 memory-bound**，KV Cache 大小 ≈ 2 × L × n_kv_head × d_head × dtype_bytes × B。MQA/GQA 直接降低 n_kv_head，**减少显存占用和访存带宽压力**，从而提升吞吐。
  - GQA 相比 MQA **质量损失更小**，是目前 Llama 2/3、Qwen 等的主流选择。
  - 追问点：「GQA 对训练有影响吗？」——训练时计算量基本不变，但可减少 KV 存储，允许更大 batch。

### Q3：**位置编码：绝对位置、相对位置、RoPE、ALiBi 的对比与选择。**

- 【考察点】外推能力理解。
- 【要点】
  - 正弦绝对编码：固定、无法外推（超出训练长度性能骤降）。
  - 可学习绝对编码：受最大长度限制。
  - **RoPE**：通过旋转矩阵把相对位置信息注入 q/k 的内积，**天然表达相对位置**，长距衰减性好，是当前主流。
  - **RoPE 的原始外推能力也不够**，所以有 NTK-aware scaling、Position Interpolation、YaRN 等扩展方案。
  - ALiBi：直接在 attention score 上加线性偏置，外推好但**在长文本上不如 RoPE + 插值**。
  - 追问点：「RoPE 为什么不能直接用在 value 上？」——因为位置信息要作用在 q·k 的匹配上，作用于 v 会破坏语义信息。

### Q4：**手写 RoPE（旋转位置编码）的实现。**

见 4.3 代码部分 Q13。

### Q5：**LayerNorm vs RMSNorm vs BatchNorm。**

- 【要点】
  - LayerNorm 对**特征维度**归一化，与 batch 无关，适合变长序列。
  - **RMSNorm 去掉了均值中心化和 bias，只做 x / sqrt(mean(x²) + eps) × γ**，少一次 mean 计算和减法，**实测在 LLM 上效果与 LN 相当但更快**（Llama、Qwen 采用）。
  - BatchNorm 依赖 batch 统计，**变长序列 + 小 batch 时不稳，且训练/推理行为不一致**，LLM 基本不用。
  - Pre-LN vs Post-LN：**Pre-LN 训练更稳定、无需 warmup**，是主流；Post-LN 表达力稍强但需谨慎调 warmup。
  - 追问点：「为什么 Pre-LN 最后要加一个 final norm？」——因为 Pre-LN 的残差流方差会随深度累积增长，最后归一化稳定输出尺度。

### Q6：**MoE 的原理、优势与工程难点。**

- 【要点】
  - 核心：**用多个 FFN expert + 一个 router，每个 token 只激活 top-k 个 expert**，实现「参数量大但计算量小」。
  - 优势：总参数可做很大，FLOPs 只随激活参数增长（如 Mixtral 8x7B 激活约 13B）。
  - 难点：① **负载不均衡**（需要 auxiliary loss / capacity factor / 专家路由优化）；② **通信开销大**（expert parallelism 引入 all-to-all，是训练效率瓶颈）；③ **推理时 batch 小则 expert 利用率低**；④ 微调时容易过拟合到少数 expert。
  - 追问点：「aux loss 的系数怎么定？」——太大会损害主任务，太小会塌缩，通常 0.01 量级并需实验；也有用 router z-loss 稳定训练的。
  - 追问点：「MoE 和稠密模型在同等激活参数下谁强？」——**通常 MoE 需要更多数据才能追平/超越**，同等激活参数下稠密模型在数据不足时更强。

### Q7：**Transformer 的参数量怎么算？一个 7B 模型大概是什么配置？**

- 【要点】能快速估算：Embedding（V×d）+ 每层（Attention 4d² + FFN 8d²，若用 SwiGLU 约 3×d×d_ff）+ 输出层（通常与 embedding 共享权重）。
- 示例：d=4096，层数 32，FFN 中间维 11008，词表 32000 → 约 6.7B。**能说出这个估算过程比背数字加分**。

### Q8：**为什么现在 LLM 都用 SwiGLU 而不是 ReLU/GELU？**

- 【要点】SwiGLU(x) = Swish(xW₁) ⊙ xW₃，门控机制带来更好表达；为保持参数量一致通常把中间维从 4d 降到约 8/3 d。

### Q9：**BN 和 Dropout 在 LLM 里为什么基本不用了 / 怎么用？**

- 现代大模型普遍**不使用 Dropout**（数据量足够大，正则化靠数据与权重衰减），且 Dropout 与大规模分布式训练的确定性有冲突。

### Q10：**Adam vs AdamW，LLM 训练怎么选优化器？**

- 【要点】AdamW 把 weight decay 从梯度更新中解耦（**decoupled weight decay**），是 LLM 标配；常用 β=(0.9, 0.95)，weight decay 0.1，grad clip 1.0，warmup + cosine decay。

### Q11：**训练中出现 loss spike 怎么办？**

- 【要点】① 检查数据（脏数据、异常长序列）；② 定位是哪一步、哪个 rank；③ 短期手段：回滚到 spike 前的 checkpoint、跳过该 batch、降低 lr、增大 grad clip；④ 长期：加 z-loss、调整初始化、检查 fp16/bf16 溢出（**bf16 动态范围大但精度低，fp16 易溢出**）；⑤ 记录并复现。

### Q12：**SFT 和 RLHF 的区别，什么时候必须上 RLHF？**

- 【要点】SFT 是模仿学习（最大化目标回复似然），RLHF 是偏好优化。当「好」难以用标注答案表达（如风格、安全性、有帮助性权衡）时，偏好数据比 SFT 数据更易得且更有效。

### Q13：**DPO 和 PPO 的对比，DPO 的局限？**

- 【要点】DPO 把 RLHF 的 reward model + PPO 化简为**在偏好数据上的分类式损失**，无需采样、稳定、便宜。局限：**离线数据分布偏移**（DPO 无法探索新回复）、对偏好数据质量敏感、可能过度压低 chosen 之外的所有回复概率。改进：IPO、KTO、ORPO、SimPO 等。

### Q14：**FLOPs / 显存怎么估算？训练一个 7B 需要多少显存？**

- 【要点】
  - 训练 FLOPs ≈ 6 × N × D（N 参数量，D token 数）。
  - 全参训练显存 ≈ 参数 2B（bf16）+ 梯度 2B + 优化器状态 12B（Adam fp32 的 m/v + fp32 master weight）≈ **16B bytes**，7B 模型约 112GB → 单卡放不下，必须 ZeRO/FSDP/TP。
  - 推理显存 ≈ 参数 2B + KV Cache。

### Q15：**ZeRO 1/2/3 分别切什么？和 TP/PP 怎么配合？**

- 【要点】ZeRO-1 切优化器状态、ZeRO-2 切梯度、ZeRO-3 切参数。TP 切单层矩阵（通信频繁，需 NVLink），PP 切层（有 bubble，需 micro-batch），DP 复制（通信在梯度 all-reduce）。**3D 并行 = TP×PP×DP**，ZeRO-3 与 TP 同时用要小心通信放大。

------------------------------------------------------------------------

## 4.2 项目深挖类

### Q16：**用 3 分钟介绍你最有代表性的项目。**

- 【考察点】表达结构化能力 + 项目含金量判断。
- 【框架】**STAR-T**：
  - S（背景）：业务问题是什么，规模多大（数据量/用户量/QPS）
  - T（目标）：你要达到的指标（明确数字，如「准确率从 78% 提到 85%」）
  - A（行动）：**技术决策链**——试过哪些方案、为什么淘汰、最终方案的核心改动
  - R（结果）：最终指标 + 业务价值（如「人工审核成本下降 40%」）
  - T（Think）：反思与如果有更多资源会怎么改
- 【要点】**不要按时间线讲「我先做了 A 再做 B」**，要按「问题→决策→结果」讲。

### Q17：**这个项目里你用了 LoRA，为什么不用全参微调？rank 怎么定的？**

- 【考察点】是否真做过微调。
- 【要点】全参需要 16B bytes 显存 + 灾难性遗忘风险 + 多任务需多份权重；LoRA 只训低秩增量。rank 选择：**从 8/16 起，看任务复杂度和数据量**，数据多且任务难可到 64/128；要提「我做过 rank 的消融，16→32 提升 X，32→64 基本持平」。

### Q18：**你怎么评估你的模型效果？指标怎么定的？**

- 【考察点】评估体系是否严谨。
- 【框架】离线（自动指标 + 人工评测 + 大模型打分）→ 小流量 A/B → 全量。
- 【要点】
  - 自动指标：分类任务看 P/R/F1（**别只说 accuracy，要提类别不均衡**）；生成任务看 BLEU/ROUGE 但要指出其局限，更推荐 **GPT-4 打分 / 人工双盲**。
  - **必须提「离线指标涨了但线上没涨」的处理经验**，这是加分项。
  - 说清样本量、置信区间、A/B 实验的分流方式和显著性。

### Q19：**这个提升是你做的还是别人做的？怎么证明是你的贡献？**

- 【考察点】真实性 + 诚信。
- 【要点】诚实区分「我主导」「我参与」「我配合」。可以讲「这个模块是我独立负责的，因为我做了 X 实验并推动了 Y 方案落地」，同时坦承团队其他人的贡献。**撒谎在追问下必然崩。**

### Q20：**如果重做这个项目，你会怎么改进？**

- 【考察点】反思能力。
- 【要点】给 2~3 条具体的、有技术含量的改进（如「当时数据没做去重，后来发现有 15% 重复，如果重做我会先上 MinHash 去重」「当时没有做多组 seed 取平均，结论可能不稳」）。

### Q21：**项目里遇到最大的困难是什么，怎么解决的？**

- 【要点】要讲「技术困难」而不是「沟通困难」。结构：现象 → 排查过程（列出你排除的假设）→ 根因 → 解法 → 验证。**排查过程比结论更重要**。

------------------------------------------------------------------------

## 4.3 手撕代码类

> 字节的手撕偏爱「核心组件复现」。以下给出可直接背诵的实现，注意 shape 注释与数值稳定性处理。

### Q22：**手写多头自注意力（含 mask）。**

    import torch, torch.nn as nn, math

    class MultiHeadAttention(nn.Module):
        def __init__(self, d_model=512, n_head=8, dropout=0.1):
            super().__init__()
            assert d_model % n_head == 0
            self.d_model, self.n_head = d_model, n_head
            self.d_head = d_model // n_head
            self.w_q = nn.Linear(d_model, d_model, bias=False)
            self.w_k = nn.Linear(d_model, d_model, bias=False)
            self.w_v = nn.Linear(d_model, d_model, bias=False)
            self.w_o = nn.Linear(d_model, d_model, bias=False)
            self.dropout = nn.Dropout(dropout)

        def forward(self, x, mask=None):
            # x: (B, L, D); mask: (B, 1, 1, L) 或 (B, 1, L, L)，1=保留 0=屏蔽
            B, L, _ = x.shape
            # (B, L, D) -> (B, h, L, dh)
            q = self.w_q(x).view(B, L, self.n_head, self.d_head).transpose(1, 2)
            k = self.w_k(x).view(B, L, self.n_head, self.d_head).transpose(1, 2)
            v = self.w_v(x).view(B, L, self.n_head, self.d_head).transpose(1, 2)

            # scores: (B, h, L, L)
            scores = (q @ k.transpose(-2, -1)) / math.sqrt(self.d_head)

            # 因果 mask（下三角），防止看到未来 token
            causal = torch.tril(torch.ones(L, L, device=x.device, dtype=torch.bool))
            scores = scores.masked_fill(~causal.unsqueeze(0).unsqueeze(0), float('-inf'))
            if mask is not None:
                scores = scores.masked_fill(mask == 0, float('-inf'))

            attn = torch.softmax(scores, dim=-1)   # 内部已做 max 减法，数值稳定
            attn = self.dropout(attn)

            out = attn @ v                          # (B, h, L, dh)
            out = out.transpose(1, 2).contiguous().view(B, L, self.d_model)
            return self.w_o(out)

> 【面试官会追问】① 为什么用 `masked_fill` 而不是乘 0？（乘 0 在 softmax 前等价，但如果后面手动算 softmax 会出错；用 -inf 更安全）② 如果整行都被 mask 掉会怎样？（softmax 全 -inf → NaN，需要兜底）③ `contiguous()` 为什么必要？（transpose 后内存不连续，view 会报错）

### Q23：**手写 LayerNorm。**

    class LayerNorm(nn.Module):
        def __init__(self, d_model, eps=1e-5):
            super().__init__()
            self.gamma = nn.Parameter(torch.ones(d_model))
            self.beta = nn.Parameter(torch.zeros(d_model))
            self.eps = eps

        def forward(self, x):                 # x: (..., D)
            mean = x.mean(dim=-1, keepdim=True)
            var = x.var(dim=-1, unbiased=False, keepdim=True)   # 有偏方差，与PyTorch一致
            x_hat = (x - mean) / torch.sqrt(var + self.eps)
            return self.gamma * x_hat + self.beta

> 【要点】`unbiased=False` 必须显式写，否则默认无偏方差会和 PyTorch 官方实现有差异；eps 放在 sqrt 内而不是外（放外面会导致 var=0 时仍 NaN）。

### Q24：**手写 RMSNorm。**

    class RMSNorm(nn.Module):
        def __init__(self, d_model, eps=1e-6):
            super().__init__()
            self.weight = nn.Parameter(torch.ones(d_model))
            self.eps = eps

        def forward(self, x):
            # 注意：用 float32 算 rms 更稳，再转回原 dtype
            dtype = x.dtype
            x = x.float()
            rms = torch.rsqrt(x.pow(2).mean(dim=-1, keepdim=True) + self.eps)
            return (x * rms).to(dtype) * self.weight

### Q25：**手写带温度的 Softmax 与数值稳定版本。**

    def stable_softmax(x, dim=-1, temperature=1.0):
        x = x / temperature
        x = x - x.max(dim=dim, keepdim=True).values   # 减最大值，防 exp 溢出
        e = torch.exp(x)
        return e / e.sum(dim=dim, keepdim=True)

> 【追问】减最大值改变结果吗？（数学上不变，因为 softmax 对常数平移不变）为什么要减？（fp16 下 exp(100) 直接 inf）

### Q26：**手写 Top-k / Top-p（nucleus）采样。**

    def top_k_top_p_filtering(logits, top_k=0, top_p=1.0, temperature=1.0):
        logits = logits / max(temperature, 1e-8)
        if top_k > 0:
            k = min(top_k, logits.size(-1))
            # 保留前 k 大，其余置 -inf
            kth = torch.topk(logits, k, dim=-1).values[..., -1, None]
            logits = logits.masked_fill(logits < kth, float('-inf'))
        if top_p < 1.0:
            # 按概率降序累加，超过 top_p 的截断
            sorted_logits, sorted_idx = torch.sort(logits, descending=True, dim=-1)
            probs = torch.softmax(sorted_logits, dim=-1)
            cum = probs.cumsum(dim=-1)
            # 右移一位，保证至少保留一个 token
            remove = cum - probs > top_p
            sorted_logits = sorted_logits.masked_fill(remove, float('-inf'))
            logits = sorted_logits.scatter(-1, sorted_idx, sorted_logits)
        return logits   # 之后直接 torch.multinomial(softmax(logits))

> 【追问】top_k 和 top_p 同时用谁先？（通常先 top_k 再 top_p；也可只用一个）temperature 的作用？（\>1 更随机，\<1 更确定，=0 时退化为贪心，需特判避免除零）

### Q27：**手写 KV Cache 的自回归生成。**

    @torch.no_grad()
    def generate_with_kv_cache(model, input_ids, max_new_tokens=64, eos_id=None):
        # 首轮：prefill 整段 prompt
        out = model(input_ids, use_cache=True)      # out.logits: (B, L, V)
        past = out.past_key_values                  # List[(k, v)], k/v: (B, h, Lk, dh)
        next_tok = out.logits[:, -1:].argmax(-1)    # (B, 1)
        generated = [next_tok]

        for _ in range(max_new_tokens - 1):
            # decode：只喂 1 个 token，复用 past
            out = model(next_tok, past_key_values=past, use_cache=True)
            past = out.past_key_values
            next_tok = out.logits[:, -1:].argmax(-1)
            generated.append(next_tok)
            if eos_id is not None and (next_tok == eos_id).all():
                break
        return torch.cat([input_ids] + generated, dim=-1)

> 【要点】要能说出 KV Cache 的**代价**：显存随序列长度线性增长，长上下文时 KV Cache 可能比模型本身还大；这也是 MQA/GQA、PagedAttention、KV 量化、prefix caching 等技术的动机。
>
> 【追问】为什么 decode 阶段 GPU 利用率低？（每步只算 1 个 token，是 memory-bound，算力闲置）怎么解决？（continuous batching 把多个请求拼批）

### Q28：**手写 RoPE 并应用到 q、k。**

    def precompute_freqs_cis(dim, max_len, base=10000.0, device='cpu'):
        # 频率：theta_i = base^(-2i/dim), i = 0..dim/2-1
        freqs = 1.0 / (base ** (torch.arange(0, dim, 2, device=device).float() / dim))
        t = torch.arange(max_len, device=device).float()
        freqs = torch.outer(t, freqs)                       # (max_len, dim/2)
        return torch.polar(torch.ones_like(freqs), freqs)   # 复数形式 e^{i*theta}

    def apply_rope(x, freqs_cis):
        # x: (B, h, L, dh) -> 视为复数 (B, h, L, dh/2)
        xc = torch.view_as_complex(x.float().reshape(*x.shape[:-1], -1, 2))
        freqs = freqs_cis[:x.shape[-2]].view(1, 1, x.shape[-2], -1)
        out = torch.view_as_real(xc * freqs).flatten(-2)
        return out.type_as(x)

> 【要点】RoPE 的本质是**对 q、k 的每两个相邻维度做二维旋转，旋转角度与位置成正比**，使得 q_m·k_n 的结果只依赖 (m-n)。**不要对 v 做 RoPE**。 【追问】RoPE 怎么扩展上下文？（Position Interpolation：把位置索引压缩；NTK-aware：调整 base；YaRN：分区+温度缩放）

### Q29：**手写 SwiGLU 前馈层。**

    class SwiGLU(nn.Module):
        def __init__(self, d_model, d_ff=None, multiple_of=256):
            super().__init__()
            d_ff = d_ff or int(8 * d_model / 3)
            d_ff = multiple_of * ((d_ff + multiple_of - 1) // multiple_of)  # 对齐
            self.w1 = nn.Linear(d_model, d_ff, bias=False)   # gate
            self.w2 = nn.Linear(d_ff, d_model, bias=False)   # down
            self.w3 = nn.Linear(d_model, d_ff, bias=False)   # up

        def forward(self, x):
            return self.w2(torch.nn.functional.silu(self.w1(x)) * self.w3(x))

### Q30：**实现交叉熵损失（含 ignore_index 与 label smoothing）。**

    def cross_entropy(logits, targets, ignore_index=-100, label_smoothing=0.0):
        # logits: (B, V) or (B*L, V); targets: (B,) or (B*L,)
        logits = logits.float()
        log_probs = torch.log_softmax(logits, dim=-1)
        nll = -log_probs.gather(-1, targets.clamp(min=0).unsqueeze(-1)).squeeze(-1)
        if label_smoothing > 0:
            smooth = -log_probs.mean(dim=-1)
            nll = (1 - label_smoothing) * nll + label_smoothing * smooth
        mask = (targets != ignore_index).float()
        return (nll * mask).sum() / mask.sum().clamp(min=1)

### Q31：**实现梯度累积 + 梯度裁剪的训练循环。**

    for step, (x, y) in enumerate(loader):
        out = model(x)
        loss = criterion(out, y) / accum_steps
        loss.backward()
        if (step + 1) % accum_steps == 0:
            torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)
            optimizer.step(); scheduler.step(); optimizer.zero_grad(set_to_none=True)

> 【追问】为什么 loss 要除以 accum_steps？（保证等效 batch 的梯度量级一致）`set_to_none=True` 的好处？（省显存，避免写 0）

### Q32：**实现一个简单的 LoRA 层。**

    class LoRALinear(nn.Module):
        def __init__(self, base: nn.Linear, r=8, alpha=16, dropout=0.0):
            super().__init__()
            self.base = base
            for p in self.base.parameters():
                p.requires_grad = False
            self.r, self.scaling = r, alpha / r
            self.A = nn.Parameter(torch.zeros(r, base.in_features))   # A 初始化为 0
            self.B = nn.Parameter(torch.zeros(base.out_features, r))  # 或 A 用 kaiming B 用 0
            nn.init.kaiming_uniform_(self.A, a=5**0.5)
            self.drop = nn.Dropout(dropout)

        def forward(self, x):
            return self.base(x) + self.drop(x) @ self.A.T @ self.B.T * self.scaling

> 【要点】初始时 B=0 保证训练开始时 LoRA 分支输出为 0，模型行为与原始一致（**这是必答点**）；scaling = alpha/r 让你改 r 时不用重调 lr。

### Q33：**实现 Top-k 注意力（长文本稀疏化）。**

    def topk_attention(q, k, v, k_top):
        scores = q @ k.transpose(-2, -1) / math.sqrt(q.size(-1))   # (B,h,L,L)
        topv, topi = scores.topk(k_top, dim=-1)
        mask = torch.full_like(scores, float('-inf')).scatter_(-1, topi, topv)
        return torch.softmax(mask, dim=-1) @ v

### Q34：算法题（LeetCode 风格，字节常见）

- **手写快排 / 归并排序**（校招高频，注意边界与稳定性）
- **Top-K 问题**：堆实现 O(n log k) vs 快选 O(n)，问「一亿个数怎么找 Top-100」
- **最长回文子串 / 最长递增子序列 / 编辑距离**（DP 三件套）
- **LRU Cache**（用 OrderedDict 或双向链表 + dict）
- **字符串解码、二叉树层次遍历、岛屿数量**
- **概率题**：蓄水池抽样（reservoir sampling）、Rand7 生成 Rand10
- **手写 BN/LN 的 numpy 版本**（部分岗位会问 numpy）

------------------------------------------------------------------------

## 4.4 场景 / 系统设计类

### Q35：**设计一个企业级 RAG 系统。**

- 【考察点】全链路工程能力。
- 【框架】按数据流分层：**离线索引（Ingest）→ 在线检索（Retrieve）→ 生成（Generate）→ 评估（Eval）**。
- 【要点】
  - **Ingest**：文档解析（PDF 表格/图片怎么办）→ 分块（固定长度 vs 语义分块 vs 层级分块）→ 元数据抽取（标题、时间、权限）→ 向量化 → 入库。**分块策略直接影响召回，是重点**。
  - **Retrieve**：向量检索（HNSW/IVF 的取舍）+ BM25 关键词检索 → **混合检索** → Rerank（cross-encoder，如 bge-reranker）→ top-k 截断。要提「多路召回 + 融合（RRF）」。
  - **Generate**：prompt 组织（引用编号、要求「不知道就说不知道」）、上下文长度控制、流式输出。
  - **Eval**：检索侧看 Recall@k / MRR / NDCG，生成侧看 Faithfulness / Answer Relevance（RAGAS 框架），端到端用人工 or LLM-as-judge。
  - **工程难点**：权限隔离（不同用户能看的文档不同，**必须在检索层过滤，不能靠 prompt**）、增量更新、缓存、query 改写（多轮对话的指代消解）、长尾 bad case 分析。
- 【追问】「怎么解决召回不准？」→ 从 query 侧（改写、扩展）、索引侧（分块、embedding 模型换更强的）、排序侧（加 rerank）三个层面答。

### Q36：**给你一批原始网页数据，怎么构建训练数据？**

- 【要点】
  1.  **抽取**：正文提取（去广告/导航）。
  2.  **语言过滤**：fastText 语言识别。
  3.  **质量过滤**：启发式规则（长度、符号占比、重复率）+ 模型打分（分类器 / 困惑度）。
  4.  **去重**：**MinHash + LSH 做近重复**（要能解释 MinHash 的 Jaccard 估计原理），段落级 + 文档级。
  5.  **隐私与安全**：PII 脱敏、有害内容过滤（用分类器）。
  6.  **配比与课程学习**：不同来源按比例混合，必要时做退火（annealing）阶段加入高质量数据。
  7.  **验证**：小规模消融实验——用不同数据配比训小模型，看 benchmark 变化，再放大。

### Q37：**怎么评估一个大模型的效果？**

- 【要点】分三层：
  - **通用能力**：MMLU、GSM8K、HumanEval 等公开 benchmark（**要指出数据污染风险**）。
  - **领域能力**：自建评测集，从业务 bad case 里采样，人工标注标准答案。
  - **线上效果**：A/B 实验，看业务指标（点击率、解决率、人工兜底率、用户满意度）+ 成本指标（token 成本、延迟）。
  - **LLM-as-judge** 的注意事项：位置偏置、长度偏置、自我偏好，需要用 GPT-4/Claude 等强模型 + 双盲 + 人工抽检校准。

### Q38：**线上模型效果突然下降，怎么排查？**

- 【考察点】系统化 debug 能力，字节高频。
- 【框架】按「数据 → 模型 → 服务 → 用户」四层排查：
  1.  **确认现象**：是全局还是某个流量/地域/版本？什么时候开始？改了什么？
  2.  **数据侧**：输入分布是否漂移（query 长度、新热点事件）、上游特征是否缺失/延迟、是否有脏数据灌入。
  3.  **模型侧**：是否发了新版本（回滚验证）、prompt 是否被改、上下文长度是否超限被截断。
  4.  **服务侧**：是否降级到小模型、超时率、KV Cache/显存是否 OOM 触发重试、并发限流。
  5.  **用户侧**：是否被灰度的新用户群不同、是否有刷量。
  6.  **验证与止血**：先回滚止血，再定位根因，最后加监控与告警。
- 【要点】**一定要提「先止血后定位」和「加监控」**，这是工程成熟度的体现。

### Q39：**怎么做一个模型的压缩/加速方案？**

- 【要点】
  - **量化**：W8A8 / W4A16（GPTQ、AWQ）、KV Cache 量化、SmoothQuant 处理激活离群值。
  - **蒸馏**：用大模型产软标签训小模型。
  - **剪枝**：结构化剪枝（去头/去层）比非结构化更实用。
  - **架构**：换更小的模型 + 更好的数据（现在往往比压缩更划算）。
  - **系统**：continuous batching、PagedAttention、投机解码（speculative decoding）、FlashAttention。
  - 评估：**必须在真实硬件上测端到端吞吐/延迟，而不是只看理论 FLOPs**。

### Q40：**长文本方案怎么选型？**

- 【要点】按成本从低到高：
  1.  **Prompt 工程 + 检索**（RAG）——最经济，适合文档问答。
  2.  **位置插值 / YaRN 扩展 RoPE**——把 8K 扩到 32K/128K，需要少量继续预训练。
  3.  **稀疏/线性注意力**（如 sliding window + global token）——降低 O(L²)。
  4.  **改架构**（Mamba/SSM 混合）——效果好但要重训。

  - 关键权衡：**「长上下文」不等于「长上下文有效」**，要提「lost in the middle」现象——模型对中间位置信息利用差，所以长上下文场景仍建议配合检索。

### Q41：**设计一个多模态内容审核系统（抖音场景）。**

- 【要点】文本（评论/标题）+ 图像 + 视频抽帧 + 音频 ASR 多路 → 各自模型打分 → 融合决策 → 分级处置（通过/人工复审/拦截）→ 申诉回流。重点：**成本控制**（先用小模型粗筛，可疑的再走大模型）、**时效性**（视频要实时/近实时）、**误杀率与漏杀率的业务权衡**。

------------------------------------------------------------------------

## 4.5 开放思考题

### Q42：**大模型的能力边界在哪？**

- 【框架】分维度谈：知识（时效性、长尾）、推理（多步、数学、符号）、可靠性（幻觉）、一致性、成本。
- 【要点】给具体例子而非空谈：「比如模型能做 90% 的代码补全，但在需要跨文件、跨仓库的架构级重构上仍不可靠，因为上下文和状态管理超出当前范式」。**结论要落到「所以现在的产品形态应该是人机协作而不是全自动」**。

### Q43：**幻觉能根治吗？怎么缓解？**

- 【要点】
  - 根因：**模型是下一个 token 的概率建模，优化目标是「像」而不是「真」**；训练数据里存在错误；RLHF 会奖励自信的表达（sycophancy）。
  - 缓解：RAG（提供依据 + 要求引用）、训练侧（事实性 RLHF、DPO 偏好）、解码侧（降低温度、self-consistency、CoVe 链式验证）、产品侧（展示来源、让用户验证）。
  - 结论：**不能根治，只能把「不可知」转化为「可知的不可知」——让模型知道自己不知道**，这需要在训练中奖励「承认不确定」。

### Q44：**AGI 的路径你怎么看？**

- 【要点】给出有结构的观点而非站队。可谈：Scaling + 后训练是否能通向 AGI、是否需要新的架构范式（世界模型、具身）、数据枯竭问题（合成数据是否可行）、评测体系缺失（我们甚至无法定义 AGI）。**展示你读过进展、有自己的判断即可**。

### Q45：**怎么量化大模型项目的业务价值？**

- 【框架】成本侧（推理成本、标注成本、人力成本）+ 收益侧（效率提升、体验指标、收入）。
- 【要点】举具体例子：「客服场景，用 LLM 做首轮应答，人工转接率从 60% 降到 35%，按每通人工成本 X 元、日均 Y 万通算，年化节省 Z」。**能给公式和数量级的候选人非常稀缺**。

### Q46：**如果给你无限算力，你会训练什么？**

- 【考察点】技术品味与想象力。
- 【要点】不要答「训一个更大的模型」（没想法）。可以答：做可控可验证的推理（RLVR 大规模化）、做真正多模态统一的世界模型、做能自我改进的数据飞轮、做可解释性研究。**关键是说出「为什么这件事现在做不了，算力能解决哪一部分」**。

### Q47：**现在大模型赛道有什么被高估/低估的方向？**

- 【要点】给出判断依据。例如：被高估——「单纯的模型参数竞赛」（边际收益递减）；被低估——「数据质量与评测体系」「Agent 的工程可靠性」「推理成本优化」。**有观点 + 有依据 + 承认可能错**。

------------------------------------------------------------------------

# 五、面试准备建议与时间线

## 5.1 简历怎么写

**1. 量化一切**

| 差 | 好 |
|----|----|
| 负责大模型微调工作 | 基于 Qwen2-7B 用 LoRA 微调客服问答模型，在 5 万条业务数据上训练，意图识别准确率从 82.3%→91.7%，线上人工转接率下降 18% |
| 熟悉 RAG | 搭建企业知识库 RAG，覆盖 3 万份文档，Recall@5 从 61% 提升至 88%，平均响应 1.2s |
| 参与模型加速 | 用 vLLM + AWQ 量化部署 13B 模型，单卡 QPS 从 3 提升到 11，GPU 成本降低 65% |

**2. 体现技术深度**

- 不要只写「用了 X」，要写「对比了 X/Y/Z，因为 A 原因选了 X，并针对 B 问题做了 C 改进」。
- 每个项目至少准备 1 个「技术亮点」可以被深挖 5 层。

**3. 关键词匹配**

- 投 Seed 就突出预训练/数据/scaling；投 AML 就突出推理优化/分布式；投业务线就突出业务指标与落地。

**4. 其他**

- 一页为宜（社招可两页）；GitHub/博客链接如果内容扎实就放上；论文写清楚是几作、什么会议。

## 5.2 项目怎么讲（STAR + 技术决策）

准备**三个版本**：

- **30 秒版**：电梯陈述，用于自我介绍里的一句话带过。
- **3 分钟版**：主版本，S/T/A/R/T 结构，用于「介绍一个项目」。
- **10 分钟版**：用于面试官深挖时展开，包含数据细节、实验对比、失败尝试。

**讲述时的高分句式**：

- 「我们当时面临的核心矛盾是 X 和 Y 不可兼得，最终判断 Y 的业务影响更小，所以选了 X。」
- 「这个方案我们试过，但实验发现 A 指标涨了 B 指标掉了，所以我改成了……」
- 「这个数字是我实测的，实验设置是……，样本量是……」
- 「这块我没有深入做过，我的理解是……，如果有机会我会从……入手验证。」

## 5.3 刷题清单

**必背手撕（按优先级）**：

1.  多头注意力（含 causal mask）
2.  LayerNorm / RMSNorm / BatchNorm（torch + numpy）
3.  Softmax（数值稳定版）
4.  KV Cache 生成循环
5.  RoPE 实现
6.  Top-k / Top-p 采样
7.  SwiGLU / FFN
8.  交叉熵损失
9.  LoRA 层
10. 梯度累积 + clip 训练循环

**算法题（刷 150~200 道即可）**：Hot 100 + 剑指 Offer 重点题，重点覆盖数组/双指针/DP/二叉树/堆/字符串。**字节爱考 DP 和链表**。

**八股复习顺序**：Transformer 架构 → 位置编码 → 归一化 → 训练（优化器/并行/混合精度）→ 微调（LoRA/QLoRA/SFT/RLHF/DPO）→ 推理优化（KV Cache/量化/FlashAttention）→ RAG/Agent。

## 5.4 反问环节该问什么

**推荐问（体现技术思考）**：

- 「团队目前在做的模型，当前最大的技术瓶颈是什么？」
- 「这个岗位的工作内容是偏研究探索还是偏业务落地？两者比例大概多少？」
- 「团队现在训练/推理用的是什么规模的资源？后续有扩展计划吗？」
- 「如果我入职，前三个月最希望我产出什么？」
- 「团队怎么评估一个算法工程师的绩效？看业务指标还是技术深度？」

**避免问**：薪资细节（留到 HR 面）、网上能查到的公开信息、「团队氛围怎么样」（太虚）。

**可以问一句**：「您觉得我这次面试在哪些方面还有明显短板？」——部分面试官会给真实反馈，对后续轮次有帮助。

## 5.5 准备时间线（以 4 周为例）

| 时间 | 任务 |
|----|----|
| 第 1 周 | 通读 docx 手册建立体系；整理简历，写好 30s/3min/10min 三个项目版本；开始刷 Hot 100 |
| 第 2 周 | 手撕代码专项：把 5.3 的 10 题手写 3 遍到不看答案；整理项目可能被追问的 30 个问题并写答案 |
| 第 3 周 | 场景题/系统设计专项（RAG、评估、排查、压缩、长文本）；开始模拟面试（找人或录音自测） |
| 第 4 周 | 查漏补缺；过开放题；准备反问清单；面试前一天只看「反问 + 避坑」 |

------------------------------------------------------------------------

# 六、常见失败原因与避坑

## 6.1 高频失败原因

1.  **项目讲不清技术决策**——只会说「我用了 X」，说不出「为什么不用 Y」。**这是字节刷人的第一原因。**
2.  **答不出数据规模**——「数据大概几万条吧」「记不清了」。面试官会直接判定项目是包装的。
3.  **手撕写不出完整可运行代码**——伪代码糊弄、忘了 mask、shape 对不上、没考虑数值稳定性。
4.  **八股只会背结论不懂原理**——问「为什么除以 √d」答不出方差分析；问「为什么 Pre-LN」答不出残差方差累积。
5.  **被追问就崩**——压力面下立刻自我否定，或者硬编答案。
6.  **过度夸大**——把团队成果说成个人成果，追问两层就露馅。
7.  **不问问题或问得太水**——最后 5 分钟放弃展示机会。
8.  **对岗位理解错位**——投 AML 却只聊论文；投 Seed 却只聊业务调参。**面试前一定研究 JD 和团队方向。**
9.  **场景题没有结构**——东一句西一句，没有分层。**永远先给框架再填细节。**
10. **数学/推导能力不过关**——被要求现场推导 attention 复杂度、KL 散度、PPO 目标函数时卡壳。

## 6.2 避坑清单（面试前必看）

- [ ] 简历上写的每个数字，我都能说出实验设置和来源
- [ ] 每个项目我能扛住 5 层「为什么」
- [ ] 手撕 10 题我能不看答案写完并说清边界
- [ ] 我准备了 3 个不同难度的项目版本
- [ ] 我知道这个岗位是研究向还是工程向还是业务向
- [ ] 我准备了 3 个反问问题
- [ ] 我能对任一场景题先给出分层框架
- [ ] 我准备好了「不知道」时的标准话术
- [ ] 我准备了「为什么离职/为什么字节」的答案（社招）
- [ ] 我知道自己项目的技术亮点是什么，以及它为什么难

## 6.3 心态提示

- **字节面试节奏快、追问密，是正常的**，不代表你表现差。面试官在压力下看你如何思考，而不是看你是否全知。
- **不会的题一定要说「我不会，但我会这样想」**——展示思考过程比正确答案更重要。
- **面试是双向选择**：如果一个团队全程只问八股、不聊技术判断，可能这个岗位本身成长性有限。

------------------------------------------------------------------------

# 附：一页速查（面试当天看）

**自我介绍模板**：姓名/学校+方向 → 一句话概括核心技术栈 → 最有代表性的 1~2 个项目（含数字）→ 为什么投这个岗位。

**项目超简模板**：问题（+规模）→ 我的方案（+为什么）→ 结果（+数字）→ 反思。

**场景题万能开头**：「我先从整体框架梳理一下，然后逐层展开……」

**不会时的标准话术**：「这块我确实没有深入做过。我的理解是……（给出基于已有知识的推测），如果有机会我会从……入手验证。谢谢您指出这个盲区。」

**手撕前先做的三件事**：确认输入输出与 shape → 说明边界情况 → 说出时间/空间复杂度。

------------------------------------------------------------------------

**本文件整理日期：2026-09-12**
