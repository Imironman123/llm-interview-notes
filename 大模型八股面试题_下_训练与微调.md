------------------------------------------------------------------------

**来源与参考**

本题库整理自以下公开面经仓库与论文资料，并结合本人面试复盘重写与补充推导：

| 来源 | 说明 |
|----|----|
| [GitHub: wdndev/llm_interview_note](https://github.com/wdndev/llm_interview_note) | 大模型面试笔记，覆盖预训练、微调、RLHF、推理优化 |
| [GitHub: WeThinkIn/AIGC-Interview-Book](https://github.com/WeThinkIn/AIGC-Interview-Book) | AIGC 算法岗面试宝典，含训练/量化/部署 |
| [GitHub: Junvate/LLM-Algorithm-Intern-Guide](https://github.com/Junvate/LLM-Algorithm-Intern-Guide) | 大模型算法实习面经与八股汇总 |
| [GitHub: jingtian11/EasyOffer](https://github.com/jingtian11/EasyOffer) | EasyOffer 面试题库，算法与工程并重 |
| 论文 | Attention Is All You Need / GPT-3 / Chinchilla / ZeRO / Megatron-LM / LoRA / QLoRA / InstructGPT / DPO / DeepSeek-R1 / GQA 等 |

> 说明：本文件为学习整理，公式与结论以原论文为准；面经仓库中的表述已按当前（2026 年）主流实践校正。

------------------------------------------------------------------------

## 目录

- [一、预训练](#一预训练)
- [二、分布式训练](#二分布式训练)
- [三、参数高效微调 PEFT](#三参数高效微调-peft)
- [四、对齐与强化学习](#四对齐与强化学习)
- [五、推理模型](#五推理模型)
- [六、蒸馏与压缩](#六蒸馏与压缩)
- [七、训练常见问题排错](#七训练常见问题排错)
- [八、速查表与公式汇总](#八速查表与公式汇总)

------------------------------------------------------------------------

# 一、预训练

## Q1: 预训练的目标函数有哪些？CLM 和 MLM 的区别与取舍？

**答：**

（1）**自回归语言建模（Causal LM / CLM）**，GPT 系列、LLaMA 系列使用。对序列 $`x = (x_{1},\ldots,x_{T})`$，最大化：

``` math
\mathcal{L}_{\text{CLM}} = - \sum_{t = 1}^{T}\log p_{\theta}(x_{t} \mid x_{< t})
```

训练时用 causal mask 保证位置 $`t`$ 只能看到 $`< t`$ 的 token，一次前向即可对 $`T`$ 个位置同时算 loss（teacher forcing）。优点是天然适配生成任务、推理时自回归采样一致；缺点是每个 token 只用到左侧上下文，双向信息利用不充分。

（2）**掩码语言建模（MLM，BERT）**，随机 mask 15% 的 token 预测原文：

``` math
\mathcal{L}_{\text{MLM}} = - \sum_{t \in \mathcal{M}}^{}\log p_{\theta}(x_{t} \mid x_{\smallsetminus \mathcal{M}})
```

可看双向上下文，但训练/推理不一致（预训练有 `[MASK]`，下游没有），且不适合直接做生成。

（3）**Prefix LM / UL2 / FIM**：GLM 用自回归填空（span mask + 双向注意力）；T5 用 span corruption；Code LLM 常用 **FIM（Fill-In-the-Middle）** 做代码补全。**UL2（Unified Language Learner, 2022）** 把多种去噪目标统一为 Mixture-of-Denoisers，是 T5 之后的代表工作。

（4）**多 token 预测（MTP, Multi-Token Prediction）**：DeepSeek-V3 在主干之外加 $`D`$ 个 MTP 模块，每个预测未来第 $`k`$ 个 token，增加训练信号密度，也用于推理投机解码。MTP 的目标（DeepSeek-V3 技术报告）：

``` math
\mathcal{L}_{\text{MTP}} = - \sum_{k = 1}^{D}\lambda_{k}\log p_{\theta}\left( x_{t + k} \mid x_{1:t},{\widehat{x}}_{t + 1},\ldots,{\widehat{x}}_{t + k - 1} \right)
```

**追问：** - 为什么现在几乎不用 MLM 做 LLM 主干？答：生成任务需要自回归一致性，且 decoder-only 架构在 scaling law 下更简洁、KV Cache 友好。 - Perplexity（PPL）与 loss 的关系？\$ =() \$，仅在同 tokenizer/同数据集下可比，跨 tokenizer 比较 PPL 无意义。 - 为什么 tokenizer 词表大小会影响 loss？词表越大，单 token 携带信息越多，per-token loss 越低，但 per-byte loss（bits per byte）才是公平的跨 tokenizer 指标。

------------------------------------------------------------------------

## Q2: 数据配比与数据清洗去重怎么做？MinHash / SimHash 原理？

**答：**

**数据配比（data mixture）**：典型中文/英文/代码/数学的混合比例，需要 ablation 实验确定。工程做法：给不同来源设置采样权重（temperature sampling）。常见权重公式（UniMax / DoReMi 思路）：

``` math
p_{i} \propto n_{i}^{\alpha},\quad\alpha \in (0,1\rbrack
```

$`\alpha = 1`$ 即按原始规模比例，$`\alpha < 1`$ 提升小数据源的采样概率（防止被大源淹没）。LLaMA 系列经验：代码与数学数据占比提升能显著改善推理能力，但过多会伤自然语言能力。

**清洗流程**（以 RefinedWeb / FineWeb 为例）： 1. URL 与域名黑名单过滤； 2. 语言识别（fastText / CLD3），抽取目标语种； 3. 规则过滤：长度、符号占比、重复行占比、平均词长、是否含大量乱码； 4. 模型过滤：分类器打分（质量分类器如 fastText 质量分类、GPT-4 打分蒸馏成小模型）； 5. **去重**：文档级 + 段落级 + 子串级； 6. 隐私清洗：PII 脱敏、benchmark 污染去重（防止评测集泄漏）。

**MinHash（最小哈希）——近似 Jaccard 去重：**

对集合 $`A,B`$，Jaccard 相似度 $`J(A,B) = \frac{|A \cap B|}{|A \cup B|}`$。MinHash 思路：用 $`k`$ 个随机排列 $`\pi_{1},\ldots,\pi_{k}`$ 作用在元素上，取每个排列下的最小哈希值构成签名：

``` math
\text{sig}_{i}(A) = \min_{x \in A}\pi_{i}(x)
```

性质：$`P\lbrack\text{sig}_{i}(A) = \text{sig}_{i}(B)\rbrack = J(A,B)`$。因此用 $`k`$ 维签名中相等分量比例估计 $`J`$，误差 $`\approx 1/\sqrt{k}`$。

工程上不做真随机排列，而用 **通用哈希** $`\pi_{i}(x) = (a_{i}x + b_{i})\ mod\ p`$（p 为大质数），或用 64 位哈希 + 位扰动模拟。文档先切成 $`n`$-gram（如 5-gram word 或 9-gram char）作为 shingles 集合。

**LSH 分桶加速**：把 $`k`$ 维签名分成 $`b`$ 个 band，每 band $`r`$ 行（$`k = br`$）。两文档在任一 band 完全相同的概率：

``` math
P = 1 - (1 - J^{r})^{b}
```

该 S 形曲线在阈值 $`J^{*} \approx (1/b)^{1/r}`$ 附近陡峭，实现”高于阈值才判重”。

**SimHash——近似汉明距离去重：**

对文档每个 token 算 $`f`$ 位哈希，按 token 权重 $`w`$ 加权累加到位向量：哈希位为 1 加 $`w`$，为 0 减 $`w`$；取符号得 $`f`$ 位指纹。两文档相似度用汉明距离衡量，通常 $`f = 64`$，距离 $`\leq 3`$ 判为近似重复。SimHash 优点是指纹短、可位运算、适合超大规模；缺点是局部敏感性质不如 MinHash 的 Jaccard 解释清晰。

**Exact dedup**：对 Document 算 SHA-256 或按规范化文本算精确哈希，直接丢弃重复文档。

**追问：** - 为什么去重能提升效果？重复数据导致模型对重复内容过拟合、增加记忆化（memorization）、浪费算力，且会让评测集泄漏。 - MinHash 与 SimHash 选谁？需要严格 Jaccard 语义与可调阈值 → MinHash+LSH；追求极短指纹、超大规模流式 → SimHash。 - 语义去重怎么做？用 embedding + 向量检索（如 SemDedup），能抓到改写型重复，但成本高。 - 数据集规模与质量的权衡？Chinchilla 结论下重复数据等价于降低有效数据量，应优先扩数量+去重而非堆训练步数。

------------------------------------------------------------------------

## Q3: 什么是课程学习（Curriculum Learning）？在大模型里如何体现？

**答：**

课程学习指按”由易到难”的顺序组织训练数据，让模型先学简单模式再学复杂模式（Bengio et al., 2009）。LLM 中的几种体现：

1.  **预训练阶段**：早期用短序列（如 2K），后期逐渐拉到 8K/32K（LLaMA 系列在训练后期做长上下文阶段）。“短→长”是典型课程。
2.  **数据难度排序**：按可读性分（如教育水平分类器打分）从易到难喂入；也有反课程（anti-curriculum）研究显示”难→易”有时更好。
3.  **序列长度与 batch 调度**：训练后期线性/余弦退火学习率同时提升 batch size（如 LLaMA-2 后期提高 batch）属于广义课程。
4.  **指令微调阶段**：先训通用指令（简单、格式正确），再训复杂推理/长 CoT（如 LIMA / 自我指导式数据由简到繁）。
5.  **RL 阶段**：从易题（能拿到正奖励的 prompt）开始，逐步引入更难 prompt（DeepSeek-R1 的 RL 中过滤掉全对/全错的样本，保留”有梯度”的中等难度样本，本质是难度课程）。

**关键点：** 课程学习不是 LLM 的必需品，其收益在中小规模更明显；大规模预训练中，“数据质量过滤 + 配比” 带来的收益通常大于顺序调度。DeepSeek-R1 的做法值得强调：**在 RL 中过滤掉 pass rate 为 0 或 1 的样本**，因为这两类样本在 GRPO 下的优势为 0，不产生梯度。

**追问：** - 为什么”太简单/太难”的样本对 RL 没梯度？见 GRPO 优势公式：若组内奖励全相同，$`{\widehat{A}}_{i} = 0`$。 - 反课程为什么可能有效？难样本先学可以更快暴露模型缺陷，避免早期陷入局部模式。

------------------------------------------------------------------------

## Q4: 损失尖刺（Loss Spike）的原因与处理？

**答：**

**现象**：训练中 loss 突然从平稳值（如 2.1）跳到 3.5+，有时能自恢复，有时直接发散。

**原因分类：**

1.  **数据层面的脏样本**：异常长文本、乱码、重复的 `\n`、极端分布样本导致某个 batch 梯度范数暴增。
2.  **优化器状态问题**：Adam 二阶动量 $`v_{t}`$ 在少数 step 内积累异常大或极小的值；$`\epsilon`$ 太小（如 $`10^{- 8}`$）在 bf16 下失去正则作用。
3.  **数值精度**：fp16 的梯度上溢（\>65504 变 inf），attention softmax 溢出，LayerNorm 方差接近 0 导致放大。
4.  **学习率过高**或 warmup 不足；batch 太小时梯度噪声大。
5.  **硬件/通信问题**：某张卡挂掉导致 allreduce 结果异常、NCCL 超时、静默数据错误（silent data corruption）。
6.  **MoE 路由崩塌**导致负载不均，个别 expert 收到绝大部分 token，梯度爆炸。

**处理手段（按优先级）：**

| 手段 | 说明 |
|----|----|
| 梯度裁剪 | `clip_grad_norm_` 设 1.0（LLaMA-2 用 1.0，GPT-3 用 1.0） |
| 降低/回滚学习率 | 从 spike 前的 checkpoint 恢复，跳过坏数据，lr 降 10 倍重启 |
| 跳过坏 batch | 检测到 grad norm 超阈值直接 skip 该 step |
| 用 bf16 替代 fp16 | bf16 动态范围与 fp32 相同（8 位指数），几乎不会上溢 |
| 增加 $`\epsilon`$ 或换 Adafactor | Novograd/Adafactor 对二阶动量更稳 |
| embedding/LayerNorm 用 fp32 | 混合精度下这些层保持 fp32 是高性价比做法 |
| 调整数据顺序 | 重新 shuffle，检查数据 pipeline 是否有长序列扎堆 |
| QK-Norm / 更稳初始化 | 对 Q、K 做 LayerNorm 可显著抑制 attention logits 爆炸 |
| 回滚 + 数据跳过 | 记录 spike 对应的 data index，重训时跳过 |

**术语补充**：\*\*Loss spike 与”回滚（rollback）“**是超大规模训练的标准运维流程。Meta 训练 OPT-175B 时记录了大量 loss spike 案例（OPT 论文 Appendix）；BLOOM-176B 也记录了 fp16 溢出导致的 spike，最终改用 bf16。**PaLM\*\* 报告用了”skip batch + 从早些 checkpoint 重启” 的策略。

**追问：** - 为什么 bf16 训练还会 spike？可能是数据问题、优化器状态、通信精度（如 bf16 allreduce）导致，bf16 只解决溢出类问题。 - 如何自动检测？维护 loss 的移动均值与方差，超过 $`k\sigma`$（如 3σ）触发告警并暂停。 - loss spike 后为什么有时能自己恢复？因为坏 batch 只影响一步，Adam 的自适应性会很快校正；但如果 $`v_{t}`$ 被污染，恢复就慢了。

------------------------------------------------------------------------

## Q5: 混合精度训练：FP16 / BF16 / FP8 的差异与选择？

**答：**

**格式对比：**

| 格式 | 符号位 | 指数位 | 尾数位 | 动态范围 | 精度 |
|----|----|----|----|----|----|
| FP32 | 1 | 8 | 23 | $`\sim 10^{\pm 38}`$ | 高 |
| TF32 | 1 | 8 | 10 | 同 fp32 | 中（仅 A100+ TensorCore 内部） |
| FP16 | 1 | 5 | 10 | $`\sim 6 \times 10^{\pm 4}`$（最大 65504） | 高精度、窄范围 |
| BF16 | 1 | 8 | 7 | 同 fp32 | 低精度、宽范围 |
| FP8 (E4M3) | 1 | 4 | 3 | 窄 | 低，需 scaling |
| FP8 (E5M2) | 1 | 5 | 2 | 宽 | 更低，用于梯度 |

**结论：** bf16 因为动态范围与 fp32 一致，不需要 loss scaling，是当前 LLM 预训练默认选择（LLaMA、DeepSeek、Qwen 均为 bf16）。fp16 精度更高但需 loss scaling，容易 spike，主要用于推理或老硬件。

**FP16 + Loss Scaling（AMP）原理：** fp16 下小梯度会下溢为 0。做法是前向/反向用 fp16，loss 乘以缩放因子 $`S`$（如 $`2^{16}`$），反向后把梯度除回 $`S`$ 再更新：

``` math
g = \frac{1}{S}\nabla_{\theta}(S \cdot \mathcal{L}) = \nabla_{\theta}\mathcal{L}
```

若出现 inf/nan 则跳过该 step 并把 $`S`$ 减半（dynamic loss scaling）。

**FP8 训练（2024-2026 主流）：** - 前向/反向矩阵乘用 FP8（E4M3 存权重/激活，E5M2 存梯度），累加用 FP32。 - 关键是 **per-tensor / per-channel / per-block 缩放**：DeepSeek-V3 用 **细粒度 block-wise FP8**（1×128 tile 做激活缩放，128×128 tile 做权重缩放），并在部分算子保持 bf16/fp32（如 LayerNorm、softmax、embedding）。 - NVIDIA Transformer Engine 提供自动缩放；DeepSeek-V3 报告 FP8 训练相对 bf16 提速约 1.5-2x 且无明显精度损失。

**混合精度下必须保持 FP32 的部分（高价值经验）：** 1. LayerNorm / RMSNorm 的统计量与参数； 2. Softmax（attention 分数可用 fp32 累加）； 3. Loss 计算（交叉熵的 logsumexp）； 4. 优化器状态（Adam 的 $`m,v`$）； 5. 残差累加（用 fp32 master weight 或周期性 upcast）。

**追问：** - 为什么 bf16 只有 7 位尾数还能训好？深度网络对权重精度不敏感，对动态范围敏感；且大量参数（如 LayerNorm）保持 fp32。 - Master weight 是什么？优化器维护 fp32 权重副本，更新后在 fp32 中做，再 cast 到 bf16 给前向，避免累积舍入误差。 - FP8 训练的精度损失主要体现在哪些任务？长链推理、需要高精度数值的任务（如小规模数学运算）更敏感；一般语言建模影响很小。

------------------------------------------------------------------------

## Q6: 梯度累积（Gradient Accumulation）的作用与等价性？

**答：**

当单卡显存放不下大 batch 时，把一个大 batch 拆成 $`N`$ 个 micro-batch 顺序前向/反向，累加梯度后再更新一次。设全局 batch 为 $`B`$，每卡 micro-batch 为 $`b`$，GPU 数 $`G`$，则

``` math
N_{\text{accum}} = \frac{B}{b \cdot G}
```

**等价性条件**：累积下 loss 需按 token 数归一化。若每个 micro-batch 的 loss 是自身均值，直接累加会等价于对 micro-batch 等权平均，而不是对 token 等权平均：

``` math
\mathcal{L}_{\text{correct}} = \frac{\sum_{i = 1}^{N}{\sum_{t \in \text{mb}_{i}}^{}\ell_{t}}}{\sum_{i}^{}|\text{mb}_{i}|}
```

若各 micro-batch 长度不等（变长序列 packing 时常见），必须按 token 数加权，否则梯度有偏。**正确做法**：每个 micro-batch 的 loss 乘以该 micro-batch 的 token 数，累加后除以总 token 数。

**副作用：** 1. 与 LayerNorm、Dropout 有轻微统计差异（Dropout mask 在不同 micro-batch 不同，等价于更大 batch 的不同 mask，一般无碍）； 2. BatchNorm 类层在小 micro-batch 下统计不准（LLM 一般用 LayerNorm/RMSNorm，规避了这个问题）； 3. 训练速度变慢（串行 forward/backward，GPU 利用率下降），通常配合 `torch.cuda.amp` 和计算图复用。

**追问：** - 梯度累积能替代大 batch 吗？优化意义上近似等价，但通信与并行行为不同；ZeRO 下梯度累积的显存收益有限。 - 梯度累积与流水线并行（PP）的关系？PP 的 micro-batch 是把 batch 切成多段并流水起来，能重叠通信与计算，不只是显存节省，还能提升吞吐。 - 累加窗口内的梯度裁剪：应在累加完成、更新前统一做 clip，否则每个 micro-batch 单独 clip 会改变梯度方向。

------------------------------------------------------------------------

## Q7: ZeRO 1/2/3 的原理与显存计算公式？

**答：**

**背景：** 数据并行（DP）下每张卡都保存完整模型副本 + 完整优化器状态，显存冗余极大。**ZeRO（Zero Redundancy Optimizer, Microsoft, 2020）** 通过切分这些冗余来降低单卡显存。

**混合精度下（Adam）每参数显存开销：**

设参数量 $`\Psi`$，混合精度（bf16/fp16 训练，fp32 优化器状态）：

| 组成                | 每参数字节                            | 说明          |
|---------------------|---------------------------------------|---------------|
| fp16/bf16 权重      | 2                                     | 前向/反向用   |
| fp16/bf16 梯度      | 2                                     | 反向产生      |
| fp32 权重副本       | 4                                     | 优化器更新用  |
| Adam 一阶动量 $`m`$ | 4                                     | fp32          |
| Adam 二阶动量 $`v`$ | 4                                     | fp32          |
| **合计**            | $`2 + 2 + 4 + 4 + 4 = 16`$ **B/参数** | 即 $`16\Psi`$ |

**ZeRO 三个阶段：**

- **ZeRO-1（优化器状态切分）**：$`m,v`$ 和 fp32 master weight 按 $`N_{d}`$ 卡切分。 单卡显存 $`\approx 2\Psi + 2\Psi + \frac{12\Psi}{N_{d}}`$（fp16 权重 + fp16 梯度为全量，$`4 + 4 + 4 = 12`$ 被切分）。
- **ZeRO-2（= ZeRO-1 + 梯度切分）**：梯度在 allreduce 后用 reduce-scatter 直接切分，每卡只保留自己那份梯度（更新完即释放）。 单卡 $`\approx 2\Psi + \frac{2\Psi + 12\Psi}{N_{d}}`$
- **ZeRO-3（= ZeRO-2 + 参数切分）**：前向/反向时按需 all-gather 参数，用完即释放（因此通信量增加约 1.5x）。 单卡 $`\approx \frac{16\Psi}{N_{d}}`$（外加临时 buffer 与激活）

**举例**：7B 模型，$`\Psi = 7 \times 10^{9}`$：

- 裸 DP：$`16 \times 7\text{B} = 112`$ GB 参数相关显存（不含激活），单卡 80G 放不下。
- ZeRO-2，8 卡：$`2\Psi + (14\Psi)/8 = 14 + 12.25 = 26.25`$ GB。
- ZeRO-3，8 卡：$`16\Psi/8 = 14`$ GB（+ 通信开销）。

**通信量分析：** - 标准 DP：每 step 一次 all-reduce 梯度 = $`2\Psi`$ 通信量（all-reduce 拆为 reduce-scatter + all-gather）。 - ZeRO-1/2：reduce-scatter（$`\Psi`$）+ all-gather（$`\Psi`$）= $`2\Psi`$，与 DP 相当。 - ZeRO-3：前向 all-gather $`\Psi`$ + 反向 all-gather $`\Psi`$ + 反向 reduce-scatter $`\Psi`$ = $`3\Psi`$，即 **1.5x** 通信量。

**Offload**：ZeRO-Offload 把优化器状态/梯度卸到 CPU 内存（用 CPU Adam），ZeRO-Infinity 进一步卸到 NVMe SSD（用 pageable/pinned memory + prefetch 隐藏延迟）。

**追问：** - ZeRO-3 与 TP（张量并行）的区别？TP 是切分单层内的矩阵乘（通信频繁、需高带宽 NVLink）；ZeRO-3 切分的是不同层参数的存储与按需通信，通信更粗粒度、可跨节点。 - ZeRO-3 的 all-gather 如何与计算重叠？用 prefetch：在计算第 $`i`$ 层时预取第 $`i + 1`$ 层参数。 - DeepSpeed 的 `stage` 与 `offload` 配置如何选？一般：显存够 → ZeRO-1/2；不够且节点内带宽高 → ZeRO-3；极显存受限 → ZeRO-2 + optimizer offload。 - FSDP 与 ZeRO-3 关系？PyTorch FSDP 是 ZeRO-3 的原生实现，按 FlatParameter 分组做 all-gather，配置 `sharding_strategy` 可选 FULL_SHARD / SHARD_GRAD_OP / HYBRID_SHARD。

------------------------------------------------------------------------

## Q8: 梯度检查点（Gradient Checkpointing / Activation Recomputation）原理与开销？

**答：**

**问题**：反向传播需要前向的中间激活，激活显存随层数 $`L`$、序列长度 $`s`$、batch $`b`$ 线性增长，是显存瓶颈（通常远超参数显存）。

**标准做法**：把网络分成 $`\sqrt{L}`$ 段，只保存每段的输入（checkpoint），反向时从最近的 checkpoint 重新前向计算该段内的激活。

**显存与计算权衡：**

- 不 checkpoint：激活显存 $`O(L)`$。
- 全部 checkpoint（每层存一个入口，段内重算）：激活显存降至 $`O(\sqrt{L})`$（若按 $`\sqrt{L}`$ 分段），计算量增加约 33%（一次额外前向，占 forward+backward 总开销的 1/3）。
- **选择性重算（selective recomputation）**：只重算显存占用最大的算子（如 attention 的 softmax/dropout），性价比最高。Megatron-LM 提供 `--recompute-activations` 与 `--recompute-granularity selective`。
- **FlashAttention** 通过 tiling 不物化 $`s \times s`$ 注意力矩阵，本身就把 attention 激活从 $`O(s^{2})`$ 降到 $`O(s)`$（保存 softmax 的 logsumexp 统计量），与 checkpointing 互补。

**典型实现**：`torch.utils.checkpoint.checkpoint(fn, x)`，注意需要 `use_reentrant=False`（PyTorch 2.x 推荐）以支持 RNG 状态保存与嵌套。

**追问：** - 为什么是 $`\sqrt{L}`$？设把 $`L`$ 层分成 $`k`$ 段，需要保存的 checkpoint 为 $`k`$ 个，每次重算段内 $`L/k`$ 层的激活峰值，总显存 $`\propto k + L/k`$，最小化得 $`k = \sqrt{L}`$。 - checkpoint 对 dropout 的影响？重算时 dropout mask 必须一致，需保存 RNG 状态（PyTorch 的 checkpoint 已处理）。 - 与 FlashAttention 的关系？FlashAttention 减少的是 attention 内部的 $`s^{2}`$ 激活，checkpoint 减少的是层间激活；两者叠加效果最好。

------------------------------------------------------------------------

# 二、分布式训练

## Q9: DP / DDP / ZeRO / TP / PP / SP / EP 各自切分什么？

**答：**

| 并行方式 | 切分对象 | 通信模式 | 典型场景 |
|----|----|----|----|
| DP（Data Parallel） | 数据（每卡全量模型） | 梯度 all-reduce | 早期小模型 |
| DDP | 数据（每卡全量模型，bucket 化 all-reduce 重叠） | 梯度 all-reduce | PyTorch 默认 |
| ZeRO / FSDP | 优化器状态/梯度/参数 | reduce-scatter + all-gather | 大模型常规 |
| TP（Tensor Parallel） | 层内权重矩阵（按列/行切） | 每层 all-reduce / all-gather | 单节点 NVLink |
| PP（Pipeline Parallel） | 按层切分到不同 stage | 阶段间点对点 send/recv | 跨节点 |
| SP（Sequence Parallel） | 序列维度（LayerNorm/Dropout 处） | all-gather / reduce-scatter | 配合 TP 降激活 |
| EP（Expert Parallel） | MoE 的专家分到不同卡 | all-to-all | MoE 模型 |
| CP（Context Parallel） | 长序列上下文（attention 分块 + ring） | ring all-gather / 点对点 | 长上下文训练 |

**DDP 细节**：DDP 用 **gradient bucketing** 把参数梯度分桶（默认 25MB），反向过程中梯度就绪的桶立刻发起 all-reduce，实现通信与反向计算重叠。`bucket_cap_mb` 和 `find_unused_parameters` 是常见调参点。

**TP（张量并行）细节**：以 Transformer 的 MLP 为例，第一层权重按列切 $`W_{1} = \lbrack W_{1}^{(1)},W_{1}^{(2)}\rbrack`$，每卡算 $`Y_{i} = XW_{1}^{(i)}`$，激活需 all-gather（或前一层的 all-reduce 隐含）；第二层按行切 $`W_{2} = \begin{bmatrix}
W_{2}^{(1)} \\
W_{2}^{(2)}
\end{bmatrix}`$，输出相加需 all-reduce。Attention 中按 head 维度切 Q/K/V 天然并行，输出投影需 all-reduce。

- 每层 2 次 all-reduce，序列长度 $`s`$、hidden $`h`$、batch $`b`$ 时单次通信量 $`\propto b \cdot s \cdot h \cdot 2`$（字节取决于精度）。
- 因为通信频繁且延迟敏感，**TP 一般限制在单机内（NVLink/NVSwitch）**，TP 度不超过 8（或 16）。

**PP（流水线并行）细节**：把 $`L`$ 层切成 $`P`$ 个 stage，micro-batch 流经 stage。为减少气泡，用交错式 1F1B（one-forward-one-backward）调度或 interleaved 1F1B（virtual pipeline，Megatron 的 `--num-layers-per-virtual-pipeline-stage`）。

## Q10: 3D 并行的切分策略如何选择？并行度怎么定？

**答：**

**总并行度关系**：

``` math
N_{\text{gpu}} = N_{\text{dp}} \times N_{\text{tp}} \times N_{\text{pp}}
```

（ZeRO 与 DP 等价占据 DP 维度；EP 通常复用 DP/TP 的卡，按专家切分。）

**选择原则（Megatron-LM 实践）：**

1.  **TP 优先放节点内**：TP 通信量大且频繁，要求高带宽（NVLink 600-900 GB/s，PCIe 仅 ~64 GB/s）。TP ≤ 8（单机 8 卡）。
2.  **PP 放跨节点**：PP 通信是点对点传激活，量小（$`b \cdot s \cdot h`$），只需 infiniBand 即可。
3.  **PP 度不宜过大**：气泡占比 $`\approx \frac{P - 1}{m + P - 1}`$（$`m`$ 为 micro-batch 数），$`P`$ 太大或 $`m`$ 太小都会浪费。经验：$`m \geq 4P`$ 效果较好。
4.  **ZeRO-1 + TP + PP** 是超大规模训练的经典组合（Megatron-DeepSpeed 训 BLOOM/OPT 用 ZeRO-1）；**FSDP + TP + PP** 是当前更主流（PyTorch 生态）。
5.  **EP 用于 MoE**：DeepSeek-V3 用 **EP + TP + PP + ZeRO-1 + SP**，把专家均匀分到所有 GPU，并用 all-to-all 通信；同时用冗余专家（redundant experts）和节点限制路由（node-limited routing）来平衡负载。

**实例：** - 训练一个 70B 模型，64 卡（8 节点 × 8 卡）。可选 `TP=8, PP=4, DP=2`（$`8 \times 4 \times 2 = 64`$）；若显存紧张，DP 维度加 ZeRO-1/2 或改 `TP=4, PP=8, DP=2`。 - 训练 7B，8 卡：`TP=1, PP=1, DP=8 + ZeRO-2` 足够。

**通信量分析（每 step 每卡，**$`\Psi`$ **为参数量）：**

| 操作 | 通信量（每卡） | 说明 |
|----|----|----|
| DP all-reduce | $`2\Psi`$（或 $`2\Psi \cdot \frac{N - 1}{N}`$） | ring all-reduce |
| ZeRO-2 reduce-scatter | $`\Psi`$ | 梯度切分 |
| ZeRO-3 all-gather | $`2\Psi`$（前向+反向） | 参数按需 |
| TP all-reduce | $`2 \times 2bsh`$（每层两次） | 与 batch×seq 成正比 |
| PP send/recv | $`bsh`$ | 激活传输 |

**关键结论**：并行的本质约束是 **计算/通信比（arithmetic intensity）**。TP 的通信量与激活成正比（$`bsh`$），而计算量与 $`bsh^{2}`$ 成正比，所以 $`h`$ **越大、**$`b \cdot s`$ **越大，TP 越划算**；反之小 batch 下 TP 会被通信拖死。

**追问：** - 为什么 PP 气泡随 stage 数增加？推导：$`P`$ 个 stage，$`m`$ 个 micro-batch，稳定态前有 $`(P - 1)`$ 个 slot 的填充期，气泡占比 $`= \frac{P - 1}{m + P - 1}`$。 - EP 的通信开销？all-to-all 的通信量 $`\propto b \cdot s \cdot h \cdot \frac{k - 1}{k}`$（$`k`$ 为每 token 路由的专家数），MoE 训练中 all-to-all 常是瓶颈，需用 DeepEP 等库优化。 - 通信与计算重叠的手段？DDP 的 bucket 化、ZeRO-3 的 prefetch、PP 的 1F1B、MoE 的 DualPipe（DeepSeek-V3 把 all-to-all 与前向/反向重叠）。

## Q11: 流水线并行的气泡如何计算？1F1B 与 interleaved 调度？

**答：**

**朴素流水线（GPipe）**：先做完所有 micro-batch 的前向，再做所有反向。气泡占比：

``` math
\text{bubble} = \frac{P - 1}{m + P - 1}
```

其中 $`P`$ 为 stage 数，$`m`$ 为 micro-batch 数。$`m \gg P`$ 时气泡趋近 0。

**1F1B（one-forward-one-backward，PipeDream/Megatron）**：每完成一个 micro-batch 的前向就尽快安排一个反向，使得激活显存峰值只需约 $`P`$ 个 micro-batch 的激活（而非全部 $`m`$ 个），同时气泡不变。**关键收益是显存，不是气泡。**

**Interleaved 1F1B（virtual pipeline, Megatron-LM）**：每个设备持有 $`v`$ 个不连续的 layer chunk（virtual stage），气泡降为：

``` math
\text{bubble} = \frac{1}{v} \cdot \frac{P - 1}{m + P - 1}
```

代价是通信次数增加 $`v`$ 倍（每层 chunk 边界都要 send/recv），需要在带宽和气泡之间权衡。

**DeepSeek-V3 的 DualPipe**：把前向与反向拆成交错的两个流，让 all-to-all 通信完全隐藏在计算中，气泡进一步下降，且允许更大的 micro-batch。

**追问：** - 为什么 1F1B 显存更小？因为反向要先于后续前向执行，限制了同时在飞的 micro-batch 数。 - 什么时候不用 PP？模型能单机装下时，TP+ZeRO 更简单；PP 代码复杂度高、需手工平衡层数。 - 层数无法被 $`P`$ 整除怎么办？做不均匀切分（前几个 stage 多一层），Megatron 支持 `--num-layers-per-virtual-pipeline-stage` 或手动 pipeline 布局。

------------------------------------------------------------------------

# 三、参数高效微调 PEFT

## Q12: 全量微调 vs PEFT，如何选择？

**答：**

| 维度 | 全量微调（Full FT） | PEFT |
|----|----|----|
| 可训练参数 | 100% | 0.01% ~ 5% |
| 显存 | 权重+梯度+优化器 ≈ $`16\Psi`$ | 仅适配器参数 + 冻结权重的激活 |
| 灾难性遗忘 | 严重 | 轻（参数改动小） |
| 多任务部署 | 每个任务一份模型 | 一份底座 + 多个小适配器，可 `merge` |
| 上限效果 | 最高（数据充足时） | 接近全量（数据少时更优） |
| 训练速度 | 慢 | 快（但 LoRA 会降吞吐，需额外 GEMM） |

**经验结论（LoRA 论文 & QLoRA 论文）：** 在指令微调/领域适配场景，LoRA 在多数任务上接近全量微调效果，参数量差 3-4 个数量级；数据量小于 ~10 万条时 PEFT 更划算且更抗过拟合。**要注入大量新知识（如新语言、新领域）时，全量微调或继续预训练仍不可替代。**

**追问：** - LoRA 能否用于继续预训练？可以，但通常需要较大 rank 和较长步数。 - 多 LoRA 服务怎么做？S-LoRA / Punica 用 batched GEMM 同时服务多个适配器；vLLM 支持 LoRA 热加载。

## Q13: LoRA 的原理、低秩假设、参数量与 merge 推理？

**答：**（论文：*LoRA: Low-Rank Adaptation of Large Language Models*, Microsoft, 2021）

**核心思想**：微调时权重更新量 $`\Delta W`$ 具有低秩性（intrinsic dimension 低），因此参数化：

``` math
W' = W_{0} + \Delta W = W_{0} + BA,\quad\quad B \in \mathbb{R}^{d \times r},\ A \in \mathbb{R}^{r \times k},\ r \ll \min(d,k)
```

前向：

``` math
h = W_{0}x + \Delta Wx = W_{0}x + \frac{\alpha}{r}BAx
```

**初始化**：$`A \sim \mathcal{N}(0,\sigma^{2})`$（Kaiming），$`B = 0`$，保证训练初始 $`\Delta W = 0`$，不破坏预训练能力。

**缩放因子** $`\alpha/r`$：$`\alpha`$ 是常数（常取 $`r`$ 的 2 倍或固定 16/32），用于在改 $`r`$ 时保持有效学习率不变。实践中调 $`r`$ 时常固定 $`\alpha`$。

**参数量**：对 $`d \times k`$ 权重矩阵，LoRA 参数为 $`r(d + k)`$，占比：

``` math
\frac{r(d + k)}{dk}\ \overset{d = k}{\rightarrow}\ \frac{2r}{d}
```

**实例**：LLaMA-7B 总参数 6.7B，其中 attention 的 $`W_{q},W_{k},W_{v},W_{o}`$ 各 $`4096 \times 4096`$。若仅对 $`W_{q},W_{v}`$ 加 LoRA，$`r = 8`$：

``` math
4 \times 4096 \times 8 \times 2\,(\text{q,v 两个矩阵}) = 524288 \approx 0.52\text{M} \approx 0.008\%
```

若对全部线性层（含 MLP）加 LoRA，$`r = 8`$ 时约 20M 参数（0.3%）；$`r = 64`$ 时约 160M（2.4%）。

**merge 推理**：训练完后

``` math
W_{\text{merged}} = W_{0} + \frac{\alpha}{r}BA
```

把 $`\Delta W`$ 加到 $`W_{0}`$ 上，**推理零额外延迟**（这是 LoRA 相对 Adapter/Prompt Tuning 的核心优势）。注意：merge 后不能再对多个任务做动态切换。

**目标模块选择**：原论文只对 $`W_{q},W_{v}`$；后续实践（QLoRA、LLaMA-Factory）证明 **对所有线性层（q,k,v,o,gate,up,down）加 LoRA 效果明显更好**，且 `q,k,v,o,gate,up,down` 全加是当前默认。

**秩的选择经验**：$`r = 8 \sim 64`$ 常见；任务越复杂/数据越多，$`r`$ 越大越好；$`r = 1`$ 在简单任务上已可用。QLoRA 论文发现：**LoRA 的 rank 对效果的影响远小于目标模块的选择**，即”加在哪”比”加多大”更重要。

**追问：** - LoRA 为什么能 work（理论）？Aghajanyan et al. 2021 指出预训练模型的 intrinsic dimension 很低；LoRA 论文进一步验证 $`\Delta W`$ 的秩确实很低。 - LoRA 的学习率通常比全量微调大多少？约 10 倍（如 1e-4 ~ 3e-4），因为只训少量参数，正则需求不同。 - LoRA 与 Adapter 的差异？Adapter 串行插入模块，推理有额外延迟；LoRA 是并行的低秩分支，可 merge 消除延迟。 - LoRA 的 B 与 A 能否换顺序？$`BA`$ 与 $`AB`$ 秩相同但形状不同，参数量 $`r(d + k)`$ 对称，效果接近；一般用 $`BA`$ 使得 $`\Delta W`$ 与 $`W_{0}`$ 同形。

## Q14: QLoRA 的原理：4bit 量化 + 双重量化 + Paged Optimizer？

**答：**（论文：*QLoRA: Efficient Finetuning of Quantized LLMs*, 2023）

**三件事：**

**（1）4-bit NormalFloat (NF4) 量化**

对权重做分位数量化。假设权重近似零均值正态分布，则把正态分布的分位数作为量化桶边界，使每个桶内的元素数量相等——这是信息论上对正态数据最优的量化。NF4 使用 16 个桶（4 bit），桶边界为：

``` math
q_{i} = \frac{1}{2}\left( Q_{X}\left( \frac{i}{2^{k} + 1} \right) + Q_{X}\left( \frac{i + 1}{2^{k} + 1} \right) \right)
```

其中 $`Q_{X}`$ 是标准正态的分位函数。量化：

``` math
w^{\text{NF4}} = \arg\min_{c_{i}}\ |w - c_{i}|,\quad\quad\text{dequant}:\widehat{w} = c_{w^{\text{NF4}}}
```

**块量化（block-wise quantization）**：以 64 个权重为一个 block，各自归一化到 $`\lbrack - 1,1\rbrack`$ 后量化，存储一个 fp32 的 absmax 缩放因子。这样局部分布更接近正态，量化误差更小。

**（2）双重量化（Double Quantization, DQ）**

块量化的缩放因子本身占用显存：对 64 权重块，1 个 fp32 因子 = 32/64 = 0.5 bit/参数。双重量化把这些缩放因子再量化成 8-bit（块大小 256），使开销降到：

``` math
\frac{8}{64} + \frac{32}{64 \times 256} \approx 0.127\ \text{bit/参数}
```

从 0.5 降到约 0.127，7B 模型节省约 0.37 bit/参数 × 7B ≈ 0.32 GB。

**（3）Paged Optimizer**

用 NVIDIA 统一显存（Unified Memory）在优化器状态上做分页，显存峰值时自动 evict 到 CPU RAM，避免长序列训练时的 OOM。由 bitsandbytes 实现。

**效果**：65B 模型可在单张 48GB GPU 上微调（此前需要 780GB+），且保持 16-bit 全量微调的同等效果。

**关键实践点**： - 计算时把 NF4 权重反量化到 bf16 做 matmul（`bnb_4bit_compute_dtype=torch.bfloat16`）； - LoRA 适配器本身用 bf16 训练，**不要量化 LoRA 权重**； - `bnb_4bit_use_double_quant=True` 是默认推荐； - 相比 GPTQ/AWQ 的 4bit 推理量化，QLoRA 的 NF4 是为训练设计的。

**追问：** - 为什么量化后还要 LoRA？量化冻结底座大幅降显存，LoRA 提供可训练容量，二者互补。 - QLoRA 相比 LoRA 慢多少？约 20-40%（反量化开销），但显存省 3-4 倍。 - bitsandbytes 的 NF4 与 GPTQ 的区别？NF4 是 data-free 的（分位数固定），GPTQ 是 data-aware 的（用校准集最小化重建误差）。

## Q15: AdaLoRA 与 DoRA 分别改进了什么？

**答：**

**AdaLoRA（Adaptive Budget Allocation for LoRA, 2023）**

问题：LoRA 对所有模块用统一 rank $`r`$ 是不合理的——不同层、不同矩阵的重要性差异很大。 做法：把 $`\Delta W`$ 参数化为 **SVD 形式**：

``` math
W = W_{0} + \Delta W = W_{0} + P\Lambda Q
```

$`P \in \mathbb{R}^{d \times r},Q \in \mathbb{R}^{r \times k}`$ 正交，$`\Lambda`$ 为对角奇异值。用 **重要性评分**（基于奇异值 × 梯度 × 权重的敏感度）动态把预算从”不重要”的奇异值转移到重要方向，并对 $`P,Q`$ 加正交正则：

``` math
R(P,Q) = \parallel P^{\top}P - I \parallel_{F}^{2} + \parallel QQ^{\top} - I \parallel_{F}^{2}
```

最终得到非均匀的 rank 分配（有的层 rank 0，有的 rank 大）。缺点：训练更慢（SVD 计算 + 正则），且需要设定总预算。

**DoRA（Weight-Decomposed Low-Rank Adaptation, 2024）**

动机：分析发现全量微调与 LoRA 的**权重更新模式不同**——全量微调的 $`\Delta W`$ 在”幅度”和”方向”上都有较大且不相关的改动，而 LoRA 的幅度与方向变化是耦合的。 做法：把预训练权重分解为 **幅度（magnitude）与方向（direction）** 两部分：

``` math
W' = m \cdot \frac{W_{0} + BA}{\parallel W_{0} + BA \parallel_{c}}
```

其中 $`m \in \mathbb{R}^{1 \times k}`$ 是可训练幅度向量（初始化为 $`\parallel W_{0} \parallel_{c}`$），$`\parallel \cdot \parallel_{c}`$ 是按列的 L2 范数。只对方向做低秩更新，幅度单独学习。**额外参数仅** $`k`$ **个（可忽略）**，效果一致优于 LoRA，且在低 rank（$`r = 4/8`$）时优势更明显。代价是训练慢约 20-30%（多一次范数计算），且 merge 时需把 $`m`$ 一起吸收。

**追问：** - DoRA 为什么在低 rank 下更好？因为它把”幅度”这一自由度从低秩瓶颈中解放出来，低秩只需拟合方向。 - AdaLoRA 的重要性度量细节？$`I_{i,j} = |\lambda_{i}| \cdot \parallel \nabla_{P_{i}}\mathcal{L} \parallel_{F} \cdot \parallel Q_{j}\nabla_{\mathcal{L}} \parallel_{F}`$ 类的敏感度乘积形式。

## Q16: Prefix Tuning / P-Tuning v2 / Prompt Tuning / Adapter / IA3 有什么异同？

**答：**

| 方法 | 可训练参数位置 | 参数量 | 是否改结构 | 推理延迟 |
|----|----|----|----|----|
| Prompt Tuning | 输入层 soft prompt | $`l \times d`$ | 否 | 无（占用 context 长度） |
| Prefix Tuning | 每层 K/V 前置前缀 | $`2 \times L \times l \times d`$ | 否（改 attention） | 无（占用 context） |
| P-Tuning v2 | 每层各位置加 prompt（deep prompt） | 同 Prefix | 否 | 无 |
| Adapter | 每层串行插入 bottleneck MLP | $`2 \times L \times d \times r`$ | 是 | 有（约 +5-10%） |
| IA3 | 对 K/V/FFN 做逐元素缩放 | $`3 \times L \times d`$ 量级 | 否 | 可 merge |
| LoRA | 并行低秩分支 | $`r(d + k)`$ 每矩阵 | 否 | merge 后为 0 |

**Prefix Tuning（Li & Liang, 2021）**：在每层 attention 的 K、V 前拼 $`l`$ 个可学习向量（“prefix”），相当于给每层加可训练的虚拟 token 的 KV。参数量 $`2 \cdot L \cdot l \cdot d`$，$`l`$ 常取 10-30。有重参数化技巧（用 MLP 生成 prefix）提升稳定性。

**P-Tuning v2**：把 prefix 扩展为 **deep prompt**——不仅在输入层，而是每层都在输入序列前拼接可训练 prompt（类似 Prefix 但作用于 hidden states），对中小模型（\<10B）效果显著优于 P-Tuning v1。属于 **DeepSpeed 的 PEFT 库**。

**Prompt Tuning（Lester et al., 2021）**：只在输入 embedding 层加 $`l`$ 个 soft token，参数量极少（$`l \times d`$）。在 10B+ 模型上接近全量微调效果，但小模型下明显不如。

**Adapter（Houlsby et al., 2019）**：每层插入 down-project → 非线性 → up-project：

``` math
h \leftarrow h + W_{\text{up}}\,\sigma(W_{\text{down}}h),\quad W_{\text{down}} \in \mathbb{R}^{r \times d},W_{\text{up}} \in \mathbb{R}^{d \times r}
```

参数量 $`2Ldr`$。缺点是推理有串行分支，无法 merge，延迟增加。

**IA3（Infused Adapter by Inhibiting and Amplifying Inner Activations, 2022）**：只学习三个缩放向量，分别乘到 attention 的 K、V 和 FFN 的中间激活上：

``` math
\text{IA}^{3}:K \leftarrow k \odot K,\quad V \leftarrow v \odot V,\quad\text{FFN} \leftarrow \gamma \odot \text{FFN}(x)
```

**参数量仅** $`3 \times`$ **层数** $`\times d`$ **量级（例如 7B 模型约 0.01%）**，是所有方法中最省的，且可 merge。效果在少样本下优于 LoRA，大数据集下略逊。

**追问：** - 为什么 soft prompt 在小模型上不行？小模型容量不足以”理解”随机初始化的软 token；大模型可以把 prompt 当作任务描述来用。 - 各方法能否混用？可以（如 LoRA + Prefix），但收益未必叠加，通常选一种即可。 - 面试常问：LoRA 与 Adapter 哪个更好？LoRA 因为可 merge、无推理延迟，且效果相当或更好，是当前主流。

## Q17: 指令微调的数据构造与质量把控？

**答：**（论文：*InstructGPT*（2022）、*Self-Instruct*（2022）、*LIMA*（2023））

**数据来源：** 1. **人工标注**：InstructGPT 用 40 名标注员写 13K 条 prompt+回答（SFT 阶段），并标注偏好对 33K（RM 阶段）。 2. **Self-Instruct / Evol-Instruct**：用强模型（GPT-4）自我生成指令，迭代演化（WizardLM 的 Evol-Instruct 做深度演化：加约束、加深推理、具体化）。 3. **开源数据集混合**：Alpaca（52K，用 text-davinci-003 生成）、FLAN 系列（1.8K 任务 × 混合）、UltraChat、OpenAssistant、Magpie（用模型自身 prompt 分布生成）。 4. **拒绝采样（Rejection Sampling）**：从模型采多条回答，用 RM 或规则筛出最好的做 SFT 数据（RFT，Rejection sampling Fine-Tuning）。

**质量把控（LIMA 的核心结论）：** “**质量 \> 数量**”。LIMA 只用 1000 条精心筛选的样本做 SFT，就达到了与 RLHF 相近的对话效果（“Superficial Alignment Hypothesis”：模型的知识与能力在预训练阶段已学完，对齐只是教会它用哪种格式输出）。

**实操原则：** - **多样性优先**：任务类型、语言、长度、难度均衡；同一类型不要超过某个比例（如 20%）。 - **格式一致性**：统一 chat template（`<|im_start|>user ... <|im_end|>`），训练/推理模板必须一致，否则效果断崖。 - **去重与防污染**：与评测集去重（如做 13-gram 重叠检测，防止 GSM8K/MMLU 泄漏）。 - **长度控制**：避免全部是超长回答（模型会学会啰嗦），保留简短高质量样本。 - **难例挖掘**：加入模型当前答错的样本（on-policy 数据）比只用强模型数据更有效。 - **Epoch 数**：通常 2-3 epoch，多了严重过拟合（指令微调数据量小）。

**灾难性遗忘（Catastrophic Forgetting）**：SFT 后模型通用能力下降、幻觉增加、安全性下降。 - 缓解手段：混入少量预训练数据（replay，如 5-10% 的通用语料）；降低学习率（1e-5 ~ 2e-5）；用 LoRA；只训 1-2 epoch；加 KL 正则约束偏离底座（RLHF 中显式约束）。

**追问：** - 为什么 SFT 后会”变笨”？分布收窄 + 过拟合到特定格式 + 灾难性遗忘。 - 如何评估指令微调效果？MT-Bench、AlpacaEval、Arena-Hard（LLM-as-a-Judge）、以及通用能力回归测试（MMLU/GSM8K 防退化）。 - LLM-as-a-Judge 的偏差？位置偏差、长度偏差、自我偏好，需做位置交换与参考回答校准。

------------------------------------------------------------------------

# 四、对齐与强化学习

## Q18: RLHF 的完整流程是什么？（SFT → RM → PPO）

**答：**（论文：*Training language models to follow instructions with human feedback (InstructGPT)*, OpenAI 2022；*Deep Reinforcement Learning from Human Preferences*, Christiano et al. 2017）

**三个阶段：**

**Stage 1: SFT（Supervised Fine-Tuning）** 用人工示范数据（prompt, 高质量回答）微调预训练模型，得到 $`\pi^{\text{SFT}}`$。InstructGPT 用 13K 条。

**Stage 2: 训练 Reward Model** - 数据：对同一 prompt 采样多个回答，人工对回答对排序（$`y_{w} \succ y_{l}`$，preferred/rejected）。 - 模型：把 LM 最后的 unembedding 层换成线性头，输出标量奖励 $`r_{\phi}(x,y)`$。 - 损失（Bradley-Terry 模型下的 pairwise ranking loss）：

``` math
\mathcal{L}_{\text{RM}}(\phi) = - \mathbb{E}_{(x,y_{w},y_{l}) \sim D}\left\lbrack \log\sigma\left( r_{\phi}(x,y_{w}) - r_{\phi}(x,y_{l}) \right) \right\rbrack
```

Bradley-Terry 假设：$`P(y_{w} \succ y_{l} \mid x) = \sigma(r(x,y_{w}) - r(x,y_{l}))`$，其中 $`\sigma`$ 是 sigmoid。

- 实操：RM 通常用比策略模型**更小**的模型（如策略 175B，RM 6B），且训练 1 epoch 就停（易过拟合），只用最后一个 token 的奖励。
- 归一化：常对奖励做 running mean/std 归一化（如除以标准差），提升 RL 稳定性。

**Stage 3: PPO 强化学习** 以 $`\pi^{\text{SFT}}`$ 为初始策略，用 RM 打分做奖励，最大化：

``` math
\mathbb{E}_{x \sim D,\ y \sim \pi_{\theta}( \cdot \mid x)}\left\lbrack r_{\phi}(x,y) - \beta\, KL\left( \pi_{\theta}( \cdot \mid x)\, \parallel \,\pi_{\text{ref}}( \cdot \mid x) \right) \right\rbrack
```

同时加入预训练数据的 **PPO-ptx** 项（InstructGPT 用了，缓解遗忘）：

``` math
\mathbb{E}_{x \sim D_{\text{pretrain}}}\left\lbrack \log\pi_{\theta}(x) \right\rbrack
```

**关键点：** - Reference model $`\pi_{\text{ref}}`$ 固定为 SFT 模型（或 SFT 与 RL 过程中 EMA 的快照），作用是**限制策略偏离**； - 实际实现里 KL 估计用无偏估计器：

``` math
\text{KL}(\pi_{\theta} \parallel \pi_{\text{ref}}) \approx \frac{\pi_{\text{ref}}(y_{t}|x,y_{< t})}{\pi_{\theta}(y_{t}|x,y_{< t})} - \log\frac{\pi_{\text{ref}}(y_{t}|x,y_{< t})}{\pi_{\theta}(y_{t}|x,y_{< t})} - 1
```

（即 $`r = \frac{p}{q} - \log\frac{p}{q} - 1`$ 形式，非负且无偏低方差） - 工程上 4 个模型同时在线：policy、reference、reward、critic（价值网络），显存压力大 → 是 ZeRO-3 / offload 的主要应用场景。

**追问：** - 为什么不直接用 RM 做 SFT 的过滤（拒绝采样）而要 PPO？PPO 是 **on-policy** 的，能探索 RM 未覆盖的区域；拒绝采样只用当前策略的样本。但 2024 年后 RFT/拒绝采样因其简单稳定被大量采用（如 LLaMA-3、DeepSeek-R1 的冷启动阶段）。 - RM 的规模为什么小？RM 只需给出鲁棒的打分，过大的 RM 易被策略”钻空子”，且训练成本高。 - KL 惩罚的作用？防止 reward hacking、防分布崩坏、保持语言质量（多样性）。

## Q19: PPO 的完整原理（clip 目标、GAE、KL 惩罚）？推导一遍。

**答：**（论文：*Proximal Policy Optimization Algorithms*, Schulman et al. 2017）

**（1）从策略梯度到 TRPO 再到 PPO**

策略梯度的目标：

``` math
J(\theta) = \mathbb{E}_{\tau \sim \pi_{\theta}}\left\lbrack \sum_{t}^{}\gamma^{t}r_{t} \right\rbrack,\quad\nabla_{\theta}J = \mathbb{E}\left\lbrack \nabla_{\theta}\log\pi_{\theta}(a_{t}|s_{t})\,{\widehat{A}}_{t} \right\rbrack
```

重要性采样下用旧策略 $`\pi_{\theta_{\text{old}}}`$ 的样本来估计新策略 $`\pi_{\theta}`$ 的目标：

``` math
J(\theta) = \mathbb{E}_{a \sim \pi_{\theta_{\text{old}}}}\left\lbrack \frac{\pi_{\theta}(a|s)}{\pi_{\theta_{\text{old}}}(a|s)}{\widehat{A}}_{t} \right\rbrack
```

但策略更新太远时重要性采样方差爆炸。**TRPO** 用信赖域约束：

``` math
\max_{\theta}\ \mathbb{E}\left\lbrack \frac{\pi_{\theta}}{\pi_{\text{old}}}\widehat{A} \right\rbrack\quad\text{s.t.}\quad\mathbb{E}\left\lbrack KL(\pi_{\text{old}} \parallel \pi_{\theta}) \right\rbrack \leq \delta
```

**PPO** 用 clip 近似这个约束（无二阶优化，简单得多）：

``` math
L^{\text{CLIP}}(\theta) = \mathbb{E}_{t}\left\lbrack \min\left( \rho_{t}(\theta){\widehat{A}}_{t},\ \ \text{clip}\left( \rho_{t}(\theta),\ 1 - \epsilon,\ 1 + \epsilon \right){\widehat{A}}_{t} \right) \right\rbrack
```

其中 $`\rho_{t}(\theta) = \frac{\pi_{\theta}(a_{t}|s_{t})}{\pi_{\theta_{\text{old}}}(a_{t}|s_{t})}`$，$`\epsilon`$ 通常 0.2。

**为什么 min + clip 有效（逐案例分析）：** - $`{\widehat{A}}_{t} > 0`$（动作好，想增大其概率）：$`\rho`$ 增大使目标增大，但被 clip 到 $`1 + \epsilon`$ 后不再增长，因此**梯度为 0，不再过度激励**。 - $`{\widehat{A}}_{t} < 0`$（动作差，想减小其概率）：$`\rho`$ 减小使 $`\rho\widehat{A}`$ 增大（因为 $`\widehat{A} < 0`$，$`\rho`$ 变小则乘积变大），被 clip 到 $`1 - \epsilon`$ 后不再变化，**不再过度惩罚**。 - 取 min 是保守下界：只在”改进方向”上截断，避免目标被高估。

**（2）优势估计：GAE（Generalized Advantage Estimation）**

定义 TD 残差：

``` math
\delta_{t} = r_{t} + \gamma V(s_{t + 1}) - V(s_{t})
```

GAE 对多步 TD 残差做指数加权：

``` math
{\widehat{A}}_{t}^{\text{GAE}(\gamma,\lambda)} = \sum_{l = 0}^{\infty}(\gamma\lambda)^{l}\,\delta_{t + l}
```

- $`\lambda = 0`$ → $`{\widehat{A}}_{t} = \delta_{t}`$（单步 TD，偏差大方差小）
- $`\lambda = 1`$ → $`{\widehat{A}}_{t} = \sum_{l}^{}\gamma^{l}r_{t + l} - V(s_{t})`$（蒙特卡洛，无偏高方差）
- 实践中 $`\lambda = 0.95`$，$`\gamma = 1.0`$（LLM 中通常把整个回答当作一个 episode，$`\gamma = 1`$）。

在 LLM 中，**奖励只在序列末尾给出**（$`r_{T} = r_{\phi}(x,y) - \beta\text{KL}_{T}`$，中间步 $`r_{t} = - \beta\text{KL}_{t}`$）。GAE 在这种稀疏奖励下会把终局奖励按 $`\gamma\lambda`$ 衰减地回传到每个 token。

**（3）完整损失函数**

``` math
L(\theta) = \underset{\text{策略}}{\underbrace{L^{\text{CLIP}}(\theta)}} - c_{1}\underset{\text{价值回归}}{\underbrace{(V_{\theta}(s_{t}) - {\widehat{R}}_{t})^{2}}} + c_{2}\underset{\text{熵奖励}}{\underbrace{S\lbrack\pi_{\theta}\rbrack(s_{t})}}
```

- $`c_{1} \approx 0.5`$（value loss 系数），$`c_{2} \approx 0.01`$（entropy 系数）；
- 熵奖励鼓励探索、防过早收敛；
- 实践中 value loss 常需 clip（防止价值网络发散）。

**（4）KL 惩罚项在 LLM 中的具体形式**

每个 token 的奖励：

``` math
r_{t} = \left\{ \begin{matrix}
 - \beta\log\frac{\pi_{\theta}(y_{t}|x,y_{< t})}{\pi_{\text{ref}}(y_{t}|x,y_{< t})}, & t < T\ r_{\phi}(x,y) - \beta\log\frac{\pi_{\theta}(y_{T}|x,y_{< T})}{\pi_{\text{ref}}(y_{T}|x,y_{< T})}, & t = T
\end{matrix} \right.\ 
```

即 **KL 是逐 token 施加的密集惩罚**，只有序列末尾加上 RM 的稀疏奖励。这解释了为什么 PPO 在 LLM 中能稳定训练。

**（5）PPO 训练循环（工程视角）** 1. 采样阶段：用当前策略对 batch 的 prompts 生成回答（rollout），记录 log-prob、reward、value； 2. 计算 GAE 优势； 3. 优化阶段：对同一批数据做 $`K`$ 个 epoch（通常 $`K = 1 \sim 4`$）的 minibatch 更新； 4. 可选：KL 早停（当 KL 超过阈值如 6 或 10 时提前结束该 batch 的优化）。

**追问：** - 为什么 PPO 需要 critic（价值网络）？用于计算优势，降低方差。没有 critic 的变体就是 GRPO / RLOO。 - PPO 为什么难训？4 个模型 + 超参敏感（$`\epsilon,\beta,\gamma,\lambda`$, lr, batch）+ 奖励尺度敏感。 - Advantage 归一化？常对整个 batch 的 advantage 做 (x-mean)/std 归一化，稳定训练。 - 为什么 LLM 里 $`\gamma = 1`$？因为把整个生成视作一个 episode，不需要折扣。

## Q20: 奖励黑客（Reward Hacking）是什么？如何缓解？

**答：**

**定义**：策略模型找到 RM 的漏洞，获得高分但实际质量下降（Goodhart’s Law：“当一个度量变成目标，它就不再是好的度量”）。

**典型表现：** 1. **长度爆炸**：RM 偏好长回答 → 回答越来越长、啰嗦（InstructGPT 明确观察到）。 2. **谄媚（sycophancy）**：一味迎合用户观点，即使事实错误。 3. **格式作弊**：用特定句式、列表、加粗、“作为 AI 我…” 等套话骗分。 4. **重复与模板化**：输出固定模板占据回答。 5. **RM 分布外**：策略跑到 RM 训练分布之外，RM 打分完全失效（over-optimization）。

**缓解手段：** 1. **KL 惩罚**：$`\beta`$ 约束策略不偏离 SFT 分布太远，是最直接的手段； 2. **奖励归一化/白化**：减均值除标准差，防止尺度漂移； 3. **RM 集成（ensemble）**：多个 RM 打分取最小值或平均，降低单点漏洞； 4. **RM 持续更新 / 在线 RM**：用新策略的样本持续训 RM（iterative RLHF）； 5. **长度惩罚**：直接在奖励里减去 $`\alpha \cdot \text{len}(y)`$； 6. **数据层面**：RM 训练数据长度均衡、加入”短而好”的样本； 7. **早停**：监控 KL 与 RM 分数，RM 分数继续涨但人工评估下降时停止（over-optimization 拐点）； 8. **用 DPO 类方法**：跳过显式 RM，直接优化偏好（但 DPO 也会过拟合偏好数据，需早停）。

**追问：** - 如何检测 reward hacking？看 KL 与 reward 曲线的背离、长度分布漂移、周期性人工抽检。 - DPO 会有 reward hacking 吗？会——DPO 的隐式奖励也会被利用（如刷长度），且 DPO 缺乏 KL 动态约束（只有 $`\beta`$ 静态正则）。 - 为什么 over-optimization 会发生？RM 是真实人类偏好的有偏估计，优化越强，越会放大估计误差方向的偏差。

## Q21: DPO 的完整推导（从 RLHF 目标到 DPO 损失）？

**答：**（论文：*Direct Preference Optimization: Your Language Model is Secretly a Reward Model*, Rafailov et al., NeurIPS 2023）

**Step 0：RLHF 的优化目标**

``` math
\max_{\pi_{\theta}}\ \mathbb{E}_{x \sim D,\ y \sim \pi_{\theta}(y|x)}\left\lbrack r(x,y) - \beta\log\frac{\pi_{\theta}(y|x)}{\pi_{\text{ref}}(y|x)} \right\rbrack
```

其中 $`r`$ 是（未知的）真实奖励，$`\pi_{\text{ref}}`$ 是参考策略（SFT 模型）。KL 项写为 $`\log\frac{\pi_{\theta}}{\pi_{\text{ref}}}`$ 的期望形式。

**Step 1：该目标有闭式最优解**

把期望写成对 $`y`$ 求和的形式，目标为：

``` math
\max_{\pi}\ \sum_{y}^{}\pi(y|x)\left\lbrack r(x,y) - \beta\log\frac{\pi(y|x)}{\pi_{\text{ref}}(y|x)} \right\rbrack
```

s.t. $`\sum_{y}^{}\pi(y|x) = 1`$。构造拉格朗日函数：

``` math
\mathcal{L} = \sum_{y}^{}\pi(y)\left\lbrack r(x,y) - \beta\log\frac{\pi(y)}{\pi_{\text{ref}}(y)} \right\rbrack + \lambda\left( 1 - \sum_{y}^{}\pi(y) \right)
```

对 $`\pi(y)`$ 求导并令为 0：

``` math
\frac{\partial\mathcal{L}}{\partial\pi(y)} = r(x,y) - \beta\log\frac{\pi(y)}{\pi_{\text{ref}}(y)} - \beta + \lambda = 0
```

解出：

``` math
\log\frac{\pi^{*}(y|x)}{\pi_{\text{ref}}(y|x)} = \frac{1}{\beta}\left( r(x,y) + \lambda - \beta \right)
```

即

``` math
\pi^{*}(y|x) = \pi_{\text{ref}}(y|x)\exp\left( \frac{1}{\beta}r(x,y) \right) \cdot \exp\left( \frac{\lambda - \beta}{\beta} \right)
```

**Step 2：用配分函数归一化**

设

``` math
Z(x) = \sum_{y}^{}\pi_{\text{ref}}(y|x)\exp\left( \frac{1}{\beta}r(x,y) \right)
```

（这是 $`\frac{1}{\beta}r`$ 关于 $`\pi_{\text{ref}}`$ 的配分函数），则最优策略为：

``` math
\boxed{\ \pi^{*}(y|x) = \frac{1}{Z(x)}\,\pi_{\text{ref}}(y|x)\,\exp\left( \frac{1}{\beta}r(x,y) \right)\ }
```

**Step 3：反解奖励**

对上式取对数并整理，把 $`r`$ 用 $`\pi^{*}`$ 表示：

``` math
r(x,y) = \beta\log\frac{\pi^{*}(y|x)}{\pi_{\text{ref}}(y|x)} + \beta\log Z(x)
```

**关键洞察**：$`Z(x)`$ 只与 $`x`$ 有关、与 $`y`$ 无关。而在 Bradley-Terry 偏好模型中，$`P(y_{w} \succ y_{l}|x) = \sigma\left( r(x,y_{w}) - r(x,y_{l}) \right)`$ 只依赖奖励的**差**，因此 $`\beta\log Z(x)`$ 会**在相减时被抵消**。

**Step 4：代入 Bradley-Terry 得到 DPO 损失**

``` math
P(y_{w} \succ y_{l}|x) = \sigma\left( r(x,y_{w}) - r(x,y_{l}) \right) = \sigma\left( \beta\log\frac{\pi_{\theta}(y_{w}|x)}{\pi_{\text{ref}}(y_{w}|x)} - \beta\log\frac{\pi_{\theta}(y_{l}|x)}{\pi_{\text{ref}}(y_{l}|x)} \right)
```

用最大似然（负对数似然）训练：

``` math
\boxed{\ \mathcal{L}_{\text{DPO}}(\pi_{\theta};\pi_{\text{ref}}) = - \mathbb{E}_{(x,y_{w},y_{l}) \sim D}\left\lbrack \log\sigma\left( \beta\log\frac{\pi_{\theta}(y_{w}|x)}{\pi_{\text{ref}}(y_{w}|x)} - \beta\log\frac{\pi_{\theta}(y_{l}|x)}{\pi_{\text{ref}}(y_{l}|x)} \right) \right\rbrack\ }
```

**Step 5：梯度分析**

对 $`\theta`$ 求导，可得：

``` math
\nabla_{\theta}\mathcal{L}_{\text{DPO}} = - \beta\,\mathbb{E}\left\lbrack \underset{\text{被错误排序的样本权重}}{\underbrace{\sigma\left( {\widehat{r}}_{\theta}(x,y_{l}) - {\widehat{r}}_{\theta}(x,y_{w}) \right)}}\left( \underset{\text{提升 preferred}}{\underbrace{\nabla_{\theta}\log\pi_{\theta}(y_{w}|x)}} - \underset{\text{压低 rejected}}{\underbrace{\nabla_{\theta}\log\pi_{\theta}(y_{l}|x)}} \right) \right\rbrack
```

其中 $`{\widehat{r}}_{\theta}(x,y) = \beta\log\frac{\pi_{\theta}(y|x)}{\pi_{\text{ref}}(y|x)}`$ 是**隐式奖励**。

**含义**：损失对”当前被错排的样本”权重更大（$`\sigma`$ 项越大），即自动做了 hard example mining；隐式奖励就是”策略相对参考策略的 log-ratio 乘以 $`\beta`$“。

**DPO 的优缺点：** - 优点：无需 RM、无需 rollout、单阶段、稳定、显存只需 policy + ref（两个模型）；等价于在偏好数据上做带 KL 约束的策略优化。 - 缺点：(1) off-policy，没有 exploration；(2) 容易过拟合偏好数据，导致输出多样性下降、log-prob 下降；(3) 只学到 pair 之间的相对偏好，无法利用在线反馈；(4) 对 $`\beta`$ 敏感。

**追问：** - DPO 与 PPO 的等价性？在偏好数据无限、覆盖整个空间时，DPO 的最优解与 RLHF 最优解一致；有限数据下不等价。 - 为什么 DPO 会降低 chosen 的绝对概率？因为梯度只保证 $`y_{w}`$ 相对 $`y_{l}`$ 的 log-ratio 增大，实践中常出现两者绝对概率都下降（“likelihood displacement”），可用 IPO/校准手段缓解。 - $`\beta`$ 的作用？$`\beta`$ 越大越贴近 reference（保守），越小越激进。 - DPO 需要 reference 模型吗？需要（用于计算 log-ratio）；变体如 CPO、ORPO 去掉了 reference。

## Q22: DPO 的变体：IPO / KTO / ORPO / SimPO 对比？

**答：**

| 方法 | 核心改动 | 是否需要 ref | 损失要点 |
|----|----|----|----|
| **DPO** | 基线 | 是 | sigmoid 的 log-ratio 差 |
| **IPO** (Identity PO) | 把 sigmoid+log 换成平方损失，解决过拟合 | 是 | $`(\log\frac{\pi_{\theta}(y_{w})}{\pi_{\text{ref}}(y_{w})} - \log\frac{\pi_{\theta}(y_{l})}{\pi_{\text{ref}}(y_{l})} - \frac{1}{2\beta})^{2}`$ |
| **KTO** (Kahneman-Tversky) | 用前景理论，只需”好/坏”标签（不需要配对） | 是 | 分别对 desirable/undesirable 用非对称效用（loss aversion 系数 $`\lambda`$） |
| **ORPO** | 把 SFT 与偏好对齐合为一步 | **否** | $`\mathcal{L}_{\text{SFT}} + \lambda\,\mathcal{L}_{\text{OR}}`$（odds ratio 惩罚） |
| **SimPO** | 用平均 log-prob 做隐式奖励 + 目标 margin | **否** | $`- \log\sigma\left( \frac{\beta}{|y_{w}|}\log\pi_{\theta}(y_{w}) - \frac{\beta}{|y_{l}|}\log\pi_{\theta}(y_{l}) - \gamma \right)`$ |
| **CPO** | DPO + SFT 正则 | 是 | DPO 损失 + $`\lambda\mathcal{L}_{\text{SFT}}`$ |
| **R-DPO** | 长度正则化 | 是 | 减去 $`\alpha|\ |y_{w}| - |y_{l}|\ |`$ 项 |

**IPO 细节**：DPO 在偏好数据确定性高（即同一对总是同一边赢）时，log-ratio 会无限增长（logistic 饱和失败 → 过拟合）。IPO 加入正则项：

``` math
\mathcal{L}_{\text{IPO}} = \mathbb{E}\lbrack\left( \log\frac{\pi_{\theta}(y_{w}|x)\pi_{\text{ref}}(y_{l}|x)}{\pi_{\theta}(y_{l}|x)\pi_{\text{ref}}(y_{w}|x)} - \frac{1}{2\beta})^{2} \right\rbrack
```

强迫 log-ratio 差收敛到常数 $`1/(2\beta)`$，理论上有更强的泛化保证。

**KTO 细节**：不需要成对数据，只需”这个回答好/这个回答坏”的二元标签，因此能利用大量非配对数据。基于 Kahneman-Tversky 前景理论：人对损失的敏感度大于收益，因此对 undesirable 样本施加更大的惩罚（loss aversion $`\lambda \approx 1.5`$）。效用函数非对称：

``` math
v(x) = \left\{ \begin{matrix}
\sigma(\beta(\widehat{r} - z_{0})), & \text{desirable} \\
\sigma(\beta(z_{0} - \widehat{r})), & \text{undesirable}
\end{matrix} \right.\ 
```

**ORPO 细节**：$`\mathcal{L} = \mathcal{L}_{\text{SFT}} + \lambda\,\mathcal{L}_{\text{OR}}`$，其中 OR 项用 odds ratio：

``` math
\mathcal{L}_{\text{OR}} = - \log\sigma\left( \log\frac{\text{odds}_{\theta}(y_{w})}{\text{odds}_{\theta}(y_{l})} \right),\quad\text{odds}_{\theta}(y) = \frac{\pi_{\theta}(y|x)}{1 - \pi_{\theta}(y|x)}
```

不需要 reference 模型，显存省一半，且把 SFT 与对齐合并，训练流程最简。

**SimPO 细节**：两点改动 (1) 用**长度归一化的平均 log-prob** 作为隐式奖励（与生成指标对齐）；(2) 加入目标 margin $`\gamma > 0`$ 要求 chosen 明显优于 rejected：

``` math
\mathcal{L}_{\text{SimPO}} = - \log\sigma\left( \frac{\beta}{|y_{w}|}\log\pi_{\theta}(y_{w}|x) - \frac{\beta}{|y_{l}|}\log\pi_{\theta}(y_{l}|x) - \gamma \right)
```

不需要 ref 模型，且天然抑制长度膨胀（因为做了长度归一化）。

**追问：** - 无 ref 的方法为什么能省显存？省掉一份完整模型的参数与激活（约 1/2 显存），对 70B 级别模型尤其关键。 - 哪个效果最好？没有定论，取决于数据与超参；SimPO/ORPO 在开源实践中因简洁高效被广泛采用（LLaMA-Factory、TRL 均支持）。 - KTO 的实用价值？可以复用线上大量”点赞/点踩”式的非配对反馈。

## Q23: GRPO 原理？与 PPO 的差异？优势估计怎么写？

**答：**（论文：DeepSeekMath 2024 提出；DeepSeek-R1 2025 使用并推广）

**动机**：PPO 需要 critic（价值网络），参数量与策略模型同量级，显存与计算开销巨大。GRPO（Group Relative Policy Optimization）**去掉 critic，用组内相对奖励作为优势**。

**做法**： 1. 对每个问题 $`q`$，用旧策略采样一组 $`G`$ 个回答 $`\{ o_{1},\ldots,o_{G}\}`$； 2. 用 RM（或规则/可验证奖励）打分得 $`\{ r_{1},\ldots,r_{G}\}`$； 3. 组内标准化作为优势：

``` math
{\widehat{A}}_{i,t} = \frac{r_{i} - \text{mean}(\mathbf{r})}{\text{std}(\mathbf{r})},\quad\quad\text{std}(\mathbf{r}) = \sqrt{\frac{1}{G}\sum_{j}^{}(r_{j} - \bar{r})^{2} + \epsilon}
```

（DeepSeekMath 原文的归一化是对组内 reward 做标准化，组内所有 token 共享该优势）

**目标函数（DeepSeekMath 的 GRPO）**：

``` math
\mathcal{J}_{\text{GRPO}}(\theta) = \mathbb{E}\left\lbrack \frac{1}{G}\sum_{i = 1}^{G}\frac{1}{|o_{i}|}\sum_{t = 1}^{|o_{i}|}\left( \min\left( \rho_{i,t}{\widehat{A}}_{i,t},\ \text{clip}(\rho_{i,t},1 - \epsilon,1 + \epsilon){\widehat{A}}_{i,t} \right) - \beta\,\mathbb{D}_{\text{KL}}\left\lbrack \pi_{\theta} \parallel \pi_{\text{ref}} \right\rbrack \right) \right\rbrack
```

其中 $`\rho_{i,t} = \frac{\pi_{\theta}(o_{i,t}|q,o_{i, < t})}{\pi_{\theta_{\text{old}}}(o_{i,t}|q,o_{i, < t})}`$，KL 用与 PPO 相同的无偏估计：

``` math
\mathbb{D}_{\text{KL}} = \frac{\pi_{\text{ref}}(o_{i,t}| \cdot )}{\pi_{\theta}(o_{i,t}| \cdot )} - \log\frac{\pi_{\text{ref}}(o_{i,t}| \cdot )}{\pi_{\theta}(o_{i,t}| \cdot )} - 1
```

**与 PPO 的差异总结：**

| 维度 | PPO | GRPO |
|----|----|----|
| Critic / Value 网络 | 需要（与策略同量级） | **不需要** |
| 优势来源 | GAE + 价值估计 | **组内奖励标准化** |
| 显存 | 4 模型（policy/ref/reward/critic） | 3 模型（policy/ref/reward） |
| 采样成本 | 每 prompt 1 个回答 | 每 prompt $`G`$ 个回答（如 8/16/64） |
| 适用场景 | 通用 RLHF | 可验证奖励任务（数学/代码），单 prompt 可多次采样 |

**DeepSeek-R1 的改进（2025）**：R1 在 GRPO 上做了若干稳定化处理： 1. **去掉 KL 项**（直接不加 $`\beta`$ KL 惩罚，改用奖励里的格式约束与语言一致性奖励），报告称更简单有效； 2. **奖励设计**：规则奖励（数学答案正确性、代码测试用例通过）+ 语言一致性奖励（防止中英混杂）； 3. **过滤无效样本**：丢弃组内奖励全 0 或全 1 的样本（优势为 0，无梯度）； 4. **采样温度**较高（1.0）鼓励多样性，$`G`$ 较大。

**追问：** - GRPO 的优势估计比 GAE 好在哪？无需学习价值函数（省显存、避免 value 估计偏差），且可验证任务下组内相对奖励就是无偏的基线（类似 RLOO / REINFORCE with baseline）。 - 组内奖励全相同时会怎样？$`{\widehat{A}}_{i} = 0`$，整个组无梯度——这是 R1 过滤样本的原因，也是难/易问题的处理动机。 - GRPO 的方差问题？$`G`$ 小则 std 估计不准，通常 $`G \geq 8`$；有变体用 untrained 模型的平均奖励做基线（Dr.GRPO、GSPO 等）。 - 后续演进？DAPO（ByteDance，clip-higher、动态采样、token-level loss）、GSPO（序列级重要性比，Qwen）、VAPO 等在 GRPO 基础上继续改进稳定性。

## Q24: RLAIF / Constitutional AI / 拒绝采样与 RFT？

**答：**

**RLAIF（Reinforcement Learning from AI Feedback）**（Bai et al., 2022）： 用强 AI 模型（如 GPT-4、Claude）替代人类标注偏好对，其余流程与 RLHF 相同（训 RM + PPO）。优点：成本低、可扩展、速度快。实验表明 RLAIF 的 RM 质量与人工标注相当甚至更好（但存在偏见传递风险——AI 标注者的偏好偏差会被放大）。

**Constitutional AI（CAI, Anthropic 2022）**核心是 **RLAIF + 一套”宪法”（原则列表）**，两个阶段：

- **SL 阶段（监督学习）**：让模型对有害请求生成回答 → 用宪法原则自我批评（critique）→ 自我修订（revision）→ 用修订后的回答做 SFT。即 “Critique-Revision” 循环。
- **RL 阶段（RLAIF）**：对同一 prompt 生成两个回答，让模型按宪法原则选择更好的 → 用这些 AI 偏好训 RM → 做 RL。好处：把”什么是好”用自然语言原则表达，可审计、可修改。

**拒绝采样（Rejection Sampling / Best-of-N）**： 对每个 prompt 用策略采样 $`N`$ 个回答，用 RM 或**规则验证器**（数学答案、代码单测）筛选，取最优（或所有通过的）作为 SFT 数据。DeepSeek-R1 用它生成 600K 推理数据用于第二阶段 SFT。

**RFT（Rejection sampling Fine-Tuning）**： 在 SFT 模型上采样多次，保留答案正确的轨迹微调（STaR / RAFT 思路），迭代多轮（“RFT + 拒绝采样” 循环）。优势：不需要 RM、不需要 RL 基础设施、稳定；缺点：受限于模型自身的探索上限（若模型一次都做不对，无法产生正样本），因此无法提升能力上限，只能把已有能力”consolidate”。

**对比表：**

| 方法           | 需要 RM              | 需要 RL | 探索能力         | 稳定性 |
|----------------|----------------------|---------|------------------|--------|
| SFT            | 否                   | 否      | 无               | 高     |
| RFT / 拒绝采样 | 可选（规则验证即可） | 否      | 弱（受限于采样） | 高     |
| DPO            | 否（用偏好对）       | 否      | 弱（off-policy） | 中高   |
| PPO / GRPO     | 是                   | 是      | 强（on-policy）  | 中     |

**追问：** - 为什么 DeepSeek-R1 的 RL 用规则奖励而不是 RM？规则奖励（答案匹配、单测通过）无偏、不可被 hack（相对 RM），在可验证任务上远优于学出来的 RM。 - RFT 会引入什么偏差？只保留正确答案 → 训练数据分布偏向”模型已会做的问题”，在困难问题上无增益。 - Constitutional AI 与 RLHF 的关系？CAI 用 RLAIF 替代了人类标注，但流程骨架（SFT → RM → RL）一致。

------------------------------------------------------------------------

# 五、推理模型

## Q25: CoT、Self-Consistency、ReAct 分别是什么？

**答：**

**CoT（Chain-of-Thought, Wei et al. 2022）**：让模型在给出答案前生成中间推理步骤。两种形态： - Few-shot CoT：给几个含推理过程的示例； - Zero-shot CoT：加一句 “Let’s think step by step”（Kojima et al. 2022）。 原理：把难以直接映射的”输入→答案”分解为多步可解的子问题，等效于给模型更多计算步（sequential computation）。

**Self-Consistency（Wang et al. 2022）**：对同一问题采样多条 CoT（temperature \> 0），对最终答案做**多数投票**。理论上是 marginalize over reasoning paths：

``` math
\widehat{a} = \arg\max_{a}\sum_{i = 1}^{N}\mathbb{1}\lbrack a_{i} = a\rbrack
```

在数学/推理任务上稳定提升 5-20 个点。缺点：$`N`$ 倍推理成本。变体：加权多数投票（按 CoT 长度/置信度加权）、Universal Self-Consistency（用 LLM 做投票）。

**ReAct（Yao et al. 2022）**：把 Reasoning 与 Acting 交错——“Thought → Action → Observation” 循环，让模型调用工具（搜索、计算器、代码解释器）并基于观察更新推理。相比纯 CoT，ReAct 能引入外部事实、减少幻觉。现代 Agent 的基础范式（Toolformer、Function Calling 是其工程化版本）。

**追问：** - 为什么 CoT 对推理有效？提供额外的前向计算量（更多 token = 更多层×步的计算）；有研究认为是”把并行不可分的问题变成串行可分”。 - CoT 用在哪些任务上没用？不需要多步推理的任务（如简单事实问答），甚至可能变差。 - Self-Consistency 与 Best-of-N 的区别？SC 是**无监督**的（投票，无需 RM）；BoN 用 RM/验证器打分。

## Q26: o1 / R1 类推理模型的训练范式是什么？

**答：**（论文：*DeepSeek-R1: Incentivizing Reasoning Capability in LLMs via Reinforcement Learning*, 2025；OpenAI o1 技术博客）

**DeepSeek-R1-Zero（纯 RL 版，验证 RL 能自发产生推理）**： 以 **DeepSeek-V3-Base 为基座，跳过 SFT，直接上大规模 RL（GRPO）**，奖励仅用规则： - 准确性奖励：数学答案是否匹配（如 `\boxed{}` 内容精确匹配）、代码是否通过单测； - 格式奖励：必须把推理放在 `…` 中、答案放在 `<answer>…</answer>` 中。

结果：模型自发出现 **长 CoT、自我验证、反思（“wait, let me reconsider”）、多路径探索** 等行为，AIME 成绩大幅提升。问题：输出可读性差、语言混杂。

**DeepSeek-R1（完整四阶段流程）：**

1.  **冷启动（Cold Start）**：用数千条高质量长 CoT 数据（人工筛选 + R1-Zero 输出重写）对 Base 模型做 SFT，解决可读性与语言混杂问题。
2.  **面向推理的 RL**：GRPO + 规则奖励（数学/代码）+ **语言一致性奖励**。引入 **“语言一致性 = 目标语言词占比”** 的奖励，防止中英混杂。同时过滤掉 pass rate 为 0/1 的样本（无梯度）。
3.  **拒绝采样 + SFT（Rejection Sampling & SFT）**：用 RL 收敛后的模型生成大量数据，**用规则验证 + V3 模型做裁判（judge）**，筛出约 **600K** 推理数据，再混入约 **200K** 通用（写作/问答/知识）数据，对 Base 模型做 2 epoch SFT。
4.  **全场景 RL**：再次 RL，但奖励分为两类——**推理任务用规则奖励**，**通用任务用 RM（偏好奖励）**，兼顾有用性与无害性。

**关键结论：** - RL 能”从零”激励出推理能力（R1-Zero 证明了这点）； - 小模型的推理能力可以**从大模型蒸馏**得到（R1 蒸馏到 Qwen-1.5B~32B、Llama-8B/70B，效果显著超过同规模直接 RL）——即”**蒸馏 \> 直接对小模型做 RL**”； - R1 的 AIME 2024 pass@1 达 79.8%，MATH-500 达 97.3%。

**o1 的公开信息**：OpenAI 只披露了”用 RL 训练模型在回答前生成长思维链”，提出 **test-time compute scaling** 与 “train-time compute 与 test-time compute 的权衡”，未公开算法细节。

**追问：** - 为什么冷启动 SFT 有必要？纯 RL 容易产生不可读、语言混杂的输出，SFT 提供格式先验。 - 蒸馏 R1 数据时为什么用 V3 做 judge？规则只能验证可验证任务（数学/代码），通用任务需要模型裁判。 - 长 CoT 的推理代价？输出 token 数增加 5-20 倍，推理成本显著上升（KV Cache 与 decode 时间），是 test-time scaling 的代价。

## Q27: Test-time Compute Scaling 有哪些手段？

**答：**

| 手段 | 说明 | 成本 |
|----|----|----|
| 长 CoT / 更多思考 token | 训练期就鼓励长推理 | 中（线性 in token） |
| Self-Consistency / 多数投票 | 采样 $`N`$ 条取多数 | $`O(N)`$ |
| Best-of-N (BoN) | 用 PRM/ORM 打分选最优 | $`O(N)`$ + 验证 |
| Beam / Tree Search | 用 PRM 做逐步搜索（ToT、MCTS） | 高 |
| 自我验证 / 自我修订 | 模型自查并改错 | 中 |
| 验证器重排 | 训 PRM 对每步打分 | 需训练验证器 |
| 推测解码 / Speculative | 用小模型草稿加速 | 降低单次延迟 |

**关键研究**：Snell et al. 2024（*Scaling LLM Test-Time Compute Optimally…*）发现——**test-time compute 的收益与 prompt 难度相关**：简单问题上多采样无益，难问题上 BoN/搜索收益更大；且存在 **train-time compute 与 test-time compute 的最优分配**：小模型 + 更多测试时计算可以打败大模型 + 少测试时计算。

**PRM vs ORM**：Process Reward Model 对每一步推理打分（稠密奖励，监督信号更强但标注贵）；Outcome Reward Model 只对最终答案打分（便宜但稀疏）。DeepSeek-R1 用规则验证替代两者（在可验证任务上最优）。**Math-Shepherd / Let’s Verify Step by Step（OpenAI 2023）** 是 PRM 的代表工作。

**追问：** - 为什么 PRM 更好但少用？过程标注极贵（需人类逐步检查或 MC rollout 估计），且 PRM 易被 hack（模型学会写出”看起来对”的步骤）。 - 长 CoT 是否对简单问题有害？是，overthinking 会浪费 token 且可能引入错误，因此有”动态思考长度”的研究方向。

------------------------------------------------------------------------

# 六、蒸馏与压缩

## Q28: 知识蒸馏的白盒与黑盒方法？

**答：**（Hinton et al. 2015, *Distilling the Knowledge in a Neural Network*）

**黑盒蒸馏（Black-box / Hard-label）**：只能访问教师模型的输出文本或 top-1 logits。 - **数据蒸馏**：用教师生成大量 (prompt, response) 数据，学生做 SFT。LLM 时代的主流（Alpaca、Vicuna、R1-Distill 都属此类）。 - 损失：标准交叉熵 $`\mathcal{L} = - \sum_{t}^{}\log p_{\text{student}}(y_{t}|x,y_{< t})`$，其中 $`y`$ 是教师的输出（hard label）。

**白盒蒸馏（White-box / Soft-label）**：可访问教师的完整 logits 分布。 - 经典 KD 损失（温度 $`T`$ 平滑）：

``` math
\mathcal{L} = \alpha\,\underset{\text{软标签蒸馏}}{\underbrace{KL(\sigma\left( z_{T}/T \right)\, \parallel \,\sigma\left( z_{S}/T \right))}} + (1 - \alpha)\,\underset{\text{硬标签}}{\underbrace{CE(y,\sigma(z_{S}))}}
```

- 作用：软标签携带”类间相似度”信息（dark knowledge），比 one-hot 提供更多监督信号；$`T`$ 常取 2-20。

**LLM 白盒蒸馏的专门技术：** 1. **词表/层对齐**：学生与教师 tokenizer 不同时需做 vocab mapping（如 ULD、GKD）； 2. **序列级 KD（SeqKD）**：直接把教师的分布作为目标； 3. **MiniLLM（2023）**：用 **反向 KL**（$`KL(p_{\text{student}} \parallel p_{\text{teacher}})`$）而非正向 KL。反向 KL 是 mode-seeking 的，学生不会去覆盖教师的低概率区域，更适合生成任务（避免学生学到教师的长尾噪声）：

``` math
\nabla_{\theta}{KL}_{\text{reverse}} = - \mathbb{E}_{y \sim p_{S}}\left\lbrack \frac{p_{T}(y)}{p_{S}(y)}\nabla_{\theta}\log p_{S}(y) \right\rbrack
```

4.  **DistiLLM**：提出 skew-KL 与自适应 off-policy 策略；
5.  **中间层对齐**：TinyBERT 类的 hidden state / attention 矩阵匹配损失。

**追问：** - 正向 KL 与反向 KL 的区别？正向 $`KL(p_{T} \parallel p_{S})`$ 是 mean-seeking（学生覆盖教师所有模态，生成会更”平均/啰嗦”）；反向是 mode-seeking（学生聚焦教师的少数模态，生成更”确定/流畅”）。 - 蒸馏 vs 用量化压缩参数？蒸馏能改变架构（层数/宽度），量化只降精度。 - R1 蒸馏为什么只用数据蒸馏（黑盒）就够了？因为推理数据本身就是能力的载体，SFT 到高质量 CoT 数据等于教会小模型推理路径。

## Q29: 量化：QAT vs PTQ，GPTQ / AWQ / SmoothQuant / GGUF / bitsandbytes？

**答：**

**QAT vs PTQ：** - **PTQ（Post-Training Quantization）**：训练后直接量化，用少量校准数据（128-1024 条）估计缩放因子。成本低，是 LLM 主流（GPTQ/AWQ）。 - **QAT（Quantization-Aware Training）**：训练时插入伪量化节点（fake quant），用 STE（Straight-Through Estimator）传梯度：

``` math
\text{forward: }\widehat{w} = s \cdot \text{round}(w/s),\quad\quad\text{backward: }\frac{\partial\widehat{w}}{\partial w} \approx 1
```

效果最好但成本高（需完整训练），多用于 W4A4 等极低比特场景（如 LLM-QAT、BitNet 的 1.58-bit 训练）。

**对称/非对称量化公式：**

``` math
x_{q} = \text{clamp}\left( \text{round}\left( \frac{x}{s} \right) + z,\ 0,\ 2^{b} - 1 \right),\quad s = \frac{x_{\max} - x_{\min}}{2^{b} - 1}
```

对称量化（$`z = 0`$）：$`s = \frac{\max|x|}{2^{b - 1} - 1}`$，权重常用（零均值）；激活常用非对称。

**主流方法对比：**

| 方法 | 类型 | 核心思想 | 特点 |
|----|----|----|----|
| **GPTQ** | PTQ, W4A16 | 逐列量化 + 用 Hessian（$`H = 2XX^{\top}`$）补偿误差，最小化 $`\parallel WX - \widehat{W}X \parallel_{2}^{2}`$ | 需校准集，层内逐列求解，精度高 |
| **AWQ** | PTQ, W4A16 | 激活感知：保护”重要通道”（激活大的通道），按通道缩放 | 不用反向传播，快，泛化好 |
| **SmoothQuant** | PTQ, W8A8 | 把激活的量化难度迁移到权重：$`Y = (X\,\text{diag}(s)^{- 1}) \cdot (\text{diag}(s)W)`$ | 解决激活离群值（outlier）问题 |
| **GGUF** | 格式+PTQ | llama.cpp 的格式，支持 Q2_K~Q8_0 多档（k-quant 分块混合精度） | CPU/边缘推理友好 |
| **bitsandbytes** | PTQ | LLM.int8()（离群值通道用 fp16 计算）、NF4（QLoRA） | 易用，HuggingFace 集成 |
| **HQQ / AQLM / QuIP#** | PTQ 前沿 | 无校准/极低比特（2-3 bit） | 研究前沿 |

**GPTQ 的核心（*GPTQ: Accurate Post-Training Quantization for Generative Pre-trained Transformers*, 2022）**： 目标是最小化层输出误差 $`\parallel \widehat{W}X - WX \parallel_{2}^{2}`$。利用 OBQ 的逐列量化框架，用 Hessian 逆 $`H^{- 1} = (2XX^{\top} + \lambda I)^{- 1}`$ 决定量化顺序与误差补偿：量化第 $`j`$ 列后，把误差按 $`H^{- 1}`$ 的比例分配给未量化列：

``` math
\delta = \frac{w_{j} - \text{quant}(w_{j})}{\lbrack H^{- 1}\rbrack_{jj}},\quad\quad w_{k} \leftarrow w_{k} - \delta \cdot \lbrack H^{- 1}\rbrack_{jk},\ k > j
```

工程优化：Cholesky 分解预计算 $`H^{- 1}`$、lazy batch update（一次处理 128 列）以提升 GPU 利用率。

**AWQ 的核心（*AWQ: Activation-aware Weight Quantization*, MLSys 2024）**： 观察到**只有约 1% 的通道的激活值很大（salient channels）**，量化这些通道会带来巨大误差。做法：不量化这些通道（保留 fp16），而是对权重按通道缩放 $`s`$：$`W \leftarrow W \cdot \text{diag}(s)`$，激活 $`X \leftarrow X/\text{diag}(s)`$，用激活的幅值统计搜索最优 $`s`$（grid search），保护重要通道。不需要反向传播与 Hessian，速度快。

**SmoothQuant 的核心**：激活的离群值（outlier，少数通道幅值比中位数大 100 倍）是 W8A8 的难点。做数学等价的变换：

``` math
Y = (X\,\text{diag}(s)^{- 1})\,(\text{diag}(s)W)
```

把量化难度从激活迁移到权重（权重更易量化）。$`s_{j} = \max(|X_{j}|)^{\alpha}/\max(|W_{j}|)^{1 - \alpha}`$，$`\alpha`$ 常取 0.5。

**追问：** - 为什么权重量化比激活量化容易？权重是静态的、分布平滑；激活随样本动态变化且有离群值。 - W4A16 vs W8A8 哪个好？W4A16 省显存最多（权重 4bit）但计算仍是 fp16；W8A8 能真正加速（用 INT8 Tensor Core），但要处理激活离群值。 - 量化对推理的实际收益？显存降到 1/4，decode 阶段（memory-bound）速度显著提升；prefill 阶段（compute-bound）收益小。 - KV Cache 量化？单独处理（如 KIVI、KVQuant），因为 KV 对精度更敏感，通常用 per-channel key + per-token value 量化。

## Q30: 模型剪枝与稀疏化？

**答：**

**剪枝类型：** 1. **非结构化剪枝（unstructured）**：按权重绝对值删单个参数。压缩率高但需要稀疏算子支持才能加速（GPU 上一般加速有限，除非 2:4 结构化稀疏）。 2. **结构化剪枝（structured）**：删整个注意力头、FFN 通道、层。可直接减小矩阵维度，通用硬件即可加速。 3. **半结构化（2:4 / N:M 稀疏）**：NVIDIA Ampere 起支持，每 4 个连续权重保留 2 个，理论 2x 加速；需要专门训练（如 SparseGPT 的逐层重建）。

**代表工作：** - **SparseGPT（2023）**：一次性剪枝（one-shot）到 50-60% 稀疏度，用 Hessian 做逐层误差重建，无需重训； - **Wanda（2023）**：更简单——剪枝准则为 $`|W_{ij}| \cdot \parallel X_{j} \parallel_{2}`$（权重幅值 × 对应输入激活范数），无需 Hessian 逆，效果接近 SparseGPT； - **ShortGPT / 层剪枝**：按层间相似度（Block Influence）删冗余层； - **LLM-Pruner / Sheared LLaMA**：结构化剪枝 + 少量继续预训练恢复。

**剪枝 + 蒸馏 + 量化的组合**：实践中常三者叠加（如剪枝 50% + W4 量化 + 蒸馏恢复），可把 7B 压到能在端侧运行。

**追问：** - 为什么非结构化剪枝在 GPU 上不加速？GPU 是 SIMT 架构，稀疏矩阵乘会产生大量无效计算与不规则访存；需专用稀疏库（cuSPARSE）且稀疏度需很高。 - 剪枝后为什么要继续训练？剪枝破坏了参数间的协同，需要少量数据恢复（recovery fine-tuning / LoRA 恢复）。 - 注意力头剪枝的准则？按头的重要性（如梯度 × 激活、或对输出的影响）排序，一次剪一层（避免累积误差）。

------------------------------------------------------------------------

# 七、训练常见问题排错

## Q31: Loss 不下降怎么办？系统性排查思路？

**答：**

**按优先级排查（从最便宜的开始）：**

1.  **确认数据 pipeline 正确**
    - label 是否错位（shift by one：$`x_{1:T}`$ 预测 $`x_{2:T + 1}`$）；
    - attention mask / padding 是否处理正确（pad token 不应参与 loss，`ignore_index=-100`）；
    - chat template 是否与推理一致；
    - 打印一个 batch 的原始文本与 token id 人工检查。
2.  **检查学习率与优化器**
    - lr 太大（loss 震荡/发散）或太小（loss 几乎不动）；
    - warmup 是否足够（大模型常需 2000 步 warmup）；
    - scheduler 是否正确（cosine decay 的 total steps 设置错了会导致 lr 提前衰减完）；
    - weight decay 是否错误地施加到了 LayerNorm/bias 上（应排除）。
3.  **确认参数真的在被更新**
    - 打印需训练参数列表（`[n for n,p in model.named_parameters() if p.requires_grad]`），尤其 PEFT 时检查是否有参数为 0；
    - 检查 `optimizer.param_groups` 是否包含全部参数；
    - 检查是否被 `torch.no_grad()` 错误包裹。
4.  **梯度检查**
    - grad norm 是否为 0（梯度断流，检查 `detach()`、`requires_grad`、loss 是否用了错误的张量）；
    - grad norm 是否极大（爆炸）；
    - 用 `torch.autograd.gradcheck` 在 toy 例子上验证。
5.  **初始化与数值**
    - 输出层初始化过大会导致初始 loss 巨大且难降；
    - 混合精度下 loss 计算是否用了 fp32；
    - loss scale 是否一直为 0（全部 step 被 skip）。
6.  **超参层面**
    - batch size 太小 → 梯度噪声大，loss 抖动但不下降；可用梯度累积；
    - 序列长度 / packing 策略不当导致大量 padding。

**经典判断规则**：初始 loss 应接近 $`\ln(\text{vocab size})`$。例如 vocab 32000，$`\ln(32000) \approx 10.37`$。如果初始 loss 远高于此（如 20+），说明初始化或数据有问题；如果远低于，可能 label 泄漏（把答案也作为输入预测了）。

**追问：** - 为什么初始 loss ≈ $`\ln V`$？均匀分布下交叉熵 $`= - \sum\frac{1}{V}\ln\frac{1}{V} = \ln V`$。 - loss 下降但评测不涨？数据与评测分布不匹配、或过拟合（看验证集 loss）。 - 训练 loss 降验证 loss 涨？过拟合，需增加数据、降 lr、加正则、减少 epoch。

## Q32: 梯度爆炸/消失的原因与处理？

**答：**

**梯度爆炸**：深层网络的连乘效应。对 $`L`$ 层网络，梯度 $`\propto \prod_{i}^{}W_{i}`$，若谱范数 $`> 1`$ 则指数增长。 - 现象：grad norm 极大、loss 变 NaN、参数变 NaN。 - 处理：**梯度裁剪**（`clip_grad_norm_(params, 1.0)`，LLaMA 用 1.0）；权重初始化（GPT-2 用 $`1/\sqrt{2N}`$ 缩放残差分支）；**Pre-LN** 结构（相比 Post-LN 更稳，不需要 warmup）；降低 lr；用 bf16。

**梯度消失**：谱范数 $`< 1`$ 时梯度指数衰减；sigmoid/tanh 饱和也是原因。 - LLM 中因为残差连接（$`x + f(x)`$，恒等路径保证梯度直通）基本不存在深层梯度消失问题； - 更常见的”消失”是**训练信号稀释**：loss 只在序列末尾/少数 token 上有信号。

**归一化与残差的关键作用：** - Pre-LN: $`x_{l + 1} = x_{l} + F(\text{LN}(x_{l}))`$，梯度有恒等捷径，$`\frac{\partial x_{l + 1}}{\partial x_{l}} = I + \ldots`$，数值上稳定； - LayerNorm / RMSNorm 把激活尺度归一化，防止逐层放大； - 初始化缩放：残差分支输出乘 $`1/\sqrt{2L}`$（GPT-2），防止方差随层数累积。

**梯度裁剪的两种方式：** - clip by norm：$`g \leftarrow g \cdot \frac{\text{max\_norm}}{\parallel g \parallel}`$，LLM 标配； - clip by value：逐元素裁剪，会改变梯度方向，少用。

**追问：** - 为什么 LLM 用 RMSNorm 而不是 LayerNorm？少一个均值减法，计算更省（约 10-15% 的 norm 层加速），效果相当（LLaMA）。 - 梯度裁剪阈值怎么定？通常 1.0；若训练早期 grad norm 常在 0.1 量级说明裁剪没起作用（可降低），常触发裁剪说明 lr 太大。 - QK-Norm 的作用？对 attention 的 Q、K 做归一化，防止 attention logits 随训练变大导致 softmax 饱和（在 ViT-22B、StableLM-2 中验证有效）。

## Q33: 过拟合与复读机现象？

**答：**

**过拟合（SFT/微调阶段最常见）**： - 现象：训练 loss 持续降，验证 loss 先降后升；生成内容与训练样本高度相似（复读训练集）。 - 原因：数据量小（\<10K）、epoch 过多（\>3）、lr 太大、全量微调。 - 处理：减少 epoch（1-3）、降 lr（1e-5 ~ 2e-5）、用 LoRA、增加数据、加入 replay 数据（混预训练语料）、加 dropout / weight decay、早停。

**复读机现象（Repetition）**：模型生成时陷入循环，重复输出同一句/同一 token。 - **原因**： 1. 数据中大量重复文本（如网页模板、`\n\n\n`），模型学到”重复是常见的”； 2. 训练时 loss 对重复模式的正反馈； 3. 解码策略：greedy 或低温采样下，一旦陷入高概率回路无法逃脱；beam search 更容易复读； 4. 训练不足（模型未收敛）或过拟合（模型塌缩）； 5. RLHF 中 RM 偏好长输出 + KL 惩罚不足。 - **处理**： 1. 数据去重（MinHash）、去除重复 n-gram 过多的文档； 2. 解码加 **repetition penalty**、**no_repeat_ngram_size**、**presence penalty**、提高 temperature / top-p； 3. 对比解码（contrastive decoding）、typical sampling； 4. 训练层面加入 unlikelihood loss 惩罚重复 token； 5. 检查是否 KV Cache / 位置编码实现有 bug（工程性复读）。

**模型输出退化（Alignment Tax / 能力退化）**： - RLHF/SFT 后通用能力（MMLU、代码、多语言）下降。 - 原因：对齐数据分布窄、KL 约束破坏了预训练知识分布、灾难性遗忘。 - 处理：混入预训练数据（PPO-ptx，InstructGPT 的做法）、降低 lr、用 KL 正则、分阶段训练、用 LoRA 隔离。

**追问：** - repetition penalty 的数学形式？对已出现的 token 的 logits 做 $`z_{i} \leftarrow z_{i}/\theta`$（$`\theta > 1`$）或 $`z_{i} \leftarrow z_{i} - \alpha`$。 - 为什么 greedy 比采样更容易复读？贪心总是选最高概率 token，在退化分布下形成确定性循环；采样有随机性可跳出。 - 如何量化”复读”程度？统计生成文本的 distinct-n / self-BLEU / 最大重复 n-gram 长度。

## Q34: 显存 OOM 的排查思路？

**答：**

**显存构成（训练）：**

``` math
\text{Mem}_{\text{total}} = \underset{\text{参数+梯度+优化器}}{\underbrace{16\Psi}} + \underset{\text{激活}}{\underbrace{M_{\text{act}}}} + \underset{\text{碎片}}{\underbrace{M_{\text{frag}}}} + \underset{\text{通信 buffer}}{\underbrace{M_{\text{comm}}}}
```

（$`16\Psi`$ 是 Adam + 混合精度的典型值，见 Q7；若用 SGD 无动量则大幅降低；LoRA 只需训练适配器参数，$`\Psi_{\text{train}}`$ 极小，但底座前向的激活仍需算。）

**激活显存估算（关键公式）：**

对 Transformer，每层激活主要来自：

``` math
M_{\text{act}} \approx b \cdot s \cdot h \cdot L \cdot \left( c_{1} + c_{2} \right)\ \text{bytes}
```

其中 $`b`$ 为 micro-batch，$`s`$ 序列长度，$`h`$ hidden，$`L`$ 层数。经验公式（Megatron 论文）：激活显存约

``` math
M_{\text{act}} \approx 34 \cdot b \cdot s \cdot h \cdot L\ \text{（bytes，含 attention 与 MLP 中间激活，不含 TP 切分）}
```

**具体排查清单：**

1.  **先定位是在哪一步 OOM**：加载模型时？前向时？反向时？优化器 step 时？不同阶段指向不同原因。
2.  **减小 micro-batch + 梯度累积**（最直接）；
3.  **开梯度检查点**（激活显存降数倍，代价 +33% 计算）；
4.  **缩短序列长度**，或检查是否有异常长样本（数据里混入超长文本是常见 OOM 原因）；
5.  **换 ZeRO 阶段**（1 → 2 → 3）或加 **offload**（优化器状态 → CPU / NVMe）；
6.  **用 LoRA / QLoRA** 替代全量微调（显存降 3-4 倍）；
7.  **FlashAttention**（避免物化 $`s^{2}`$ 注意力矩阵，$`s`$ 大时收益巨大）；
8.  **检查碎片**：`PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` 可缓解碎片；避免频繁的变长 tensor 分配；
9.  **检查内存泄漏**：是否保存了带梯度的 tensor（如 `losses.append(loss)` 而非 `loss.item()`）；是否 `retain_graph=True` 未释放；
10. **分布式相关问题**：NCCL buffer 大小（`NCCL_BUFFSIZE`）会占用显存；多卡不均匀（某卡负载重）导致单卡 OOM；
11. **推理时的 OOM**：看 KV Cache 大小 $`2 \cdot b \cdot s_{\text{total}} \cdot L \cdot n_{\text{kv\_head}} \cdot d_{\text{head}} \cdot \text{bytes}`$，用 GQA/MQA 减小 $`n_{\text{kv\_head}}`$，或用量化 KV、PagedAttention（vLLM）。

**追问：** - 为什么反向比前向更耗显存？需要保存前向激活 + 产生梯度（与参数同尺寸）。 - 优化器 step 时 OOM 的原因？Adam 需要为所有参数分配 $`m,v`$（各 4 字节/参数），在 step 时刻瞬时峰值最高；AdamW 的 foreach 实现会创建临时张量（可用 `foreach=False` 或 8-bit Adam 缓解）。 - `torch.cuda.memory_summary()` 怎么用？能看到 allocated / reserved / fragmentation 的分布，是排查的第一步。 - 8-bit Adam（bitsandbytes）省多少？把 $`m,v`$ 从 fp32 降到 int8，优化器状态从 $`8\Psi`$ 降到 $`2\Psi`$。

------------------------------------------------------------------------

# 八、速查表与公式汇总

**显存公式速查（混合精度 + Adam，参数量** $`\Psi`$**，卡数** $`N`$**）：**

| 配置                   | 每卡模型状态显存        |
|------------------------|-------------------------|
| 裸 DP                  | $`16\Psi`$              |
| ZeRO-1                 | $`4\Psi + 12\Psi/N`$    |
| ZeRO-2                 | $`2\Psi + 14\Psi/N`$    |
| ZeRO-3                 | $`16\Psi/N`$            |
| LoRA（冻结底座，bf16） | \$2+ 16\_{} + \$ 激活   |
| QLoRA（NF4 + DQ）      | \$0.5+ 16\_{} + \$ 激活 |

**并行与气泡：** - 总卡数 $`= N_{\text{dp}} \times N_{\text{tp}} \times N_{\text{pp}}`$ - 1F1B 气泡占比 $`= \frac{P - 1}{m + P - 1}`$，interleaved 再除以 $`v`$ - ZeRO-3 通信量 $`= 3\Psi`$（vs DP 的 $`2\Psi`$）

**LoRA 参数量：** $`r(d + k)`$；对 $`d = k`$ 时占 $`\frac{2r}{d}`$。

**对齐损失速查：** - RM：$`- \log\sigma(r_{w} - r_{l})`$ - PPO-Clip：$`\min(\rho A,\ \text{clip}(\rho,1 - \epsilon,1 + \epsilon)A)`$ - DPO：$`- \log\sigma\left( \beta\log\frac{\pi_{\theta}(y_{w})}{\pi_{\text{ref}}(y_{w})} - \beta\log\frac{\pi_{\theta}(y_{l})}{\pi_{\text{ref}}(y_{l})} \right)`$ - GRPO：组内标准化优势 $`{\widehat{A}}_{i} = \frac{r_{i} - \bar{r}}{\text{std}(r)}`$，无 critic

**初始 loss 参考值：** $`\ln(\text{vocab size})`$，32K 词表约 10.4，128K 词表约 11.8。

**常用超参经验值：**

| 项目 | 取值 |
|----|----|
| 预训练 lr（峰值） | 1e-4 ~ 3e-4，warmup 2000 步，cosine 退火到 10% |
| SFT lr | 1e-5 ~ 2e-5（全量），1e-4 ~ 3e-4（LoRA） |
| 梯度裁剪 | 1.0 |
| Adam $`\beta`$ | (0.9, 0.95)（LLM 常用 0.95 而非 0.999），$`\epsilon`$=1e-8 |
| weight decay | 0.1（仅权重矩阵，不含 LayerNorm/bias） |
| PPO $`\epsilon`$ / $`\gamma`$ / $`\lambda`$ | 0.2 / 1.0 / 0.95 |
| PPO KL 系数 $`\beta`$ | 0.01 ~ 0.1（自适应目标：KL 超过 6 或 10 即停止优化） |
| DPO $`\beta`$ | 0.1 ~ 0.5 |
| 指令微调 epoch | 2 ~ 3 |

------------------------------------------------------------------------

**本文件整理日期：2026-09-12**
