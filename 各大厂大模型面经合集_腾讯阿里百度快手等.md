> 说明：字节跳动有单独文档，本文不重复。文中所有流程、轮次、题目均**据公开面经整理，具体以实际为准**；同一公司不同部门、不同年份差异可能很大，请勿把本文当作精确的待遇或流程承诺。凡是标注「传闻 / 据说 / 疑似」的内容，均为社区讨论，未经官方证实。

------------------------------------------------------------------------

# 来源与参考

本文内容基于以下公开面经仓库与社区资料的整理、归纳与交叉印证（不作原文转载）：

- **jingtian11/EasyOffer** —— 其中的「LLM大厂面经合集」目录，覆盖腾讯、阿里、百度、快手、蚂蚁、小米、科大讯飞、旷视等公司的大模型岗面经，是本文公司分节的主要骨架来源。
- **datawhalechina/daily-interview** —— Datawhale 的每日面试题合集，包含大量算法 / NLP / 大模型方向的题目与解析，适合用来提炼「跨公司通用高频题」。
- **WeThinkIn/AIGC-Interview-Book** —— AIGC 与大模型方向面试八股整理（含扩散模型、多模态、训练与推理优化），本文中扩散 / 视频生成 / 多模态部分参考其知识框架。
- **liyupi/mianshiya（面试鸭）** —— 程序员面试题库社区，包含后端、算法、AI 应用方向的题目与讨论，用于补充工程侧与 AI 应用落地岗的考察点。
- 以及牛客网、知乎、小红书、脉脉等平台上的匿名面经帖（仅作趋势参考，不引用具体个人经历）。

整理原则： 1. 只保留在**多个独立来源中重复出现**的说法，孤例会标注「个别面经提到」。 2. 不编造精确的薪资、HC 数量、面试官姓名等无法核实的细节。 3. 题目给「答案要点」而非完整标准答案，原因是面试中真正拉开差距的是**表达结构与取舍理由**，而不是背诵。

------------------------------------------------------------------------

# 0. 先看结论：大厂大模型岗的总体格局

在进入分节之前，先给一张「心智地图」，方便后续对照：

| 维度 | 典型代表 | 面试重心 |
|----|----|----|
| 基础模型自研、追求 SOTA | 阿里通义、腾讯混元、DeepSeek、月之暗面 | 架构细节、训练稳定性、数据配比、评测方法论 |
| 大模型 + 现有业务落地 | 腾讯 WXG/PCG、阿里淘天、百度搜索、美团 | 业务抽象、RAG / Agent 工程、成本与延迟 |
| 多模态 / 视频生成 | 快手可灵、旷视、商汤 | 扩散模型、视频时序建模、数据与算力调度 |
| 硬件 + 算子 + 训推一体 | 华为盘古/诺亚、部分阿里云岗位 | 昇腾生态、算子优化、并行策略、通信 |
| 垂直行业大模型 | 科大讯飞、小米、蚂蚁 | 领域数据、合规、端侧部署 |

一个反复被验证的规律：**大模型岗的面试 = 传统算法八股（约 30%）+ 大模型专项（约 40%）+ 项目深挖（约 30%）**，但不同公司这三者的配比差异极大，这正是分节要讲清楚的东西。

------------------------------------------------------------------------

# 1. 腾讯

## 1.1 业务与团队背景

腾讯的大模型相关团队分布很散，面试体验差异也因此很大，务必先搞清楚自己投的是哪个 BG：

- **TEG（技术工程事业群）**：混元大模型的主要研发力量所在，包含混元基座训练、数据、推理加速、评测等方向。技术深度要求最高，偏「研究 + 工程混合」。
- **WXG（微信事业群）**：微信内部的 AI 应用，如微信输入法、搜一搜的 AI 能力、微信内的智能助手相关的算法岗。特点是**业务封闭、要求稳、对线上质量极度敏感**。
- **IEG（互动娱乐事业群）**：游戏相关的 AIGC，包括游戏 NPC 对话、AI 队友、美术资产生成（文生图/图生图）、语音。对多模态和低延迟要求高。
- **PCG（平台与内容事业群）**：内容理解、推荐与大模型结合、视频号相关的多模态理解、内容生成。
- **CSIG（云与智慧产业事业群）**：腾讯云上的大模型 To B 交付，偏应用与工程化。
- **AI Lab / 各实验室**：偏研究，论文要求高。

一个经常被忽略的点：腾讯不同 BG 的**面试题库几乎是独立的**。TEG 会问很深的分布式训练细节，WXG 会问 C++ 和线上问题定位，IEG 会问渲染 / 图形学相关。所以「腾讯面经」这个说法本身其实是不精确的。

## 1.2 面试流程与轮次

典型流程（据公开面经整理，具体以实际为准）：

1.  **简历筛选 / 笔试**：校招有统一笔试（选择 + 2~3 道编程题，难度中等偏上）；社招通常直接约面。
2.  **一面（技术，约 60 分钟）**：自我介绍 + 项目深挖 + 基础八股 + 1 道手撕代码。
3.  **二面（技术 / 组长，约 60~75 分钟）**：项目再挖一层 + 场景设计题 + 可能再手撕一道。
4.  **三面（总监 / 交叉面，约 45~60 分钟）**：更偏宏观、业务理解、技术选型权衡，代码较少或没有。
5.  **HR 面**：意向、薪资、稳定性。
6.  **GM 面 / 加签**：部分核心岗位（如混元）会有额外的委员会或 GM 面。

腾讯的特色是**「交叉面」**——由非本部门的面试官来面，题目往往更偏基础和思维，不会死磕你的项目细节，但会看你「是不是只会自己那一亩三分地」。

## 1.3 考察重点与风格特点

- **C++ / 计算机基础占比高于同行**。腾讯整体技术栈偏 C++，即便是算法岗，也常问：
  - C++ 内存模型、虚函数表、智能指针（shared_ptr 循环引用）、移动语义、RAII
  - Python 的 GIL、多进程 vs 多线程、生成器
  - 操作系统：进程/线程/协程、内存分页、零拷贝、epoll
  - 网络：TCP 三次握手、拥塞控制、HTTP/2、gRPC
- **场景题非常多**。腾讯特别爱问「如果让你做一个 XX 功能，你怎么设计」，考察的是**工程闭环思维**，而不是背模型结构。比如「微信里要做每天千万级调用的 AI 问答，你怎么控制成本和延迟」。
- **线上问题定位能力**。WXG / PCG 尤其喜欢问「线上模型效果突然下降，你怎么排查」，期待你给出分层排查路径（数据 → 特征 → 模型 → 服务 → 灰度）。
- **对「性价比」敏感**。腾讯是典型的「工程务实派」，会追问「你这个方案比基线好多少，成本增加多少」。

## 1.4 高频真题与答案要点

**题 1：介绍一下 Transformer 的注意力机制，为什么除以 √d_k？** - 答案要点：QK^T 的点积在维度 d_k 增大时，方差随之增大（假设各分量独立、均值 0 方差 1，则点积方差为 d_k），导致 softmax 输入数值过大、梯度趋于饱和（softmax 在极端输入下梯度接近 0）。除以 √d_k 把方差拉回 1 量级，保持梯度稳定。 - 加分点：可以延伸到「为什么不是除以 d_k」——因为要保证方差归一到 1；以及 attention 的 O(n²) 复杂度与 FlashAttention 的关系。

**题 2：KV Cache 是什么？它占多少显存？怎么优化？** - 答案要点：自回归解码时，历史 token 的 K、V 不变，缓存起来避免重复计算，把每步复杂度从 O(n²) 降到 O(n)。显存 = 2 × batch × seq_len × num_layers × num_kv_heads × head_dim × dtype_bytes。 - 优化手段：MQA / GQA（减少 KV 头数）、量化 KV Cache（INT8/INT4）、PagedAttention（分页管理减少碎片）、前缀共享（prefix caching）、滑动窗口 / StreamingLLM 丢弃远端 KV。 - 腾讯加分项：能说清 GQA 对**推理吞吐**的具体影响，以及为什么训练时用 MHA、推理时可以转 GQA（需要 uptraining）。

**题 3：手撕 —— 实现一个 LRU Cache。** - 答案要点：哈希表 + 双向链表，get / put 均 O(1)。注意点：哨兵头尾节点简化边界、put 已存在 key 时要更新值并移到头部、超容量时删尾部。腾讯面试官常要求「不用 OrderedDict，手写双向链表」。

**题 4：手撕 —— 实现多头注意力（含 forward）。** - 答案要点：线性投影 → reshape 成多头 → scaled dot-product → 拼接 → 输出投影。注意 mask 的处理（padding mask 与 causal mask 的区别与相加方式）、以及 reshape/view 与 transpose 的 contiguous 问题（这是 C++ 背景面试官爱抠的点）。

**题 5：场景题 —— 微信里要做「AI 总结聊天记录」，你怎么设计？** - 答案要点： 1. 需求拆解：输入长度（聊天记录可能几万 token）、延迟要求（秒级？）、隐私要求（绝对不能出端）。 2. 模型选型：小模型（端侧 / 私有化）vs 云侧大模型，权衡质量与隐私成本。 3. 长文本方案：分块 + 层次化摘要（map-reduce）、或者长上下文模型。 4. 工程：缓存、限流、灰度、A/B 评测。 5. 评测：人工评测标准 + 自动指标（ROUGE 只是参考，重点是事实一致性）。 - 加分点：主动提到「幻觉风险在聊天记录场景不可接受」，并给出让模型只做抽取不做生成的设计。

**题 6：LoRA 的原理，为什么能减少显存？和全量微调比效果如何？** - 答案要点：冻结原权重 W，学习低秩增量 BA（B∈R^{d×r}, A∈R^{r×k}），r ≪ min(d,k)。可训练参数量从 dk 降到 r(d+k)。初始化 A 用高斯、B 用零，保证训练初始 ΔW=0。显存减少来自优化器状态和梯度的减少。 - 加分点：提到 LoRA 的 rank 选择经验、alpha 缩放（ΔW 乘 α/r）、QLoRA（4bit 量化基座 + LoRA）与 DoRA、rsLoRA 等变体；提到「LoRA 在小数据上常不输全量，大数据上全量更强」。

**题 7：线上模型响应变慢了，怎么排查？** - 答案要点：分层排查——客户端/网关（网络、超时配置）→ 服务层（QPS、队列积压、线程池、批处理 batch 变小）→ 推理层（KV Cache 显存、是否触发 swap、batch 组成、长请求占比、GPU 利用率、是否降级）→ 模型层（是否切了更大的模型、输出长度变化）。工具：监控面板、trace、profiler。

**题 8：C++ —— shared_ptr 的循环引用怎么解决？** - 答案要点：weak_ptr 打破环；解释引用计数 + 控制块；提到 make_shared 的一次分配优势与 weak_ptr 导致对象延迟释放的代价。

## 1.5 面试体验与建议

- 体验评价（社区普遍反馈）：面试官整体友善、愿意引导，但**手撕代码要求写完整可运行**，不给太多提示。
- 建议：
  1.  投递前先确认 BG，针对性地准备（TEG 备分布式训练，WXG 备 C++ 与线上，IEG 备多模态）。
  2.  项目要准备「三层深度」：做什么 → 为什么这么做 → 如果重做会怎么改。
  3.  一定准备一个**成本 / 性能权衡**的故事，腾讯面试官吃这一套。
  4.  C++ 基础别裸考，会痛。

------------------------------------------------------------------------

# 2. 阿里

## 2.1 业务与团队背景

阿里的 AI 部门在 2023 年之后经历了多轮重组，投递时需要留意团队的最新归属：

- **通义千问（Qwen）团队**：基座模型研发，覆盖 Qwen 系列稠密与 MoE 模型、多模态 Qwen-VL、音频 Qwen-Audio、代码 Qwen-Coder。研究氛围浓，论文产出多，对 SOTA 追求强。
- **达摩院**：偏前沿研究，包括视觉（如 M6、OFA 系列的历史积累）、医疗 AI、以及部分大模型基础研究。
- **淘天集团（淘宝天猫）**：电商场景的大模型落地 —— 商品理解、搜索推荐、AI 导购、客服、图文生成（电商图）。**工程与业务结合最紧**。
- **阿里云（含 PAI 平台、百炼平台）**：大模型平台化，MaaS 服务、模型部署与推理优化、训推加速。Infra 味道重。
- **蚂蚁集团 / 百灵大模型**：蚂蚁自研大模型（百灵系列），偏金融场景，对合规、安全、可控性要求高。

## 2.2 面试流程与轮次

典型的阿里流程（据公开面经整理，具体以实际为准）：

1.  **简历评估 + 可能的内推**：阿里很看重内推和简历匹配度。
2.  **一面（技术，60 分钟）**：项目 + 八股 + 手撕。
3.  **二面（技术，60 分钟）**：项目深挖 + 场景设计 + 手撕。
4.  **三面（主管 / 交叉，45~60 分钟）**：业务理解 + 技术判断力。
5.  **HR 面**。
6.  **部分岗位有 P9/P10 级的终面或委员会评审**。

阿里的特点是**轮次多、层级评审严格**，尤其是 P7 及以上，会有「晋升委员会式」的技术评审，要求你对技术有体系化的理解，而不仅是会做。

## 2.3 考察重点与风格特点

- **工程与业务结合是核心差异化**。阿里尤其淘天，常常把「业务指标」摆到台面上：你做了一个模型，它带来的 GMV / CTR / 转化率变化是多少？如果答不出「这个模型在业务上值多少钱」，会被认为不接地气。
- **对「数据」的重视程度高于同行**。会出现「数据从哪来 / 怎么清洗 / 怎么去重 / 怎么构造负样本 / 数据配比怎么定」的连环追问。通义团队的数据问题会非常细。
- **手撕代码偏向实际工程题**，而非纯算法脑筋急转弯。常考：手写 Attention、手写 Beam Search、手写 Top-k/Top-p 采样、实现一个简单的 tokenizer（BPE）、实现多路归并、实现带优先级的任务调度。
- **八股偏「为什么」而非「是什么」**。比如不会只问「什么是 LayerNorm」，而会问「为什么大模型用 Pre-LN 而不是 Post-LN」。
- **喜欢问开放性的评测与对齐问题**：你怎么评估一个大模型的好坏？你的评测集怎么构造？如何避免评测集泄漏？

## 2.4 高频真题与答案要点

**题 1：为什么现在的大模型都用 Pre-LN / RMSNorm？Post-LN 有什么问题？** - 答案要点：Post-LN（LN 放在残差之后）在深层网络中需要 learning rate warmup 才能稳定训练，梯度在输出层附近较大；Pre-LN 把 LN 放在子层输入处，残差路径是「恒等映射」，梯度可以无衰减地回传，训练更稳定、对 warmup 不敏感。代价是 Pre-LN 的表达能力略弱，实践中常配合 final LN。 - RMSNorm 的动机：LayerNorm 的均值中心化贡献有限，去掉均值计算只保留缩放，减少计算量、更易并行；同时在大模型上效果与 LN 相当。 - 加分点：提到 DeepNorm、Sandwich-LN 等折中方案，以及「为什么 Pre-LN 下深层网络的表示会「坍缩」，需要 final norm」。

**题 2：手撕 —— 实现 Top-k / Top-p（nucleus）采样。** - 答案要点： - Top-k：对 logits 排序取前 k，其余置 -inf，然后 softmax 采样。注意数值稳定性（减最大值）。 - Top-p：按概率降序累加，取累积概率刚超过 p 的最小集合，重新归一化。注意「至少保留 1 个 token」。 - 加分点：提到 temperature 缩放的位置（应在 softmax 之前除以 T）、repetition penalty 的实现、以及实际推理框架中这些操作的融合 kernel。

**题 3：多模态 —— 一个 VLM 是怎么把图像接进 LLM 的？** - 答案要点：Vision Encoder（如 ViT / SigLIP）提特征 → 投影层（MLP / Q-Former / Perceiver Resampler）映射到 LLM 的词嵌入空间 → 作为「视觉 token」与文本 token 拼接送入 LLM。训练通常分阶段：先冻结 LLM 训投影层（对齐），再联合微调。 - 加分点：讨论分辨率处理（动态切图、AnyRes）、视觉 token 数量与上下文长度的权衡、Q-Former 类方法的 token 压缩优势与信息损失。

**题 4：RAG 系统中，检索和生成怎么配合？召回不准怎么办？** - 答案要点（阿里很爱这道，因为业务落地多）： 1. 链路：文档解析 → 分块（chunk）→ 向量化 → 索引 → 查询改写 → 召回（向量 + BM25 混合）→ 重排（rerank）→ 生成。 2. 召回不准的排查路径：分块策略是否破坏语义（表格、跨页）→ embedding 模型是否匹配领域 → 是否需要 query 改写 / HyDE → 是否需要混合检索 → rerank 模型是否需要领域微调。 3. 生成侧：约束模型只依据检索内容回答，引文标注，无法回答时兜底。 - 加分点：主动提「RAG 的评测要拆成检索指标（Recall@k、MRR）和生成指标（忠实度、答案相关性）分别优化」。

**题 5：混合专家（MoE）模型为什么能省算力？它的难点在哪？** - 答案要点：MoE 用多个 FFN 专家 + 门控路由，每个 token 只激活 top-k 个专家，因此**总参数量大但激活参数量小**，在相同 FLOPs 下获得更大的模型容量。难点： - 负载均衡（专家塌缩，需 aux loss 或 DeepSeek 的无辅助损失偏置路由） - 通信开销（专家并行带来的 all-to-all） - 训练不稳定、专家冗余 - 推理时的显存（需要装下全部专家权重） - 加分点：提到 DeepSeek-V3 的 fine-grained expert + shared expert 设计，以及「MoE 的推理是显存瓶颈而非算力瓶颈」。

**题 6：业务场景 —— 淘宝商品标题生成，怎么设计和评估？** - 答案要点：数据（商品属性、类目、历史标题）→ 模型（SFT 一个基座，或 LoRA）→ 约束（类目关键词必现、长度限制、违禁词过滤）→ 解码（受约束解码 / 后处理）→ 评估（自动指标 BLEU/ROUGE 不可靠，应以人工 + 线上 CTR/转化指标为准）→ 上线（A/B，灰度）。 - 加分点：提到「生成式任务在电商场景的评测难点：正确答案不唯一」，以及用「LLM-as-Judge + 人工抽检」的组合。

**题 7：数据方面 —— 预训练数据怎么去重？为什么去重重要？** - 答案要点：精确去重（哈希、后缀数组）、近似去重（MinHash + LSH、SimHash）。去重重要是因为重复数据会导致模型过拟合、记忆化（引发隐私与版权问题）、并降低有效数据多样性。 - 加分点：提到「跨数据集去重」和「文档内去重」，以及去重粒度对下游 benchmark 的影响。

**题 8：训练不稳定 —— loss 出现 spike 怎么办？** - 答案要点：排查方向 —— 数据（脏样本、异常长序列）→ 数值（fp16 溢出，改用 bf16）→ 优化器（梯度裁剪、Adam 的 eps、beta2）→ 学习率（warmup 不够、后期过大）→ 并行策略（通信导致的梯度不一致）。应急手段：跳过该 batch、回滚到最近 checkpoint、降低 lr 重跑。 - 加分点：提到 z-loss 稳定 logits、以及 MoE 场景下路由引起的尖峰。

## 2.5 面试体验与建议

- 体验评价：节奏快、信息密度高，面试官水平普遍较高。部分面经反馈「压力面」存在，会追问到答不出来为止，但态度专业。
- 建议：
  1.  **准备「业务数字」**。哪怕项目是学术性的，也要思考它的应用价值和成本。
  2.  通义的岗位要复习**训练侧细节**（并行、混合精度、checkpoint、数据管线）；淘天的岗位要复习**落地链路**（RAG、Agent、评测、A/B）。
  3.  简历上写了的每一个指标都要能解释来源和计算方式。
  4.  阿里的 HR 面相对重要，会问职业规划，别只谈技术。

------------------------------------------------------------------------

# 3. 百度

## 3.1 业务与团队背景

百度的 AI 布局在国内是最早的，团队构成很有特点：

- **文心一言 / 文心大模型团队**：基座模型研发，覆盖 ERNIE 系列（含 ERNIE 4.0/4.5 以及后续开源版本）。历史积累深，NLP 传统强。
- **飞桨（PaddlePaddle）**：国产深度学习框架，涉及框架研发、算子开发、编译器、分布式训练。这是**偏系统 / Infra 的岗位**，面试内容与「大模型算法岗」差别很大。
- **搜索团队**：搜索是百度的核心业务，大模型在搜索中的应用包括 query 理解、召回、排序、生成式搜索（AI 摘要）。**NLP 与搜索结合是百度最独特的考察方向**。
- **智能云（百度智能云）**：千帆平台，To B 的模型服务与行业落地。
- **各研究院 / 实验室**：偏研究。

## 3.2 面试流程与轮次

典型流程（据公开面经整理，具体以实际为准）：

1.  **笔试**（校招）：选择题 + 编程题，算法岗笔试难度中上。
2.  **一面（技术，约 60 分钟）**：基础 + 项目 + 手撕。
3.  **二面（技术，约 60 分钟）**：项目深挖 + 开放问题。
4.  **三面（经理 / 总监，约 45 分钟）**：综合。
5.  **HR 面**。

相较腾讯阿里，百度的核心大模型岗位**轮次略少但每轮信息密度大**，且**手撕代码的比例偏高**（常见 1~2 道）。

## 3.3 考察重点与风格特点

- **NLP 传统强项**是最明显的差异化。百度会问到很多「老派 NLP + 大模型」的结合题：
  - 分词、词性标注、NER 的传统做法与现在的做法对比
  - BERT 与 GPT 的区别、MLM vs CLM
  - 文本匹配（双塔 vs 交互式）、语义相似度
  - 序列标注的 CRF、指针网络
- **搜索结合**：query 改写、纠错、意图识别、多路召回融合、相关性排序、生成式摘要的事实性问题。
- **手撕偏经典算法 + NLP 实现**：实现 Trie、实现编辑距离、实现 Beam Search、实现 BPE 分词、实现 NMS（视觉岗）、实现 IoU。
- **对基础理论要求扎实**：会问概率统计（贝叶斯、最大似然、EM）、信息论（交叉熵、KL、互信息）。
- **相对「学院派」**：面试官常来自研究背景，喜欢问「你怎么证明这个改进是有效的」。

## 3.4 高频真题与答案要点

**题 1：BERT 和 GPT 的本质区别是什么？各自的预训练目标怎么设计？** - 答案要点： - 结构：BERT 用 Transformer Encoder（双向注意力），GPT 用 Decoder（因果注意力）。 - 目标：BERT 用 MLM（随机 mask 15% 预测）+ NSP（后续被 RoBERTa 证明无用）；GPT 用自回归语言建模（预测下一个 token）。 - 适用：BERT 类适合理解任务（分类、抽取、匹配），GPT 类适合生成与统一建模，且在规模化上更好（因为训练目标与推理方式一致，没有 train-test 不一致问题）。 - 加分点：指出现代大模型选择 decoder-only 的核心原因是 **scaling law 更友好 + 训练目标统一 + 上下文可复用（KV Cache）**。

**题 2：手撕 —— 实现编辑距离（Levenshtein Distance）。** - 答案要点：DP，dp\[i\]\[j\] 表示 s1 前 i 个字符转到 s2 前 j 个字符的最少操作数；转移为 min(删除、插入、替换)，相同字符时取 dp\[i-1\]\[j-1\]。空间可优化到 O(min(m,n))。 - 变体：加权编辑距离、只求是否 ≤ k（用带宽优化）。

**题 3：手撕 —— 实现 Beam Search。** - 答案要点：维护 beam_size 个候选序列及其累计 log 概率；每步对每个候选扩展词表大小，取 top-beam；注意长度归一化（避免偏向短句）、EOS 处理、batch 实现时用 (beam, vocab) 扁平化。 - 加分点：提到 beam search 在开放生成任务上会导致「安全但平庸」的输出，因此实际对话系统多用采样；以及 diverse beam search、constrained beam search。

**题 4：搜索场景 —— 生成式搜索的 AI 摘要，怎么保证不胡说？** - 答案要点： 1. 检索侧：保证召回内容高相关、来源权威（多源交叉验证）。 2. 生成侧：约束模型只做「抽取 + 归纳」，标注引用来源；对低置信内容拒绝生成（兜底到传统摘要或直接展示结果页）。 3. 后置校验：事实一致性模型（NLI 类）对生成内容与原文做蕴含判断，不一致则丢弃。 4. 评测与监控：人工抽检 + 线上用户反馈（点踩）+ 幻觉率监控。 - 加分点：区分「忠实度（faithfulness）」与「事实正确性（factuality）」——前者是相对检索文档，后者是相对世界真相。

**题 5：传统 NLP —— 文本匹配的双塔和交互式模型有什么区别？** - 答案要点：双塔（如 Sentence-BERT）分别编码两个文本后计算相似度，可离线建索引，适合大规模召回，但缺少细粒度交互；交互式（如 Cross-Encoder / BERT 句对分类）拼接后编码，效果好但无法预先计算，只适合精排小规模候选。 - 加分点：提到「召回用双塔，精排用交互」的经典范式，以及在大模型时代被 reranker（如 bge-reranker）承接。

**题 6：为什么大模型会产生幻觉？怎么缓解？** - 答案要点：来源包括 训练数据中的噪声与错误、模型被训练成「总是要给出一个答案」（next-token prediction 目标不区分「不知道」）、长尾知识不足、解码策略的随机性、以及 RLHF 的「谄媚」倾向。 - 缓解：RAG 外部知识、允许说「不知道」（拒答训练）、解码约束、后置事实校验、降低随机性（temperature）、思维链（对推理类幻觉有效）。 - 加分点：区分「事实性幻觉」与「忠实性幻觉」；提到「幻觉不可能完全消除，工程上要做的是让幻觉可检测、可兜底」。

**题 7：概率题 —— 抛硬币 10 次 7 次正面，判断硬币是否公平。** - 答案要点：二项检验，计算在 p=0.5 下出现 ≥7 次正面的概率（单尾约 0.172，双尾约 0.344），大于 0.05，不能拒绝原假设。扩展：可以讲贝叶斯视角（后验分布 Beta(8,4)，均值 0.667）以及样本量与检验功效的关系。

## 3.5 面试体验与建议

- 体验评价：普遍反馈面试官较为温和、愿意听你讲完，但**基础问题很扎实，答不上来很难糊弄**。
- 建议：
  1.  复习 NLP 经典知识，不要只准备大模型。百度是少数还会认真问 BERT / 传统 NLP 的公司。
  2.  手撕代码要多练，尤其是「实现类」题目（实现 XX 算法），而非 ACM 竞赛题。
  3.  投搜索岗要理解搜索全链路（召回、粗排、精排、重排），大模型只是其中一环。
  4.  投飞桨 / 框架岗要准备 C++、CUDA、算子优化、编译原理。

------------------------------------------------------------------------

# 4. 快手

## 4.1 业务与团队背景

快手的大模型布局集中在两个方向，面试内容差异很大：

- **可灵（Kling）团队**：视频生成大模型，国内视频生成的头部产品之一。涉及扩散模型（DiT 架构）、视频时序建模、训练与推理加速、数据管线。**这是快手最核心的 AI 招牌**。
- **MMU（Multimedia Understanding，多媒体理解）/ 多模态团队**：视频理解、内容标签、视频检索、图文多模态。支撑快手主站的内容理解与推荐。
- **大模型应用团队**：快手的 AI 对话助手、搜索、客服等落地场景。

## 4.2 面试流程与轮次

典型流程（据公开面经整理，具体以实际为准）：

1.  **一面（技术，60 分钟）**：项目 + 基础 + 手撕。
2.  **二面（技术 / 组长，60 分钟）**：项目深挖 + 场景 + 手撕。
3.  **三面（总监 / 交叉，45 分钟）**：综合。
4.  **HR 面**。

可灵团队的流程有时会增加一轮**专门的算法 / 论文讨论**，会深入聊你读过什么扩散模型论文。

## 4.3 考察重点与风格特点

- **多模态与视频生成是绝对重点**。即使是 NLP 背景的候选人，如果投多模态组，也必须准备扩散模型。
- **扩散模型细节问得很深**：DDPM / DDIM / 潜在扩散（LDM）/ DiT / Flow Matching 的原理与区别，采样步数与质量的关系，CFG（classifier-free guidance）的作用与副作用。
- **视频特有的问题**：时序一致性、运动建模、长视频生成、3D VAE 压缩、帧间注意力设计。
- **工程与成本问题**：视频生成的算力消耗极大，会问「怎么降低推理成本」「怎么提高吞吐」。
- **手撕偏实现题**：实现卷积、实现自注意力、实现简单的扩散采样循环、实现 IoU/NMS。

## 4.4 高频真题与答案要点

**题 1：DDPM 的训练目标和采样过程是什么？为什么加噪用高斯噪声？** - 答案要点：前向过程 q(x_t\|x\_{t-1}) 逐步加高斯噪声，可由 x_0 一步采样到任意 t（重参数化，闭式解）。训练目标简化为预测噪声 ε（或等价地预测 x_0 / v）。采样是反向去噪，逐步从纯噪声恢复。用高斯噪声的原因：高斯分布的良好性质使前向过程有闭式解、反向过程的条件分布仍近似高斯，且多个小高斯噪声的叠加仍是高斯，便于数学处理。 - 加分点：提到「预测 ε 在 t 较大时等价于预测 x_0，在 t 小时两者差异大」，以及 v-prediction / flow matching 的动机是均衡不同 t 的训练难度。

**题 2：DDIM 和 DDPM 的区别？为什么 DDIM 能少步采样？** - 答案要点：DDIM 构造了同一前向过程的**非马尔可夫**反向过程，采样变为确定性（当 η=0 时），因此可以在任意子序列上采样而不必逐步。少步采样的关键是「训练目标相同但采样路径可跳跃」，代价是细节质量在高步数下不如 DDPM，实践中靠蒸馏（如 LCM、SDXL-Turbo）进一步加速。 - 加分点：提到一致性模型、蒸馏、以及 rectified flow（SD3 / Flux 采用）把路径拉直以支持更少步数。

**题 3：Latent Diffusion 为什么在潜空间做扩散？** - 答案要点：像素空间扩散计算量随分辨率的平方增长，代价极高。LDM 先用 VAE 把图像压缩到空间下采样 8 倍的潜空间（通道数增加），在潜空间做扩散，计算量大幅下降，同时保留语义信息。代价是 VAE 的重建损失会引入细节误差，这也是为什么 VAE 的质量对生成上限影响很大。 - 加分点：视频场景需要 3D VAE（时空联合压缩），对时序一致性和压缩率的要求更高。

**题 4：Classifier-Free Guidance 是什么？guidance scale 调大有什么副作用？** - 答案要点：同时训练条件与无条件（丢弃条件）的预测，采样时用 ε = ε_uncond + s·(ε_cond - ε_uncond) 放大条件方向。s 增大提高条件对齐度（prompt 更被遵从）和对比度，但过大会导致色彩饱和、细节僵硬、多样性下降、甚至崩坏。 - 加分点：提到「CFG 需要两次前向，推理成本翻倍」，因此有 guidance distillation 等工作来消除这个开销。

**题 5：视频生成中怎么保证时序一致性？** - 答案要点： 1. 架构：3D VAE + 时空注意力（帧间注意力），或在 DiT 中交替使用空间注意力和时间注意力。 2. 训练：在视频片段上训练，让模型学到运动先验。 3. 数据：高质量、运动合理的视频数据是关键。 4. 工程：滑窗生成 + 重叠帧融合、关键帧 + 插帧、光流约束。 - 加分点：提到「视频生成的最大瓶颈是数据而非模型」，以及长视频生成的误差累积问题。

**题 6：多模态 —— CLIP 是怎么训练的？它的对比学习目标是什么？** - 答案要点：双塔结构（图像编码器 + 文本编码器），在一个 batch 内，配对的图文为正样本、其余为负样本，用对称的 InfoNCE 损失（in-batch negatives）。训练数据规模大（4 亿对）是关键。 - 加分点：CLIP 的局限——只能算相似度不能生成、对细粒度关系不敏感、zero-shot 依赖 prompt 模板。以及 SigLIP 用 sigmoid 损失替代 softmax，对 batch 内负样本不再全局耦合，训练更稳。

**题 7：手撕 —— 用 PyTorch 实现一个带 mask 的缩放点积注意力。** - 答案要点：注意 scale、mask 的填充值用 -1e9（或 dtype 的 min）而不是 -inf 以避免 NaN、softmax 的 dim 选择、以及 dropout 的位置。面试官常追问「如果 mask 整行都是被 mask 掉，softmax 会出现什么」（全 -inf 导致 NaN），以及怎么处理（保留至少一个位置，或用 masked 后置零）。

**题 8：视频生成模型的推理成本怎么降？** - 答案要点：少步采样（蒸馏、一致性模型、flow matching 拉直路径）、量化（INT8/FP8）、缓存复用（相邻去噪步的 attention 复用）、并行（序列并行、CFG 并行）、批处理与调度优化、分辨率与帧数的阶梯式生成（低分辨率草稿 + 超分）。 - 加分点：提到「视频生成的瓶颈在显存带宽而非纯算力」，因此量化与 kernel 融合收益很大。

## 4.5 面试体验与建议

- 体验评价：社区反馈普遍不错，面试官对技术细节有兴趣，愿意讨论；可灵团队节奏快、要求高。
- 建议：
  1.  投可灵必读 diffusion 经典论文：DDPM、DDIM、LDM、DiT、SD3/rectified flow、以及视频方向（Sora 技术报告、可灵公开资料）。
  2.  把「时序一致性」「长视频」「成本」三个关键词准备好。
  3.  投 MMU 要复习视频理解（视频分类、时序定位、视频问答）和经典多模态。
  4.  简历上的生成任务要能展示视觉结果，准备好作品集（面试中常要求展示）。

------------------------------------------------------------------------

# 5. 美团 / 京东 / 小红书 / 滴滴

这四家的共同特点：**业务导向强、模型规模相对小、落地要求高、面试更看重工程能力与业务理解**。它们通常不会像通义那样追问训练细节，而是问「你怎么把这个模型塞进现有系统并让它产生价值」。

## 5.1 美团

**业务背景**：本地生活（外卖、到店、酒旅）的 AI 应用 —— 智能客服、商家助手、点评内容理解与生成、搜索推荐、无人配送。美团在大模型上的策略被普遍认为是「不硬冲基座，专注场景落地」。

**流程**：一面技术 → 二面技术/组长 → 三面总监 → HR。轮次相对紧凑。

**考察重点**： - 客服场景的对话系统（多轮、意图、槽位、兜底策略） - 搜索 / 推荐与大模型结合（生成式推荐、query 理解） - 成本控制（美团对 ROI 极度敏感，会追问「这个模型每次调用多少钱」） - 数据闭环（业务反馈怎么回到模型训练）

**高频题示例与要点**： - **「怎么做一个外卖场景的智能客服？」**要点：意图分类（是否需要大模型，还是小模型够用）→ 知识库 RAG → 多轮状态管理 → 转人工策略 → 评测（首解率、转人工率、满意度）→ 成本（用小模型做路由，简单问题小模型答，复杂问题升级到大模型）。加分点：主动提「拒答与转人工策略的设计比模型本身更重要」。 - **「生成式推荐怎么做？」**要点：把推荐建模为序列生成（item 作为 token）、用 LLM 做用户兴趣理解、语义 ID（semantic ID）的构造、与现有双塔/排序模型的融合。加分点：提到生成式推荐的**延迟和候选规模**约束，指出它更适合做召回而非精排。 - **手撕**：中等难度算法题（DP、二叉树、滑动窗口），通常 1 道。

**建议**：准备一个「模型上线后业务指标提升」的完整故事；对成本极其敏感，务必准备 token 成本计算。

## 5.2 京东

**业务背景**：电商 —— 商品理解、搜索推荐、智能客服（言犀）、内容生成（商品文案、营销素材）、供应链预测、以及京东探索研究院的大模型研究（言犀大模型基座）。

**流程**：技术一面 → 技术二面 → 主管面 → HR。

**考察重点**： - 电商 NLP：商品标题 / 属性抽取、类目预测、评论情感分析、query 改写 - 大模型在客服中的应用（言犀） - 多模态：商品图理解、图搜 - 供应链与预测类岗位会有时序预测（这与大模型关系不大，是独立的算法方向）

**高频题示例与要点**： - **「商品标题的生成式改写，怎么保证不违规、不夸大？」**要点：约束解码（类目词必现、违禁词黑名单）→ 生成后置过滤 → 事实一致性校验（属性与生成的对比）→ 人审兜底 → A/B 验证点击与转化。 - **「怎么做商品的多模态表征？」**要点：图文双塔对比学习、以类目/属性为监督信号、用 LLM 生成商品描述作为弱监督、统一到一个向量空间用于图搜和推荐。 - **手撕**：偏经典，可能问「实现字符串的相似度计算」或「合并区间」类。

**建议**：京东面试对「业务理解」很看重，会问「你觉得京东现在最大的 AI 机会在哪」，要有观点。

## 5.3 小红书

**业务背景**：社区 + 搜索 —— 内容理解（笔记的标签、质量、违规识别）、搜索（小红书搜索使用率很高，query 理解与语义检索是重点）、推荐、以及 AIGC 创作工具（文案助手、图片生成/编辑）。

**流程**：一面技术 → 二面技术/leader → 三面（交叉/总监）→ HR。小红书的面试节奏快，反馈也快。

**考察重点**： - **多模态与内容理解**（图文笔记是核心资产） - **语义检索 / 向量召回**（搜索是小红书的战略重点） - AIGC 创作（文生图、图片编辑、文案生成） - 用户行为与内容生态（会问「模型优化会不会导致内容同质化」这类生态问题）

**高频题示例与要点**： - **「小红书搜索里，怎么用大模型改 query？」**要点：query 改写（补全、纠错、改写为检索友好的形式）→ 意图识别（是找攻略、找商品还是找人）→ 混合检索（向量 + 词）→ rerank → 生成式结果聚合。加分点：提到「搜索场景的改写必须可控，不能让模型改变用户原意」，以及改写的收益要用点击和后续行为衡量。 - **「笔记内容的向量表征怎么做？」**要点：多模态编码（图文拼接 or 分别编码后融合）、用互动数据做弱监督、对比学习、长文截断策略、以及「同一篇笔记在不同意图下应有不同表征」的多向量思路。 - **手撕**：1 道，中等难度，常与字符串 / 哈希 / 堆相关。

**建议**：小红书面试官普遍年轻、交流轻松，但对「你自己怎么看这个业务」的追问较多，要有独立观点。

## 5.4 滴滴

**业务背景**：出行 —— 供需预测、派单、定价、安全（行程中的风险识别）、客服、以及地图与自动驾驶相关感知。大模型的应用主要在客服、司乘对话理解、安全内容审核、以及内部的研发提效。

**流程**：技术一面 → 技术二面 → 主管面 → HR。

**考察重点**： - 时序预测与因果推断（这是滴滴的特色，传统强项） - 强化学习（派单、定价本质是 RL 问题） - 大模型在客服和文本审核中的落地 - 对「模型可解释性」的要求（安全场景需可解释）

**高频题示例与要点**： - **「行程中的风险对话识别，怎么设计？」**要点：这是典型的「高召回优先」问题 —— 先保证召回（宁可误报不可漏报）→ 多级过滤（规则 + 小模型 + 大模型）→ 人工审核兜底 → 关注误报率带来的审核成本。加分点：提到「正负样本极度不平衡」和「标签延迟（风险事件要事后才知道）」带来的训练困难。 - **「供需预测用什么模型？和大模型有关系吗？」**要点：传统时序（LightGBM、DeepAR、Transformer 类时序模型）仍是主力；大模型的价值在于**融合多源异构信息**（天气、事件、文本描述）而非直接预测。要有判断力，不要硬套大模型。 - **手撕**：常考 DP 和贪心（与派单问题一脉相承）。

**建议**：滴滴的算法岗有很大一部分与传统机器学习 / 运筹优化相关，投递前看清岗位描述，不要以为都是大模型。

## 5.5 四家横向小结

| 公司 | 大模型岗位的核心场景 | 面试最看重 | 特别提醒 |
|----|----|----|----|
| 美团 | 本地生活客服、搜索推荐 | 成本 ROI、业务闭环 | 必须会算 token 成本 |
| 京东 | 电商客服、商品理解、供应链 | 业务理解、多模态 | 部分岗位是时序预测不是大模型 |
| 小红书 | 内容理解、语义搜索、AIGC | 多模态、检索、生态观 | 要有自己的业务观点 |
| 滴滴 | 客服、安全审核、司乘对话 | 时序 / RL、可解释性 | 岗位差异大，看清 JD |

------------------------------------------------------------------------

# 6. 华为（盘古 / 诺亚）、小米、科大讯飞、旷视 / 商汤

## 6.1 华为

**业务背景**： - **盘古大模型**：华为的基座模型体系，包含 NLP、CV、多模态、科学计算（气象、药物）等，主打「行业大模型」。 - **诺亚方舟实验室**：华为的研究机构，偏前沿算法研究。 - **昇腾（Ascend）+ CANN + MindSpore**：这是华为最独特的地方 —— 软硬件全栈自研，因此算法岗常常要懂硬件。

**流程**：华为的流程相对特殊，通常是「机考（编程）→ 技术一面 → 技术二面 → 主管面 → HR」，部分岗位有性格测评（华为的性格测试是出了名的，需要认真对待，避免前后矛盾）。校招的「机考」是硬门槛。

**考察重点与风格特点**： - **硬件与算子结合是最大差异化**。会问： - 昇腾 NPU 的架构特点、与 GPU 的差异（如达芬奇架构、Cube 单元） - 算子如何在昇腾上实现与优化（TBE / Ascend C） - 混合精度训练在昇腾上的实践 - 通信库 HCCL、并行策略（数据并行、流水并行、张量并行）在国产集群上的调优 - **MindSpore 框架**：如果投框架相关岗，会问 MindSpore 的图模式、自动并行、动静态图。 - **工程严谨性**：华为的面试风格偏严谨、流程化，问题相对标准化，答案要结构化。 - **对国产化 / 自主可控的理解**：会问「你怎么看国产 AI 芯片的生态」。

**高频题示例与要点**： - **「FP16 训练为什么会溢出？怎么解决？」**要点：FP16 表示范围有限（最大约 65504），梯度或激活过大时溢出为 inf/nan。解决：loss scaling（静态 / 动态，动态更常用）→ 用 BF16（范围与 FP32 相同但精度低）→ 关键算子（如 softmax、LayerNorm）保持 FP32 计算 → 梯度裁剪。加分点：解释 loss scaling 的原理是「放大 loss 使小梯度也落在 FP16 可表示范围」。 - **「张量并行和流水并行的区别与适用场景？」**要点：张量并行切分单层的矩阵运算（如按列切分 MLP），通信频繁（每层都要 all-reduce），适合节点内高带宽场景；流水并行按层切分到不同设备，通信量小但存在气泡（bubble），需要 micro-batch 和调度（1F1B）来降低。加分点：提到序列并行（Sequence Parallel）作为张量并行的补充以降低激活显存，以及 3D 并行的组合使用。 - **「怎么把一个算子从 GPU 迁移到昇腾？」**要点：先保证语义正确（用 CPU 参考实现做对比）→ 用 Ascend C 或 TBE 重写 → 性能调优（内存搬运、计算与搬运重叠、算子融合）→ 精度对齐（注意浮点累加顺序不同带来的误差）。

**面试体验与建议**：流程长、环节多（含机考与测评），需要耐心。建议提前准备机考（华为机考难度中等，但要求通过率），并复习并行训练与硬件相关基础。

## 6.2 小米

**业务背景**：小米的大模型团队（MiLM 系列）聚焦「端侧 + 云侧」结合 —— 手机端的小爱同学、端侧小模型、汽车（SU7）的智能座舱语音助手、以及 AIoT 场景。小米 AI 实验室也有基座模型研究。

**考察重点**： - **端侧部署是最大差异化**。会问： - 模型量化（INT8/INT4、GPTQ、AWQ、SmoothQuant） - 剪枝与蒸馏（结构化剪枝、层丢弃） - 端侧推理框架（MNN、NCNN、TFLite） - 内存与功耗约束下的模型设计 - 语音相关（ASR、TTS、唤醒词）因为小爱同学的关系占比高 - 多模态（手机影像相关）

**高频题示例与要点**： - **「怎么把一个 7B 模型塞进手机？」**要点：量化（4bit 权重量化 + 分组量化，如 GPTQ/AWQ；激活量化更难）→ 蒸馏到更小模型（1B/3B）→ 结构优化（GQA 减少 KV Cache、稀疏化）→ 端侧推理引擎优化（算子融合、内存复用、NPU 加速）→ 分场景调度（简单请求端侧，复杂请求云侧）。加分点：提到「端侧的瓶颈是内存带宽和功耗，不是算力」，以及「KV Cache 在长对话下会吃光内存」。 - **「GPTQ 和 AWQ 的区别？」**要点：GPTQ 基于逐层重建误差，用 Hessian 的二阶信息决定量化顺序与补偿，是「训练后量化 + 误差补偿」；AWQ 观察到权重中少量「显著通道」对输出影响大，因此按激活分布对重要通道做缩放保护，不需要反向传播更新。加分点：提到两者都需要校准集，校准集分布影响量化效果。

**建议**：投小米要准备端侧相关的知识，这在其他公司很少被问到，是差异化的加分项。

## 6.3 科大讯飞

**业务背景**：语音起家（ASR/TTS 长期国内领先），星火大模型是基座，落地在教育（学习机、智慧课堂）、医疗、办公（讯飞听见、智能录音笔）、汽车语音。**语音 + 大模型的结合是最大特色**。

**考察重点**： - 语音：ASR（声学模型、语言模型、端到端）、TTS（声学模型 + 声码器、情感与韵律）、语音大模型（Speech LLM） - 教育场景的落地（作文批改、口语评测、知识问答） - 大模型基座训练（星火） - 评测方法论（教育场景对正确性要求极高）

**高频题示例与要点**： - **「语音大模型怎么把语音接进 LLM？」**要点：三条路线 —— (a) 级联（ASR 转文本 → LLM → TTS），工程简单但丢失韵律与情感且误差累积；(b) 语音编码器 + 投影层接到 LLM（类似 VLM 做法，如用 Whisper encoder），保留声学信息；(c) 原生语音 token（把语音离散化为 token，与文本 token 统一建模），最彻底但数据与训练难度最大。 - **「口语评测怎么打分？」**要点：多维度（发音准确度、流利度、完整度、韵律）；传统方法是 GOP（Goodness of Pronunciation）+ 声学模型后验；现在可用端到端模型直接回归分数。难点是标注主观、需要大量人工评分数据，以及跨设备鲁棒性。 - **手撕**：常见中等难度题，可能结合字符串处理（与语音文本处理相关）。

**建议**：语音背景的候选人在讯飞有明显优势；非语音背景要补 ASR/TTS 基础，并想清楚「语音 + LLM」怎么讲出深度。

## 6.4 旷视 / 商汤

**业务背景**：这两家是传统的 CV 强厂（旷视的检测 / 人脸、商汤的通用视觉与「日日新」大模型、AI 大装置算力平台）。近年都在向多模态大模型与行业落地转型。

**考察重点**： - **视觉基础非常扎实**：卷积网络细节、检测（Faster R-CNN / YOLO / DETR）、分割（Mask R-CNN、SAM）、分类（ResNet、ViT）、损失函数（Focal Loss、IoU Loss）、NMS 与后处理 - **多模态大模型**：视觉编码器选型、图文对齐、视觉指令微调 - **生成模型**（商汤）：扩散模型、可控生成、数字人 - **工程与部署**：模型压缩、TensorRT、多卡推理

**高频题示例与要点**： - **「NMS 的原理和缺陷？有哪些改进？」**要点：按置信度排序，保留最高分框，抑制与其 IoU 超阈值的框，迭代。缺陷：密集场景（人群）会误抑制真阳性；对遮挡、同类相邻目标不友好。改进：Soft-NMS（用分数衰减替代直接删除）、IoU 换成 DIoU/GIoU、以及现在更多用端到端检测器（DETR 系列）来回避 NMS。 - **「ViT 和 CNN 的区别？ViT 为什么需要大量数据？」**要点：ViT 把图像切成 patch 当 token，用全局自注意力，没有 CNN 的局部性、平移等变和层级结构等归纳偏置。因此在小数据上不如 CNN，需要大数据（JFT-300M）预训练或强数据增强（DeiT）来弥补。加分点：提到 Swin 用窗口注意力恢复局部性、以及混合架构（CNN stem + Transformer）。 - **「DETR 是怎么做到无需 NMS 的？」**要点：用可学习的 object queries 与图像特征做交叉注意力，通过二分图匹配（匈牙利算法）建立预测与真值的唯一对应，强制一对一的预测，因此天然不需要 NMS。缺点是收敛慢（后续 Deformable DETR 改进）。 - **手撕**：手写 NMS、手写 IoU、手写卷积（含 padding/stride 计算）。

**建议**：CV 基础必须过硬，NMS / IoU / 卷积几乎是必考；同时准备好「传统 CV 到多模态大模型的转型」的叙事。

## 6.5 小结对比

| 公司 | 核心差异化考察 | 必须准备的内容 |
|----|----|----|
| 华为 | 硬件 + 算子 + 并行 + 国产生态 | 昇腾 / MindSpore、混合精度、并行策略、机考 |
| 小米 | 端侧部署、量化、语音 | GPTQ/AWQ、蒸馏、MNN、内存与功耗 |
| 科大讯飞 | 语音 + 大模型、教育场景 | ASR/TTS、Speech LLM、口语评测 |
| 旷视 / 商汤 | CV 基础、多模态、生成 | NMS/IoU/卷积手撕、检测分割、扩散模型 |

------------------------------------------------------------------------

# 7. 创业公司 / AI 独角兽（月之暗面、智谱、MiniMax、DeepSeek 等）

## 7.1 总体特点

这类公司的面试风格与前面的大厂有本质区别：

- **论文与前沿追踪的权重极高**。面试官往往本身就是论文作者，会问「你读过我们最近的哪篇工作」「你怎么看 XX 方向」。如果你的回答停留在「我看过 XX 的博客」，会很快露怯。
- **轮次少但强度大**。常见 2~3 轮技术面，每轮 60~90 分钟，可能包含长时间的开放讨论。
- **不太问传统八股**（如操作系统、网络），除非岗位偏 Infra。取而代之的是**深度技术讨论 + 代码 + 研究品味**。
- **HC 少、要求高**，且常偏向有顶会论文或开源贡献的候选人。
- **代码环节可能是「给你一个问题背景，让你实现一个算法模块」**，偏向实际而非竞赛。

## 7.2 月之暗面（Moonshot AI）

**背景**：Kimi 系列，以长上下文（早期主打 20 万 token，后续扩展到更长）和 Agent 能力著称。

**考察重点**： - **长上下文**：位置编码外推（RoPE 的 NTK 插值、YaRN、ABF）、长文本的训练策略与数据配比、长上下文的评测（大海捞针 Needle-in-a-Haystack 的局限）、长上下文的注意力优化（FlashAttention、稀疏注意力、线性注意力） - **Agent 与工具使用**：function calling 的训练、多步推理、错误恢复 - **强化学习**（后续版本强调推理能力）：RLHF、DPO、以及推理链的 RL 训练（如 GRPO）

**典型问题与要点**： - **「RoPE 外推有哪些方法？」**要点：RoPE 用旋转矩阵编码相对位置，但训练长度之外的频率分量未见过的会导致外推失败。方法：(a) 位置插值（PI）—— 压缩位置索引到训练范围内，代价是高频信息丢失，需要少量微调；(b) NTK-aware 插值 —— 按频率维度非线性插值，对高频少插值、低频多插值；(c) YaRN —— 分频段处理 + 温度缩放，兼顾外推与短文本性能；(d) 直接增大 base（ABF）。加分点：提到「外推能力不只看能不能跑长，还要看长文本中的有效检索能力」。 - **「长文本训练的数据怎么配比？」**要点：长文档数据（书籍、代码仓库、长对话）要占一定比例，同时保留短数据以维持短文本性能；训练长度要逐步扩展（如 4k → 32k → 128k）而不是一步到位；打包（packing）策略会影响注意力模式。

**建议**：准备一个关于「长上下文 / Agent」的深度观点，能讲清 trade-off。

## 7.3 智谱 AI（GLM）

**背景**：清华系团队，GLM 系列模型，覆盖基座、多模态（CogVLM、GLM-4V）、代码、Agent（AutoGLM）。学术氛围浓，开源活跃。

**考察重点**： - **GLM 的架构特点**（自回归填空 / GLM 的注意力设计历史演变） - **多模态**（CogVLM 的视觉专家设计、视觉 token 与语言的融合） - **Agent**（AutoGLM 的 GUI Agent、工具调用） - **对齐**（RLHF、DPO、以及 ChatGLM 时代的对齐实践）

**典型问题与要点**： - **「CogVLM 为什么在每一层都插入视觉专家，而不是只在输入层拼接视觉 token？」**要点：只在输入层拼接时，视觉信息需要经过所有层才能与语言深度融合，且视觉 token 会占用上下文长度、与文本 token 竞争注意力；CogVLM 的设计是让视觉信息通过专门的 QKV 专家在每层参与，避免视觉 token 直接进入注意力序列，从而在保持语言能力的同时实现深层视觉-语言融合。加分点：提到这种设计对训练稳定性和参数效率的影响。 - **「DPO 和 RLHF（PPO）的区别？」**要点：PPO 需要训练 reward model + 在线采样 + 优势估计，流程复杂、不稳定但上限高；DPO 直接在偏好数据上用闭式解推导出一个分类式损失，等价于在特定约束下优化 RLHF 目标，无需 reward model，实现简单、稳定，但对数据的利用效率和对齐上限不如在线 RLHF。加分点：提到 IPO、KTO、ORPO 等变体，以及「DPO 容易过拟合偏好数据、导致输出多样性下降」。

**建议**：智谱面试会比较学术，准备好论文阅读的深度（不只是结论，要讲得出方法和动机）。

## 7.4 MiniMax

**背景**：产品线覆盖文本（abab 系列）、语音（海螺 AI 的语音合成质量口碑很好）、视频（海螺 AI 视频）、以及出海产品（Talkie）。**语音与视频是差异化重点**。

**考察重点**： - 语音大模型（TTS 的自然度、零样本音色克隆、情感控制） - 视频生成 - 产品化的快速迭代能力（面试会聊产品理解） - 强化学习（用于对齐与产品指标优化）

**典型问题与要点**： - **「零样本语音克隆怎么做？」**要点：用 speaker encoder 提取音色嵌入（如 ECAPA-TDNN 或参考音频编码）→ 作为条件送入 TTS 模型（如基于 VALL-E 风格的 codec language model，或 flow matching 的声学模型）→ 声码器还原波形。难点是音色相似度与自然度的平衡、稳定性（避免跳字、重复）、以及跨语言的音色保持。加分点：提到「用大规模多说话人数据训练是零样本能力的前提」。 - **「音频的离散 token 和连续表征，各自适合什么？」**要点：离散 token（如 SoundStream / EnCodec 的 RVQ）便于用语言模型建模范式，但量化损失影响音质；连续表征（mel、latent）保留更多细节，适合扩散 / flow 类生成模型。实践中常有「离散预测 + 连续精修」的组合。

**建议**：MiniMax 面试偏实际与产品，会问「你觉得我们的产品哪里可以改进」，要有准备。

## 7.5 DeepSeek

**背景**：以 DeepSeek-V2/V3 的 MoE 架构、MLA 注意力、以及 DeepSeek-R1 的推理能力（RL 训练推理链）著称，开源影响力极大。

**考察重点**： - **MLA（Multi-head Latent Attention）**：几乎必问 - **MoE 的工程与训练**（无辅助损失的负载均衡策略） - **FP8 训练** - **强化学习训推理**（GRPO、规则奖励、可验证任务） - **推理优化与成本**（DeepSeek 以极致推理成本著称）

**典型问题与要点**： - **「MLA 解决什么问题？原理是什么？」**要点：目标是同时解决「KV Cache 太大」和「MHA 效果好但 GQA/MQA 有性能损失」的矛盾。做法是把 K、V 通过低秩压缩到潜向量 c_t（压缩维度远小于完整 KV），推理时只缓存 c_t（大幅减少 KV Cache），再通过上投影矩阵恢复 K、V。为了兼容 RoPE，采用「解耦的 RoPE」—— 额外保留一小部分携带位置信息的维度单独做旋转，不参与压缩。结果是 KV Cache 显著小于 MHA，性能不低于甚至优于 MHA。 - **「V3 怎么解决 MoE 的负载不均衡而不引入辅助损失？」**要点：辅助损失会损害主任务性能且权重难调。DeepSeek-V3 用**可学习的路由偏置** b_i：对每个专家维护一个偏置项，只用于影响 top-k 选择（不进入最终的加权计算），训练中根据专家负载动态调整 —— 负载过高则降低其偏置，过低则提高。这样路由是「无梯度」地通过偏置调整，不干扰语言建模目标。加分点：提到序列级辅助损失（防止单序列内极度不均衡）与节点限制路由（减少通信）。 - **「FP8 训练需要注意什么？」**要点：FP8 有两种格式（E4M3 精度高范围小、E5M2 范围大精度低）；需要**分块量化（tile-wise / block-wise scaling）**以适配离群值；关键部分（如 LayerNorm、softmax、残差、优化器状态、累加器）保持高精度；需要高精度累加。收益是显存与带宽减半、算力吞吐提升。

**建议**：DeepSeek 的技术报告几乎必读，面试会直接问报告里的设计动机；准备「为什么这么设计」而不是「是什么」。

## 7.6 其他值得关注的公司

- **阶跃星辰（StepFun）**：多模态方向突出（Step 系列多模态模型），面试重多模态与训练。
- **百川智能**：医疗与通用模型，关注领域数据与对齐。
- **零一万物（01.AI）**：开源模型 + 企业落地，面试偏全栈。
- **面壁智能**：端侧小模型（MiniCPM），面试会问端侧与高效训练。
- **幻方 / 深度求索相关**：偏底层与训练系统。

**共同建议**：面试前必读该公司最近的技术报告与开源代码；准备一个「我对 XX 技术方向的判断」的观点陈述。

------------------------------------------------------------------------

# 8. 横向对比表

下表为**趋势性对比，据公开面经整理，具体以实际为准**。不同部门、不同年份差异很大。

## 8.1 流程与轮次

| 公司 | 技术轮次 | 单轮时长 | 每轮手撕 | 有无终面/委员会 | 备注 |
|----|----|----|----|----|----|
| 腾讯 | 3~4 轮 | 60~75 min | 1 道（几乎必考） | 部分岗位有 GM 面 | BG 差异极大，交叉面常见 |
| 阿里 | 3~4 轮 | 60 min | 1 道（偏实现题） | P7+ 有委员会评审 | 业务面权重高 |
| 百度 | 3 轮 | 60 min | 1~2 道 | 总监面 | 手撕比例最高之一 |
| 快手 | 3 轮 | 60 min | 1 道 | 总监面 | 可灵可能加论文讨论轮 |
| 美团 | 3 轮 | 45~60 min | 1 道 | 总监面 | 流程紧凑 |
| 京东 | 3 轮 | 45~60 min | 1 道 | 主管面 | 业务导向 |
| 小红书 | 3 轮 | 45~60 min | 1 道 | 交叉面 | 反馈快 |
| 滴滴 | 3 轮 | 45~60 min | 1 道 | 主管面 | 岗位差异大 |
| 华为 | 3 轮 + 机考 + 测评 | 60 min | 机考 3 题 | 主管面 | 流程最长，机考是门槛 |
| 小米 | 3 轮 | 45~60 min | 1 道 | 主管面 | 端侧问题多 |
| 科大讯飞 | 3 轮 | 45~60 min | 1 道 | 主管面 | 语音相关必问 |
| 旷视/商汤 | 3 轮 | 60 min | 1~2 道（CV 实现） | 主管面 | CV 手撕是重点 |
| 创业公司 | 2~3 轮 | 60~90 min | 0~1 道，或开放式实现 | 创始人面 | 论文与观点权重高 |

## 8.2 考察维度雷达（定性打分，5 分制）

| 公司           | 八股深度 | 手撕比例 | 项目深挖 | 业务理解 | 研究/论文 | 系统/Infra |
|----------------|----------|----------|----------|----------|-----------|------------|
| 腾讯           | 4.5      | 4.5      | 4.0      | 3.5      | 2.5       | 4.0        |
| 阿里（通义）   | 4.5      | 3.5      | 4.5      | 3.0      | 4.0       | 4.0        |
| 阿里（淘天）   | 4.0      | 3.5      | 4.0      | 5.0      | 2.0       | 3.5        |
| 百度           | 4.5      | 4.5      | 4.0      | 3.5      | 3.5       | 3.0        |
| 快手（可灵）   | 4.0      | 3.5      | 4.5      | 2.5      | 4.5       | 3.5        |
| 美团/京东/滴滴 | 3.0      | 3.0      | 4.0      | 5.0      | 1.5       | 3.0        |
| 小红书         | 3.5      | 3.5      | 4.0      | 4.5      | 2.5       | 3.0        |
| 华为           | 4.0      | 4.0      | 4.0      | 3.0      | 3.0       | 5.0        |
| 小米           | 3.5      | 3.0      | 4.0      | 4.0      | 2.0       | 4.5        |
| 旷视/商汤      | 4.5      | 4.5      | 4.0      | 3.0      | 3.5       | 3.5        |
| 创业公司       | 2.5      | 2.5      | 4.5      | 3.0      | 5.0       | 3.5        |

（表中分数为综合公开面经的**主观归纳**，仅用于体现相对差异，不代表绝对水平。）

## 8.3 关键差异速查

- **八股最重的三家**：腾讯（C++/OS）、百度（NLP 传统）、旷视/商汤（CV 传统）。
- **手撕最重的三家**：百度、旷视/商汤、腾讯（腾讯偏实现类）。
- **项目深挖最狠的**：阿里通义、快手可灵、创业公司（会追问到你的实验细节和失败经验）。
- **业务理解最看重的**：美团、京东、淘天、滴滴。
- **研究味最浓的**：创业公司、智谱、通义、快手可灵。
- **Infra 味最浓的**：华为、小米（端侧）、阿里云、飞桨。

------------------------------------------------------------------------

# 9. 跨公司高频通用题 Top 榜（附答案要点）

以下题目在多家公司的面经中**反复出现**，是性价比最高的复习清单。建议按此清单逐条能讲 2~3 分钟。

## Top 1：Transformer 自注意力原理与复杂度

**出现频率**：几乎 100% 的算法岗。 **答案要点**：QKV 的来源与含义 → scaled dot-product 的除法原因 → 多头的作用（不同子空间、多视角）→ 复杂度 O(n²d)，长序列的瓶颈 → FlashAttention 的 IO 感知优化原理（分块 + 在线 softmax，减少 HBM 读写）→ 变体（稀疏、线性、滑动窗口）。 **追问预警**：为什么多头要降维？位置编码怎么加？

## Top 2：KV Cache 的原理、显存计算与优化

**出现频率**：约 90%。 **答案要点**：自回归解码的重复计算问题 → 缓存 K、V → 公式 2·b·s·l·h_kv·d·bytes → MQA/GQA/MLA 的演进逻辑 → PagedAttention、量化、前缀共享 → 长上下文下 KV Cache 是主要显存开销。 **追问预警**：batch 变大时显存怎么变？为什么 prefill 和 decode 是两种负载？

## Top 3：预训练 / 微调 / 对齐的区别与流程

**出现频率**：约 90%。 **答案要点**：预训练（海量无标注、next-token、学到通用能力）→ SFT（指令数据、学会遵循指令）→ 对齐（RLHF / DPO，学会符合人类偏好）。三阶段的数据量级、目标函数、常见坑（灾难性遗忘、对齐税、多样性下降）。 **追问预警**：SFT 数据多少条够？为什么 SFT 后模型会变「笨」？

## Top 4：LoRA / PEFT 的原理与实践

**出现频率**：约 85%。 **答案要点**：低秩假设、BA 分解、初始化、参数量、alpha 与 rank 的作用、显存收益来源（优化器状态）、QLoRA、与全量微调的对比、推理时合并权重。 **追问预警**：r 怎么选？LoRA 加在哪些层上？多任务 LoRA 怎么管理？

## Top 5：RAG 的完整链路与优化

**出现频率**：约 80%（应用岗接近 100%）。 **答案要点**：解析 → 分块 → 向量化 → 索引 → 检索（混合）→ rerank → 生成 → 引用。各环节的常见问题与优化手段。检索与生成分开评测。 **追问预警**：chunk 多大合适？向量检索和 BM25 各自擅长什么？

## Top 6：幻觉的成因与缓解

**出现频率**：约 80%。 **答案要点**：训练目标、数据噪声、长尾知识、解码随机性、RLHF 的谄媚 → 缓解：RAG、拒答训练、解码约束、后置校验、CoT。区分事实性幻觉与忠实性幻觉。 **追问预警**：怎么度量幻觉率？

## Top 7：分布式训练并行策略

**出现频率**：约 75%（训练岗 100%）。 **答案要点**：数据并行（DDP / ZeRO 三阶段）→ 张量并行（Megatron 的列切/行切、通信模式）→ 流水并行（气泡、1F1B）→ 序列并行 → 3D 并行与通信量分析。ZeRO-1/2/3 分别切什么。 **追问预警**：ZeRO-3 的问题是什么（通信量大）？梯度累积怎么与并行配合？

## Top 8：模型量化（训练后量化与量化感知训练）

**出现频率**：约 70%（端侧 / Infra 岗 100%）。 **答案要点**：量化的动机（显存与带宽）→ 对称/非对称、per-tensor / per-channel / per-group → GPTQ（二阶误差补偿）、AWQ（激活感知的缩放）→ QAT 与 PTQ 的取舍 → 激活量化的难点（离群值，需 SmoothQuant）。 **追问预警**：为什么权重量化比激活量化容易？

## Top 9：RLHF / PPO / DPO 的对比

**出现频率**：约 70%。 **答案要点**：RLHF 四模型（actor、critic、reward、ref）→ PPO 的 clip 目标与优势估计 → DPO 的闭式推导与无需 RM → 优缺点对比（稳定性 vs 上限、数据效率、多样性）→ 变体（IPO/KTO/ORPO/GRPO）。 **追问预警**：reward hacking 是什么？KL 约束的作用？

## Top 10：位置编码的演进

**出现频率**：约 65%。 **答案要点**：绝对位置编码（可学习、正弦）→ 相对位置（T5 bias）→ RoPE（旋转、相对性、远程衰减）→ ALiBi（线性偏置、外推好）→ 外推方法（PI、NTK、YaRN、ABF）→ 长度外推的评测。 **追问预警**：RoPE 为什么能编码相对位置？

## Top 11：Batch Norm vs Layer Norm，为什么 Transformer 用 LayerNorm

**出现频率**：约 65%。 **答案要点**：BN 沿 batch 维度统计，依赖 batch size，序列长度可变时统计不稳定，且推理时的 running stats 与训练不一致；LN 沿特征维度统计，与 batch 无关，适合变长序列和自回归推理。RMSNorm 进一步简化。 **追问预警**：Pre-LN 和 Post-LN 的区别？

## Top 12：MoE 的原理、路由与工程难点

**出现频率**：约 60%。 **答案要点**：稀疏激活、容量与算力解耦、top-k 路由、负载均衡（aux loss vs 偏置调整）、专家并行与 all-to-all 通信、推理显存瓶颈。 **追问预警**：MoE 为什么不总能超过同激活量的稠密模型？

## Top 13：显存占用怎么估算、怎么省显存

**出现频率**：约 60%。 **答案要点**：参数（含精度）、梯度、优化器状态（Adam 是参数量的 2 倍）、激活值、临时 buffer。粗略规律：全量微调 Adam fp16 约需 16~20 字节/参数。省显存：混合精度、ZeRO、梯度检查点、LoRA、FlashAttention、offload。 **追问预警**：梯度检查点的时间代价？

## Top 14：手撕 Top-k 采样 / Beam Search / 手写 Attention

**出现频率**：约 60%。 **答案要点**：见前文各公司分节的实现要点。核心是**能写完整的、边界正确的代码**，并说清数值稳定性的处理。

## Top 15：评测方法论 —— 怎么评估一个大模型

**出现频率**：约 55%（但越来越高频）。 **答案要点**：基准（MMLU、GSM8K、HumanEval 等）的局限 → 领域自建评测集 → LLM-as-Judge 与人工评测的取舍 → 位置偏见、长度偏见等 Judge 问题 → 评测集泄漏与污染 → 线上 A/B 是最终标准。 **追问预警**：LLM-as-Judge 靠谱吗？怎么校准？

## Top 16：显存/速度优化 —— FlashAttention、投机解码

**出现频率**：约 55%。 **答案要点**：FlashAttention（分块计算、在线 softmax、不物化 n² 矩阵、IO 感知）；投机解码（小模型起草 + 大模型验证、接受率决定加速比、Medusa/EAGLE 等多头方案）。 **追问预警**：投机解码在什么情况下会变慢？

## Top 17：过拟合 / 欠拟合 / 正则化

**出现频率**：约 50%（经典题，常作为开场）。 **答案要点**：判断依据（train/val 曲线）→ 手段（数据增强、Dropout、权重衰减、早停、标签平滑、减小模型）→ 大模型时代的特殊性（数据量巨大时过拟合关注点不同，更多关注数据质量与去重）。 **追问预警**：大模型的过拟合怎么判断？（loss 与下游指标不一致时）

## Top 18：多模态 —— VLM 的架构与训练

**出现频率**：约 50%（多模态岗 100%）。 **答案要点**：视觉编码器 + 投影 + LLM → 两阶段训练（对齐 / 指令微调）→ 分辨率与 token 数 → 视觉 token 压缩 → 多模态评测。

## Top 19：位置 —— 长上下文的处理

**出现频率**：约 45%。 **答案要点**：外推方法、训练长度扩展、注意力优化、长文本评测的陷阱（只用 NIAH 会高估）、以及「有效上下文」与「名义上下文」的差距。

## Top 20：场景设计 —— 怎么把大模型落进一个业务

**出现频率**：约 45%（业务岗 90%）。 **答案要点**：需求拆解 → 模型选型（大模型 vs 小模型 vs 规则）→ 数据闭环 → 评测 → 成本与延迟 → 灰度与兜底 → 迭代。**结构化的回答框架比具体方案更重要**。

------------------------------------------------------------------------

# 10. 不同岗位方向的差异

同一家公司，投「算法研究」「算法工程」「应用落地」「AI Infra」，面试内容可能完全不同。这一节给出各方向的准备重点。

## 10.1 算法研究岗（Research Scientist / 研究员）

**典型公司**：阿里通义、腾讯混元、智谱、月之暗面、DeepSeek、快手可灵、华为诺亚、旷视/商汤研究院。

**考察重心**： 1. **论文**：有无顶会（NeurIPS/ICML/ICLR/CVPR/ACL/EMNLP）论文几乎是硬门槛；面试会逐篇问你的动机、方法、实验设计、以及「如果重做你会改什么」。 2. **研究品味**：你关注什么问题？为什么这个问题重要？你怎么判断一个方向是否有前途？ 3. **数学与推导**：会要求现场推导（如 DPO 的推导、扩散模型的反向过程、变分下界 ELBO）。 4. **前沿追踪**：最近读了什么论文，怎么看某个热点。 5. **代码相对次要**，但仍会有 1 道实现题。

**准备建议**： - 每篇论文准备 3 分钟的「电梯陈述」和 15 分钟的深度版本。 - 准备一个「我读过的最有启发的一篇论文」的回答，能讲透。 - 准备一个「我看好的方向 + 理由 + 可能的验证路径」的回答。 - 数学推导要多练手推，不要只看。

## 10.2 算法工程岗（Algorithm Engineer / 算法工程师）

**典型公司**：几乎所有公司的算法岗主体，包括腾讯各 BG、百度、快手、美团、京东、小红书、滴滴、小米。

**考察重心**： 1. **项目落地能力**：你做的模型上线了吗？QPS 多少？延迟多少？成本多少？效果提升多少？ 2. **工程能力**：代码质量、系统设计、性能优化、线上排障。 3. **八股广度**：从传统 ML 到大模型都要会一点，但不要求极深。 4. **业务理解**：能不能把技术翻译成业务价值。 5. **手撕**：必考，通常是中等难度算法题 + 一道实现题。

**准备建议**： - 每个项目都要能量化：数据规模、模型规模、指标提升、资源消耗。 - 准备一套「排障故事」：线上出问题你怎么定位和解决。 - 准备一套「成本优化故事」：你怎么在效果不降的前提下把成本降下来（这是最受青睐的能力之一）。 - 刷题保持手感，重点是中等难度 + 实现类题目。

## 10.3 应用 / 落地岗（AI 应用、Agent、解决方案）

**典型公司**：阿里淘天/云、腾讯 CSIG/WXG、美团、京东、科大讯飞、以及大量创业公司的应用团队。

**考察重心**： 1. **业务抽象能力**：能不能把一个模糊的业务需求拆成可执行的技术方案。 2. **RAG / Agent / Prompt 工程**：具体到 chunk 策略、rerank 选型、工具调用的错误处理、多轮状态管理。 3. **评测与迭代**：怎么定义成功？怎么快速迭代？ 4. **成本与合规**：token 成本、数据安全、内容合规。 5. **前端/全栈意识**：很多应用岗要求能自己搭 demo。

**准备建议**： - 准备 2~3 个完整的应用案例：从需求到上线到迭代的完整链路。 - 深入一个 RAG 或 Agent 框架（LangChain / LlamaIndex / Dify 等），能讲清它们的抽象和局限。 - 准备好「模型效果不好怎么办」的排查清单。 - 关注 prompt 注入、越狱、数据泄漏等安全问题。

## 10.4 AI Infra 岗（训练框架、推理引擎、算力平台）

**典型公司**：华为（昇腾/CANN）、阿里云/PAI、腾讯 TEG、字节（本文不展开）、小米（端侧）、以及各家的大模型训练平台团队。

**考察重心**： 1. **分布式训练**：并行策略、通信原语（all-reduce/all-gather/all-to-all）、通信量分析、ZeRO、Megatron 实现细节。 2. **推理引擎**：vLLM 的 PagedAttention 与调度、TensorRT-LLM、SGLang 的 RadixAttention、连续批处理（continuous batching）、量化推理 kernel。 3. **CUDA / 算子**：kernel 编写、访存优化、算子融合、profiling（Nsight）。华为岗要换成昇腾算子开发。 4. **系统基础**：C++、操作系统、网络、内存管理、编译原理（部分岗位）。 5. **硬件理解**：GPU 架构、显存带宽与算力的比、NVLink / InfiniBand 拓扑。

**准备建议**： - 精读 vLLM 和 FlashAttention 的论文与代码，这是最常被问的两个工作。 - 会算通信量：比如 all-reduce 的数据量和带宽需求。 - 准备一个「我把某个算子的性能提升了 X 倍」的案例，包含 profiling 方法和优化手段。 - Infra 岗的八股偏系统，算法八股反而问得少。 - 华为岗额外准备昇腾生态（CANN、MindSpore、Ascend C）。

## 10.5 四类岗位速查

| 维度 | 研究岗 | 算法工程岗 | 应用落地岗 | AI Infra 岗 |
|----|----|----|----|----|
| 论文权重 | 极高 | 低 | 很低 | 中 |
| 八股类型 | 数学推导、前沿 | ML + 大模型广度 | RAG/Agent/工程 | 系统、并行、算子 |
| 手撕类型 | 实现题 | 算法题 + 实现题 | 简单算法 + 场景代码 | C++/CUDA、系统题 |
| 项目深挖 | 论文细节 | 落地指标 | 业务闭环 | 性能数字 |
| 关键能力 | 研究品味 | 工程闭环 | 业务抽象 | 性能极致 |
| 最对口背景 | 博士/顶会 | 硕士/工程经验 | 全栈/产品感 | 系统/体系结构 |

------------------------------------------------------------------------

# 11. 通用准备建议（跨公司适用）

1.  **一份主简历，多份侧重版本**。投研究岗强调论文与创新点，投工程岗强调落地与指标，投 Infra 岗强调性能数字。不要一份简历投所有。
2.  **准备三档深度的自我介绍**：1 分钟、3 分钟、10 分钟。面试官说「简单介绍一下」时用 1 分钟版本。
3.  **项目要准备「四问」**：为什么做（动机）、怎么做（方法）、效果如何（量化）、如果重做怎么改（反思）。第四问最容易拉开差距。
4.  **建立自己的「问题清单」**。把高频题整理成文档，每题写 3 个要点 + 1 个加分点，反复口述练习。
5.  **手撕要真写**。在纸上或编辑器里完整写出可运行的代码，而不是「想清楚思路」。面试中 bug 往往出在边界和 API 细节上。
6.  **准备反问问题**。好的反问能体现你的水平，比如「团队现在最大的技术瓶颈是什么」「模型评测体系是怎么建的」。
7.  **信息核实**。本文所有内容均为公开面经归纳，实际流程、题目、HC、薪资请以官方渠道和实际面试为准，不要因为面经说「腾讯必考 C++」就放弃投递。
8.  **心态**。大模型岗位竞争激烈，被拒不代表能力问题，匹配度（方向、部门、时机）占很大比重。多投多面，把每次面试当作一次免费的行业调研。

------------------------------------------------------------------------

# 12. 参考文献与延伸阅读

- GitHub: **jingtian11/EasyOffer** —— 「LLM大厂面经合集」（含腾讯、阿里、百度、快手、蚂蚁、小米、科大讯飞、旷视等目录）
- GitHub: **datawhalechina/daily-interview** —— 每日面试题合集（算法 / NLP / 大模型）
- GitHub: **WeThinkIn/AIGC-Interview-Book** —— AIGC 与大模型面试八股（含扩散模型、多模态、训练与推理优化）
- GitHub: **liyupi/mianshiya（面试鸭）** —— 面试题库社区
- 论文（高频被问）：Attention Is All You Need、FlashAttention、LoRA、QLoRA、DPO、DDPM、DDIM、Latent Diffusion、DiT、CLIP、CogVLM、DeepSeek-V2（MLA）、DeepSeek-V3（MoE / FP8）、DeepSeek-R1（GRPO）

------------------------------------------------------------------------

> **免责声明**：本文由公开面经、技术博客与社区讨论整理而成，用于面试准备与知识梳理。文中涉及的面试流程、轮次、题目、考察风格均为**趋势性归纳**，不代表任何公司的官方标准，也不构成任何形式的承诺。各公司组织架构与招聘策略变动频繁，具体请以实际面试与官方信息为准。「据公开面经整理，具体以实际为准」适用于本文全部内容。

**本文件整理日期：2026-09-12**
